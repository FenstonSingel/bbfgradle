¿iexportTaggdefaultnoriginalSampleyÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
gmutantsŸyö// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty


val u = false
when (u) {
 true -> {fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}}
 else -> {fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}}
}


public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y_// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}


val i = true
when (i) {
 true -> {public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}}
 else -> {public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}}
}

y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() 
val u = true
try
{{
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}}
catch(e: Exception){}
finally{}


public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yC// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = 
val j = true
if (j) {object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}} else {object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}}

yý// Original bug: KT-14833
// Duplicated bug: KT-14833

import 
val u = true
if (u) {kotlin.properties} else {kotlin.properties}
.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import 
val d = true
try
{kotlin.reflect.KProperty}
catch(e: Exception){}
finally{}


fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    
val e = true
try
{var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )}
catch(e: Exception){}
finally{}


    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    
val x = true
if (x) {blah += 1} else {blah += 1}

//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <
val l = false
try
{T}
catch(e: Exception){}
finally{}
> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    
val i = false
try
{initialValue: T}
catch(e: Exception){}
finally{}
,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yS// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    
val z = false
when (z) {
 true -> {crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean}
 else -> {crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean}
}
,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yO// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    
val d = true
when (d) {
 true -> {crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit}
 else -> {crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit}
}

): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yD// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = 
val q = false
if (q) {object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}} else {object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}}

yò// Original bug: KT-14833
// Duplicated bug: KT-14833

import 
val h = true
if (h) {kotlin} else {kotlin}
.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.
val x = false
try
{properties}
catch(e: Exception){}
finally{}
.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import 
val g = true
when (g) {
 true -> {kotlin.reflect}
 else -> {kotlin.reflect}
}
.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.
val l = true
try
{KProperty}
catch(e: Exception){}
finally{}


fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yð// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    
val x = true
if (x) {blah} else {blah}
 += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yï// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah 
val o = false
if (o) {+=} else {+=}
 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yí// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 
val o = true
if (o) {1} else {1}

//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> 
val t = true
try
{Delegates}
catch(e: Exception){}
finally{}
.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): 
val s = true
when (s) {
 true -> {ReadWriteProperty}
 else -> {ReadWriteProperty}
}
<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import 
val h = false
when (h) {
 true -> {kotlin}
 else -> {kotlin}
}
.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yó// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.
val o = true
if (o) {reflect} else {reflect}
.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by 
val u = true
try
{Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )}
catch(e: Exception){}
finally{}


    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    
val t = true
try
{override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }}
catch(e: Exception){}
finally{}


    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    
val f = false
try
{override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }}
catch(e: Exception){}
finally{}


}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: 
val t = true
when (t) {
 true -> {Int}
 else -> {Int}
}
 by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yö// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by 
val x = false
if (x) {Delegates} else {Delegates}
.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y£// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.
val d = false
when (d) {
 true -> {blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )}
 else -> {blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )}
}


    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: 
val v = false
when (v) {
 true -> {T}
 else -> {T}
}
,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : 
val x = false
if (x) {ObservableProperty<T>} else {ObservableProperty<T>}
(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y=// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean 
val g = false
when (g) {
 true -> {{
        return validate(property, oldValue, newValue)
    }}
 else -> {{
        return validate(property, oldValue, newValue)
    }}
}


    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y<// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) 
val n = true
when (n) {
 true -> {{
        return onChange(property, oldValue, newValue)
    }}
 else -> {{
        return onChange(property, oldValue, newValue)
    }}
}


}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.
val c = true
try
{blah}
catch(e: Exception){}
finally{}
(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (
val x = false
try
{property: KProperty<*>}
catch(e: Exception){}
finally{}
, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, 
val z = false
try
{oldValue: T}
catch(e: Exception){}
finally{}
, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y
// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, 
val c = true
when (c) {
 true -> {newValue: T}
 else -> {newValue: T}
}
) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (
val g = true
when (g) {
 true -> {property: KProperty<*>}
 else -> {property: KProperty<*>}
}
, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, 
val c = false
try
{oldValue: T}
catch(e: Exception){}
finally{}
, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, 
val w = false
when (w) {
 true -> {newValue: T}
 else -> {newValue: T}
}
) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(
val w = true
try
{property: KProperty<*>}
catch(e: Exception){}
finally{}
, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y
// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, 
val l = true
when (l) {
 true -> {oldValue: T}
 else -> {oldValue: T}
}
, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y
// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, 
val t = true
when (t) {
 true -> {newValue: T}
 else -> {newValue: T}
}
): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        
val e = true
try
{return validate(property, oldValue, newValue)}
catch(e: Exception){}
finally{}

    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(
val a = true
when (a) {
 true -> {property: KProperty<*>}
 else -> {property: KProperty<*>}
}
, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y
// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, 
val b = true
when (b) {
 true -> {oldValue: T}
 else -> {oldValue: T}
}
, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y
// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, 
val z = true
when (z) {
 true -> {newValue: T}
 else -> {newValue: T}
}
) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        
val u = true
try
{return onChange(property, oldValue, newValue)}
catch(e: Exception){}
finally{}

    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> 
val r = false
when (r) {
 true -> {Boolean}
 else -> {Boolean}
}
,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> 
val e = true
when (e) {
 true -> {Unit}
 else -> {Unit}
}

): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, 
val s = false
when (s) {
 true -> {T}
 else -> {T}
}
> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(
val x = false
when (x) {
 true -> {initialValue}
 else -> {initialValue}
}
) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): 
val d = true
when (d) {
 true -> {Boolean}
 else -> {Boolean}
}
 {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return 
val m = true
try
{validate(property, oldValue, newValue)}
catch(e: Exception){}
finally{}

    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return 
val n = true
try
{onChange(property, oldValue, newValue)}
catch(e: Exception){}
finally{}

    }

}
yí// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 
val b = true
if (b) {0} else {0}
,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = 
val u = false
try
{{ property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }}
catch(e: Exception){}
finally{}
,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yx// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = 
val d = true
when (d) {
 true -> {{ property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }}
 else -> {{ property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }}
}

    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yð// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<
val s = false
if (s) {Any} else {Any}
?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yþ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : 
val g = true
if (g) {ObservableProperty} else {ObservableProperty}
<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return 
val x = false
try
{validate}
catch(e: Exception){}
finally{}
(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return 
val w = true
when (w) {
 true -> {onChange}
 else -> {onChange}
}
(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        
val y = true
try
{initialValue}
catch(e: Exception){}
finally{}
 = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        
val o = true
try
{validate}
catch(e: Exception){}
finally{}
 = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÛ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = 
val i = true
when (i) {
 true -> {{ property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }}
 else -> {{ property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }}
}
,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        
val n = false
if (n) {onChange} else {onChange}
 = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = 
val f = true
try
{{ property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }}
catch(e: Exception){}
finally{}

    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y	// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: 
val d = false
when (d) {
 true -> {KProperty}
 else -> {KProperty}
}
<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: 
val j = false
try
{T}
catch(e: Exception){}
finally{}
, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: 
val h = true
when (h) {
 true -> {T}
 else -> {T}
}
) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: 
val n = false
try
{KProperty}
catch(e: Exception){}
finally{}
<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: 
val c = false
try
{T}
catch(e: Exception){}
finally{}
, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yí// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: 
val a = true
if (a) {T} else {T}
) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: 
val l = false
try
{KProperty}
catch(e: Exception){}
finally{}
<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: 
val q = true
when (q) {
 true -> {T}
 else -> {T}
}
, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: 
val u = false
when (u) {
 true -> {T}
 else -> {T}
}
): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: 
val d = true
if (d) {KProperty} else {KProperty}
<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: 
val m = true
try
{T}
catch(e: Exception){}
finally{}
, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: 
val k = true
try
{T}
catch(e: Exception){}
finally{}
) {
        return onChange(property, oldValue, newValue)
    }

}
y£// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            
val c = true
when (c) {
 true -> {if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true}
 else -> {if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true}
}

        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y-// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            
val v = true
if (v) {println("onChange: ${property.name} from $oldValue to $newValue")} else {println("onChange: ${property.name} from $oldValue to $newValue")}

        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(
val o = false
if (o) {property} else {property}
, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, 
val p = false
try
{oldValue}
catch(e: Exception){}
finally{}
, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yô// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, 
val i = true
if (i) {newValue} else {newValue}
)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yô// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(
val z = true
if (z) {property} else {property}
, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, 
val j = true
when (j) {
 true -> {oldValue}
 else -> {oldValue}
}
, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, 
val q = false
try
{newValue}
catch(e: Exception){}
finally{}
)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { 
val b = false
when (b) {
 true -> {property}
 else -> {property}
}
, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yô// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, 
val f = true
if (f) {oldValue} else {oldValue}
, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, 
val d = true
try
{newValue}
catch(e: Exception){}
finally{}
 ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
ys// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            
val l = true
if (l) {if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }} else {if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }}

            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yý// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            
val k = false
if (k) {return@blah true} else {return@blah true}

        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { 
val j = false
when (j) {
 true -> {property}
 else -> {property}
}
, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yô// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, 
val p = true
if (p) {oldValue} else {oldValue}
, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, 
val b = false
when (b) {
 true -> {newValue}
 else -> {newValue}
}
 ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            
val q = false
try
{println("onChange: ${property.name} from $oldValue to $newValue")}
catch(e: Exception){}
finally{}

        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah 
val r = true
try
{true}
catch(e: Exception){}
finally{}

        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yó// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            
val l = true
if (l) {println} else {println}
("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<
val s = true
when (s) {
 true -> {T}
 else -> {T}
}
>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (
val j = true
try
{newValue < 0}
catch(e: Exception){}
finally{}
) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) 
val g = true
try
{{
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }}
catch(e: Exception){}
finally{}

            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return
val v = false
when (v) {
 true -> {@blah}
 else -> {@blah}
}
 true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (
val e = true
try
{newValue}
catch(e: Exception){}
finally{}
 < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue 
val o = true
when (o) {
 true -> {<}
 else -> {<}
}
 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yî// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 
val b = false
if (b) {0} else {0}
) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                
val c = true
if (c) {println("validation failed: ${property.name} >= 0")} else {println("validation failed: ${property.name} >= 0")}

                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yý// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                
val t = true
if (t) {return@blah false} else {return@blah false}

            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(
val o = true
try
{"onChange: ${property.name} from $oldValue to $newValue"}
catch(e: Exception){}
finally{}
)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                
val t = false
try
{println}
catch(e: Exception){}
finally{}
("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yñ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah 
val y = true
if (y) {false} else {false}

            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return
val t = true
when (t) {
 true -> {@blah}
 else -> {@blah}
}
 false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${
val n = true
try
{property.name}
catch(e: Exception){}
finally{}
} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $
val b = false
if (b) {oldValue} else {oldValue}
 to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yõ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $
val o = false
if (o) {newValue} else {newValue}
")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(
val j = true
try
{"validation failed: ${property.name} >= 0"}
catch(e: Exception){}
finally{}
)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${
val n = false
try
{property}
catch(e: Exception){}
finally{}
.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yð// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.
val v = true
if (v) {name} else {name}
} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${
val o = false
try
{property.name}
catch(e: Exception){}
finally{}
} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yô// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${
val t = true
if (t) {property} else {property}
.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.
val q = true
when (q) {
 true -> {name}
 else -> {name}
}
} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.(properties).*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah (+= 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import (kotlin).reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.(reflect).KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : (ObservableProperty<T>)(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: (T), newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            (println("onChange: ${property.name} from $oldValue to $newValue"))
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${(property.name)} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T!!> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )!!

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate!! = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange!! = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T!!) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue!!, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue!!)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue!!, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true!!
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name!!} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> (Delegates)?.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: (Int)? by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: (T)?,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: ((property: KProperty<*>, oldValue: T, newValue: T) -> Boolean)?,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: ((property: KProperty<*>, oldValue: T, newValue: T) -> Unit)?
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> (Boolean)?,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> (Unit)?
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, (T)?> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): (Boolean)? {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : (ObservableProperty<T>)?(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: (KProperty<*>)?, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: (T)?, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: (T)?) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: (KProperty<*>)?, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: (T)?, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: (T)?) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: (KProperty<*>)?, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: (T)?, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: (T)?): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: (KProperty<*>)?, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: (T)?, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: (T)?) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<(T)?>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties!!.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin!!.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect!!.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin!!.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property!!.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            $
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from return@blah trueoldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ*
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<// Original bug: KT-14833>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        <*>
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty}, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    fun
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> ),
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValueBoolean {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${?} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Anyproperty.name, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
oldValue

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, // Duplicated bug: KT-14833, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, Any? ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<newValue, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    newValue

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                ,("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>println oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Any?.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Delegates, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, Any, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<oldValue?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, initialValue> = object : ObservableProperty<T>(T) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property0 oldValue, newValue ->
            if (newValue < ,) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += KProperty
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: 1<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: T by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: Int) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, override> = object : ObservableProperty<T>(initialValue) {

    T fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < property) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { 0, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, <T>> = object : ObservablePropertyT(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValuecrossinline$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
     to  validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah ?
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Anyfalse, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.nameoverride fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    } >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import newValue.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, kotlin: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T:
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange, (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue (initialValue) 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>< {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from ObservableProperty<T>oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : $(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}KProperty<*>")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property:  >= 0, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> ,.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: TDelegates newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: newValue, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, T)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override onChange(property, oldValue, newValue) afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return fun
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        ) = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: Tvalidate: Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${propertyblah += 1name} from $oldValue to $newValue")
        }
    )

    .
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: newValue, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, T: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: validate,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValueBoolean$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) ->  to ,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.T(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: blah, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                returnReadWriteProperty false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): @blah<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) override
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    { fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return property(onChange, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, *) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<newValue: T>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun mainT) {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: (, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int (property: KProperty<*>, oldValue: T, newValue: T) -> Unit

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah < 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty+=Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, >: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*oldValue, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    initialValue

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>()) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: KProperty<*> by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: Int, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<}, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    Any?

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int fun

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ) afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.afterChange
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun *(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> property,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(Boolean, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: Tproperty newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(,, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(initialValue)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>("validation failed: ${property.name} >= 0") {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue"override
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    ) fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

) main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: Tfun: Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.property.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (reflect: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = oldValue: T,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate , { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue= newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValueT$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue:  to , newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KPropertyoldValue, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, <*>, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    T: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: initialValue, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int :

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T)by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ) Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.propertiesT*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: .) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            printlnpublic inline
        }
    )

    blah += 1
//    blah = blah + 1
}

("onChange: ${property.name} from $oldValue to $newValue") fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: property: KProperty<*>
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange((property: KProperty<*>, oldValue: T, newValue: T) -> Unit, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        :,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange} (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

} = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = onChange
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to TnewValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <$> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> :.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValueDelegates T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import ,.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0kotlin.reflect
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun mainT {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: ()) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.,

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: TKProperty newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    ( blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            printlnvar"onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: return, newValue: T) {
        T onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import "onChange: ${property.name} from $oldValue to $newValue".*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(kotlin.properties)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: true by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah Int
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println()validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue""
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <ToldValue: T Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, >, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) oldValue
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        : = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValueonChange T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlinoldValuereflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, ., newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(Boolean: KProperty<*>, oldValue: T, newValue: T): property {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        } = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
initialValue

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            newValue: T@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, return) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.oldValue

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ), newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        },
        onChange = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    KProperty<*> fun afterChange(property: override, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah "onChange: ${property.name} from $oldValue to $newValue"
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(false)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun KProperty<*>T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: <, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: return
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        (property: KProperty<*>, oldValue: T, newValue: T) -> Unit validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            printlnoverride"onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    ( fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

println("onChange: ${property.name} from $oldValue to $newValue") main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            fun
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

{ property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = import kotlin.properties.*
import kotlin.reflect.KProperty,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

newValue
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, import kotlin.properties.*: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: oldValue,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, T, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun <() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWritePropertymainAny?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KPropertyBoolean*>, oldValue: T, newValue: T): < {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

return
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = > property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T{(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public : fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(propertyinline KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

override kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    import fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: KProperty<*> >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: ${property.name}, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: newValue) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, T)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    {
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    },
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    onChange fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return override(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.).*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: Tproperties: Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = property
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(<, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KPropertyproperty: KProperty<*>*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah ObservableProperty<T>
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : true(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, (> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChangeTproperty: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: override) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    T fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate ) { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue"=
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, property, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(oldValue: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        beforeChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun onChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<property>, oldValue: T, newValue: T): Boolean {
        return validate(*, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += .
//    blah = blah + 1
}

public inline fun <T> Delegates1blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: }<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

KProperty
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main(property {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(), oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

T
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: import kotlin.properties.*): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: ->,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import //    blah = blah + 1.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
kotlin.properties
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
newValue

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, import kotlin.reflect.KProperty)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValueoldValue$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property,  to , newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
newValue

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (import kotlin.reflect.KProperty < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline Delegates <T> fun.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) ,
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T{
                println("validation failed: ${property.name} >= 0")
                return@blah false
            } newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                )
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
return@blah false: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: :, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(propertyKProperty<*> KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) $newValue
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

validate(property, oldValue, newValue) main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return fun
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: :,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValueT T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, T, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, oldValue> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            (
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
)
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blahif (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public KProperty<*> fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: inline, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (override < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    newValue fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, T)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: newValue) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: T by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<Int>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.$newValue} from $oldValue to name")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $*")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<newValue>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main(ObservableProperty<T> {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : )(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue))
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T" {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Delegates by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Int.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            ObservableProperty<T>@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : return(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.((
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validateblahproperty, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blahAny: ReadWriteProperty<(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
)?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = ObservableProperty<T>,
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${).name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValueproperty
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import println("onChange: ${property.name} from $oldValue to $newValue")

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            kotlin.reflect.KProperty
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.->(
        initialValue = 0,
        validate = { property, oldValue, newValue blah
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.ObservableProperty.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : reflect<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Boolean?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Any {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue } 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

<
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    ObservableProperty<T>(initialValue)

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() Any?
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<{, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> ,,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValueBoolean newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from } to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

$oldValue
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>T {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: (initialValue)) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: return validate(property, oldValue, newValue), oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        KProperty<*>
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return :
    }

    override fun afterChange(property: KProperty<*>, oldValuevalidate(property, oldValue, newValue) T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    ,

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: Tvar blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ) newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: oldValue): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, T, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("fun${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline validation failed:  <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from , to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>$oldValue oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        , = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>initialValue oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

Unit fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> public inline
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.T.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: properties,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah blah
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    true += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : oldValue {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, ObservableProperty<T>(initialValue), newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main(( {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>)initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blahnewValue Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, : ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = property {, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                property
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(return@blah false, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < *) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<0>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> override.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    Delegates fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(), oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValueproperty: KProperty<*>
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: initialValue, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(T) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import T.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <kotlin> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property( oldValue, newValue ->
            if ,newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            >
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*println("onChange: ${property.name} from $oldValue to $newValue"), oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.public.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

reflect inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            T
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: println("onChange: ${property.name} from $oldValue to $newValue"), newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin from properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name}.$oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValueoldValue: T T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, :, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline (: onChangeproperty: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (Unit: KProperty<*>, oldValue: T, newValue: T) -> property
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
:
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue//    blah = blah + 1 T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

} = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) validate
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = oldValue,
        validate = { property, 0, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate onChange { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return =(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate property { =, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                (
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blahprintln("validation failed: ${property.name} >= 0"): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, KProperty: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: oldValue<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: property) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(T, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValueoldValue$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property,  to , newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import <.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservablePropertykotlinT>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}T")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?,  >= 0> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return" false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(@blahonChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>KProperty<*> oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: ,, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

T

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: property: KProperty<*> >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(${property.name}, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) : Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property-> KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) (
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange{property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.inline} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public name fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: T, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<KProperty<*>>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun return@blah false() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                main
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (Unit) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> newValue < 0
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { T, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: property, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        beforeChange
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun }(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValueT T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: :, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        T = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: onChange) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: TObservableProperty<T>(initialValue) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

T
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: import kotlin.properties.*, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

T
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<import kotlin.properties.*>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import T.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: kotlin) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

blah
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@import kotlin.properties.* true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun {() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) main
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    *,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<initialValue: T>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*initialValue, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(>) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.:(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (propertyblah KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = (property: KProperty<*>, oldValue: T, newValue: T)
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        } -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, += ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah newValue 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<AnyT, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: ?): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var {
        return validate(property, oldValue, newValue)
    }: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean blah

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        <*> = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KPropertyonChange, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                ,@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: Treturn newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return, true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>@blah oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    false,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = (
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
) property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah{: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${<*>} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KPropertyproperty.name, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            }
        }
    )

    blah += 1
//    blah = blah + 1
println("onChange: ${property.name} from $oldValue to $newValue")

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange T { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: =, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { KProperty ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: property, oldValue, newValue<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    KProperty<*>

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: ), oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (("validation failed: ${property.name} >= 0") < 0) {
                printlnnewValue
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.property.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(properties, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    crossinline blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    var validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    property
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(>validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T" = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (ObservableProperty<T>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : property: KProperty<*>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        onChange = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        initialValue = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<Any>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<*?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: KProperty<*>, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: T, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { propertynewValue < 0 oldValue, newValue ->
            if (,) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty,T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue< newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0> {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T)(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < newValue) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, 0: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: newValue,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@T true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: blah, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $public")
        }
    )

    blah += 1
//    blah = blah + 1
}

newValue inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    "onChange: ${property.name} from $oldValue to $newValue",
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

newValue main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, fun)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(newValue)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, "validation failed: ${property.name} >= 0")
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to ObservableProperty<T>(initialValue)")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : $newValue {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $initialValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(newValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(ObservableProperty<T>(initialValue))
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : "onChange: ${property.name} from $oldValue to $newValue" {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    <*> += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KPropertyblah, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    T += 1
//    blah = blah + 1
}

public inline fun <blah> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.:.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValueproperties T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.false

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah KProperty
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}property")
                return@blah false
            }
            return@blah true
        },
        onChange = {  >= 0, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

oldValue: T main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, fun, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

newValue inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, public)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValueproperty
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(), oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: ,<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: TKProperty newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(fun)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline "validation failed: ${property.name} >= 0" <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import Int.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: kotlin by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to T")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: $newValue, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

(property: KProperty<*>, oldValue: T, newValue: T)

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
} -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                blah("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    println += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(@validation failed: ${property.name} >= 0")
                return"blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    oldValue onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, crossinline, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: newValue) {
        return onChange(property, oldValue, T)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange } { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        =
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                )("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
println: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        Delegates = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> initialValue.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: newValue: T -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, (property: KProperty<*>, oldValue: T, newValue: T)) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: T,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (property, oldValue, newValue)newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<Tvalidate(property, oldValue, newValue)(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return >
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import onChange.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline kotlin.reflect: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue crossinline 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    < onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): override {
        return validate(property, oldValue, newValue)
    }

    Boolean fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validateproperty
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange((property, oldValue, newValue), oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun (afterChangeproperty: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <{
        return validate(property, oldValue, newValue)
    }> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean T

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?T T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: ,): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: Toverride
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    , fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import }.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    kotlin.reflect

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        ) = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValueinitialValue {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    KProperty

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += T
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: 1, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<newValue>, oldValue: T, *: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { propertyvalidate(property, oldValue, newValue) oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return ,
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main("onChange: ${property.name} from $oldValue to $newValue" {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println())
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

oldValue main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, fun, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, :> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(propertyT KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<TT(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: >, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue:$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue to  T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValueproperty T, newValue: T): Boolean {
        return validate(:, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int newValue

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    ))
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
                return@blah false
            }
            return@blah true
        },
        println("validation failed: ${property.name} >= 0")
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue newValue: T
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, ->) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, <> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KPropertyT*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import ObservableProperty<T>.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : kotlin(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.T} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: name, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return" true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue@blah)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (,) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }newValue < 0
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}Unit")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) ->  >= 0
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: : by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValueInt T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.onChange.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return reflect(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        += = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah validate 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*{
        return onChange(property, oldValue, newValue)
    }, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) >

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> oldValue: T.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, Delegates, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, "validation failed: ${property.name} >= 0", newValue ->
            if (newValue < 0) {
                println(oldValue)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.Unit.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> properties
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                return("validation failed: ${property.name} >= 0")
                return@blah false
            }
            println@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> property: KProperty<*>.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(Delegates, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blahT
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: () -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.(property: KProperty<*>, oldValue: T, newValue: T) -> Unit.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: properties
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue: T) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue < 0) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue println("validation failed: ${property.name} >= 0") 0) {
                <
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, Boolean ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> newValue,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, {) newValue: T
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: Boolean
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): (property: KProperty<*>, oldValue: T, newValue: T) -> Unit {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue KProperty<*>
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: ->, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KPropertyKProperty*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: <<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        newValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, initialValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.->} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) name Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import T.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, kotlin> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $:")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(propertynewValue KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@* true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<blah>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue override
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    -> fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return<Any?, T>blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty@ = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    Delegates

    blah += 1
//    blah = blah + 1
}

public inline fun <T> ).blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.onChange(property, oldValue, newValue)(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return blah
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> property.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(Delegates, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: (, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>TinitialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue property 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(<, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            newValue
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $return@blah true")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<newValue>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, *)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import oldValue.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, kotlin: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, <, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KPropertyoldValue: T*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed:  from property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name}${$oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = blah
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue<)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty"*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                returnonChange false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return @blah(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += {
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) 1

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

:

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValuefun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
} T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import KProperty<*>.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: kotlin, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("<${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KPropertyonChange: *>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.=.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> reflect object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue,  to  ->
            println("onChange: ${property.name} from $oldValuenewValue$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, ,)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValuenewValue newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(TonChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<">(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from T to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: $oldValue, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline Unit: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> validate
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { (property: KProperty<*>, oldValue: T, newValue: T) ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: property, oldValue, newValue -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: }, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

KProperty<*>
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.property.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { reflect, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(KProperty: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: property<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): Any?<ReadWriteProperty, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun *() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<main>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<T>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: *) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>ReadWriteProperty oldValue: T, newValue: T) -> Unit
): ,<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate * { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<=>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return$ false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to @blahnewValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> ,.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> BooleanDelegates
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    ObservableProperty<T> validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : crossinline(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("(${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blahonChange: 
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(fun) {

    override initialValue beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by newValue.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, Delegates ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

KProperty main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: fun<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline KProperty<*> <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: fun, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $Unit")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> newValue
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, onChange> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return T(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            )
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
println("onChange: ${property.name} from $oldValue to $newValue"): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, T: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: oldValue, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }T
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: ,) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate T { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: =, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, () -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validatenewValue: Tproperty, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange>property: KProperty<*(, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: Boolean) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): T {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<validate(property, oldValue, newValue)>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return T
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValueproperty T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(:, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = {
        return validate(property, oldValue, newValue)
    },
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean 0

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<newValue: T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(:onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property" KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (initialValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(newValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty{*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) <

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("(${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChangevalidation failed: property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue ReadWriteProperty<Any?, T> 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): < = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) )
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: <*>property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty${, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        oldValue = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, validate: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty,T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property< oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KPropertyoldValue, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, <*>, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: newValueproperty: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, ()
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, ,, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValueoldValue newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(return onChange(property, oldValue, newValue))
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        "validation failed: ${property.name} >= 0"
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("${onChange: property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import (.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: kotlin.reflectproperty: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    * fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<override>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: ?, newValue: T) -> Unit
): ReadWriteProperty<AnyT, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah) Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue:
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.crossinline

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    KProperty onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: result) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: UInt) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, init_o, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> get
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Long,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.toShort

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $String to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Int,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y½// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : String(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $x")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Int {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> test.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: String) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    enum validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, reversed ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<String>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 23,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yß// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(try { zap(string) } catch (e: Exception) { "" })
    }

}
y\// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { Z(42) }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $OK")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y™// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (Str) -> R,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, r)
    }

}
y±// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : Result() {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $String")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: Int, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: string, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    inline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            box("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y½// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : box<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        List = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y·// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return AImpl("fail: $z")
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $sb")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(actual, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                test2()
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yæ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
    val result = (A::Inner)((::A)(), 111).result + (A::Inner)(A(), 222).result
    if (result != 333) return "Fail $result"
    return "OK"
}
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${a.size} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, other: Int) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: String, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, val s: String) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y·// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(S("OK"))
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: Int, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): String<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $Float to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÂ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, i) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    result: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return A(property, oldValue, newValue)
    }

}
y£// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (p:String) -> String,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun AsAny(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(bar: Bar.() -> Unit, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yº// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : Int(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: Int,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : z<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return x(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@A true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<C>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÖ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
    var s = "11test"
    return if (++s == "done") "OK" else "FAIL"
}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yœ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: () -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, a5: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(test1, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: Array<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÂ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        my = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@Foo false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("OK${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.String.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÚ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: Short.Companion) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yv// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
    outer()
    return "OK"
}
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yV// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = {
        var x = 0L
        val y = x[0]++
        return if (y == 0L) "OK" else "Failed, y=$y"
    },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÔ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

fun box(): String {
    if (Test().test() != 1) return "inner getter or setter failed"
    return "OK"
}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, String, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${b.a.pp} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: foo,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, foo)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.T.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    a += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    operator onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.calc(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<T>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y˜// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: String
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: s<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    in validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<String>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: Int): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<Int?>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> result.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${Long.MAX_VALUE} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÓ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, runBlock> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return toDouble(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yº// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import from.i

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<bar?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    open fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> KProperty<*>.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${String.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÐ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: value, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: ULong by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÒ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: builder,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline String: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÂ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    k : K,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y·// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            {
    return (Base::result).get(Derived())
}
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}O")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, a: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $s to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        a = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, String> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// KJS_WITH_FULL_RUNTIME
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): x {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, x: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<String>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: String, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: e) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $list1")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: Long) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
    B().foo
    C().foo

    D().foo
    E().foo
    F().foo
    G().foo
    H().foo
    I().foo
    J().foo
    K().foo

    return "OK"
}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    data onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> box,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name}OK$oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (list3.size > 23) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: String) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Float {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: result, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (public val x: String, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÖ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
            sideEffects += outerProp + "#" + prop + "first"
        }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (TestClass: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, i ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, "OK")
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <prop> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y•// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: String,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import A("Fail").B("OK").testB()

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: foo<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        PREFIX = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<map?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        Delegate = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    abstract validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(b, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¤// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {}
y¦// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("Z1")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: xx, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¬// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : Tr<Int> {
        override val prop = 42
    }
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Int
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: B, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { String, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<String, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (Int: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<arr>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, n: N, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { call, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValuefail$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// KJS_WITH_FULL_RUNTIME

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (T: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, 240, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: Int) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (c: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.UIntArray} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, box: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, String: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÐ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { val value: T, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, x)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    val x: Int,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yr// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) throw Exception("$a != $b")
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {
    open fun x() : Int = 10
}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: d<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValueOK$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@N true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline Byte: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yO// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    fun useAny(x: Any) {}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: Int, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, star(), newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yø// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    return foo(B())
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<foo?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    run += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÒ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $AssertionError to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.Box} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(42, 42) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.result

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, block: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: a by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(width, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y½// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (x: Any?, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, "q")
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        String = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÓ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<TInput, TValue> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : t<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (other: Int = 5, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÐ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: value) {
        return onChange(property, oldValue, newValue)
    }

}
y°// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange()
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun containsKey(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: Any, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: MaxUI<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, A, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, Any, newValue)
    }

}
y£// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                x(a, "OK")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y°// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return { ok() }()
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÕ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(::createContainer, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y˜// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(null)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yq// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = {
            i = 20
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.Inner(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y^// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = {x -> C(x)}
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : AbstractIterator<String>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, l, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 0
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yG// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

// KJS_WITH_FULL_RUNTIME
fun box(): String {
    val sb = StringBuilder("OK")
    return "${sb.get(0)}${sb[1]}"
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, "OK", newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("4, 1, 2, 3${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import String.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(String, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(size: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${foo()} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¹// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                {
    object : Test {

    }
}()
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun Outer(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: array2, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $index")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Int.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y›// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(1, 2, 3)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.Int(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: String,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, i: Int) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yß// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
    val test = TestClass()
    val ok = "OK"

    val x = test { return ok }
}

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, B)
    }

}
yf// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) return "fail 5"
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(5, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var String: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name}OK")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¼// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange((a, b), oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(result, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(messages) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<ULong> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                returnfoo@ false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yù// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
// This test just checks that no internal error happens in backend
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { box, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>() {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yw// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (result != "fromC_OK") return "fail 1: $result"
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@box true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): String {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y]// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) result
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(String, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> tt.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${Int.MAX_VALUE} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, A, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 1) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y˜// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("OK")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yá// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    fun box(): String {
    val test1 = TestClass { "K" }
    if (test1 != "K") return "fail1, 'test1' == $test1"

    val ok = "OK"

    val x = TestClass { return ok }
}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y›// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: ()-> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÒ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<Boolean>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¼// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import l.add(1)

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@My false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> count
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: toLong, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, String)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return "OK"
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValuefail 1$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yã// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
    when(En.A) {
        En.A -> "s1"
        En.B -> "s2"
    }
    return "OK"
}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    inline fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y‹// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    fun boom(a: Any) {
    when (a) {
        is B -> run(a::magic)
    }
}

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    Throwable += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (i2 > i1) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yV// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    val r = Subclass().getKindValue() + Subclass().kind
    return if(r == "Subclass Subclass ") "OK" else "fail"
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@loop false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> box
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: String<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, T: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<a?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: String): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import 6.valThisRef2().KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y		// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    fun box(): String {
    val ax: Short? = 0.toShort()
    val an: Short? = null
    val bx: Short = 0.toShort()
    val by: Short = 1.toShort()

    return when {
        0.toShort() != nx -> "Fail 0"
        1.toShort() == nx -> "Fail 1"
        !(0.toShort() == nx) -> "Fail 2"
        !(1.toShort() != nx) -> "Fail 3"
        x != nx -> "Fail 4"
        y == nx -> "Fail 5"
        !(x == nx) -> "Fail 6"
        !(y != nx) -> "Fail 7"
        0.toShort() == nn -> "Fail 8"
        !(0.toShort() != nn) -> "Fail 9"
        x == nn -> "Fail 10"
        !(x != nn) -> "Fail 11"
        0.toShort() != ax -> "Fail 12"
        1.toShort() == ax -> "Fail 13"
        !(0.toShort() == ax) -> "Fail 14"
        !(1.toShort() != ax) -> "Fail 15"
        x != ax -> "Fail 16"
        y == ax -> "Fail 17"
        !(x == ax) -> "Fail 18"
        !(y != ax) -> "Fail 19"
        bx != ax -> "Fail 20"
        by == ax -> "Fail 21"
        !(bx == ax) -> "Fail 22"
        !(by != ax) -> "Fail 23"
        0.toShort() == an -> "Fail 24"
        !(0.toShort() != an) -> "Fail 25"
        x == an -> "Fail 26"
        !(x != an) -> "Fail 27"
        bx == an -> "Fail 28"
        !(bx != an) -> "Fail 29"
        else -> "OK"
    }
}

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue as? 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yº// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println((MinS + 5).toInt(), (MinS + 2).toInt())
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return xs(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@C true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { getValue, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÐ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, "don't care")
    }

}
yª// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return "OK"
    }

}
yÒ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
    val t = Derived(arrayOf("", "", "", ""))
    t.iterate()
    return if (t.test == 1234) "OK" else "Fail: ${t.test}"
}
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (foo() == 4L) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yº// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(a: T, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yœ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange() {
        return onChange(property, oldValue, newValue)
    }

}
y)// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var list = ArrayList<Int>()

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        s = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>("fail") {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): bByte<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÖ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@AWrapperFabric true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    Float += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: this) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import s.toString()

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    B: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        NestedFoo = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÙ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { private val s: String, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y£// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(2)
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = {
            this@Foo.s
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, w, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(z) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y•// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(1)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("fail${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (val x: Int, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> String.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¹// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            {
    return A(ClassDescriptorImpl()).result
}
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(A()) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¿// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : super<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÒ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: valProp,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import A().foo().*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $result to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÍ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: IFoo by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÔ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${COMPARABLE_RANGE.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÑ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (n.get(0) == "239") {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${ICLong.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¦// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("OK")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(FOO: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<B?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÎ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (FloatArray: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (s < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline s: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÂ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun T(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÏ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { val i : Int, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(val x: Int, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: Int, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return foo(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    task: () -> T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

override fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, Int: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline A: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÐ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: cycle,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<*, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { a: Any, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : toByte<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: A, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÌ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
    initialValue: T,

public inline fun <T> Delegates.blah(
}
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>,oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $ to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yl// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}


yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun () {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah  1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: , oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                @blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public  fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

     fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y’// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: , newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: , newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@ false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: <*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: ) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
importkotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> ,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline : (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah:Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, > = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinlinevalidate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÂ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin..*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { , oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue newValue)
    }

}
y|// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue <0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = ,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
             (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> =object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlinreflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if ( < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¼// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed:  >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue,newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: , newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: ): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, : T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

 inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println"onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T)-> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline : (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yš// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate:  -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property,oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> .blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue:T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: ,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, , newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T,newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property:KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: <*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yš// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange {
        return onChange(property, oldValue, newValue)
    }

}
y™// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah+= 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, , newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue)
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) 
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yá// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() 

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

publicinline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, : T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        returnvalidate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return (property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah 
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yH// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T)-> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yy// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    ,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T):  {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yu// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = 
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, ): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange:(property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name}$oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¶// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: ) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¢// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println()
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yº// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : <T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y·// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1

}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object: ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { , oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: <*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y // Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
         = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    overridefun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, )
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean 
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¾// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T> {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
     blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import .reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y-// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah 
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>,oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by .blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*> oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

     += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y·// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : (initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y’// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: 
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        }
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KPropertyfun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y”// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println()
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var : Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, ) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue:T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue,  ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return (property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, , newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: ,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue< 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegatesblah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if () {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): <Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: , oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue:T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue$newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: <*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¢// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) 
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, : T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>,oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, )
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*> oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> 
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Intby Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¿// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    varblah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWritePropertyAny?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if(newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        returnonChange(property, oldValue, newValue)
    }

}
yŸ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${propertyname} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by 

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import .properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y)// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¿// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

 fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yš// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange:  -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int byDelegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÃ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")}
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue 
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
         = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) ->Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue,  ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T>  object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override  beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, , newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
     validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, , newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange  { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
: ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¿// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) ->Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÁ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, ) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) 

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y©// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object :  {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChangeproperty: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÀ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
         = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y»// Original bug: KT-14833
// Duplicated bug: KT-14833

import .*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y°// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = {  ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T>Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: , newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflectKProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T){
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChangeproperty, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T,newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inlinefun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0"
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yŸ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T)  Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any? T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

funmain() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÈ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue  0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println(onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÊ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int  Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÉ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>,oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChangeproperty: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÅ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin..KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÆ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            @blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÄ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, : T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println(validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T: Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y´// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import 

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y±// Original bug: KT-14833


import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y‹// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¹// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue:T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yW// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) 
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y³
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property:KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¿// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override funafterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        })

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yÇ// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)}

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y½// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    ,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int 

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
y¦// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return 
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main(){
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() 
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlinproperties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }

}
yË// Original bug: KT-14833
// Duplicated bug: KT-14833

import kotlin.properties.*
import kotlin.reflect.KProperty

fun main() {
    var blah: Int by Delegates.blah(
        initialValue = 0,
        validate = { property, oldValue, newValue ->
            if (newValue < 0) {
                println("validation failed: ${property.name} >= 0")
                return@blah false
            }
            return@blah true
        },
        onChange = { property, oldValue, newValue ->
            println("onChange: ${property.name} from $oldValue to $newValue")
        }
    )

    blah += 1
//    blah = blah + 1
}

public inline fun <T> Delegates.blah(
    initialValue: T,
    crossinline validate: (property: KProperty<*>, oldValue: T, newValue: T) -> Boolean,
    crossinline onChange: (property: KProperty<*>, oldValue: T, newValue: T) -> Unit
): ReadWriteProperty<Any?, T> = object : ObservableProperty<T>(initialValue) {

    override fun beforeChange(property: KProperty<*>, oldValue: T, newValue: T): Boolean {
        return validate(property, oldValue, newValue)
    }

    override fun afterChange(property: KProperty<*>, oldValue: T, newValue: T) {
        return onChange(property, oldValue, newValue)
    }


ÿÿ