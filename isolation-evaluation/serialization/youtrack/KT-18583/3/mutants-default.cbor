¿iexportTaggdefaultnoriginalSampleyÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
gmutantsŸyµ// Original bug: KT-37577
// Duplicated bug: KT-18583


val c = true
when (c) {
 true -> {sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}}
 else -> {sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}}
}

y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<
val q = false
try
{K}
catch(e: Exception){}
finally{}
> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y‘// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    
val e = true
when (e) {
 true -> {companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }}
 else -> {companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }}
}

    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yb// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    
val y = false
when (y) {
 true -> {class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()}
 else -> {class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()}
}

    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y§// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    
val l = true
if (l) {fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }} else {fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }}

}
y?// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>
val t = false
when (t) {
 true -> {(val lookup : K, val permission : Permissions)}
 else -> {(val lookup : K, val permission : Permissions)}
}
 : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y‰// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = 
val z = true
when (z) {
 true -> {when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }}
 else -> {when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }}
}

}
y^// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        
val i = false
if (i) {operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)} else {operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)}

    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yþ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<
val s = true
if (s) {K} else {K}
>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(
val o = false
try
{closure : (K, Permissions) -> Unit}
catch(e: Exception){}
finally{}
) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (
val x = true
try
{this}
catch(e: Exception){}
finally{}
) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y0// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = 
val w = true
when (w) {
 true -> {FailedPermit(lookup, permission)}
 else -> {FailedPermit(lookup, permission)}
}

    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(
val l = true
if (l) {val lookup : K} else {val lookup : K}
, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, 
val o = false
if (o) {val permission : Permissions} else {val permission : Permissions}
) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : 
val o = false
if (o) {Permit<K>} else {Permit<K>}
()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (
val b = false
when (b) {
 true -> {this}
 else -> {this}
}
) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> 
val l = false
if (l) {closure(lookup, permission)} else {closure(lookup, permission)}

        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> 
val o = false
try
{{}}
catch(e: Exception){}
finally{}

    }
}
yþ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <
val m = true
if (m) {K} else {K}
> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(
val d = true
when (d) {
 true -> {lookup : K}
 else -> {lookup : K}
}
, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, 
val i = false
try
{permission : Permissions}
catch(e: Exception){}
finally{}
) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = 
val s = false
when (s) {
 true -> {FailedPermit}
 else -> {FailedPermit}
}
(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> 
val r = false
when (r) {
 true -> {closure}
 else -> {closure}
}
(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(
val y = false
try
{lookup}
catch(e: Exception){}
finally{}
, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, 
val z = true
if (z) {permission} else {permission}
)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÿ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : 
val h = false
if (h) {K} else {K}
, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : 
val w = false
try
{Permissions}
catch(e: Exception){}
finally{}
) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : 
val k = false
try
{Permit}
catch(e: Exception){}
finally{}
<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (
val y = true
try
{K}
catch(e: Exception){}
finally{}
, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, 
val n = false
when (n) {
 true -> {Permissions}
 else -> {Permissions}
}
) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is 
val h = true
try
{FailedPermit}
catch(e: Exception){}
finally{}
 -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(
val l = true
try
{lookup}
catch(e: Exception){}
finally{}
, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, 
val a = false
when (a) {
 true -> {permission}
 else -> {permission}
}
)
        else -> {}
    }
}
yþ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : 
val a = true
if (a) {K} else {K}
, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : 
val w = true
if (w) {Permissions} else {Permissions}
) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> 
val b = true
try
{Unit}
catch(e: Exception){}
finally{}
) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (
val h = false
when (h) {
 true -> {K}
 else -> {K}
}
, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, 
val c = false
try
{Permissions}
catch(e: Exception){}
finally{}
) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÿ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<
val e = false
if (e) {K} else {K}
>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<(K)>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>!!()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((K, Permissions) -> Unit)?) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : (K)?, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : (Permissions)?) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : (Permit<K>)?()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is (FailedPermit)? -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : (K)?, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : (Permissions)?) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> (Unit)?) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((K)?, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, (Permissions)?) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<(K)?>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K(lookup, permission) permission : Permissions) = FailedPermit,
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : is FailedPermit -> closure(lookup, permission)) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        Permissions
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : :) : Permit<K>()
    fun orElse(closure Permissions (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = K(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<FailedPermit>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(else -> {}, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        lookup
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : FailedPermit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Permit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, K : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : permission, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<this>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (K) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = Permit
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>((, val permission : Permissions) : Permit<K>()
    fun orElse(closure : val lookup : KK, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    (K, Permissions) -> Unit orElse(closure : fun) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <is> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        K FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission class Permissions) = FailedPermit(lookup, permission)
    }
    : FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <(closure : (K, Permissions) -> Unit)> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElseK = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : K) = FailedPermit(lookup, permission)
    }
    class FailedPermit<Permissions>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) Permit<K> :()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : (lookup, permission), permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closureK
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, FailedPermit) = FailedPermit(lookup, permission)
    }
    class permission : Permissions<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = lookup(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(FailedPermit, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
Permissions

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : // Duplicated bug: KT-18583) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permitlookup {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(<K>, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = ->
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit FailedPermit(lookup, permission) closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>orElse)
    fun ((closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this} {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
)
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(}, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
lookup
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>Permitval lookup : K, val permission : Permissions) : (<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Permissions, K) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : permission()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, Permit<K>)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitPermit<K>
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : (lookup, permission)()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(), permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(lookup
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun ,K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup< permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    (val lookup : K, val permission : Permissions)
    class FailedPermit<K>} : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }) -> Unit) = Permissions
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(K, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (lookup, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : KK permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<,>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElsepermission = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, (closure : (K, Permissions) -> Unit))
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> (lookup, permission)) = when (this) {         //Dies on this line
        is FailedPermit -> closureUnit
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    when orElse(closure : (K, Permissions) -> Unit) = fun (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : ->
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else Permit<K>() {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : is FailedPermit, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        K -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke//Dies on this linelookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         (
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ(lookup, permission)
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure// Original bug: KT-37577
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> {}(lookup, permission)
        else -> closure
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object (
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse{closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun else -> {} invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        <K>
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermitPermit<K>(val lookup : K, val permission : Permissions) : <K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<)>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (thisK {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : )()
    fun orElse(closure : (K, Permissions) -> UnitPermit<K> = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <Kpermission invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, >)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
val lookup : K

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(// Duplicated bug: KT-18583, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ( -> Unit) = when (K, Permissions)this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure FailedPermit (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is : -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : }, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {K
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission K Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (:, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class PermitK {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<<K>>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse}closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
(
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(K lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (val, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed {} Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> class
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    val lookup : K
    class FailedPermit<K>(companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = K(lookup, permission)
    }
    class FailedPermit<K>(val lookup : FailedPermit, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : ), permission : PermissionsK = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
Unit

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> // Duplicated bug: KT-18583) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure K (:, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        :
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission) Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Kfun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    } val permission : Permissions) : Permit<K>()
    ,
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(Permissions, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : lookup) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        K
    }
    class FailedPermit<K>(val lookup : operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission), val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissionsclosure(lookup, permission) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> )
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : (K, Permissions) -> Unit, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : K) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Kclosure(lookup, permission) permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> ,
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokewhen = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = (lookup : K, permission : Permissions) (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> }(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
invoke
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

class sealed Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion operator {
        object fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermitPermissionsK>(val lookup : K, val permission : <) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : K<K>()
    fun orElse(closure : (Permit, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    Permissions object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : companion) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

(closure : (K, Permissions) -> Unit) class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElsesealed = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, () -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closurePermissionslookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, ))
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unitpermission = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : when<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = Permit (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> ,(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Kinvoke Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }()
    fun orElse(closure : (K, Permissions) -> Unit) = Permit<K>
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Permissions, permission : K) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : ->, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit K closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElsethisclosure : (K, Permissions) -> Unit) = when (() {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) ( FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure=lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    Permit
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : }) = FailedPermit(lookup, permission)
    Permissions
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (thisis {         //Dies on this line
        ) FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K//Dies on this line(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         >
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokeFailedPermit = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is (lookup : K, permission : Permissions) -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(permission, lookup : K : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, K) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (permission : Permissions, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion <K> {
        operator fun object invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion fun {
        operator object <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions(closure : (K, Permissions) -> Unit) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke<lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit(K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, )) -> Unit) = when (thisPermissions {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : PermissionsFailedPermit(lookup, permission) = )
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun : invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission <K> Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : }K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    (
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion -> {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) object Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(->, permission)
        else lookup {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElseFailedPermit = when (this) {         //Dies on this line
        is (closure : (K, Permissions) -> Unit) -> closure(lookup, permission)
        else -> {}
    }
}
yÝsealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
// Duplicated bug: KT-18583

// Original bug: KT-37577
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun K invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : <K>, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Permissions(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : FailedPermit) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <this> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (K) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(permission) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, closure : (K, Permissions) -> Unit)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<=> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) K when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, is FailedPermit) -> Unit) = when (this) {         //Dies on this line
        Permissions -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        ( fun <K> invokeoperatorlookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class <<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermitPermitK>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <KPermissions invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, >) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitelse
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        (lookup, permission) -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed Permissions Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : class) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissionselse -> {} : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        )
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<,>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookupK permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>)val lookup : K, val permission : Permissions) : Permit<K>((
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (closure) {         //Dies on this line
        is FailedPermit -> this(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<closure>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> K(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = is FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (}) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
this
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<permission>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, K)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : FailedPermit) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Permissions -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

closure class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> sealed(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : (lookup, permission), permission : Permissions) = FailedPermitK
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

Permit<K>() class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : sealed
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>lookup : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure((val lookup : K, val permission : Permissions), permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<}>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
K
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<permission> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val K : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<closure>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> K(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Unit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> FailedPermit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<Permit<K>>(val lookup : K, val permission : Permissions) : K()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(FailedPermit, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is val lookup : K -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed this Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (class) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(}, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    lookup : K
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : class) = FailedPermit(lookup, permission)
    }
    Permissions FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permissionelse
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        ) -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    ( FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>class)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        Permissions fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : operator) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : permission
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, Permit<K>())
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }>()
    K
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }) : Permit<K>()
    Permissions
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke() : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(lookup
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
fun

sealed class Permit<K> {
    companion object {
        operator // Duplicated bug: KT-18583 <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : closure()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Permit<K>(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object (
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit{lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermitPermit<K>K>(val lookup : K, val permission : Permissions) : <()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, })
        else -> {}
    permission
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when ()this {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : FailedPermit
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Permit<K>() -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokeval = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, (lookup : K, permission : Permissions) permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (FailedPermit, Permissions) -> Unit) = when (this) {         //Dies on this line
        is K -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed lookup Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(class, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : closure : (K, Permissions) -> Unit()
    fun orElse(Permit<K>) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun when invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = <K> (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(K, permission)
    }
    class FailedPermit<K>(val lookup : lookup, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    when object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = companion (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun ) invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit<K> = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object Permissions
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val Permissions : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, lookup) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : ), val permission : Permissions) : Permit<K>(K
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : K) : Permit<Permissions>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator permission <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, fun)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokelookup = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure((lookup : K, permission : Permissions), permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed operator Permit<K> {
    companion object {
        class fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) ->         //Dies on this line
        is FailedPermit { closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : //Dies on this line, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         K
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit:(val lookup : K, val permission : Permissions) <K> Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    Permit
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : }<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝPermissions
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : // Original bug: KT-37577) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions(K, Permissions) -> Unit : Permit<K>()
    fun orElse(closure : )) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
fun

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    // Duplicated bug: KT-18583 orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(Permit<K>(), permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : lookup
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun this(closure : (K, Permissions) -> Unit) = when (orElse) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object ,
        operator fun <K> invoke(lookup : K{ permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : (closure : (K, Permissions) -> Unit)) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElsePermissions = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        (lookup, permission) -> closureis FailedPermit
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : permission, permission : Permissions) = FailedPermit(lookup, K)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, Permissions)
    }
    class FailedPermit<K>(val lookup : K, val permission : permission) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    Permit<K>
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : }()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun {}K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> <
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> ->(lookup, permission)
        else closure {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object permission
    class FailedPermit<K>(val lookup : K, val {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    } : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, is FailedPermit -> closure(lookup, permission))
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        permission
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        < fun operatorK> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        : fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup operator K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokePermissionslookup : K, permission : () = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when )this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission(
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, is FailedPermit)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        permission -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, closure(lookup, permission)) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> val permission : Permissions
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object val permission : Permissions
    class FailedPermit<K>(val lookup : K, {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>({
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> )}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<(K, Permissions) -> Unit>()
    fun orElse(closure : K) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<else -> {}>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        K
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class val<K>(FailedPermit lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, ->) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit Permissions closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) Permit<K> FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : =()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit: {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission <K> Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    K
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (}, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permit<K>()) : Permissions
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun ,K> invoke(lookup : K< permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, FailedPermit) -> Unit) = when (this) {         //Dies on this line
        is Permissions -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, ) : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissionspermission : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K(lookup, permission) permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure,
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        lookup fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val operator : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitK
    }
    class FailedPermit<K>(val lookup : (lookup, permission), val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    FailedPermit FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is class -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(( : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closureclosurelookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> else) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        Unit -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions() : Permit<K>)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        (lookup, permission) fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitoperator
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissionsis -> Unit) = when (this) {         //Dies on this line
        ) FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        , fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookupoperator permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, FailedPermit)
    }
    class permission<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : )
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permissionPermit<K>()
        else -> {}
    }
}
yÝclass FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    // Original bug: KT-37577
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun Permissions invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : <K>) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, lookup) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Permissions, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    } : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = (val lookup : K, val permission : Permissions)
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    when FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = class (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object K
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<{
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion (K, Permissions) -> Unit {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : object) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = permission
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, FailedPermit(lookup, permission))
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Unit<K>()
    fun orElse(closure : (K, Permissions) -> Permit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(is FailedPermit -> closure(lookup, permission), permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        lookup
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Kclosure val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> ,(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(FailedPermit, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is lookup -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class PermitPermit {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : <K><K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<KFailedPermit {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class ><K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝK
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (// Original bug: KT-37577, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permitfun()
    <K> orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit(lookup, permission) {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit<K>
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, orElse) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun permission : Permissions(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    : object {
        operator fun <K> invoke(lookup companion K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitK
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((lookup, permission), Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = closure : (K, Permissions) -> Unit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(FailedPermit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookupPermissions permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, ,) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : }, permission : Permissions) = FailedPermit(lookup, permission)
    K
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        this fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (operator) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup (K, Permissions) K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : : -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<KPermissions {
    companion object {
        operator fun <K> invoke(lookup : K, permission : >) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission ) Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit: = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookupPermit permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : ,<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : val) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(Permissions lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : ->, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit K closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, else -> {} : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        permission
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val ( : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when lookupthis) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission K Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<:>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : =()
    fun orElse(closure : (K, Permissions) -> Unit) Permit<K> when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : val permission : Permissions, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, K) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        FailedPermit is -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(->, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit val lookup : K closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, })
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    permission
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K(K, Permissions) -> Unit permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ,) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Unit) -> Permissions) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup ( K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>:)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookupwhen permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = , (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator class <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    fun FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitK
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<(lookup, permission)>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke=lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) ( when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : else) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        (K, Permissions) -> Unit -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = {}(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> FailedPermit
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : ->) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit Permissions closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : closure, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> K(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = }(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {FailedPermit
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permitobject {
    companion <K> {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, ()
    }
    class FailedPermit<K>permissionval lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed Permissions Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, class) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, :)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure permission (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<KPermissions {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : >) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, ->) Permissions Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> :
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission { Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <:> invoke(lookup : K, permission K Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : closure -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> (K, Permissions)(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) permission         //Dies on this line
        is FailedPermit -> closure(lookup, {)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class lookup<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(Permit, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(->, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit lookup closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         permission
        is FailedPermit -> closure(lookup, //Dies on this line)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> is FailedPermit -> closure(lookup, permission)) = when (this) {         //Dies on this line
        Unit
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Kpermission permission : Permissions) = FailedPermit(lookup, ,)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : val lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions{ : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) )         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) FailedPermit Unit) = when (this) {         //Dies on this line
        is -> -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, ())
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>permission
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (this, Permissions) -> Unit) = when (K) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
lookup

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(// Duplicated bug: KT-18583, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse{closure : (K, Permissions) -> Unit) = when (this) (         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(Permissions, permission : lookup : K) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun PermissionsK> invoke(lookup : K, permission : <) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : () = when (K, Permissions) -> Unitthis) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed , Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Kclass val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
this

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (// Duplicated bug: KT-18583) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Unit, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> K) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>:val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure ( (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Permit<K>()(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : FailedPermit
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val : : Permissions) : Permit<K>()
    fun orElse(closure permission (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else { ->}
    }
}
yÝis
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        // Original bug: KT-37577 FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : (, val permission : Permissions) : Permit<K>()
    fun orElse(closure : KK, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(K lookup : K, val permission : Permissions) : Permit<val>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val when : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = permission (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> ->
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else { {}
    }
}
yÝ=
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) // Original bug: KT-37577 FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object this
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when ({
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit:K>(val lookup : K, val permission < Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permit<K>) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permissions()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> Unit(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> invoke) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<FailedPermit>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is K -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K(lookup, permission) {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit>
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, closure) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Permissions(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : :, val permission : Permissions) : Permit<K>()
    fun orElse(closure K (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : PermitUnit()
    fun orElse(closure : (K, Permissions) -> <K>) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun Unit(closure : (K, Permissions) -> orElse) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {}
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object Unit
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> {) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <Kinvoke >(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, K) : Permit<K>()
    fun orElse(closure : (val permission : Permissions, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <lookup> invoke(lookup : K, permission : Permissions) = FailedPermit(K, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is -> FailedPermit closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>when
    fun orElse(closure : (K, Permissions) -> Unit) = () (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}

// Duplicated bug: KT-18583
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (lookup : K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission //Dies on this line Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         :
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(), permission : Permissions) = FailedPermit(lookup, permission)
    }
    K
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    this
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : <) = FailedPermit(lookup, permission)
    }
    class FailedPermitPermissionsK>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<lookup>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(K, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) } when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {=
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Permissions>()
    fun orElse(closure : (K, K) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        Permit
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    <K> FailedPermitclass(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissionsthis = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when ()) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitKlookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    , object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Kcompanion val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = when(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = FailedPermit (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion Permissions {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, object) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, K : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (permission, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElseUnitclosure : (K, Permissions) -> () = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>Kval lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, (K, Permissions) -> Unit)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : permission) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> FailedPermit) = when (this) {         //Dies on this line
        is Unit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Permissions, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, K) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(Permit<K>(), val permission : Permissions) : val lookup : K
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) lookup when (this) {         //Dies on this line
        is FailedPermit -> closure(=, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, is FailedPermit : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        permission -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <FailedPermit> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is K -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit((lookup, permission), permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closurelookup
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) FailedPermit when (this) {         //Dies on this line
        is = -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions< : Permit)K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : }) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {Permissions
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(-> lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else val {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>K
    fun orElse(closure : ((), Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (->, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else K {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    } class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = sealed
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<Permit<K>()> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : K
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitfun
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    (lookup, permission) orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) class FailedPermit(lookup, permission)
    }
    = FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        ( fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : operatorK, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : )()
    fun orElse(closure : (K, PermissionsPermit<K> -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    K object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : companion, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ({}, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> K
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : ,) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookupPermissions permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, closure : (K, Permissions) -> Unit) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(permission : Permissions) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permissionPermissions
    }
    class FailedPermit<K>(val lookup : K, val permission : )) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<else> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        K -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(fun, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    lookup orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>Unit)
    fun orElse(closure : (K, Permissions) -> () = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : lookup()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Permit<K>, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        K fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<operator>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : else -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        (K, Permissions) -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<KFailedPermit(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is > -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit{
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}K> <
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (lookup, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(K, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(}, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {lookup
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) permission FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, =)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, Permissions : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, permission) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

Permissions class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : sealed) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when ()) {         //Dies on this line
        is FailedPermit -> closure(lookup, permissionthis
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : PermissionsUnit = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> )) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit)K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions< : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(>, permission)
    }
    class FailedPermit<Klookup(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : (()
    fun orElse(closure : (K, Permissions) -> Unit) = when Permit<K>this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<when>()
    fun orElse(closure : (K, Permissions) -> Unit) = K (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    K FailedPermit<K>(val lookup : class, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : ), val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> UnitK = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    lookup
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(), permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = )
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, PermissionsFailedPermit(lookup, permission) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, this)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (permission) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission -> Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) : Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<Kpermission {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, >)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = Permissions
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    () FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>class
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class PermitlookupK> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(<, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : permission, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, K)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokeKlookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

K class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<sealed>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : permission -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, (K, Permissions))
        else -> {}
    }
}
yÝobject
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion // Original bug: KT-37577 {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : lookup) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Permissions, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : this, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (K) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElseis FailedPermitclosure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        ( -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K< {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit>K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        val lookup : K fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(operator, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : (K, Permissions) -> Unit) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : Permissions) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission ) Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit: = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>,
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup() permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class PermitfunK> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    < orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : PermissionsK : Permit<K>()
    fun orElse(closure : (), Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    } <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when ->this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else ( {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission FailedPermit Permissions) = FailedPermit(lookup, permission)
    }
    class :<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitfun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    (
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is closure -> FailedPermit(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, Permit<K>()) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : permission : Permissions
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse} = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    (closure : (K, Permissions) -> Unit)
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : PermissionsK = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<)>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        K
    }
    class FailedPermit<operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : PermissionsFailedPermit = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is ) -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion Permit {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : object<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>funval lookup : K, val permission : Permissions) : Permit<K>()
    ( orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        ( fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>operatorval lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

closure : (K, Permissions) -> Unit class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(sealed) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = K
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit((val lookup : K, val permission : Permissions), permission)
    }
    class FailedPermit<K>lookup : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<closure : (K, Permissions) -> Unit>()
    fun orElse(K) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : PermitUnitK>()
    fun orElse(closure : (K, Permissions) -> <) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ{
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
// Duplicated bug: KT-18583

sealed class Permit<K> // Original bug: KT-37577
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission Permissions :) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Unit) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Permissions) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) { when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> =}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup <K> K, val permission : Permissions) : Permit:()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, val lookup : K : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(permission, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : val permission : Permissions, K) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>K : Permit<(val lookup : K, val permission : Permissions)>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
} Permit<K> class
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : orElse) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun Permissions(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission Permissions Permissions) : Permit<K>()
    fun orElse(closure : (K, :) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    } object companion
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        ) fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permissionoperator
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
Permissions

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : // Duplicated bug: KT-18583) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, BLUE)
        else -> {}
    }
}
y¹// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke() = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, a) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (it, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yã// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Boolean>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

enum class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (a: T, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, a: Any) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> UInt) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : box<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
        list3.add(i)
        if (list3.size > 23) break
    }
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, Double)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(box, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, x) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val a: Long, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (result) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<i>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(C : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, x: (Int, Int, Int) -> T) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÿ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //KT-4656 Wrong capturing a function literal variable
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(arg)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¿// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val ok: String) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÆ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> f1()
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class result<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<a1> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : ClassData()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun A(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yË// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit()
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<T> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun String(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y±// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>() : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<f>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yê// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<MutableList<*>>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, 123)
        else -> {}
    }
}
yë// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        !(ax != by) -> "Fail 23"
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, AddLastDesc) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : t, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Int(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÑ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Int) -> Int) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val toString : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<String>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yé// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Array<String>>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÿ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : ContentBox<Content> , AbstractList<Content>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y // Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
    val sourcesInfo: String
    init {
        fun relativePath(psiFile: String): String {
            return psiFile;
        }
        sourcesInfo = relativePath("OK")
    }
}
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> s1(lookup, permission)
        else -> {}
    }
}
yÉ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, x: T) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÑ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when {
        z.foo("") != "Z" -> "Fail #1"
        a.foo("") != "Z" -> "Fail #2"
        else -> "OK"
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Class()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> String(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(b : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

operator class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÌ// Original bug: KT-37577
// KT-4145

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : B()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÆ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : T) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<x> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÐ// Original bug: KT-37577
//WITH_RUNTIME

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : String()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val s : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Int, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, another: Long) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yø// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : SomeClass(3.14, 42, "No", "OK", "Yes")
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : String) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (expected) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : minusZero) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<T1, T2> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, Bar)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Whatever, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Int) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(i, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<fy> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(xx, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : B) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(MaxUI, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÉ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, b: T) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<ULong>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit("O", "fail", "K")
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yå// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<T: Double>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : String, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : A, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y’// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        private val pv = "OK"
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : String) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : MaxUI, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yï// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        x >= y    -> "Fail $x >= $y"
    }
}
yy// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        Test.A -> {
            when (
                val t2 = when(val y = peek()) {
                    Test.A -> Test.A
                    Test.B -> Test.B
                    else -> Test.OTHER
                }
            ) {
                Test.A ->
                    when (val t3 = peek()) {
                        Test.A -> "OK"
                        else -> "other 3"
                    }

                else -> "other 2"
            }
        }
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : String<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Int>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
    fun OK() {}

    return ::OK.name
}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> D()
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Byte>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÌ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(1)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Local()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    operator object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(i : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <T> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(null, permission)
        else -> {}
    }
}
y¼// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val x: Long) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÃ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Test()
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, value)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Any?>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (String) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, val x: Int) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<s> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yü// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
    val a1: Byte? = 1 + 1
    val a2: Short? = 1 + 1
    val a3: Int? = 1 + 1
    val a4: Long? = 1 + 1
    val a5: Double? = 1.0 + 1
    val a6: Float? = 1f + 1
    val a7: Char? = 'A' + 1
    val a8: Int? = 'B' - 'A'

    if (a1!! != 2.toByte()) return "fail 1"
    if (a2!! != 2.toShort()) return "fail 2"
    if (a3!! != 2) return "fail 3"
    if (a4!! != 2L) return "fail 4"
    if (a5!! != 2.0) return "fail 5"
    if (a6!! != 2f) return "fail 6"
    if (a7!! != 'B') return "fail 7"
    if (a8!! != 1) return "fail 8"

    return "OK"
}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : It, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        1, 2 -> Unit
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, foo2) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÌ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (x) {
        0 -> a = "OK"
        1 -> a = "???"
        2 -> exit()
        else -> exit()
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, test1) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yå// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Ordinaled, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : MACOSX, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (p, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class A<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(size, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun x(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <s> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y Í// Original bug: KT-37577
// Duplicated bug: KT-18583

class Foo(private val s: String) {
    inner class Inner {
        private val x = {
            this@Foo.s
        }()
    }

    val f = Inner()

}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Int()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class Outer {
    class Nested {
        fun fn() = s
    }

    companion object {
        private val s = "OK"
    }
}
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Box<Long>(-1)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : s, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        !is Float -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (x: Int, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : () -> String) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(val x: Int) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÐ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(i, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = String(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <box> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, sayResult)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yä// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : isIntBox, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, arg1) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : ok, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : () -> Boolean) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yœ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when(s) {
        "abc" -> s
        else -> "xyz"
    }
}
y ¯// Original bug: KT-37577
// Duplicated bug: KT-18583

class Outer() {
  inner class Inner() {
    val outer: Outer get() = this@Outer
  }

  public val x : Inner = Inner()
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, R(0))
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y­// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    data class Box(var value: String)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y·// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    object : T {
    val a = "OK"
    val f = {
        a
    }()

    override fun foo() = f
}
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (y) {
        !is Double -> throw AssertionError()
        0.0 -> {}
        else -> throw AssertionError()
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class String<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¿// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val a: Boolean) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Int -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, f3) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> j(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Int) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Int, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(it, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, M)
        else -> {}
    }
}
yË// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure()
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(D : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : foo<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<test3>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(false, true)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> S) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, StringBuilder)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (msg, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = fn(this)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (foo, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class ss<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <Str> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Long>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yË// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : String) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(Double : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun arg(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Unit>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ((a, b), Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¦// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class B(val data : String)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (Outer) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yé// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        is Season.Warm -> "OK"
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Float -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun prop(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is String -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// !LANGUAGE: +InlineClasses

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// D = 1101 C = 1100

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit("O", permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, list2)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

override class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yì// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (val ignored: Int, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> value(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Any) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : UInt()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y–// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
    override fun bar(): String = "OK"
}
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, String) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (x, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Int) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(3u, 5u, 7u)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, Base)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : () -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(id, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure("papa", permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun ArrayList(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// IGNORE_BACKEND: JVM_IR
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        private fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
x]// Original bug: KT-37577
// Duplicated bug: KT-18583

class A {
    val s : String = "OK"
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(a, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yî// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         // with possible local optimizations
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : C()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Base(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : x, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y€// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when {
        nx != 0.toByte() -> "Fail 0"
        nx == 1.toByte() -> "Fail 1"
        !(nx == 0.toByte()) -> "Fail 2"
        !(nx != 1.toByte()) -> "Fail 3"
        nx != x -> "Fail 4"
        nx == y -> "Fail 5"
        !(nx == x) -> "Fail 6"
        !(nx != y) -> "Fail 7"
        nn == 0.toByte() -> "Fail 8"
        !(nn != 0.toByte()) -> "Fail 9"
        nn == x -> "Fail 10"
        !(nn != x) -> "Fail 11"
        ax != 0.toByte() -> "Fail 12"
        ax == 1.toByte() -> "Fail 13"
        !(ax == 0.toByte()) -> "Fail 14"
        !(ax != 1.toByte()) -> "Fail 15"
        ax != x -> "Fail 16"
        ax == y -> "Fail 17"
        !(ax == x) -> "Fail 18"
        !(ax != y) -> "Fail 19"
        ax != bx -> "Fail 20"
        ax == by -> "Fail 21"
        !(ax == bx) -> "Fail 22"
        !(ax != by) -> "Fail 23"
        an == 0.toByte() -> "Fail 24"
        !(an != 0.toByte()) -> "Fail 25"
        an == x -> "Fail 26"
        !(an != x) -> "Fail 27"
        an == bx -> "Fail 28"
        !(an != bx) -> "Fail 29"
        else -> "OK"
    }
}
yä// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (val t: T, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : b, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(x, permission)
        else -> {}
    }
}
yÑ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : () -> String) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÀ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(k : K) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <a> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = assert(coll !is MutableCollection<*>) { "C should satisfy '!is MutableCollection'" }
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is B -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : A()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (String, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y // Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    var count: Int = 0
        get() = field++
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is IntArray -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is Result.Failure -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure({x -> x}, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        fun box() : String {
    val b = Box<Long>((-1))
    val expected: Long? = -1L
    return if (b.value == expected) "OK" else "fail"
}
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun e(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : count<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<arg1> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<T>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, foo)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> String(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun box(): String {
    val a = A()
    a.setMyStr()
    return a.getMyStr()
}
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(4, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(Int : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : () -> Any?) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : u1) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : IntArray) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÌ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(0)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Foo()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = Foo2(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Result<Int>(null)
        else -> {}
    }
}
yË// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> A("test")
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : I
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> s(lookup, permission)
        else -> {}
    }
}
y®// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        @PublishedApi
    internal fun published() = "OK"
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit?) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val a: Any, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, null)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(x, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, val t: T) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class box<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<U> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y»// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse() = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(a, permission)
        else -> {}
    }
}
y // Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        fun box(): String {
    val list1 = ArrayList<Int>()
    for (i in (3..5).reversed()) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>(5, 4, 3)) {
        return "Wrong elements for (3..5).reversed(): $list1"
    }

    val list2 = ArrayList<Int>()
    for (i in (3.toShort()..5.toShort()).reversed()) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>(5, 4, 3)) {
        return "Wrong elements for (3.toShort()..5.toShort()).reversed(): $list2"
    }

    val list3 = ArrayList<Long>()
    for (i in (3L..5L).reversed()) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Long>(5, 4, 3)) {
        return "Wrong elements for (3L..5L).reversed(): $list3"
    }

    val list4 = ArrayList<Char>()
    for (i in ('a'..'c').reversed()) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Char>('c', 'b', 'a')) {
        return "Wrong elements for ('a'..'c').reversed(): $list4"
    }

    return "OK"
}
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : toLong<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is x -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> MaxUI(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<E> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(p: T, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, a: T) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Long, permission)
        else -> {}
    }
}
y“// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        val test by Delegate()
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yì// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Base({ x + y }, { y + x })
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, Boolean : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        !doSimple("123") -> "failed 3"
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, "")
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<TInput>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(F, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, i) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(k, v)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : test<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y // Original bug: KT-37577
// Duplicated bug: KT-18583

class Outer {
    val x = "O"
    inner class Inner {
        val y = x + "K"
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(Comparable, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : eqLD) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> Int(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Int, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, b)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Foo<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Delegate) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Unit()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(Int, permission)
        else -> {}
    }
}
y¸// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        1 -> "OK"
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(A, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is list4 -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, id) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, propInc)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(10)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÄ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(s: String) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÁ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val prop: String) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : B<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, e: Exception) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yã// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : WIN2000, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <T, U> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : ()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : name2) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <R> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val b : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(sum : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, component2)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is index -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun InnerA(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÄ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        val A: Foo by Delegate(Foo(11))
        var B: Foo by Delegate(Foo(11))
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Test) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yâ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : String, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yå// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : Exception, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(Float : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(a: AsInt?, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y^// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    operator fun A.divAssign(y: Int) { x /= y }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : T) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : R, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> b(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, String)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<map> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(index: Int) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y†// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (a) {
    A.V -> b = true
  }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : M()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(try { zap(s2) } catch (e: Exception) { "" }, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : Book, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yá// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<list4>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y›// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        fun box(): String {
    if (!(0.0 in DOUBLE_RANGE)) return "fail 1 in Double"
    if (0.0 !in DOUBLE_RANGE) return "fail 1 !in Double"
    if (!(-0.0 in DOUBLE_RANGE)) return "fail 2 in Double"
    if (-0.0 !in DOUBLE_RANGE) return "fail 2 !in Double"

    if (PZERO in COMPARABLE_RANGE) return "fail 3 in Comparable"
    if (!(PZERO !in COMPARABLE_RANGE)) return "fail 3 !in Comparable"
    if (NZERO in COMPARABLE_RANGE) return "fail 4 in Comparable"
    if (!(NZERO !in COMPARABLE_RANGE)) return "fail 4a !in Comparable"

    return "OK"
}
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> R(lookup, permission)
        else -> {}
    }
}
yµ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
    if (Z::test.invoke(Z(42)) != 42) throw AssertionError()
    if (L::test.invoke(L(1234L)) != 1234L) throw AssertionError()
    if (S::test.invoke(S("abcdef")) != "abcdef") throw AssertionError()

    return "OK"
}
    }
}
yä// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Delegate, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is toChar -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is test -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : i) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : foo, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÎ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(str)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    // IGNORE_BACKEND: JVM_IR
enum class A1(val prop1: String) {
    X("asd"),
    Y() {
        override fun f() = super.f() + "#Y"
    },
    Z(5);

    val prop2: String = "const2"
    var prop3: String = ""

    constructor(): this("default") {
        prop3 = "empty"
    }
    constructor(x: Int): this(x.toString()) {
        prop3 = "int"
    }

    open fun f(): String = "$prop1#$prop2#$prop3"
}
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = foo(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yð// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {
    result = "OK"
}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(C(2))
        else -> {}
    }
}
yë// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        is String -> return this
    }
}
yÀ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = A()
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yà// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<Char>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Long) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : My<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(x: Array<Int>, y: Array<Int>) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yß// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> String) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÞ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(myArray, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, A) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<*>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is array -> closure(lookup, permission)
        else -> {}
    }
}
yã// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    override public object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yã// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (Boolean, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yã// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : List<String> by x
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> arrayListOf<Long>(1)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, 0)
        else -> {}
    }
}
y Í// Original bug: KT-37577
// Duplicated bug: KT-18583

class Test {
    private val Int.foo: String
        get() {
            return "OK"
        }

    fun test(): String {
        return 1.foo
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<c>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yç// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : toUpperCase, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<T>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is D -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, In) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val to: C, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(String, permission)
        else -> {}
    }
}
yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    companion object {
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÝ
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
// Original bug: KT-37577yÝ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
        else -> {}
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
    }
    }
}
yÝ        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
// Original bug: KT-37577
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : , permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when () {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
         -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit ->closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : ) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit)  when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) ->Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : <K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit)= when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
     orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> 
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure: (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÑ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class <K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup,permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y}// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
x8// Original bug: KT-37577
// Duplicated bug: KT-18583


y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun (closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : , val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke( : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealedclass Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object 
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K>invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, )
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invokelookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElseclosure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup: K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
xO// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> 
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse( : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this lineis FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

 class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun  invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closurelookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, ) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }

yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÍ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure :  -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K>{
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¹// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission: Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) =when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup,permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) =  (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val  : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup :K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÑ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is  -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yŒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : 
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y·// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>(
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission  Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(vallookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : ) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÒ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : ) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operatorfun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¯// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K,permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit-> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else  {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermitK>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
     FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y3// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission  Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÖ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> (lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, ) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit  closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companionobject {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K,Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÏ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> 
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions)  Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions)-> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K,  permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÕ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
         fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup: K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}}
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup  K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
         FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y¯// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K> : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun<K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÙ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> ) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class <K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion  {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    funorElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this){         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>)
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÉ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions): Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission: Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup  K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission :Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : PermitK>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure :(K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÊ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup :K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else-> {}
    }
}
ym// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object 
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator  <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure  (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y\// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÄ
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yd// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = 
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions)= FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) :Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)}
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÎ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
         -> closure(lookup, permission)
        else -> {}
    }
}
yÓ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, )
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, valpermission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission
        else -> {}
    }
}
yÉ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object{
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermitlookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> (lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else ->{}
    }
}
y×// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val  : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        isFailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÔ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)else -> {}
    }
}
yÛ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions)  Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed classPermit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yØ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÜ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> }
    }
}
yÚ// Original bug: KT-37577
// Duplicated bug: KT-18583

sealed class Permit<K> {
    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
yÅ// Original bug: KT-37577
// Duplicated bug: KT-18583


    companion object {
        operator fun <K> invoke(lookup : K, permission : Permissions) = FailedPermit(lookup, permission)
    }
    class FailedPermit<K>(val lookup : K, val permission : Permissions) : Permit<K>()
    fun orElse(closure : (K, Permissions) -> Unit) = when (this) {         //Dies on this line
        is FailedPermit -> closure(lookup, permission)
        else -> {}
    }
}
ÿÿ