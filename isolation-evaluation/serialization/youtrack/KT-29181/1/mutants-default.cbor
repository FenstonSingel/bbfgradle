¿iexportTaggdefaultnoriginalSampley// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
gmutantsŸyí// Original bug: KT-30720
// Duplicated bug: KT-29181


val s = false
if (s) {fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}} else {fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}}

yõ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() 
val q = true
when (q) {
 true -> {{
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}}
 else -> {{
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}}
}

y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  
val k = true
try
{hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
catch(e: Exception){}
finally{}

    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    
val g = false
try
{println("this point is unreachable")}
catch(e: Exception){}
finally{}

}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  
val w = false
try
{listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
catch(e: Exception){}
finally{}

    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    
val n = true
try
{println}
catch(e: Exception){}
finally{}
("this point is unreachable")
}
y%// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  
val x = true
if (x) {hh@} else {hh@}
  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  
val z = false
if (z) {listOf(1, 2, 3, 4, 5)} else {listOf(1, 2, 3, 4, 5)}
.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).
val x = true
try
{forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
catch(e: Exception){}
finally{}

    println("this point is unreachable")
}
y(// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  
val q = true
if (q) {listOf} else {listOf}
(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).
val u = false
try
{forEach}
catch(e: Exception){}
finally{}
 h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yP// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(
val z = true
when (z) {
 true -> {"this point is unreachable"}
 else -> {"this point is unreachable"}
}
)
}
y¤// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach 
val c = false
when (c) {
 true -> {h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
 else -> {h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
}

    println("this point is unreachable")
}
y6// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(
val q = true
when (q) {
 true -> {1}
 else -> {1}
}
, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y6// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 
val r = true
when (r) {
 true -> {2}
 else -> {2}
}
, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 
val n = false
try
{3}
catch(e: Exception){}
finally{}
, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 
val l = false
when (l) {
 true -> {4}
 else -> {4}
}
, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y6// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 
val m = true
when (m) {
 true -> {5}
 else -> {5}
}
).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y¢// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@
val b = false
when (b) {
 true -> {{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
 else -> {{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}
}

    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach 
val b = false
when (b) {
 true -> {h@}
 else -> {h@}
}
{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yŽ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@
val g = true
if (g) {{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }} else {{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }}

    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        
val d = false
if (d) {if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)} else {if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)}

    }
    println("this point is unreachable")
}
yL// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        
val l = false
when (l) {
 true -> {if (it == 3) return@hh}
 else -> {if (it == 3) return@hh}
}
 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y+// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        
val j = true
if (j) {print(it)} else {print(it)}

    }
    println("this point is unreachable")
}
y(// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        
val s = false
if (s) {print} else {print}
(it)
    }
    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (
val g = true
try
{it == 3}
catch(e: Exception){}
finally{}
) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) 
val w = false
try
{return@hh}
catch(e: Exception){}
finally{}
 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (
val y = true
try
{it}
catch(e: Exception){}
finally{}
 == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y7// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it 
val y = true
when (y) {
 true -> {==}
 else -> {==}
}
 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y6// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 
val e = true
when (e) {
 true -> {3}
 else -> {3}
}
) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y$// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(
val m = true
if (m) {it} else {it}
)
    }
    println("this point is unreachable")
}
y9// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return
val b = false
when (b) {
 true -> {@hh}
 else -> {@hh}
}
 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, (2), 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println!!("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)!!.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ((it)?.equals(3) ?: (3 === null)) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5it.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print())
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  "
    println(listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf"this point is unreachable"1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(()
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  4(1, 2, 3, listOf, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        print (it == 3) return@hh // non-local return directly to the caller of foo()
        if(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5h.forEach )@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh5  listOf(1, 2, 3, 4, @).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach hprintln{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    @("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf)1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it(
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  it  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(hh@)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, ), 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"2
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(")
    }
    println("this point is unreachableit)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

( foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if funit == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 3).forEach h@{
        if (it == 5) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() listOf
  hh@  {(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2" 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable,)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it println("this point is unreachable") 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    ==
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach print(it){
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        h@
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  listOf(1, 2, 3, 4, 5)  hh@.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() hh@
  {  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 3, 2, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
println("this point is unreachable")

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    // Duplicated bug: KT-29181
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  forEach(1, 2, 3, 4, 5).listOf h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(), 2, 3, 4, 51.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  )@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3hh return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).println h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    forEach("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).)
    println("this point is unreachable"forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  3(1, 2, listOf, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(it, 2, 3, 4, 5).forEach h@{
        if (1 == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach fun
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  1(listOf, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4"this point is unreachable" 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(,)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return// non-local return directly to the caller of foo() @hh
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5}.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
)
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, {).forEach h@5
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(4, 2, 3, 1, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  4  listOf(1, 2, 3, hh@, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooprintln) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    (("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooforEach) {
  hh@  listOf(1, 2, 3, 4, 5).( h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, it, 5).forEach h@{
        if (4 == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } 3, 4, 5).,
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  print(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        listOf(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh}  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    @
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).("this point is unreachable")
    printlnforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        ( (it == 3) return@hh // non-local return directly to the caller of foo()
        printifit)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, return).forEach h@{
        if (it == 3) 5@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yfun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
// Duplicated bug: KT-29181

// Original bug: KT-30720
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, it, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(3)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).print(it) h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        forEach
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf((, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print1it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
{

fun foo() // Duplicated bug: KT-29181
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo4) {
  hh@  listOf(1, 2, 3, (, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == it) return@hh // non-local return directly to the caller of foo()
        print(3)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, it, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(2)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }, 3, 4, 5).forEach h@2
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hhh  listOf(1, 2, 3, 4, 5).forEach @@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        this point is unreachable (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("if")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  @(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnlistOfhh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach println{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    h@("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach ){
        if (it == 3h@ return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, ", 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable2)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } 3, 4, 5).forEach ,
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it @ 3) return==hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)forEach. h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo3) {
  hh@  listOf(1, 2, (, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == @hh) return3 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach "this point is unreachable"
    println(h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        println(it)
    }
    print("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it()
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, if, 5).forEach h@{
        4 (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() listOf(1, 2, 3, 4, 5)
  hh@  {.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}

// Duplicated bug: KT-29181
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf"this point is unreachable".forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println((1, 2, 3, 4, 5))
}
y// Original bug: KT-30720
foo

fun // Duplicated bug: KT-29181() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf((it), 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print1
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)).forEach h@{
        5
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

hh foo() {
  fun@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (this point is unreachable) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("it == 3")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, (it), 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print3
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1print(it) 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        ,
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }  listOf(1, 2, 3, 4, 5).forEach h@hh@
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it ) 3) return@hh // non-local return directly to the caller of foo()
        print(it==
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@(
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, print(it)).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        5
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach return@hh{
        if (it == 3) h@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(2, 1, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@} // non-local return directly to the caller of foo()
        print(it)
    hh
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3it == 3 4, 5).forEach h@{
        if (,) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, forEach, 5).4 h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  it@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(hh)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, (, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print3it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  ).forEach h@{
        if (it == 3listOf(1, 2, 3, 4, 5) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach it == 3{
        if (h@) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  this point is unreachable@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("hh")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (== it 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() println
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    {("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)  listOf(1, 2, 3, 4, 5).forEach h@{
        hh@
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  return  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) hh@@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5this point is unreachable.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(")")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h=={
        if (it @ 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@this point is unreachable
    println("{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh)  listOf(1, 2, 3, 4, 5@.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).( h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    printlnforEach"this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, forEach).5 h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, @hh, 3, 4, 5).forEach h@{
        if (it == 3) return2 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, .)5forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach this point is unreachable
    println("h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh1  listOf(@, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun forEach() {
  hh@  listOf(1, 2, 3, 4, 5).foo h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@println("this point is unreachable")
    {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5it == 3.forEach h@{
        if ()) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo"this point is unreachable" {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(())
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooreturn@hh) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) ( // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@it // non-local return directly to the caller of foo()
        print(hh)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 4, 3, 2, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  return@hh(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) listOf // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return}hh // non-local return directly to the caller of foo()
        print(it)
    @
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, @, 3, 4, 5).forEach h@{
        if (it == 3) return2hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  "this point is unreachable"
    println(hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, if (it == 3) return@hh, 5).forEach h@{
        4 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  {(1, 2, 3, 4, 5).forEach h@listOf
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }  hh@
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, ("this point is unreachable"), 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println3
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOfforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }1, 2, 3, 4, 5).(
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(println("this point is unreachable") {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    )
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).) h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"forEach
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2( 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print,it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() 1
  hh@  listOf({, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yhh
// Duplicated bug: KT-29181

fun foo() {
  // Original bug: KT-30720@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return} // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
@hh
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  (1, 2, 3, 4, 5)@  listOfhh.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foohh {
  ()@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf41, 2, 3, (, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) println("this point is unreachable") // non-local return directly to the caller of foo()
        print(it)
    }
    return@hh
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @hh{
        if (it == 3) returnh@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  "this point is unreachable"  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(hh@)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  println
    hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } foo() {
  fun
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)(forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println."this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } foo() {
  hh@  fun
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)."
    println("this point is unreachableforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
yprint(it)
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        // Original bug: KT-30720
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)itforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(.)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooprintln("this point is unreachable")) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    (
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  this point is unreachable  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("hh@")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4h@ 5).forEach ,{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnit // non-local return directly to the caller of foo()
        print(@hh)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  h@(1, 2, 3, 4, 5).forEach listOf{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, h, 4, 5).forEach 3@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        print // non-local return directly to the caller of foo()
        if (it == 3) return@hh(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(this point is unreachable)
    }
    println("it")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return( // non-local return directly to the caller of foo()
        print(it)
    }
    println@hh"this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, if, 3, 4, 5).forEach h@{
        2 (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh4  listOf(1, 2, 3, @, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach "this point is unreachable"{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(h@)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, this point is unreachable, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("3")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return( // non-local return directly to the caller of foo()
        print@hhit)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h({
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println@"this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) // non-local return directly to the caller of foo()@hh return
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, }, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
2
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }  listOf(1, 2, 3, 4, 5).hh@
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, ., 3, 4, 5)2forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).== h@{
        if (it forEach 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 45 ,).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it), 5).forEach h@{
        4
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5){
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }forEach h@.
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(., 2, 3, 4, 5)1forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it @hh 3) return== // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(it {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print())
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2. 3, 4, 5),forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh,  listOf(1, 2@ 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, (, 3, 4, 5).forEach h@{
        if 2it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).h@ forEach{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, return@hh, 4, 5).forEach h@{
        if (it == 3) 3 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, println("this point is unreachable"), 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    4
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(3, 2, 1, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  )  listOf(1, 2, 3, 4, 5hh@.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if 3it == () return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf31, 2, (, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 35 4, ,).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo,) {
  hh@  listOf(1, 2, 3, 4( 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }() {
  hh@  listOf(1, 2, 3, 4, 5).forEach foo
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, println("this point is unreachable"), 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    2
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() h
  hh@  listOf(1, 2, 3, 4, 5).forEach {@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }, 4, 5).forEach 3
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, ), 5).forEach h@{
        if (it == 34 return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() ==
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it { 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

3 foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == fun) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) "@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(returnthis point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo("this point is unreachable") {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println()
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(print, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        1(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@"this point is unreachable"
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println({)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOfreturn@hh.forEach h@{
        if (it == 3) (1, 2, 3, 4, 5) // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachableit == 3)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ("this point is unreachable") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(it == 3)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  return@hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }1, 2, 3, 4, 5).forEach h@(
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        printprintln("this point is unreachable")
    }
    (it)
}
y// Original bug: KT-30720
listOf

fun foo() {
  hh@  // Duplicated bug: KT-29181(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(println("this point is unreachable"))
    }
    it
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (return@hh) it == 3 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it it 3) return@hh // non-local return directly to the caller of foo()
        print(==)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, it == 3, 5).forEach h@{
        if (4) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, println, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    4("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).3 h@{
        if (it == forEach) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(@hh, 2, 3, 4, 5).forEach h@{
        if (it == 3) return1 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach "this point is unreachable"@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(h)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).h forEach@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

hh foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@fun // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach }
    println("this point is unreachable")
h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        this point is unreachable
    }
    println("if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it), 2, 3, 4, 5).forEach h@{
        1
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } {
  hh@  listOf(1, 2, 3, 4, 5).()
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == print) return@hh // non-local return directly to the caller of foo()
        3(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

return@hh foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) fun // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@"this point is unreachable"
    println({
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }, 3, 4, 5).forEach 2
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

2 foo() {
  hh@  listOf(1, fun, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println"(this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() "this point is unreachable"
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println({)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, h@).forEach 5{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  "this point is unreachable"@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(hh)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @{
        if (it == 3) returnh@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun h() {
  hh@  listOf(1, 2, 3, 4, 5).forEach foo@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  if(1, 2, 3, 4, 5).forEach h@{
        listOf (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(("this point is unreachable"))
    }
    printlnit
}
y// Original bug: KT-30720
forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).// Duplicated bug: KT-29181
    println("this point is unreachable")
}
yprintln("this point is unreachable")
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    // Original bug: KT-30720
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3this point is unreachable 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(",")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)hforEach .@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3return )@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (println("this point is unreachable")) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    it == 3
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun return@hh() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) foo // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

) foo() {
  hh@  listOf(1, 2, 3, 4, 5fun.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh==  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it @ 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == ("this point is unreachable")) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println3
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  @hh(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnlistOf // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == (it)) return@hh // non-local return directly to the caller of foo()
        print3
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
@hh

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return// Duplicated bug: KT-29181 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } 3, 4, 5).forEach h@,
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, @).forEach h5{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(if (it == 3) return@hh, 2, 3, 4, 5).forEach h@{
        1 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, ), 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"4
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, // non-local return directly to the caller of foo(), 5).forEach h@{
        if (it == 3) return@hh 4
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, }).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
5
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } forEach
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it ) 3== return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, this point is unreachable).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("5")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, ", 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(2this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2print(it) 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        ,
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (// non-local return directly to the caller of foo() == 3) return@hh it
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4h 5).forEach ,@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  "this point is unreachable"
    println(listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  3@  listOf(1, 2, hh, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun it() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(foo)
    }
    println("this point is unreachable")
}
y{
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@// Original bug: KT-30720
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, println("this point is unreachable"), 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    3
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  )@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(ithh
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  "this point is unreachable"(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(listOf)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, ,3 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  "
    println(hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hhlistOf(1, 2, 3, 4, 5)  @.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@hh  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).@ h@{
        if (it == 3) returnforEachhh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnhhhh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnprint // non-local return directly to the caller of foo()
        @hh(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun listOf() {
  hh@  foo(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).// Original bug: KT-30720
    println("this point is unreachable")
}
y5
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, // Original bug: KT-30720).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun hh() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@foo // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    )("this point is unreachable"println
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    println
    }("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 3, 3, 4, 5).forEach h@{
        if (it == 2) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).} h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    forEach
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it 3 ==) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foohh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }) {
  (
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh)  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"@
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach // non-local return directly to the caller of foo(){
        if (it == 3) return@hh h@
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOfif (it == 3) return@hh1, 2, 3, 4, 5).forEach h@{
        ( // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }

fun foo() {
  // Duplicated bug: KT-29181
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        ) (it == 3if return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3h 4, 5).forEach ,@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(println, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    1("this point is unreachable")
}
y("this point is unreachable")
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println// Original bug: KT-30720
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOfprintln.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    (1, 2, 3, 4, 5)("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).@hh h@{
        if (it == 3) returnforEach // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == "this point is unreachable") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(3)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  it  listOf(1, 2, 3, 4, 5).forEach h@{
        if (hh@ == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

{
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
} foo() fun
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnprint(it) // non-local return directly to the caller of foo()
        @hh
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

== foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it fun 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  it == 3  listOf(1, 2, 3, 4, 5).forEach h@{
        if (hh@) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4( 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print,it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(}, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
1
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (println) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    it == 3("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

( foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        printfunit)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(it {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if () == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, // non-local return directly to the caller of foo(), 4, 5).forEach h@{
        if (it == 3) return@hh 3
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, it == 3).forEach h@{
        if (5) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun @hh() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnfoo // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }, 5).4
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo({
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@)
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  2  listOf(1, hh@, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5(.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print)it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, "this point is unreachable", 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(3)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(listOf {
  hh@  )(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (print) return@hh // non-local return directly to the caller of foo()
        it == 3(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) // non-local return directly to the caller of foo() return@hh
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooreturn) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) (@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ("this point is unreachable" == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(it)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  (  listOf(1, 2, 3, 4, 5).forEach h@{
        if hh@it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  return@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) hh@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

1 foo() {
  hh@  listOf(fun, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(( {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println)"this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2it 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(,)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  println("this point is unreachable")(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    listOf
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hhif (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)  listOf(1, 2, 3, 4, 5).forEach h@{
        @
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }.forEach h@listOf(1, 2, 3, 4, 5)
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(})
"this point is unreachable"
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5return@hh.forEach h@{
        if (it == 3) ) // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (@hh == 3) returnit // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo{
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
} ()
y(
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print// Original bug: KT-30720it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() 2
  hh@  listOf(1, {, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh it
        print(// non-local return directly to the caller of foo())
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(( {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if )it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  1@  listOf(hh, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnhh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print())
    }
    println("this point is unreachable"it
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, ), 4, 53.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it"
    }
    println()this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println"this point is unreachable"()
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun (foo) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  ).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(itlistOf(1, 2, 3, 4, 5)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) print // non-local return directly to the caller of foo()
        return@hh(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    "this point is unreachable"(println)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 5, 4, 3).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(. {
  hh@  listOf(1, 2, 3, 4, 5))forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, ("this point is unreachable")).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println5
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach hreturn@hh{
        if (it == 3) @ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

print foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        fun(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
1

fun foo() {
  hh@  listOf(// Duplicated bug: KT-29181, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, @hh, 4, 5).forEach h@{
        if (it == 3) return3 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, it).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(5)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(("this point is unreachable"), 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println1
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }.listOf(1, 2, 3, 4, 5)
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3( 4, 5).forEach h@{
        if ,it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun 2() {
  hh@  listOf(1, foo, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        "(it)
    }
    println(printthis point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 5, 4).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, h@, 4, 5).forEach 3{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(5 {
  hh@  listOf(1, 2, 3, 4, )).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  }
    println("this point is unreachable")
hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, ., 5)4forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  it(1, 2, 3, 4, 5).forEach h@{
        if (listOf == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  "(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(listOfthis point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(print(it), 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        1
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3"this point is unreachable" return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println())
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    "
    println(}this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, if (it == 3) return@hh, 3, 4, 5).forEach h@{
        2 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)printforEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        .(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }, 5).forEach 4
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(,, 21 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it ( 3) return@hh // non-local return directly to the caller of foo()
        print==it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo( {
  hh@  listOf()1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  println("this point is unreachable")
    listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooit == 3 {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (()) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return(hh // non-local return directly to the caller of foo()
        print@it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5// non-local return directly to the caller of foo().forEach h@{
        if (it == 3) return@hh )
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4// non-local return directly to the caller of foo() 5).forEach h@{
        if (it == 3) return@hh ,
        print(it)
    }
    println("this point is unreachable")
}
y()
// Duplicated bug: KT-29181

fun foo// Original bug: KT-30720 {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(h@, 2, 3, 4, 5).forEach 1{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, @, 5).forEach h4{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

, foo() {
  hh@  listOf(1, 2, 3fun 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
return

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) // Duplicated bug: KT-29181@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@()
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == ") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable3)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(==, 2, 3, 4, 5).forEach h@{
        if (it 1 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo3) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == () return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(,1 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooprintln("this point is unreachable") {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    ()
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  3  listOf(1, 2, hh@, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)print(it)forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        .
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  ).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"listOf(1, 2, 3, 4, 5)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

it foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (fun == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
{
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}

fun foo() // Duplicated bug: KT-29181
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3} 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    ,
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    "
    println("this point is unreachable})
}
yh@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach // Original bug: KT-30720
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  if  listOf(1, 2, 3, 4, 5).forEach h@{
        hh@ (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (} == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    it
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (("this point is unreachable") == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    printlnit
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (// non-local return directly to the caller of foo()) return@hh it == 3
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@it
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print({)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnprintln("this point is unreachable") // non-local return directly to the caller of foo()
        print(it)
    }
    @hh
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print("this point is unreachable")
    }
    println(it)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, ").forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable5)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }() {
  hh@  foo
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  it == 3.forEach h@{
        if (listOf(1, 2, 3, 4, 5)) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@"
    println("this point is unreachable{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    })
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        "this point is unreachable" // non-local return directly to the caller of foo()
        print(it)
    }
    println(if (it == 3) return@hh)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foohh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    } {
  ()
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(hh, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@1 // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh,  listOf(1, 2, 3@ 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun fooreturn@hh {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) () // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, return, 5).forEach h@{
        if (it == 3) 4@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1) 2, 3, 4, 5).forEach h@{
        if (it == 3, return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5".forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println()this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if itit == 3) return@hh // non-local return directly to the caller of foo()
        print(()
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, // non-local return directly to the caller of foo()).forEach h@{
        if (it == 3) return@hh 5
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }

fun foo() {
  hh@  // Duplicated bug: KT-29181
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (this point is unreachable == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("it")
}
y í// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    (A::Inner)(A())
}
yH// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) {
        return "Wrong elements for (5uL downTo 3uL).reversed(): $list4"
    } // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y õ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  ZImpl3().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 1L).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  d@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print()
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnd@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y œ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).ArrayList<Int>()
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 0, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(19)
    }
    println("this point is unreachable")
}
y-// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ((minusZero as Double) < (plusZero as Double)) return "fail 0" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yM// Original bug: KT-30720
// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        box(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 0, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(0)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 1..3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @foo  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        eq(x, y)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(xs.size + 42)
    }
    println("this point is unreachable")
}
xa// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
            block()
        }
y ò// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  foo().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // <- invocation of literal itself is generated instead
        print(it)
    }
    println("this point is unreachable")
}
y õ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 11).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y(// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  {
            foo(foo, 1)
            foo("K")
        } ().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Singleton(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (squares == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y
// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @unaryPlus{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    Int("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @innerGetter  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, "OK").forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 1) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ñ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  Z1().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  x@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 5, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(val result: String) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ž// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach { return }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, "O", 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ‚// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  label@{ it("K") }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@loop // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y è// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(0)
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  It(from, to).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y3// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        {
    val s = "notA"
    val id = when (s) {
        "a" -> 1
        else -> null
    }

    if (id == null) return "OK"
    return "fail"
}
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).Color h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        lastIndex(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @getter{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        A(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Box<T>(z[0])
    }
    println("this point is unreachable")
}
y ð// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(listOf(1))
}
y í// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(Z(123))
}
y ö// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  foo(null).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ¸// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach l@ fun (): Boolean { return@l true }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 1, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@iterator // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun Trait1() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        size(it)
    }
    println("this point is unreachable")
}
y ë// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("OK")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(logged("i;", 1.let { it }))
}
y ½// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        i -> A(i, i.toString())
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, -0.0, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    list1("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ((::Nested)().o + (A::Nested)().k) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    a("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun R() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).B()
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// KJS_WITH_FULL_RUNTIME

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (_ == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, "Should be executed once", 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 1, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach test1@ for(i in 1..2) {
        continue@test1
        return false
    }
    println("this point is unreachable")
}
y š// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).joinToString()
    println("this point is unreachable")
}
y ž// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{x -> x}
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, "OK", 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@C // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(xs)
    }
    println("this point is unreachable")
}
y ú// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(Z, NZ).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Ò// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach {
    val f = object {
        val a by del()
    }

    f.a
}
    println("this point is unreachable")
}
y	// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @iterator{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Double(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 21) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return "OK" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print("Playing with " + friend)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 1414, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y œ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach { 2*it }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 0, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y1// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach {
                val iterator = this.iterator()
                fun yielder() : TItem? {
                    while(iterator.hasNext())
                    {
                        val next = iterator.next()
                        if (predicate(next))
                            return next
                    }
                    return null
                }
                    { yielder() }
            }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 6).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    String("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(toString)
    }
    println("this point is unreachable")
}
y ²// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach l2@ if (l3@ true) s1 else null
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (x == 0) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 1, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(String)
    }
    println("this point is unreachable")
}
y ÷// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  hashCode().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y š// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach foo@ x
    println("this point is unreachable")
}
y õ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(42, null, "OK")
}
y ï// Original bug: KT-30720
// error

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y â// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    A(0)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 0) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 4) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (String == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y è// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(3)
}
y œ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ it }
    println("this point is unreachable")
}
y þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).foo h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, "$div").forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ú// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach {
        var x = 0L
        val y = x[0]++
        return if (y == 0L) "OK" else "Failed, y=$y"
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @B  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// KJS_WITH_FULL_RUNTIME
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (i == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 10) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, object: B {}, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it != 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        fooInClassNoReceiver()
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  String(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).list h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y¸// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(try {
                    try {
                        zap(string)
                    }
                    catch (e: Exception) { "" }
                } catch (e: Exception) { "" }, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  count(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// !LANGUAGE: +ProperIeee754Comparisons

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(b, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @a  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
xs// Original bug: KT-30720
// Duplicated bug: KT-29181

override fun foo(): String {
            return p
        }
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 239).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        toInt(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  Nothing(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnloop@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// !LANGUAGE: +InlineClasses
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnb@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Boolean(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(true, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@a // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@id // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ò// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  b(2L).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 99, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ¶// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
    val l = ArrayList<Boolean>()
    l.add(true)
    val x = !l[0]
    if (x) return "Fail: $x}"
    return "OK"
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        KProperty(it)
    }
    println("this point is unreachable")
}
y
// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (x != 2) return "Fail: $x}" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y  // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ result }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 2) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    x("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) "OK" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (l == 11) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(foo)
    }
    println("this point is unreachable")
}
y“// Original bug: KT-30720
// Exception in thread "main" java.lang.VerifyError: (class: org/jetbrains/kannotator/controlFlowBuilder/GraphBuilderInterpreter, method: binaryOperation signature: (Lorg/objectweb/asm/tree/AbstractInsnNode;Lorg/jetbrains/kannotator/controlFlowBuilder/AsmPossibleValues;Lorg/jetbrains/kannotator/controlFlowBuilder/AsmPossibleValues;)Lorg/jetbrains/kannotator/controlFlowBuilder/AsmPossibleValues;) Wrong return type in function

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ñ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("0;1;2;3;")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 10).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ª// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach { ++subjectEvaluated }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).index h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ƒ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  A("OK")._kind
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it - 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 0).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 23, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  t@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach Z@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it + 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return "Fail 1" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
xQ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun box() = My().delegate
y ¢// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ prop = 2 }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        String(it)
    }
    println("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    y("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it as 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return "fail 2" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(msg)
    }
    println("this point is unreachable")
}
y ç// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println()
}
y ï// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("Fail 1")
}
x}// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  My().my
    println("this point is unreachable")
}
y	// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        AssertionError()
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).N()
    println("this point is unreachable")
}
y
// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  foo(null, value ?: return 1L).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return "fail 4" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// !LANGUAGE: +InlineClasses

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (!(1 == s?.length)) "A" else "B" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(-10.toInt())
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun typed() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, a, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(b)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @A{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ð// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("child19")
}
y ß// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // FILE: util.kt
        print(it)
    }
    println("this point is unreachable")
}
y ö// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  changer().forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Array(it)
    }
    println("this point is unreachable")
}
xD// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {}
y è// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(x)
}
y ã// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    bar()
}
y Þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach f@{
        if (a > 0)
          return@f "OK"
        else "Fail 1"
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @N  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y í// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("AMAF")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  longListMax(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    property("this point is unreachable")
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  compareTo(b).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).Map h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @B{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (b.value == expected) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ‘// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).b(2L)
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(-0.0F, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y µ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        (x, y) -> x + y
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (inner == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (x = "OK") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        checkA(Test.C)
    }
    println("this point is unreachable")
}
y ë// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // !LANGUAGE: +InlineClasses
        print(it)
    }
    println("this point is unreachable")
}
y Ò// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ i, (id, name, distance) -> result += "$id$name$distance" }
    println("this point is unreachable")
}
y// !LANGUAGE: +NewInference
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (box == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y8// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (!('1' !in '1' until '3') != range0.contains('1')) throw AssertionError() // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yX// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) {
        if (b.a == 2.0 && b.b == 66 && b.c == "cc") {
            return "OK"
        }
    } // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ß// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // FILE: test.kt
        print(it)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  x(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    ko("this point is unreachable")
}
y é// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(42)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach ub@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach Int@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@A // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  list4@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    k("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun to() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@N // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y º// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  test1@ for(i in 1..2) {
        continue@test1
        return false
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  foo(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @Bar  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        result(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @id  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y •// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  l@ fun (): Boolean { return@l true }
    println("this point is unreachable")
}
y º// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
    val ok: String? = "OK"
    var res = ""

    do {
        res += ok ?: break
    } while (false)

    return res
}
y	// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @unaryPlus  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (x == until) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ A(it) }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5u).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        Outer(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).string h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, "$s", 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  isNullable(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y
// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (log != "logged") return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ©// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ outerProp + arg }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@getter // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(i)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  A(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 42, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ¥// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach label@{ it("K") }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (r == "BBB") "OK" else "Fail: $r" // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
yK// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  loop@ for (color in arr) {
        when (color) {
            Color.RED -> return color
            Color.GREEN -> break@loop
            Color.BLUE -> if (arr.size == 1) return color else continue@loop
        }
    }
    println("this point is unreachable")
}
y ¢// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{ { ok }() }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  append(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 4).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 42) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).String h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ¨// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach { throwIt("oops!") }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun s() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
xe// Original bug: KT-30720
// Duplicated bug: KT-29181

fun box(): String {
    return Foo().test()
}
y ö// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(a).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y €// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  list1.size
    println("this point is unreachable")
}
y ë// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        {
        return "Wrong elements for MaxB..MaxB step 1: $list2"
    }
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("Wrong elements for 3u..8u step 2: $list1")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (z == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    println("this point is unreachable")
    }
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        print(it)
    }
    println("this point is unreachable")
}
y    }
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
// Original bug: KT-30720
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    
    println("this point is unreachable")
}
xv// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  
    println("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) @hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y û// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if () return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y é// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo( {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        printit)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ç// Original bug: KT-30720


fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ¦// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        
    }
    println("this point is unreachable")
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, , 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ”// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach 
    println("this point is unreachable")
}
y –// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
xq// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it  3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

 foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y û// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5). h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3)  // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
    listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Ï// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh 
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, , 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEachh@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, , 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, ).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == ) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3,4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh// non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it== 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo(){
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  @  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable)
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)}
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() 
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo) {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2,3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print
    }
    println("this point is unreachable")
}
xB// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() 
y Þ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")}
y // Original bug: KT-30720
// Duplicated bug: KT-29181fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable"
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if ( == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it ==3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3 return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3)return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1,2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")

y ó// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if(it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        (it)
    }
    println("this point is unreachable")
}
y é
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y û// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    ("this point is unreachable")
}
y ü// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  (1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println(this point is unreachable")
}
y ù// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()print(it)
    }
    println("this point is unreachable")
}
y ý// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) returnhh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y ÿ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun () {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach @{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5)forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Œ// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).
    println("this point is unreachable")
}
y ì// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
         // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
x8// Original bug: KT-30720
// Duplicated bug: KT-29181


y// Original bug: KT-30720
// Duplicated bug: KT-29181

funfoo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4,5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5.forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y å// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println
}
y í// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  .forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
         (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println"this point is unreachable")
}
y// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y // Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@ // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
y Ú// Original bug: KT-30720
// Duplicated bug: KT-29181

fun foo() {
  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }

}
y ÷// Original bug: KT-30720
// Duplicated bug: KT-29181


  hh@  listOf(1, 2, 3, 4, 5).forEach h@{
        if (it == 3) return@hh // non-local return directly to the caller of foo()
        print(it)
    }
    println("this point is unreachable")
}
ÿÿ