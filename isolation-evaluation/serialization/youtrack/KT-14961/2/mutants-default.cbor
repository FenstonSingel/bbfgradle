¿iexportTaggdefaultnoriginalSampleyÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
gmutantsŸyæ// Original bug: KT-37845
// Duplicated bug: KT-14961


val a = false
if (a) {tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}} else {tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}}

sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}

val x = false
try
{sealed class Rope}
catch(e: Exception){}
finally{}


private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yS// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope


val k = false
if (k) {private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}} else {private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}}

y£// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = 
val u = true
if (u) {when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}} else {when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}}

sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yð// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode
val j = true
if (j) {(val size: Int)} else {(val size: Int)}
 : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(
val g = true
try
{rope: Rope}
catch(e: Exception){}
finally{}
, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yû// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, 
val w = false
when (w) {
 true -> {i: Int}
 else -> {i: Int}
}
): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (
val q = true
try
{rope}
catch(e: Exception){}
finally{}
) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y(// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    
val i = true
when (i) {
 true -> {inline fun iter(f: (child: Rope) -> Boolean) {
    }}
 else -> {inline fun iter(f: (child: Rope) -> Boolean) {
    }}
}

}
yå// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): 
val c = true
if (c) {Pair} else {Pair}
<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> 
val c = true
try
{{
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }}
catch(e: Exception){}
finally{}

}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(
val x = false
when (x) {
 true -> {val size: Int}
 else -> {val size: Int}
}
) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : 
val i = false
try
{Rope}
catch(e: Exception){}
finally{}
() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yè// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) 
val p = true
if (p) {{
    }} else {{
    }}

}
y\// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        
val c = false
when (c) {
 true -> {rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
 else -> {rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
}


        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÿ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        
val h = false
if (h) {throw IllegalStateException()} else {throw IllegalStateException()}

    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(
val u = false
try
{f: (child: Rope) -> Boolean}
catch(e: Exception){}
finally{}
) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: 
val w = false
try
{Rope}
catch(e: Exception){}
finally{}
, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: 
val q = true
try
{Int}
catch(e: Exception){}
finally{}
): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yæ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        
val h = false
if (h) {rope} else {rope}
.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yW// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.
val g = false
when (g) {
 true -> {iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
 else -> {iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
}


        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw 
val v = true
when (v) {
 true -> {IllegalStateException()}
 else -> {IllegalStateException()}
}

    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yë// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is 
val s = false
if (s) {InnerNode} else {InnerNode}
 -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yæ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.
val v = false
if (v) {iter} else {iter}
 { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y
// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw 
val l = false
when (l) {
 true -> {IllegalStateException}
 else -> {IllegalStateException}
}
()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yä// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: 
val u = true
if (u) {Int} else {Int}
) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yù// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : 
val k = false
when (k) {
 true -> {Rope}
 else -> {Rope}
}
() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<
val d = false
try
{Rope}
catch(e: Exception){}
finally{}
, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, 
val a = true
try
{Int}
catch(e: Exception){}
finally{}
> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter 
val f = false
try
{{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
catch(e: Exception){}
finally{}


        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter 
val o = false
try
{{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }}
catch(e: Exception){}
finally{}


        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yí// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (
val t = false
if (t) {child: Rope} else {child: Rope}
) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            
val h = false
if (h) {if (i < 1) return leafContaining(child, i)
            false} else {if (i < 1) return leafContaining(child, i)
            false}

        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> 
val x = false
try
{Boolean}
catch(e: Exception){}
finally{}
) {
    }
}
yú// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { 
val v = false
when (v) {
 true -> {child}
 else -> {child}
}
 ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            
val h = true
if (h) {if (i < 1) return leafContaining(child, i)} else {if (i < 1) return leafContaining(child, i)}

            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yù// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            
val o = true
when (o) {
 true -> {false}
 else -> {false}
}

        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yå// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: 
val p = true
if (p) {Rope} else {Rope}
) -> Boolean) {
    }
}
yæ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (
val m = true
if (m) {i < 1} else {i < 1}
) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) 
val f = true
try
{return leafContaining(child, i)}
catch(e: Exception){}
finally{}

            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yâ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (
val q = true
if (q) {i} else {i}
 < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i 
val k = false
try
{<}
catch(e: Exception){}
finally{}
 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y÷// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 
val s = false
try
{1}
catch(e: Exception){}
finally{}
) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yù// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return 
val b = true
if (b) {leafContaining(child, i)} else {leafContaining(child, i)}

            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return 
val f = true
try
{leafContaining}
catch(e: Exception){}
finally{}
(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(
val s = true
try
{child}
catch(e: Exception){}
finally{}
, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yö// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, 
val c = true
try
{i}
catch(e: Exception){}
finally{}
)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        (rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        })

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw (IllegalStateException)()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, (Int)> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: ((child) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) (return leafContaining(child, i))
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope!!) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair!!<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope!!, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i!!)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): (Pair<Rope, Int>)? = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: (Rope)?, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: (Int)?): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is (InnerNode)? -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: (Int)?) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : (Rope)?() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<(Rope)?, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, (Int)?> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: ((child: Rope) -> Boolean)?) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> (Boolean)?) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: (Rope)?) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { ) ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(child {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when :rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size( Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is leafContaining -> {
        rope.iter { child ->
            if (i < 1) return InnerNode(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Ropeclass i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed , Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.inline

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        } fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        } {
    is InnerNode -> {
        rope.iter )

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (inline fun iter(f: (child: Rope) -> Boolean) {
    } < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    i
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

Boolean
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode)val size: Int) : Rope(( {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

when fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = tailrec (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁrope: Rope
// Duplicated bug: KT-14961

tailrec fun leafContaining(// Original bug: KT-37845, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i}
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    )
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (:) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val sizerope Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

() fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptiontailrec
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, })
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
i
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: }) -> Boolean) {
    Rope
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    fun
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw inline()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    IllegalStateException fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { ) ->
            if (i < 1child return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Rope> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Int() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

child: Rope
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        child: Rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed val size: Int Rope

private data class InnerNode(class) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode class {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed -> Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) (val size: Int) leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodereturn : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: if (i < 1) return leafContaining(child, i)
            false): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            Int
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, private)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

i data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): ) = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: RopePair<Rope, Int> -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: <): PairIntRope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

class fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed tailrec Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: ()): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptionInt
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (IllegalStateException < 1) return leafContaining(child, i)
            false
        }

        throw i()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Rope, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : child() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.} { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    iter
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, fun: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline i iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: InnerNode, i: Int): Pair<Rope, Int> = when (rope) {
    is Rope -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: =): Pair<Rope, Int> Int when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode Rope {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : ->() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is : -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (childInnerNode Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(<, i: Int): Pairrope: RopeRope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        } -> {
        is InnerNode

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < :) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size1 Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < child) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (1: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) )
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException({
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, iter> = when (rope) {
    is InnerNode -> {
        rope.Int { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun child: Rope(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (leafContaining) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(): (child: Rope) -> Booleanf {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter return leafContaining(child, i) child ->
            if (i < 1) {
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, :> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(fInt (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<:, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val sizeRope Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i) return leafContaining(child, i < 1)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): return = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) Pair<Rope, Int> leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, iprivate
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

) data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return ()(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : RopeleafContaining {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if () < 1i return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        } = when (rope) {
    is InnerNode -> {
        rope.iter >

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContainingRoperope: (, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(}, i)
            false
        }

        throw IllegalStateException()
    child
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}: Int) : Rope() size
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < () return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: 1child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: if (i < 1) return leafContaining(child, i)
            false, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            Rope
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: rope: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.) { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(iter
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<1, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < Rope) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if () < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(i {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < (val size: Int)) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode1 : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): ( = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodePair<Rope, Int>val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> Rope
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter return child ->
            if (i < 1) { leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        f

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(}: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) ->
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) { Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: IntRope: Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: )) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode false {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            ->
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if ((child, i)) return leafContainingi < 1
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair)Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope< -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(InnerNode, i: Int): Pair<Rope, Int> = when (rope) {
    is rope: Rope -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, {> = when (rope) {
    is InnerNode -> Int
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, IllegalStateException: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw i()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: RopeRope() i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : , {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

Boolean data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> private) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }, Int> = when (rope) {
    is InnerNode -> {
        rope.iter Rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): false<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            Pair
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairchild = when (rope) {
    is InnerNode -> {
        rope.iter { <Rope, Int> ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        Rope
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: throw IllegalStateException()) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Int, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Rope) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw (val size: Int)()
    }
}
sealed class Rope

private data class InnerNodeIllegalStateException : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    i -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, is InnerNode)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is class -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data InnerNode InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class child(val size: Int) : Rope() {
    inline fun iter(f: (InnerNode: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) 1
    is InnerNode -> {
        rope.iter { child ->
            if (i < {) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            Rope
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: false) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, private data> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

Int class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) Rope Rope() {
    inline fun iter(f: (child: :) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, (val size: Int))
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodei : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairIntRope, <> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope{
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> ,
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child i < 1
            if (->) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = i (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, when)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, i < 1> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (Int) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, IntBoolean = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> >) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child( i)
            false
        }

        throw IllegalStateException,)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Int, i: Int): Pair<Rope, Rope> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter ()

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        } {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode{
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}val size: Int) : Rope() (
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope} Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    :
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): :<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(fPair (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

inline
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
} fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        (child: Rope)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: throw IllegalStateException() -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): <PairRope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() i: Int
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, ifalse Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            :
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

data fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private tailrec class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: RopeleafContaining i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return ,(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Inti = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, >)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (Rope) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : i < 1() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Intsize = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val >: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

Boolean class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> private data) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Boolean, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Rope) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is } -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
InnerNode
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(val size: Int, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(child) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is Boolean -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> InnerNode) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, ,> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(childInt i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i ) 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int< : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: IntInt: Pair<Rope, )> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

rope
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        :.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) rope Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            () (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Ropeif {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Rope(), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : child {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode i {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, ->)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }<Rope, Int> = when (rope) {
    Pair
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Ropei < 1 Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (,) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<}, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    Rope
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
sealed class Rope
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> )Boolean {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException:)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) ( Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.Rope { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : iter() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(ropedata Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private : class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) throw IllegalStateException()
            false
        }

        return leafContaining(child, i)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun iter(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.leafContaining { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptionRope)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : (() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Int, Rope> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> .
        rope{iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairiterRope, Int> = when (rope) {
    is InnerNode -> {
        rope.< { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter Rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, leafContaining> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return Int(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i(
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter)f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): ) = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, iPair<Rope, Int>
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return fun(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline leafContaining iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

private fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

tailrec data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
{
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) }
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : rope: Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope(), Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(fun, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline child iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, private> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

Int data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i ( 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope<) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

(child, i) fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingtailrec
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<false, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            Rope
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : fun() {
    inline Rope iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode() : Rope(val size: Int) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun Rope(rope: Rope, i: Int): Pair<leafContaining, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, {
    })
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) i
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Boolean): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Int) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    rope InnerNode -> {
        is.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContainingchildrope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining((, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is ( -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iterInnerNodef: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw (child: Rope)()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: IllegalStateException -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): rope = when (Pair<Rope, Int>) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        ropeIntiter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: .) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (val size: Int) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(rope) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining}child, i)
            false
        (

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        class IllegalStateException()
    }
}
sealed throw Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    inline
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) ) Rope() {
    inline fun iter(f: (child: Rope: -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        ropeinline fun iter(f: (child: Rope) -> Boolean) {
    }iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    .
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) IllegalStateException()
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw {
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: (child: Rope)) : Rope() {
    inline fun iter(f: Int -> Boolean) {
    }
}
yÁ{
    }
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) // Original bug: KT-37845
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException->)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) ( Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        child.iter { child ->
            if (i < 1) return leafContaining(rope, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

InnerNode fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is tailrec -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairprivate data = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

<Rope, Int> class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, Rope): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : i: Int() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(class, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed rope: Rope Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = sealed (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
when class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, leafContaining(child, i): Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return i
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

leafContaining fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return tailrec(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return private(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

leafContaining data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        i < 1.iter { child ->
            if (rope) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, iinline fun iter(f: (child: Rope) -> Boolean) {
    }
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    )
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        inline IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    throw fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair( = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter<Rope, Int>f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            Rope
        }

        throw IllegalStateException()
    }
}
sealed class if (i < 1) return leafContaining(child, i)
            false

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, )> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> BooleanInt {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(()) : Ropeval size: Int {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            false (i < 1) return leafContaining(child, i)
            if
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(->, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode rope: Rope {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<child: Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        } {
        ->

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope{
    } i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) ,
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data : InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (childclass Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair=Rope, Int> < when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = throw IllegalStateException() (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        when
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Boolean, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Rope) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> inline
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: sealed class Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (IllegalStateException) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw rope()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < val size: Int) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(1) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    tailrec
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter)f: (child: Rope( -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairInt = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: <Rope, Int>) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.{
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
} { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() iter
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<->, Int> = when (rope) {
    is InnerNode Rope {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: (, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iterRopef: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i Rope 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : <() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptionRope
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : ()() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

i fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, tailrec)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, iPair<Rope, Int> Int): : = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            Int
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: if (i < 1) return leafContaining(child, i)) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i ) 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean< {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }> = when (rope) {
    Int
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

Int
sealed class Rope

private data class InnerNode(val size: tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { Int ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: child) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<RopeInnerNode Int> = when (rope) {
    is , -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope((child: Rope) -> Boolean {
    inline fun iter(f: )) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            )
            false
        }

        throw IllegalStateException(if (i < 1) return leafContaining(child, i)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<rope, Int> = when (rope) {
    is InnerNode -> {
        Rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

val size: Int
sealed class Rope

private data class InnerNode(tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (child < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (i: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        ropeprivate dataiter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

. class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, IllegalStateException()> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw Int
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: )): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1Int return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, ichild Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(:, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if ((val size: Int)) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodei < 1 : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, <> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i Int 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, :): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(fi: Int (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : )() {
    inline fun iter(f: (child: RopeRope -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(ropeInt Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: :) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(val size: Int))
    }
}
sealed class Rope

private data class InnerNode( : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair) = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean<Rope, Int> {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

1
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = ) (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Booleanwhen {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Rope()> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Int {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

( fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodetailrecval size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(childInt i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: ,) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

inline fun iter(f: (child: Rope) -> Boolean) {
    }
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}

private data class InnerNode(val size: Int) : Rope() Rope
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<i, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, Rope)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ}
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        // Original bug: KT-37845

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodef : Rope() {
    inline fun iter((val size: Int): (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (val size: Int) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(i < 1) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() f: (child: Rope) -> Boolean
    inline fun iter({) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(fun size: Int) : Rope() {
    inline val iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < )1 return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    ( InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodeisval size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { if (i < 1) return leafContaining(child, i)
            false ->
            child
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    Rope
}
sealed class Rope

private data class InnerNode(val size: Int) : is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: 1): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < Int) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: ()): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : RopeInt {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairreturn leafContaining(child, i)Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) <
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is Rope() -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : InnerNode {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): 1<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < Pair) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            Int
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: if (i < 1) return leafContaining(child, i)
            false) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, if: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            i (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec ) leafContaining(rope: Rope, i: Intfun: Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: child): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Int, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<(child: Rope), Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: Rope -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

-> fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) tailrec Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(childRope i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: ,) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

} fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        tailrec

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter i child ->
            if ({ < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException))
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(( {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec Rope leafContaining(rope: Rope, i: Int): Pair<fun, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int-> = when (rope) {
    is InnerNode > {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (( < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: ichild: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int)child Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(:, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁRope
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : // Original bug: KT-37845() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            IllegalStateException()
            false
        }

        throw if (i < 1) return leafContaining(child, i)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
)
sealed class Rope

private data class InnerNode(val size: Int) : Rope(} {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<(, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingRopechild, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
Rope

private data class InnerNode(val size: Int) : sealed class Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> private data
}
sealed class Rope

{
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException})
    }
(
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        } = when (rope) {
    is InnerNode -> {
        rope.Pair<Rope, Int>

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, leafContaining(child, i)): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return i: Int
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter i child ->
            if (i < 1) return leafContaining(child, {)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is (child: Rope) -> Boolean -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: InnerNode) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Rope()<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Pair {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodechild: Rope : Rope() {
    inline fun iter(f: ((val size: Int)) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(ropeleafContaining(child, i) Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return :
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i(child: Rope) -> Boolean
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: )) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, child> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Int, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<(, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iterRopef: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> } when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
=
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter Boolean

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
} -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() InnerNode
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode child {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (->: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

= fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> tailrec when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is f: (child: Rope) -> Boolean -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(InnerNode) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) child
    is InnerNode -> {
        rope.iter { { ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: sealed class Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data ) InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Booleanclass {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<(val size: Int), Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodeRope : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: ,): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(childInt i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    class
}
sealed class Rope

private data } InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child(child: Rope) -> Boolean i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: ,) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingchild: Ropechild, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (() -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(return, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) rope: Rope leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        {
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() throw IllegalStateException()
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairchildRope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(<, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, iter)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun i(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

{
    } data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) private
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if ,i < 1) return leafContaining(child( i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        return leafContaining(child, i).iter { child ->
            if (i < 1) rope
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope} {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
)
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < IllegalStateException()) return leafContaining(child, i)
            false
        }

        throw 1
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining{
    }
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) (child, i)
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (IllegalStateException) return leafContaining(child, i)
            false
        }

        throw i < 1()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

child fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { tailrec ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = rope (when) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is { -> InnerNode
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    Rope InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : is() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: }) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    Int
}
yÁ:
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) // Original bug: KT-37845 Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Ropewhen Int> = , (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Rope, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: child) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: ,, i: Int): Pair<RopeRope Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope{ Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() ,
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair}Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    <
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, InnerNode: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class i(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            private (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

if data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is if -> {
        rope.iter { child ->
            InnerNode (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter Int

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair= <Rope, Int> when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptionInt
    }
}
sealed class Rope

private data class InnerNode(val size: ()) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is rope -> {
        InnerNode.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Intchild : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Ropewhen (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
} i: Int): Pair<Rope, Int> = ,
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun }(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        leafContaining

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { IllegalStateException ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw child()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Pair, i: Int): Rope<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: {) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) Int
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope)iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int. : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if () < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Inti : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairInnerNode = when (rope) {
    is <Rope, Int> -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }
yÁ// Original bug: KT-37845
false

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            // Duplicated bug: KT-14961
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (leafContaining) return i < 1(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(f: (child: Rope) -> Boolean, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(child) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: (, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if Ropei < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        private.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

rope data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun (iterf: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        Rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i(child, i) Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining:
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: rope): Pair<Rope, Int> = when (Int) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, (child: Rope) -> Boolean)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: i) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data (child: Rope) -> Boolean InnerNode(val size: Int) : Rope() {
    inline fun iter(f: class) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (sealed < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
i class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if inlinei < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    ( fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, Int)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: i) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is ) -> {
        rope.iter { child ->
            if (i < 1InnerNode return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun IllegalStateException()(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw leafContaining
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

> fun leafContaining(rope: Rope, i: Int): Pair<Rope, Inttailrec = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    {
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = 1 (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < when) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(child {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child )
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(->
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed } Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
class
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { ( ->
            if childi < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class )

private data class InnerNode(val size: Int) : Rope(Rope {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) ( Rope() {
    inline fun iter:f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairiter = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun <Rope, Int>(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<->, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child Rope
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter )

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        } : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, ()> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : RopeInt {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun Rope(rope: leafContaining, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.() { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptioniter
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : : {
    inline fun iter(fRope() (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, false)
            i
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> }
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
{
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Rope<Pair, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int). Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope:iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
} leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

return
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<), Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1Rope return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairRope<, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<iter, Int> = when (rope) {
    is InnerNode -> {
        rope.Rope { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: (child: Rope) -> Boolean, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: Rope) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Rope() = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Pair<Rope, Int> {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): child<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { Pair ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: )): Pair<Rope, Int> = when (ropeInt {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when ((val size: Int)) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNoderope : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw Rope()()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : IllegalStateException {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    } = when (rope) {
    Pair<Rope, Int>
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, Rope)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : i() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingBoolean
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> (child, i)) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (:) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (childrope Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    if InnerNode -> {
        rope.iter { child ->
            is (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining({ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(data, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private child class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, ii Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (: < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            ()
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Ropeif (i < 1) return leafContaining(child, i) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

) fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(tailrec {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) ( Rope:) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is child -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (InnerNode: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Ropei i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, ,)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(: {
    inline fun iter(f: (child) Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        )

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean} {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        Boolean IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> throw) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    i < 1 -> {
        rope.iter { child ->
            if (is InnerNode) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int(child: Rope) = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: > -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodeBooleanval size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> () {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, i> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (Int < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int( = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter>f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining((child: Rope), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: child -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining((child: Rope), i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: rope: Rope -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            return (i < 1) if leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(InnerNode
    }
}
sealed class Rope

private data class )(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) : leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) return Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Rope()) : Int {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter sealed

        throw IllegalStateException()
    }
}
{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        } class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = f
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}() Rope
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: }): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    Int
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            class (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed if Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairi = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, <Rope, Int>)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    Rope -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: is InnerNode) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, :> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val sizeInt Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: (child, i), i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingRope
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) sealed class Rope leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
return

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, =: Int): Pair<Rope, Int> i when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: >): Pair<Rope, IntInt = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, Pair): i: Int<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
(child: Rope) -> Boolean

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: sealed class Rope) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, child): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(i: Int, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }> = when (rope) {
    is InnerNode -> {
        rope.iter Int

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining((f: (child: Rope) -> Boolean), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iterchild {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.if (i < 1) return leafContaining(child, i) { child ->
            iter
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

) fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Booleantailrec {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i child: Rope 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (<) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    Rope
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: }) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pairrope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }Rope, Int> = when (rope) {
    is InnerNode -> {
        <

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope} i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
,
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContainingRopechild, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: () -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is ( -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateExceptionInnerNode)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Intf = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(>: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed inline Rope

private data class InnerNode(val size: Int) : Rope() {
    class fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter (

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < inline) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    1 fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: return leafContaining(child, i)): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) Int
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: }child: Rope) -> Boolean) {
    }
(
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(IllegalStateException(), i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw rope: Rope
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    : -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val sizeis InnerNode Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter val size: Int

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode({ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if ((f: (child: Rope) -> Boolean) < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iteri {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContainingrope(: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<}, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        Rope

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            inline
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    false fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<RopeRope Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : ,() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw :()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) IllegalStateException Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw inline fun iter(f: (child: Rope) -> Boolean) {
    }
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    IllegalStateException()
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode:val size: Int) : Rope() {
    inline fun iter(f: (child( Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i i 1) return leafContaining(child, <)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<>, IntRope = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Rope) : Rope() {
    inline fun iter(f: (child: Int) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, InnerNode> = when (rope) {
    is Int -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed inline fun iter(f: (child: Rope) -> Boolean) {
    } Rope

private data class InnerNode(val size: Int) : Rope() {
    class
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope) i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(, {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        inline fun iter(f: (child: Rope) -> Boolean) {
    }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): false = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            Pair<Rope, Int>
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, Rope)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: i) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Intchild = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(>, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(is InnerNode: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    rope -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : inline fun iter(f: (child: Rope) -> Boolean) {
    }() {
    Rope
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): val size: Int = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(Pair<Rope, Int>) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<}, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    Rope
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Rope<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Pair) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i iter 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun <(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val Boolean: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> size) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = val
sealed class Rope

private data class InnerNode(when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
} size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: IntPair<Rope, Int>: ) = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope. Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope:iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: }): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        Int

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        ropeprivate data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

.
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: String) {
    }
}
yÌ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (a == 1 && b == 2) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { f: A.() -> Unit ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 0) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i - 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { (a: Any) ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÕ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining("Should be executed once", i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Int = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : A, B {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
class A {
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): p<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¦// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun test() = runExt { r }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: s, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1u) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun box(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Any() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { box ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yq// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

inline class UInt(val value: Int)
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (str2 = str) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): x<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yœ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

open class Foo {
    open fun foo(x: CharSequence = "O"): CharSequence = x
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(x, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(box: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: foo) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : BaseClass() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int?) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.Outer { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(String, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, ULong> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Father, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¦// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) "OK"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val withIndex: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(5)
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// WITH_RUNTIME

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÎ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (c1 == 3 && c2 == 3) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> x) {
    }
}
y¶// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: () -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return x(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : equals() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): String<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y)// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Base(
            object : IFn {
                override fun invoke(): String = Host.ok()
            }
    ) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining()
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yŸ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
class Test {
    var storage = "Fail"

    var Int.foo: String
        get() = storage
        set(value) {
            storage = value
        }

    fun test(): String {
        val i = 1
        i.foo = "OK"
        return i.foo
    }
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

inline class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is B -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    private companion object {
        val result = "OK"
    }

    class Nested {
        fun foo() = result
    }

    fun test() = Nested().foo()
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: String, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return String(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<T, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yf// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {}

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y*// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

class M {
  operator fun Int.component1() = this + 1
  operator fun Int.component2() = this + 2

  fun doTest(): String {
      var s = ""
      for ((a, b) in 0..2) {
        s += "$a:$b;"
      }
      return s
  }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(x: Any, y: Any) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : A<String>() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Char, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i == 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, f> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Int, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { i ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yê// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {
        fun foo(x: String) {
            s += x
        }

        {
            foo(foo, 1)
            foo("K")
        } ()
    }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is Int -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: T2) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (K) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class n(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y'// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> this
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yB// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
    c
    d
    return "OK"
}
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<String, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<b, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(test, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Int) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: String) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            true
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÈ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.subSequence { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Item, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : A() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yx// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

class Box<T>(t: T) {
    var value = t
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: arrayOf) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 42) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun box() = if (B().foo(1) == 42) "OK" else "Fail"
}
y°// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw peek()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yw// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (
    val width: Int,
    val height: Int,
    // This function tells the constructor which cells are alive
    // if init(i, j) is true, the cell (i, j) is alive
    init: (Int, Int) -> String
) -> Boolean) {
    }
}
yª// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: K) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(a: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y8// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    C2().toString() != "C" -> "Failed #2"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: S) -> Boolean) {
    }
}
yÏ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, public val x: String): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is String -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : AssertionError() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> ArrayList) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> String) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Inner() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: ss, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<prop, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class MinUS

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: X) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is T -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
ye// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.add(i)

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// KJS_WITH_FULL_RUNTIME

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¶// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw component1()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(A, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: () -> String) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { y ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 2) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (Inner) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yi// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.Outer("O")

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, s: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.prop { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : A(value), B {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

operator class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: elements) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yô// Auto-generated by GeneratePrimitiveVsObjectEqualityTestData. Do not edit!
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Any<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is toUShort -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return f(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining("", i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, String> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw Exception()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inner fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    open fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : f() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(thisRef: Any?, property: KProperty<*>) {
    }
}
yò// Original bug: KT-37845
// Auto-generated by GeneratePrimitiveVsObjectEqualityTestData. Do not edit!

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y·// WITH_RUNTIME
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yá// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            {
        item() {
            a {
                text = item
            }
        }
    }
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (equals7) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Double) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun check(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y3// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when(En.A) {
    En.A -> "OK"
    En.B -> "Fail 1"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode() : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (Long) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { y: Int ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (x: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(step: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw AssertionError()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    operator fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : String() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (var value: Int) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Int() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val l: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÓ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw AssertionError("Should be executed once")
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

const class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yä// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (UninitializedPropertyAccessException < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: i, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : demo() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¨// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) "Fail"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yl// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { ok() }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return "fail 1: ${test(null)}"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (sum != 149) throw AssertionError()
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { i: Int = 1 ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÈ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Comparable<A> {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Tr<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y°// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(val x: Int) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

open inner fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yõ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
class Outer {
    val ok = "OK"

    inner class Inner : Base(::ok)
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y/// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    !doChain(null) -> "failed 6"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

fun box(): String {
    return object : Base2, Base by Delegate() {
    }.test()
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y›// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun foo(): Any
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return minus(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

public open fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i != 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(x: T): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, val a: String = {
            open class B() {
                open fun s() : String = "O"
            }

            val o = object : B() {
                override fun s(): String = "K"
            }

            B().s() + o.s()
        }()): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y[// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    plusZero == minusZero -> {
                return "fail 3"
            }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> value) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<ULong, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { aDouble ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (w < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : () {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : OsType() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val AssertionError: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yø// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (this) {
        is String -> return this
        is Level -> {
            when (this) {
                Level.O -> return Level.O.name
                Level.K -> return Level.K.name
            }
        }

        else -> return "fail"
    }
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { () ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(l : ArrayList<C>) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, "O")
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y1// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    0.toShort() == an -> "Fail 24"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): ArrayList<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(2.toLong(), i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(arr: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : reflect() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(a: Int = 1): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): String = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yª// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: B) {
    }
}
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw bar()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¦// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter() {
    }
}
yÈ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(val f: () -> Unit, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (i) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÍ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (map.get("a") != 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
ys// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

open class A(val array: Array<Any>)
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw i()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Base() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¶// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return bar(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    val a = A()
    if (a.size != 56) return "fail: ${a.size}"

    return "OK"
}
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (x < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yj// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        TestPackage().OK

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw typeName(ArrayList<Int>())
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (b < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yœ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {
            foo("O")
            foo(foo, 1)
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val b: B) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÊ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: KProperty<*>) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y®// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw II()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (Derived1) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (box < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : l() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Comparable<Book> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Test, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yg// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun box(): String {
    val list1 = ArrayList<Int>()
    for (i in MaxI..MaxI step 1) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>(MaxI)) {
        return "Wrong elements for MaxI..MaxI step 1: $list1"
    }

    val list2 = ArrayList<Int>()
    for (i in MaxB..MaxB step 1) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>(MaxB.toInt())) {
        return "Wrong elements for MaxB..MaxB step 1: $list2"
    }

    val list3 = ArrayList<Int>()
    for (i in MaxS..MaxS step 1) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Int>(MaxS.toInt())) {
        return "Wrong elements for MaxS..MaxS step 1: $list3"
    }

    val list4 = ArrayList<Long>()
    for (i in MaxL..MaxL step 1) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Long>(MaxL)) {
        return "Wrong elements for MaxL..MaxL step 1: $list4"
    }

    val list5 = ArrayList<Char>()
    for (i in MaxC..MaxC step 1) {
        list5.add(i)
        if (list5.size > 23) break
    }
    if (list5 != listOf<Char>(MaxC)) {
        return "Wrong elements for MaxC..MaxC step 1: $list5"
    }

    return "OK"
}
}
y‡// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

class OneImpl : One {
  public override fun foo() = 1
}
y¬// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: Int) {
    }
}
yy// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
// KJS_WITH_FULL_RUNTIME
// WITH_RUNTIME
class World() {
  public val items: ArrayList<Item> = ArrayList<Item>()

  inner class Item() {
    init {
      items.add(this)
    }
  }

  val foo = Item()
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÈ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val fn: () -> String) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.flushIt { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yo// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {x -> C(x)}

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¶// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Long = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (box) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yn// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { ls[it] }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: List) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, "1")
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÉ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is Result.ParseError -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {
        foo(foo) // 1st foo is a local fun, second is a captured local var
        foo()
    }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÍ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { cause: Throwable? ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yk// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.Nested("OK")

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yŸ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {
        (A) {
            A.result = "OK"
        }
    }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw let()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return ArrayList(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (String < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÇ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodeprivate constructor() : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i + 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÊ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val x1: T1, val x2: T2) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (N < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yd// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.Any()

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(str2: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (toShort: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

override class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : x() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun order(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): String? = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw b()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, TInput> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<result, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: String) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<box, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (C) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y-// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    var s = ""
    operator fun plus(x : String) : MyString {
        s += x
        return this
    }

    override fun toString(): String {
        return s
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when {
        !inInt(1L) -> "Fail !inInt"
        inInt(0L) -> "Fail inInt"
        notInInt(1L) -> "Fail notInInt"
        !notInInt(0L) -> "Fail !notInInt"
        !inLong(1) -> "Fail !inLong"
        inLong(0) -> "Fail inLong"
        notInLong(1) -> "Fail notInLong"
        !notInLong(0) -> "Fail !notInLong"
        !inFloat(1.0) -> "Fain !inFloat"
        inFloat(0.0) -> "Fain inFloat"
        notInFloat(1.0) -> "Fail notInFloat"
        !notInFloat(0.0) -> "Fail !notInFloat"
        !inDouble(1.0F) -> "Fail !inDouble"
        inDouble(0.0F) -> "Fail inDouble"
        notInDouble(1.0F) -> "Fail notInDouble"
        !notInDouble(0.0F) -> "Fail !notInDouble"
        else -> "OK"
    }
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class T

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(1) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return AssertionError(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<ULong> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, UInt> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> KProperty<*>) {
    }
}
yÿ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            {
    val fnZ = ::fooZ
    if (fnZ.invoke(Z(42)).int != 42) throw AssertionError()

    val fnStr = ::fooStr
    if (fnStr.invoke(Str("str")).string != "str") throw AssertionError()

    val fnNStr = ::fooNStr
    if (fnNStr.invoke(NStr(null)).string != null) throw AssertionError()
    if (fnNStr.invoke(NStr("nstr")).string != "nstr") throw AssertionError()

    return "OK"
}
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Target> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, 20)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, 0)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return "OK"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (Test) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: Float?) {
    }
}
yª// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: T) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (sum < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { Any ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : o() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(p: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Test {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yØ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun box(): String {
    Class.test()
    Interface.test()
    return "OK"
}
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return Delegate()
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(i, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y™// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            {
    return A().a
}
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Two, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(String: (child: Rope) -> Boolean) {
    }
}
yÖ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (a5 !is Double || a5 != 2.0) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<AImpl, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, 999)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 10) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (D < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw A()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÎ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : ResultReceiver<T>() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yC// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (a) {
        A.B -> b = true
        is A.C -> b = false
    }
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 3) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.String { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun myNotEquals0(a: Float, b: Float) = a != b
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(doTest: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val b: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÖ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {

    var cond1: String = ""

    var cond2: String = ""

    val prop1 = if (cond1(p)) p1 else null

    val prop2 = if (cond2(p)) p2 else null;


    fun cond1(p: String): Boolean {
        cond1 = "cond1"
        return p == "test"
    }

    fun cond2(p: String): Boolean {
        cond2 = "cond2"
        return p == "test"
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(Derived, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : D() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, outer: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
    if (s == null) return 0
    return when(s) {
        "abc" -> s
        else -> "xyz"
    }.length
}
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) TEST
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val string: String) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¤// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    open inner class InnerA
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (nx < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<String, String> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÇ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return IntentionsBundle.message2("O")
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class String

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return MyAlias(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÉ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Base<Long>(-1) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<R, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class Nothing(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// KJS_WITH_FULL_RUNTIME
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (l: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.List { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is prop -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return list2(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val x: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Long) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yO// Original bug: KT-37845
// Duplicated bug: KT-14961

fun box(): String {
    if (Z(800).test() != 842) throw AssertionError()
    if (Z(400).test(32) != 432) throw AssertionError()

    return "OK"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, foo)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (IntArray) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÒ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
inline class AsAny<T>(val x: Any?)

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw ppInput()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(x: Comparable<Int>): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// FILE: lateinit.kt

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
ym// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    fun box(): String {
    val foo = Foo()
    if (foo.test()) return "Fail 1"
    if (!foo.test()) return "Fail 2"

    val bar = Bar
    if (bar.test()) return "Fail 3"
    if (!bar.test()) return "Fail 4"

    return bar.p
}
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Local() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÏ// Original bug: KT-37845
// assignPlus on a smart cast should work

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÊ// Original bug: KT-37845
// with possible local optimizations

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : b() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Boolean = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (range2 < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¹// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return Number(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÃ// !LANGUAGE: +NewInference
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yZ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
    val ax: Char? = '0'
    val an: Char? = null
    val bx: Char = '0'
    val by: Char = '1'

    return when {
        '0' != nx -> "Fail 0"
        '1' == nx -> "Fail 1"
        !('0' == nx) -> "Fail 2"
        !('1' != nx) -> "Fail 3"
        x != nx -> "Fail 4"
        y == nx -> "Fail 5"
        !(x == nx) -> "Fail 6"
        !(y != nx) -> "Fail 7"
        '0' == nn -> "Fail 8"
        !('0' != nn) -> "Fail 9"
        x == nn -> "Fail 10"
        !(x != nn) -> "Fail 11"
        '0' != ax -> "Fail 12"
        '1' == ax -> "Fail 13"
        !('0' == ax) -> "Fail 14"
        !('1' != ax) -> "Fail 15"
        x != ax -> "Fail 16"
        y == ax -> "Fail 17"
        !(x == ax) -> "Fail 18"
        !(y != ax) -> "Fail 19"
        bx != ax -> "Fail 20"
        by == ax -> "Fail 21"
        !(bx == ax) -> "Fail 22"
        !(by != ax) -> "Fail 23"
        '0' == an -> "Fail 24"
        !('0' != an) -> "Fail 25"
        x == an -> "Fail 26"
        !(x != an) -> "Fail 27"
        bx == an -> "Fail 28"
        !(bx != an) -> "Fail 29"
        else -> "OK"
    }
}
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yˆ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() { }
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (AssertionError) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, *> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun x2(f: (child: Rope) -> Boolean) {
    }
}
yÉ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { val s: String ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

annotation fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¯// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(arg : Int) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        reflect.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÂ// Original bug: KT-37845
// Duplicated bug: KT-14961

operator fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y±// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(val x: Long) {
    }
}
yf// Original bug: KT-37845
// Duplicated bug: KT-14961

fun box(): String {
    Unit

    val a = Unit
    val b = Unit
    if (a != b) return "Fail a != b"

    if (Unit != Unit) return "Fail Unit != Unit"

    return "OK"
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): B = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class String(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yH// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (x) {
        0 -> return "OK"
        else -> return "Fail 2"
    }
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yª// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: A) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Boolean) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: y, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (s2: String) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Array<T>, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÅ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    operator fun getValue(t: Any?, p: KProperty<*>): Int = 1
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    companion fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return "fail 000: ${a0.size}"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yß// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return if (j == 10 && 11 == i) "OK" else "fail j = $j i = $i"
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
    var i : Int?
    i = 10
    // Postfix increment on a smart cast should work
    // Specific: i.inc() type is Int but i and j types are both Int?
    val j = i++

    return if (j == 10 && 11 == i) "OK" else "fail j = $j i = $i"
}
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yæ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
interface A {
    fun f(cause: Int? = null): Boolean
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> box) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(i: Int, j: Int = i) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return t2(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y²// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return Derived()
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(b: Boolean, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yË// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: AssertionError, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is Long -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(box: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return d(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÏ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, val fn: () -> String): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yn// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
class A {
    open inner class Inner(val result: String)

    fun box(): String {
        val o = object : Inner("OK") {
            fun ok() = result
        }
        return o.ok()
    }
}

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y²// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            {
            throw Exception3("K")
        }
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÄ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { contains ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y‰// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { (x, y, z) -> x + y + z.toString() }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁtailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
// Duplicated bug: KT-14961

// Original bug: KT-37845
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961
    inline fun iter(f: (child: Rope) -> Boolean) {
tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {

    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

            if (i < 1) return leafContaining(child, i)
    is InnerNode -> {
        rope.iter { child ->
tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÁ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
    }
        }

        throw IllegalStateException()
            false
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    isInnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec  leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline funiter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when(rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: ) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope,Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> ) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return (child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNodeval size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope)-> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y°// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}


private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope(){
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

privatedata class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun (f: (child: Rope) -> Boolean) {
    }
}
y ÿ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = 
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class 

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
     InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inlinefun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(valsize: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y…// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> =when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y—// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if () return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) :  {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y©// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return 
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
     fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is  -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yd// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter 

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y³// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun (rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): PairRope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {  ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter {child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if(i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope) {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        .iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, )
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

 fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() 
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y…// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() 
y#// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> 
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
             (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child 
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec funleafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: ): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data classInnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: , i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val : Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode-> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1 return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) ->Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child:Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when () {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1)  leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y_// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
     -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size:Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope( {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, > = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int>  when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private  class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        ropeiter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¦// Original bug: KT-37845


tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y­// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining: Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¤// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter {
    }
}
y©// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: ) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

         IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int)  Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline  iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iterf: (child: Rope) -> Boolean) {
    }
}
y´// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f:  -> Boolean) {
    }
}
yº// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) 
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContainingrope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i <1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealedclass Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrecfun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {}
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode ->{
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): <Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f:(child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int) Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¶// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y·// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i:Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter{ child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y ½// Original bug: KT-37845
// Duplicated bug: KT-14961


sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false}

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int): Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¾// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode( size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¬// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw ()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean){
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) 
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope)  Boolean) {
    }
}
yP// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope


yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: ) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
 class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Ropeprivate data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) returnleafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    
}
y²// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int):  = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i< 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < ) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, : Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y·// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child,i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed classRope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¢// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) 
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throwIllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¿// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope. { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope:Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y½// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> =  (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope,i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i  1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¨
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y²// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException(
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¤// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y»// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, ): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }

yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int: Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¸// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class (val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data  InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yZ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if ( < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yµ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

 class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) 
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y·// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (: Rope) -> Boolean) {
    }
}
yÀ// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {
    }
}
y¼// Original bug: KT-37845
// Duplicated bug: KT-14961

tailrec fun leafContaining(rope: Rope, i: Int): Pair<Rope, Int> = when (rope) {
    is InnerNode -> {
        rope.iter { child ->
            if (i < 1) return leafContaining(child, i)
            false
        }

        throw IllegalStateException()
    }
}
sealed class Rope

private data class InnerNode(val size: Int) : Rope() {
    inline fun iter(f: (child: Rope) -> Boolean) {

}
ÿÿ