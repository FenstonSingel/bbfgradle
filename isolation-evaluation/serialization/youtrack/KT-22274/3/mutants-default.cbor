øiexportTaggdefaultnoriginalSampley// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
gmutantsüy	// Original bug: KT-33259
// Duplicated bug: KT-22274


val c = false
if (c) {fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}} else {fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}}

yˇ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() 
val p = false
if (p) {{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}} else {{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}}

y„// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    
val j = false
when (j) {
 true -> {lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
 else -> {lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
}

    print(" done with explicit label")
}
yS// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
val h = false
if (h) {print(" done with explicit label")} else {print(" done with explicit label")}

}
y›// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 
val n = true
when (n) {
 true -> {listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
 else -> {listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
}

    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
val p = true
try
{print}
catch(e: Exception){}
finally{}
(" done with explicit label")
}
yH// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    
val m = false
when (m) {
 true -> {lit@}
 else -> {lit@}
}
 listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yF// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 
val g = false
try
{listOf(1, 2, 3, 4, 5)}
catch(e: Exception){}
finally{}
.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y«// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).
val t = true
when (t) {
 true -> {forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
 else -> {forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
}

    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 
val p = true
try
{listOf}
catch(e: Exception){}
finally{}
(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y8// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).
val b = false
if (b) {forEach} else {forEach}
 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(
val w = true
try
{" done with explicit label"}
catch(e: Exception){}
finally{}
)
}
y≠// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach 
val e = false
if (e) {{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }} else {{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}

    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(
val k = true
try
{1}
catch(e: Exception){}
finally{}
, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yD// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 
val i = true
when (i) {
 true -> {2}
 else -> {2}
}
, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yD// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 
val v = true
when (v) {
 true -> {3}
 else -> {3}
}
, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yF// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 
val h = false
try
{4}
catch(e: Exception){}
finally{}
, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yF// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 
val b = false
try
{5}
catch(e: Exception){}
finally{}
).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y¨// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach 
val s = true
if (s) {{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }} else {{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}

    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        
val m = true
try
{if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)}
catch(e: Exception){}
finally{}

    }
    print(" done with explicit label")
}
yG// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        
val u = true
if (u) {if (it == 3) return@lit} else {if (it == 3) return@lit}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yL// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
val l = true
when (l) {
 true -> {print(it)}
 else -> {print(it)}
}

    }
    print(" done with explicit label")
}
y6// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
val g = false
if (g) {print} else {print}
(it)
    }
    print(" done with explicit label")
}
yK// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (
val a = false
when (a) {
 true -> {it == 3}
 else -> {it == 3}
}
) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yF// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) 
val t = false
try
{return@lit}
catch(e: Exception){}
finally{}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y3// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (
val o = false
if (o) {it} else {it}
 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yF// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it 
val n = false
try
{==}
catch(e: Exception){}
finally{}
 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y2// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 
val n = false
if (n) {3} else {3}
) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yE// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(
val t = true
when (t) {
 true -> {it}
 else -> {it}
}
)
    }
    print(" done with explicit label")
}
yH// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return
val w = false
when (w) {
 true -> {@lit}
 else -> {@lit}
}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ (listOf(1, 2, 3, 4, 5)).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        (if (it == 3) return@lit) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return(@lit) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print!!(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4!!, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5)!!.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 4(1, 2, 3, listOf, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// local return to the caller of the lambda, i.e. the forEach loop
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // Original bug: KT-33259
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ @.forEach {
        if (it == 3) returnlistOf(1, 2, 3, 4, 5)lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(2, 1, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(", 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label1)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 5, 4).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) }@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    return
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(it {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print())
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print()" done with explicit label"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) )@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(itreturn
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 35 4, ,).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (
    }
    printprint(it)" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, (, 4, 5).forEach {
        if 3it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it), 3, 4, 5).forEach {
        2
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)).forEach {
        5
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print"" done with explicit label()
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, it, 4, 5).forEach {
        if (3 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2. 3, 4, 5),forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf done with explicit label1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("(")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
3

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == // Duplicated bug: KT-22274) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, ), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"3
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, print, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    3(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, print, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        3(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return) // local return to the caller of the lambda, i.e. the forEach loop
        print(it@lit
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, print(it), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        3
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, print(" done with explicit label"), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    3
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach fun
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    lit@ {
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).( {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        printforEachit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }).forEach 5
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1it 2, 3, 4, 5).forEach {
        if (, == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, return@lit, 5).forEach {
        if (it == 3) 4 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, print, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    2(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return)lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"@
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4" done with explicit label" 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(,)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == () return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print3it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    it listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(lit@)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun 4() {
    lit@ listOf(1, 2, 3, foo, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foolit@) {
    ( listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(3 {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == )) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, " done with explicit label").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(5)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, if).forEach {
        5 (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, }, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    2
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo( {
    lit@ listOf()1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, ").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(5 done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 3.forEach {
        if (it == listOf(1, 2, 3, 4, 5)) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 5).,
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(print(" done with explicit label"), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    1
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }) {
    lit@ listOf(1, 2, 3, 4, 5).forEach (
    print(" done with explicit label")
}
y{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
// Duplicated bug: KT-22274

fun foo() // Original bug: KT-33259
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    4 listOf(1, 2, 3, lit@, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, print, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    4(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5)." {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(forEach done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    " done with explicit label"
    print(lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it done with explicit label
    }
    print(")")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach print
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
         done with explicit label // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("if (it == 3) return@lit")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, @lit, 3, 4, 5).forEach {
        if (it == 3) return2 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
listOf

fun foo() {
    lit@ // Duplicated bug: KT-22274(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
} foo() fun
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3@ 4, 5).forEach {
        if (it == 3) return,lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }() {
    lit@ listOf(1, 2, 3, 4, 5).foo
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        it == 3 (if) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }).5
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2print 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    ,(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }() {
    lit@ foo
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(" done with explicit label" {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print())
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(@lit, 2, 3, 4, 5).forEach {
        if (it == 3) return1 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foolit {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(", 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(1 done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf( done with explicit label, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("1")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        // local return to the caller of the lambda, i.e. the forEach loop if (it == 3) return@lit
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, print).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    5(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfit == 31, 2, 3, 4, 5).forEach {
        if (() return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(lit@ {
    ) listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    return listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) lit@@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5){forEach .
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3 done with explicit label return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(")")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}() foo
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        }
    if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).@lit {
        if (it == 3) returnforEach // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun fooforEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    lit@ listOf(1, 2, 3, 4, 5).()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, (" done with explicit label"), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print4
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlit@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@  done with explicit label.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("listOf(1, 2, 3, 4, 5)")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnit // local return to the caller of the lambda, i.e. the forEach loop
        print(@lit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(" done with explicit label", 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(1)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (return@lit == 3) it // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo") {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label()
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4lit 5).forEach {
        if (it == 3) return@, // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ @lit.forEach {
        if (it == 3) returnlistOf(1, 2, 3, 4, 5) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5){
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }forEach .
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).it == 3 {
        if (forEach) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ it == 3(1, 2, 3, 4, 5).forEach {
        if (listOf) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ )(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"listOf
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, ", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label4)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, ==, 4, 5).forEach {
        if (it 3 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
it

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(// Duplicated bug: KT-22274)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3( return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print)" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return// local return to the caller of the lambda, i.e. the forEach loop @lit
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfreturn@lit1, 2, 3, 4, 5).forEach {
        if (it == 3) ( // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ it.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(listOf(1, 2, 3, 4, 5))
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (print) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    it == 3(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(5, 2, 3, 4, 1).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2( 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print," done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 3(1, 2, listOf, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}

// Duplicated bug: KT-22274
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ return@lit(1, 2, 3, 4, 5).forEach {
        if (it == 3) listOf // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print}
    }
    print(" done with explicit label")
(it)
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).)
    print(" done with explicit label"forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() @
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return{lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach print(" done with explicit label")
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun fooit == 3 {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (()) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (@lit == 3) returnit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() print(" done with explicit label")
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    {
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

lit foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@fun // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun fooit {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(())
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(print, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    1(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (it)print
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

" done with explicit label" foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(fun)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) ( // local return to the caller of the lambda, i.e. the forEach loop
        printreturn@litit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, @lit, 4, 5).forEach {
        if (it == 3) return3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, it).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(5)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo{) (
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    (
    printlit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }" done with explicit label")
}
y// Original bug: KT-33259
print(" done with explicit label")

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    // Duplicated bug: KT-22274
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfforEach.(1, 2, 3, 4, 5) {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfit1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(()
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf==1, 2, 3, 4, 5).forEach {
        if (it ( 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun  done with explicit label() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("foo")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 3, 3, 4, 5).forEach {
        if (it == 2) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    it@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(lit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4print 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ,(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).( {
        if forEachit == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfprint(" done with explicit label").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    (1, 2, 3, 4, 5)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ )(1, 2, 3, 4, 5listOf.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yfun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
// Duplicated bug: KT-22274

// Original bug: KT-33259
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2,  done with explicit label, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("3")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, return, 5).forEach {
        if (it == 3) 4@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach print
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    {(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    3 listOf(1, 2, 3, 4, 5).forEach {
        if (it == lit@) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
         done with explicit label
    }
    print("if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfif1, 2, 3, 4, 5).forEach {
        ( (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 4, 5).forEach 3
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @lit listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlit@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, if (it == 3) return@lit, 4, 5).forEach {
        3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach (it)
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print{
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun 3() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == foo) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it } 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
==
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, it == 3, 5).forEach {
        if (4) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(" done with explicit label")1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(,, 21 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it == 3)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, (, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print3it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).// Duplicated bug: KT-22274
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, if (it == 3) return@lit, 5).forEach {
        4 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun fooit) {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(()
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }.forEach (1, 2, 3, 4, 5)
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)  done with explicit label // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("return@lit")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnprint // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    @lit(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it @ 3) return==lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3print return@lit // local return to the caller of the lambda, i.e. the forEach loop
        )(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun )() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(itfoo
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    litit listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(@)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it) {
        forEach
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

2 foo() {
    lit@ listOf(1, fun, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 24 3, ,, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
return@lit

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) // Duplicated bug: KT-22274 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(print(it), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        1
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } forEach
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5) done with explicit labelforEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(".")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it ) 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"==
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, }, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
2
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print}it)
    (
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2forEach 3, 4, 5)., {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, forEach, 4, 5).3 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfreturn.forEach {
        if (it == 3) (1, 2, 3, 4, 5)@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    5 listOf(1, 2, 3, 4, lit@).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, // local return to the caller of the lambda, i.e. the forEach loop, 4, 5).forEach {
        if (it == 3) return@lit 3
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnprint // local return to the caller of the lambda, i.e. the forEach loop
        @lit(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(,1 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, " done with explicit label", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(4)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).}
    print(" done with explicit label")
forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(3, 2, 1, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it print 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ==(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, it).forEach {
        if (5 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y{
// Duplicated bug: KT-22274

fun foo() // Original bug: KT-33259
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(")" done with explicit label
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() print(it)
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        {
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ if (it == 3) return@lit(1, 2, 3, 4, 5).forEach {
        listOf // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf3.forEach {
        if (it == (1, 2, 3, 4, 5)) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ )(1, 2, 3, 4, 5).forEach {
        if (it == 3listOf return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach // Duplicated bug: KT-22274
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ "
    print(" done with explicit labellistOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@it // local return to the caller of the lambda, i.e. the forEach loop
        print(lit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@// local return to the caller of the lambda, i.e. the forEach loop lit
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfprint.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (1, 2, 3, 4, 5)(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }

fun foo() {
    // Duplicated bug: KT-22274
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ {.forEach listOf(1, 2, 3, 4, 5)
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(print)
    }
    it(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun ==() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it foo 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ it(1, 2, 3, 4, 5).forEach {
        if (listOf == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo)) {
    lit@ listOf(1, 2, 3, 4, 5(.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, ==).forEach {
        if (it 5 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}) (
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(), 2, 3, 4, 5).forEach {
        if (it == 31 return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(")
    }
    print(" done with explicit labelit)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    ) listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3lit@ return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == print) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        3(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 2, 3, 4, 5).forEach ,
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } listOf(1, 2, 3, 4, 5).lit@
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4{ 5).forEach ,
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3it return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print())
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        " done with explicit label"(it)
    }
    print(print)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).it {
        if (forEach == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() "
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label{)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

listOf foo() {
    lit@ fun(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
{
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}

fun foo() // Duplicated bug: KT-22274
y// Original bug: KT-33259
// Duplicated bug: KT-22274

it foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (fun == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() ==
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it { 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y==
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it // Original bug: KT-33259 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == print) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    3(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foolistOf(1, 2, 3, 4, 5) {
    lit@ ().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        } (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
if
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo({
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
} )
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, it, 5).forEach {
        if (4 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

() foofun {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        ) (it == 3if return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1. 2, 3, 4, 5),forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).print(" done with explicit label")
    forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(it, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(1)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

lit@ foo() {
    fun listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ }.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
listOf(1, 2, 3, 4, 5)
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    " done with explicit label"(print)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(., 2, 3, 4, 5)1forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2" 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(, done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ )(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(itlistOf
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf({
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 2, 3, 4, 5).forEach 1
    print(" done with explicit label")
}
ylit@
// Duplicated bug: KT-22274

fun foo() {
    // Original bug: KT-33259 listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
     done with explicit label("print")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, }, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
4
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach }
    print(" done with explicit label")
{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
ylit
// Duplicated bug: KT-22274

fun foo() {
    // Original bug: KT-33259@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5 done with explicit label.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(")")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    lit@ listOf(1, 2, 3, 4, 5).forEach ()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5(.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print)it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    print@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        lit(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun it() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (foo == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, " done with explicit label", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(3)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun (() {
    lit@ listOffoo1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ {(1, 2, 3, 4, 5).forEach listOf
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    lit@ listOf(1, 2, 3, 4, 5).forEach {
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo2) {
    lit@ listOf(1, (, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ print(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        listOf(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun print() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        foo(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 4, 3, 2, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf((it), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print1
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    litlistOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } @
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
        {
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, }, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    4
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5)." done with explicit label"
    print(forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2if (it == 3) return@lit 3, 4, 5).forEach {
        , // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, (, 5).forEach {
        if 4it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) print@lit // local return to the caller of the lambda, i.e. the forEach loop
        return(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3return )@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    " listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(lit@ done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    litforEach listOf(1, 2, 3, 4, 5).@ {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        " done with explicit label"
    }
    print(print(it))
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print done with explicit labelit)
    }
    print("(")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit }
        print(it)
    // local return to the caller of the lambda, i.e. the forEach loop
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    print(it)@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        lit
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3" return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label))
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return} // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    @lit
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun "() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit labelfoo)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }(1, 2, 3, 4, 5).forEach listOf
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    1 listOf(lit@, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it " 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(== done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(1 {
    lit@ listOf(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 5).4
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun fooprint {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    ()(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit// local return to the caller of the lambda, i.e. the forEach loop listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit @
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return)lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it@
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun lit() {
    foo@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, it == 3).forEach {
        if (5) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, ), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"4
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, " done with explicit label", 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(2)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    it@ listOf(1, 2, 3, 4, 5).forEach {
        if (lit == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    { listOf(1, 2, 3, 4, 5).forEach lit@
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5)// local return to the caller of the lambda, i.e. the forEach loopforEach {
        if (it == 3) return@lit .
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if it == 3() return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOfprint(it).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (1, 2, 3, 4, 5)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, it, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(4)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() " done with explicit label"
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print({)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (" done with explicit label")
    }
    printprint(it)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    print listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    lit@(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print((" done with explicit label"))
    }
    printit
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print"
    }
    print(" done with explicit label(it))
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() 3
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == {) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5print.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        )(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, return, 3, 4, 5).forEach {
        if (it == 3) 2@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 4, 3, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) print // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    return@lit(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3lit 4, 5).forEach {
        if (it == 3) return@, // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf.(1, 2, 3, 4, 5)forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (== it 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (" done with explicit label") return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(it == 3)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo((1, 2, 3, 4, 5) {
    lit@ listOf).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 33 4, 5).forEach {
        if (it == ,) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnitlit // local return to the caller of the lambda, i.e. the forEach loop
        print(@)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foolit@ {
    () listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return( // local return to the caller of the lambda, i.e. the forEach loop
        print@litit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }@ listOf(1, 2, 3, 4, 5).forEach lit
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, ), 3, 4, 5).forEach {
        if (it == 32 return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it).forEach {
        listOf(1, 2, 3, 4, 5)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, ", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label3)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ (.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    printlistOf(1, 2, 3, 4, 5)" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).} {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
forEach
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 3, 4, 5).forEach ,
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5@lit.forEach {
        if (it == 3) return) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    lit@ listOf(1, 2, 3, 4, 5).)
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(})
    }
    print(" done with explicit label")
it
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    String(" done with explicit label")
}
y Ù// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        foo(foo) // 1st foo is a local fun, second is a captured local var
        foo()
    }
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Z {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (d.toString() != "OK") return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ú// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    vf()
}
y ã// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ (L::xx).get(L(1234L))
    print(" done with explicit label")
}
yö// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    val i: Int = 10000
    if (!(i === i)) return "Fail int ==="
    if (i !== i) return "Fail int !=="

    val j: Long = 123L
    if (!(j === j)) return "Fail long ==="
    if (j !== j) return "Fail long !=="

    val d: Double = 3.14
    if (!(d === d)) return "Fail double ==="
    if (d !== d) return "Fail double !=="

    return "OK"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        o(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 42) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// !LANGUAGE: +InlineClasses
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˘// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("OK")
}
y ˆ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(5)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 0, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 4u, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 20, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(Pair(1))
    }
    print(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("O")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @getter listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        T(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ aC(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y µ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
       this.article = "OK"
   }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, "b", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Delegate {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y û// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { TODO() }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, "OK").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ withSideEffect().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˜// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(42)
}
y2// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    OUTER@while (true) {
        var x = ""
        try {
            do {
                x = x + break@OUTER
            } while (true)
        } finally {
            return "OK"
        }
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 3, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).String {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    Outer("O")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @foo listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 9) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(null, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˆ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(1)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (String == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(value)
    }
    print(" done with explicit label")
}
y ö// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { it }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Variable {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, A.ENTRY).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, Project()).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 2, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ x(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@getter // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y,// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (a1.prop != "1#2#3#") return "fail1: ${a1.prop}" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y È// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // !LANGUAGE: +NewInference
        print(it)
    }
    print(" done with explicit label")
}
xx// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    l1@ "s"
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return "fail 3" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(0.0F)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(3, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 2).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y]// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 1, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print("")
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(::f, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ç// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 239.toLong()
    print(" done with explicit label")
}
y ‰// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit //test for appropriate
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// !LANGUAGE: +InlineClasses

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 1) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
xD// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {}
y // Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return "fail 1: ${b.size}" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y&// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return "Fail: ${a.component1()}" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Any(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun s() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Bar()
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    s(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, arrayOf("OK"), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ›// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // See KT-12865
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(null)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { ->
        object {
            override fun toString(): String {
                return "child" + y
            }
        }
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@withLabel1 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return "OK" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @C listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 1).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Base(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(l).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ä// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 0.toLong()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    B@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 0) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Inner(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf("a", 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        i(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (y == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
x~// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ a.result
    print(" done with explicit label")
}
y ö// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).listOf<UInt>()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @loop listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Long {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
xU// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    return "OK"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (x.length == 0) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (Long == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@TestClass // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, true, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˝// Original bug: KT-33259
// unbox

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ String(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, "OK", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ﬁ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { index, uInt ->
        sum = sum * 10 + (index + 1) * uInt.toInt()
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    logged("k", 2.let { it })
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    As(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 0).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 10, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Í// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // !LANGUAGE: +InlineClasses
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (x == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 0U, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    getValue@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y û// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {"$a:$b;"}
    print(" done with explicit label")
}
y Ú// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    loop@ for (i in a.indices) {
    for (j in a.indices) {
      if (i != j && a[i] == a[j]) continue@loop
    }
    return a[i]
  }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, b, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ç// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    label@{ it("K") }
    print(" done with explicit label")
}
y õ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {x = 3}
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).SECOND {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y¿// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    // with possible local optimizations
    if ((-1).toShort() in charSequence.indices != range0.contains((-1).toShort())) throw AssertionError()
    if ((-1).toShort() !in charSequence.indices != !range0.contains((-1).toShort())) throw AssertionError()
    if (!((-1).toShort() in charSequence.indices) != !range0.contains((-1).toShort())) throw AssertionError()
    if (!((-1).toShort() !in charSequence.indices) != range0.contains((-1).toShort())) throw AssertionError()
    // no local optimizations
    if (element1 in charSequence.indices != range0.contains(element1)) throw AssertionError()
    if (element1 !in charSequence.indices != !range0.contains(element1)) throw AssertionError()
    if (!(element1 in charSequence.indices) != !range0.contains(element1)) throw AssertionError()
    if (!(element1 !in charSequence.indices) != range0.contains(element1)) throw AssertionError()
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@B // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        toInt(it)
    }
    print(" done with explicit label")
}
y ˆ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(a)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 1, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˝// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("fail: ")
}
y!// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (result != 2) return "fail2: $result" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun times3() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ú// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        {
    Foo.foo()
    return if (result == "(Stuff)") "OK" else "Fail $result"
}
    }
    print(" done with explicit label")
}
y ı// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // !LANGUAGE: +ProperIeee754Comparisons
        print(it)
    }
    print(" done with explicit label")
}
y Ä// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ list1.size
    print(" done with explicit label")
}
y ˚// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("fail")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ toLong(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    x(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("1")
}
y5// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return if(map["239"] == "932") "OK" else "fail" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print()
    }
    print(" done with explicit label")
}
y ∆// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    loop@ while(true) {
        when (v) {
            1, 2 -> break@loop
        }
    }
    print(" done with explicit label")
}
y// !DIAGNOSTICS: -DEBUG_INFO_SMARTCAST
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).s {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, false, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (condition) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y è// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).B()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// See also KT-6299

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 5, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y/// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return if( v1 == v2 ) "OK" else "$v1 $v2" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        array(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ this(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
x~// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ true.f()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(Z(42))
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(width)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, x, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ bar(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, i, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if(c.prop != 1) return "fail get" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, Pair("O", "K"), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// KJS_WITH_FULL_RUNTIME

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(i, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    a(" done with explicit label")
}
y É// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 9u.toUShort()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Any {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Companion {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(box)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ MIN_VALUE(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
xe// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(x: Array<Int>, y: IntArray) = A(x, y)
y ˆ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    toByte()
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @A listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@id // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    L(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 10).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(result)
    }
    print(" done with explicit label")
}
y µ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    val res = C.Obj.o + C.Obj.InnerObj.k()  + C.Obj.D().ko
    return if (res == "OKKO") "OK" else "Fail: $res"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @withLabel1 listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlabel@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y9// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (ua.compareTo(ub) > 0) {
        throw AssertionError()
    } // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
yz// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (a == null) {
        A()
    }
    else {
        if (aOther == null) {
            return A()
        }
        aOther
    } // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (isZeroUInt(null)) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    loop@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        sideEffects(it)
    }
    print(" done with explicit label")
}
y ç// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    d@ fun (): Int { return@d 4}
    print(" done with explicit label")
}
y&// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, linkedMapOf("O" to "K"), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y •// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        a
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @a listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@a // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1L, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// TARGET_BACKEND: JVM
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @B listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 1, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y õ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { fld }
    print(" done with explicit label")
}
y ™// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {invokeOrder+="0_"; i}
    print(" done with explicit label")
}
yæ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun box() : String {
   if( A(null,19).hashCode() != 19) "fail"
   if( A(239,19).hashCode() != (239*31+19)) "fail"
   if( B(null).hashCode() != 0) "fail"
   if( B(239).hashCode() != 239) "fail"
   if( C(239,19).hashCode() != (239*31+19)) "fail"
   if( C(239,null).hashCode() != 239*31) "fail"
   if( D(239).hashCode() != (239)) "fail"
   if( D(null).hashCode() != 0) "fail"
   return "OK"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ toShort().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(toString)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(String)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    box(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 42, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Int {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y.// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (ss == "0:a;1:b;2:c;3:d;") "OK" else "fail: '$ss'" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, f).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    ls@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, "", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ∑// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        {
        "s"
    }
    }
    print(" done with explicit label")
}
y ö// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { ok }
    print(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(":")
}
y ñ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).Subclass()
    print(" done with explicit label")
}
y+// Original bug: KT-33259
// Duplicated bug: KT-22274

fun box(): String {
    val list1 = ArrayList<Int>()
    for (i in 1 until 5) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>(1, 2, 3, 4)) {
        return "Wrong elements for 1 until 5: $list1"
    }

    val list2 = ArrayList<Int>()
    for (i in 1.toByte() until 5.toByte()) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>(1, 2, 3, 4)) {
        return "Wrong elements for 1.toByte() until 5.toByte(): $list2"
    }

    val list3 = ArrayList<Int>()
    for (i in 1.toShort() until 5.toShort()) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Int>(1, 2, 3, 4)) {
        return "Wrong elements for 1.toShort() until 5.toShort(): $list3"
    }

    val list4 = ArrayList<Long>()
    for (i in 1L until 5L) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Long>(1, 2, 3, 4)) {
        return "Wrong elements for 1L until 5L: $list4"
    }

    val list5 = ArrayList<Char>()
    for (i in 'a' until 'd') {
        list5.add(i)
        if (list5.size > 23) break
    }
    if (list5 != listOf<Char>('a', 'b', 'c')) {
        return "Wrong elements for 'a' until 'd': $list5"
    }

    return "OK"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        box(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 1L, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 56, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y î// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).doTest()
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@innerGetter // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˇ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        {
    fun rmrf(i: Int) {
        if (i > 0) rmrf(i - 1)
    }
    rmrf(5)
    return "OK"
}
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, "2", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
x`// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
        return 1 foo 1
    }
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, this, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    A(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    aExtensionProperty(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ notImplemented().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
x[// Original bug: KT-33259
// Duplicated bug: KT-22274

operator fun C.component1() = i + 1
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    iterator(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @testFun4 listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@A // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y õ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach { x++ }
    print(" done with explicit label")
}
y*// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print("coll as? MutableCollection")
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    instance@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("Should be executed once")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (x + y) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// not nullable variable

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y π// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    loop@ for (i in a) {
        if (c >= 5) continue@loop
        c++
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// !LANGUAGE: -ProperIeee754Comparisons

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ñ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {}
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).x {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ç// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ a.iterator()
    print(" done with explicit label")
}
xS// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
        2
    }
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ AssertionError("$div").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 42L, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 3).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y-// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, try { zap(string) } finally {}, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        other(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        r(it)
    }
    print(" done with explicit label")
}
y$// Original bug: KT-33259
// Duplicated bug: KT-22274

// KJS_WITH_FULL_RUNTIME
fun box(): String {
    val l = ArrayList<Int>()
    l.add(2)
    val sb = StringBuilder()
    for (i in l[0]..3) {
        sb.append(i)
    }
    if (sb.toString() != "23") return "Fail: $sb}"
    return "OK"
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ arrayOf("$x-$y").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun a() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y!// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        getUserIdFromEnvironment()
    }
    print(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print('b')
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(B)
    }
    print(" done with explicit label")
}
y ˛// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("Fail 29")
}
xK// Original bug: KT-33259
// Duplicated bug: KT-22274

fun bar() = Inner()
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        sb(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(toFloat)
    }
    print(" done with explicit label")
}
y ¯// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    hashCode()
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(plusZero)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).foo {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(100)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(true, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, "abcde", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, null, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) "OK" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Outer()
    }
    print(" done with explicit label")
}
y'// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (!(p!! == z!!)) {
        return "OK"
    } // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˆ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(2)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (A(42) == 42) "OK" else "fail" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
fun foo() {

// Duplicated bug: KT-22274
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
// Original bug: KT-33259y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y î// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach 
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2,3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun () {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1,2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it ==3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, , 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, , 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Œ// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit 
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach 
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˚// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ .forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4,5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, , 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")

y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    (" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    @ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˘// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
         // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit// local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y	// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if () return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
xv// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ 
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo) {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ (1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print( done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)  // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y	// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5). {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it  3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if(it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, ).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)}
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if ( == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y Ó// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
x8// Original bug: KT-33259
// Duplicated bug: KT-22274


y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() 
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it== 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
xq// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label)
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
         (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ı// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print()
}
y Û// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print
}
y ˜// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print("")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print" done with explicit label")
}
y// Original bug: KT-33259// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ı// Original bug: KT-33259


fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y §// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y
// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) @lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5)forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y å// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
     listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label"
}
xB// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() 
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == ) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

 foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3,4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        printit)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y ˜
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo(){
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3 return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

funfoo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo() {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loopprint(it)
    }
    print(" done with explicit label")
}
y// Original bug: KT-33259
// Duplicated bug: KT-22274

fun foo( {
    lit@ listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    print(" done with explicit label")
}
ˇˇ