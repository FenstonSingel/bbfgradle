øiexportTaggdefaultnoriginalSampleyú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
gmutantsüy“// Original bug: KT-31946
// Duplicated bug: KT-38681


val t = false
try
{// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}}
catch(e: Exception){}
finally{}


// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y—// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}


val l = true
try
{// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}}
catch(e: Exception){}
finally{}


fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y.// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}


val v = true
if (v) {fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}} else {fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}}

y#// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) 
val l = true
when (l) {
 true -> {{
    // Must omit argument for parameter with default value
    Derived().foo(42)
}}
 else -> {{
    // Must omit argument for parameter with default value
    Derived().foo(42)
}}
}

y—// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<
val r = false
when (r) {
 true -> {T}
 else -> {T}
}
> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    
val z = false
when (z) {
 true -> {// Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}}
 else -> {// Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}}
}

}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    
val h = false
try
{override fun foo(a: Int, b: Any?) {}}
catch(e: Exception){}
finally{}

}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yœ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(
val v = true
if (v) {args: Array<String>} else {args: Array<String>}
) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y·// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    
val f = false
when (f) {
 true -> {Derived().foo(42)}
 else -> {Derived().foo(42)}
}

}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) 
val a = false
when (a) {
 true -> {{}}
 else -> {{}}
}

}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y∆// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : 
val t = false
if (t) {Base<Int>} else {Base<Int>}
() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yæ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) 
val i = true
if (i) {{}} else {{}}

}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    
val c = false
try
{Derived()}
catch(e: Exception){}
finally{}
.foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().
val k = false
try
{foo(42)}
catch(e: Exception){}
finally{}

}
y—// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(
val j = true
try
{a: T}
catch(e: Exception){}
finally{}
, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y›// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, 
val s = true
when (s) {
 true -> {b: Any? = null}
 else -> {b: Any? = null}
}
) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y÷// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(
val q = false
when (q) {
 true -> {a: Int}
 else -> {a: Int}
}
, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y—// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, 
val e = true
try
{b: Any?}
catch(e: Exception){}
finally{}
) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yƒ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    
val v = false
if (v) {Derived} else {Derived}
().foo(42)
}
y¿// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().
val j = false
if (j) {foo} else {foo}
(42)
}
y¿// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = 
val f = true
if (f) {null} else {null}
) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¬// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: 
val m = false
if (m) {Array} else {Array}
<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : 
val r = false
try
{Base}
catch(e: Exception){}
finally{}
<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y—// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(
val r = true
try
{42}
catch(e: Exception){}
finally{}
)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: 
val s = false
try
{T}
catch(e: Exception){}
finally{}
, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: 
val m = true
when (m) {
 true -> {Int}
 else -> {Int}
}
, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y“// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: 
val q = true
when (q) {
 true -> {Any}
 else -> {Any}
}
? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yø// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: 
val c = true
if (c) {Any} else {Any}
?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y√// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<
val d = false
if (d) {String} else {String}
>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y”// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<
val s = false
when (s) {
 true -> {Int}
 else -> {Int}
}
>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yA// Original bug: KT-31946
// Duplicated bug: KT-38681

(// Also reproes on interface
abstract

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int!!, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: (Array<String>)?) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : (Base<Int>)?() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: (T)?, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: (Int)?, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<(String)?>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<(Int)?>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived()!!.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) Base<Int>
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun foo(42)(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().main
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Must omit argument for parameter with default value
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Also reproes if abstract
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: ), b: Any? = nullT {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract foo(42) Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().class
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

Base
class Derived : // Concrete type HAS to be primitive<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, bInt Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: :, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

<String>
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array// Concrete type HAS to be primitive) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Array<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Base<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooAny? {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: (a: T, b: Any? = null)) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Intargs b: Any?) {}
}

fun main(,: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ArrayDerivedString>) {
    // Must omit argument for parameter with default value
    <().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, }) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {b: Any? = null
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Derived fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    open().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : )<Int>() {
    override fun foo(a: Int, b: Any?Base {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

()
class Derived : Base<Int>// Concrete type HAS to be primitive {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Tnull {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = >) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class 42 : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(Derived)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<IntInt() {
    override fun foo(a: >, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T) {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42>
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: ,T b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) )
}

// Concrete type HAS to be primitive
class Derived : Base<Int>({} {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo? {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any(a: T, b: Any? = null)) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>(String {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<)>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class foo(42) : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().Derived
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : )() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>Base<Int> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = {) null}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: ), b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>T {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Array<String>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Base<Int>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, {}: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) b
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

>
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String// Also reproes on interface) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
T

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: // Duplicated bug: KT-38681, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Basea: T {
    // Also reproes if abstract
    open fun foo(<T>, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract Int Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<class>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: (, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    DerivedInt).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(args: Array<String>)a: Int, b: Any?) {}
}

fun main( {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: }, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
T
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    b fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, open: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Toverride fun foo(a: Int, b: Any?) {} b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    ,
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    Base
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : // Also reproes if abstract<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
fun class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override abstract foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun ((a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainfooargs: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: (? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooAnya: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<foo>() {
    override fun Int(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: {}, b: Any?) Int
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: // Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}, b: Any? = null) {}
}

T

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooBase {}
}

// Concrete type HAS to be primitive
class Derived : (a: T, b: Any? = null)<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
} class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
abstract

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() <

fun main(args: Array{
    override fun foo(a: Int, b: Any?) {}
}String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: null? = Any) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>String {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<()>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, bString Any?) {}
}

fun main(args: Array<:>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> fun
    // Also reproes if abstract
    open { foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override foo foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().fun(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open a foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(fun: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?Derived() {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    ).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<(args: Array<String>)>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainInt {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived String Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<:>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: <, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseTInt>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

( main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derivedfun).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class BaseBase {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : <T><Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: }, b: Any?) {Int
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b<Int> Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base:() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseIntInt>() {
    override fun foo(a: <, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> ?
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any{) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

(42)

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Int() {
    override fun foo(a: Base<Int>, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooBasea: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : (<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: )?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42Any
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<a>() {
    override fun foo(Int: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}<T> Base

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    String fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<override>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> Any?
    // Also reproes if abstract
    open fun foo(a: T, b: { = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: {?) Any}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(aInt T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: :, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a42 T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(:)
}
yúclass
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
// Original bug: KT-31946 Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Int fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: open, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: foo? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().Any(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: String<Array>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<(> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>T) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : {
    override fun foo(a: Int, b: Any?) {}
}<Int>() Base

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base(args: Array<String>)() {
    override fun foo(a: Int, b: Any?) {}
}

fun main<Int> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<{>() Int
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override String foo(a: Int, b: Any?) {}
}

fun main(args: Array<fun>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
Derived()

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    }.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Int {
    override fun foo(a: Base<Int>(), b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42()
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<String> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<T>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(ab Int, :: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    {} fun foo(a: Int, b: Any?) override
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class BaseIntT> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<<>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
// Must omit argument for parameter with default value Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    class
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(?, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Anya: T) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>(42 {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo())
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: TAny? b: , = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Basea: TT> {
    // Also reproes if abstract
    open fun foo(<, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Derived, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Int().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<foo> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().T(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foomaina: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun ((args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Int> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<T>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Any?<Int>() {
    override fun foo(a: Int, b: Base) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú:
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a// Original bug: KT-31946 Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b// Must omit argument for parameter with default value Any?) {}
}

fun main(args: Array<String>) {
    :
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    {} fun foo(a: T, b: Any? = null) open
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base42Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(<)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    :
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a// Also reproes if abstract Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo)a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived((.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo((, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooa: Ta: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

String

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: TInt b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<,>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Original bug: KT-31946

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>args: Array<String>) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(() {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) )}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>{ {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = :) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(argsnull Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : <String><Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ArrayBase) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: {} = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) Any?
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun )(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42foo
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Derived>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Int().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

foo(42)

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: TBase<Int>() b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : , {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Baseclass {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
<T> Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: > = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<IntAny?() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(aAny T, b: :? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<)>) {
    // Must omit argument for parameter with default value
    Derived().foo(42String
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Array) {}
}

fun main(args: Any?<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
Any?

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: // Duplicated bug: KT-38681 = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T} b: Any? = null) {,
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open Any foo(a: T, b: fun? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun Derived(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    foo().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<foo>) {
    // Must omit argument for parameter with default value
    Derived().String(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : )(Base<Int> {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo>a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int(() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: (42), b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().fooT
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int} b: Any?) {,
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Int, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: T, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, fun) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override b: Any? = null foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override (a: Int, b: Any?) foofun {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
Base<Int>

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : // Duplicated bug: KT-38681() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun }(a: T, b: Any? = null) {foo
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open Int foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: fun, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    Any fun foo(a: Int, b: override?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> (

// Concrete type HAS to be primitive
class Derived : Base<Int>{
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú=
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? // Original bug: KT-31946 null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<}> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
T

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

Int

// Concrete type HAS to be primitive
class Derived : Base<// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Tclass {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
> Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Int? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Any, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

(42)
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo// Also reproes on interface
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) ()}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>{ {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    null
    open fun foo(a: T, b: Any? = // Also reproes if abstract) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: 42<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(Array)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: TArray<String> b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ,) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: AnyString) {}
}

fun main(args: Array<?>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<)> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = nullT {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: String? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Any>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: fun, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override T foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) Derived}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    {().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any= ? null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : String() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Base<Int>>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: AnyBase<Int>() = null) {}
}

// Concrete type HAS to be primitive
class Derived : ? {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

fun
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

// Concrete type HAS to be primitive main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

Array
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: // Concrete type HAS to be primitive<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: {<String>) Array
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
Base

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : // Duplicated bug: KT-38681<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    (
}

// Concrete type HAS to be primitive
class Derived : Base<Int>// Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Array>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Int<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
null

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = // Duplicated bug: KT-38681) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T() {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived>.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<:>() {
    override fun foo(aInt Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(Base: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : a<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base{
    // Must omit argument for parameter with default value
    Derived().foo(42)
}() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) <Int>
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: args, b: Any?) {}
}

fun main(Int: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main()args: Array<String> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Derived().foo(42)) {
    // Must omit argument for parameter with default value
    Array<String>
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) :}
}

fun main(args{ Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<{
    override fun foo(a: Int, b: Any?) {}
}>() Int

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun Int(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<foo>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T(args: Array<String>) {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Array?) {}
}

fun main(args: Any<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class BaseBaseT> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : <<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, Array) {}
}

fun main(args: b: Any?<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<IntAny?() {
    override fun foo(a: Int, b: >) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooArray<String>a: Int, b: Any?) {}
}

fun main(args: () {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<foo(42)>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().Int
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Anyfun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
} = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

?
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: foo(42)?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().Any
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: // Must omit argument for parameter with default value, b: Any?) {}
}

fun main(args: Array<String>) {
    Int
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
Int class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: abstract, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Derived(), b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    T.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {Any?
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: }) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    ()Derived.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base)T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null< {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = Int) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: null, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

// Also reproes on interface
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: foo(42)) {
    // Must omit argument for parameter with default value
    Derived().Array<String>
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: : = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, bAny? Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    <String> fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Arrayopen) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

(42)

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    < fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ArrayopenString>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
{
    override fun foo(a: Int, b: Any?) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() }

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    foo().Derived(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args( Array<String>) {
    // Must omit argument for parameter with default value
    Derived:).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooString {}
}

fun main(args: Array<(a: Int, b: Any?)>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

a

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

)
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?// Also reproes on interface {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: (, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>T) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
Any Derived : Base<Int>() {
    override fun foo(a: Int, b: class?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    (
    Derived// Must omit argument for parameter with default value).foo(42)
}
yú<Int>
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base// Original bug: KT-31946() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    foo fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().override(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>Any? {
    override fun foo(a: Int, b: ()) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(ab: Any? Int, :) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseString() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<<Int>>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open a: Int foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(fun, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: {
    override fun foo(a: Int, b: Any?) {}
}, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() T

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Any() {
    override fun foo(a: Int, b: Base<Int>?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: }? = null) {}
Any

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Base<Int>() fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : open {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: a? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(Any: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<foo>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().Int(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : override<Int>() {
    Base fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Int><T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(Array<String>, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: a: T) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    {
    // Must omit argument for parameter with default value
    Derived().foo(42)
} fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) open
yú:
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b// Original bug: KT-31946 Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Int? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Any>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<>>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<StringInt) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, (: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derivedb).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main{ (args: Array<String>)
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

{
    // Must omit argument for parameter with default value
    Derived().foo(42)
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) // Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a? T, b: Any: = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    (args: Array<String>) fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainopen {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

fun
abstract class Base<T> {
    // Also reproes if abstract
    open // Also reproes on interface foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base:T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived < Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    foo
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().override fun foo(a: Int, b: Any?) {}(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Any<Int>() {
    override fun foo(a: Int, b: Base?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() (
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived{).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) )
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>{} {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived <Int> Base:() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T) {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null> {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Any fun foo(a: T, b: open? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = Any) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: null?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foofooa: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().((42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<(>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().fooInt42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?( {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived)).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: <) {}
}

fun main(args: ArrayAny?String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T: {
    // Also reproes if abstract
    open fun foo(a> T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Array, b: Any?) {}
}

fun main(args: Int<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<{> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) T
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<<Int>> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseT() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(aT :, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(b: Any?, a: Int) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array42) {
    // Must omit argument for parameter with default value
    Derived().foo(<String>)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<()>Int {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : } {
    override fun foo(a: Int, b: Any?) {Base<Int>()
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T) {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null{
    override fun foo(a: Int, b: Any?) {}
} {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() )

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooclassa: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
( Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo()42
}
yúString
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<// Original bug: KT-31946>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<)>(Int {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: String, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<T>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yúoverride
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    // Original bug: KT-31946 fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? Derived null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    =().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T< {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array>String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
< class BaseabstractT> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
foo

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().}(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, ()) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derivedb: Any?.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() :
    override fun foo(a{ Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
Int

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<// Duplicated bug: KT-38681>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<}>() {
    override fun foo(a: Int, b: Any?) {}
Int

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, Derived) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    b: Any? = null().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: main) {}
}

fun Any?(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: (42)?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().fooAny
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = {}) null
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, Derived().foo(42): Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    b
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Basea: Int {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(<T>, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : ,() {
    override fun foo(a: IntBase<Int> b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, Base<Int>) {}
}

// Concrete type HAS to be primitive
class Derived : b: Any? = null() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo{a: Int, b: Any?) {}
}

fun main(args: Array<String>) (
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(()) {
    // Must omit argument for parameter with default value
    Derivedargs: Array<String>.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<}>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
Int
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Anyargs) {}
}

fun main(?: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: // Must omit argument for parameter with default value?) {}
}

fun main(args: Array<String>) {
    Any
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Base<Int>, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : T() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

)

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Array() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Base<Int><String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class args: Array<String><T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(Base) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(aString T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<:>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: <? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseAnyInt>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo( {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(a: Int, b: Any?)42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> Int

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<(>) {
    // Must omit argument for parameter with default value
    DerivedString).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T? {
    // Also reproes if abstract
    open fun foo(a: T, b: Any> = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
b class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, abstract: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) <Int>
}

// Concrete type HAS to be primitive
class Derived : Base{}() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    Any
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Any?> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: T) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
foo(42) Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().class
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : (<Int>Base) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: IntString b: Any?) {}
}

fun main(args: Array<,>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, // Must omit argument for parameter with default value) {}
}

fun main(args: Array<String>) {
    b: Any?
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a) T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>: {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? (a: Int, b: Any?) null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo= {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> >
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int{() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: : = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(argsAny? Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base: {
    // Also reproes if abstract
    open fun foo(a: T, b<T> Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Base fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : open<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

b
class Derived : Base<Int>() {
    override fun foo(a: Int, // Concrete type HAS to be primitive: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
Array

fun main(args: }<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: {}?) Any
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: override fun foo(a: Int, b: Any?) {}, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    T
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun }(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
foo
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<(>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainIntargs: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array{) <String>
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

42
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(// Also reproes on interface)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract b: Any? Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, class) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    Any?
    open fun foo(a: T, b: // Also reproes if abstract = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: { = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) Any?}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: )? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42Any
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {:
}

// Concrete type HAS to be primitive
class Derived } Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}) {}
}

Any?
yú// Original bug: KT-31946
?

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any// Duplicated bug: KT-38681) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Derived().foo(42)<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Base
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo{} {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) (a: T, b: Any? = null)
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) main}
}

fun {(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) foo(42)}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().{
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) a: Int
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo({}, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : 42() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(Base<Int>)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun ,(a: Tfoo b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: )) {}
}

fun main(args: Array<String>Any? {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, ): Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42b
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().}(42)
foo
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    : fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived open Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
< class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ArrayabstractString>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Base<Int>(), b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : T {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

)

// Concrete type HAS to be primitive
class Derived : Base<Int>(// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
} {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
) class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42abstract
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: TString b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<,>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun {(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) foo
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {)
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42}
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Derived, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class T : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    }
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
override fun foo(a: Int, b: Any?) {}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any{} = null) ?
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : , {
    override fun foo(a: IntBase<Int>() b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Baseb {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, <T>: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {main
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun }(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<>Int() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String)> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int,() {
    override fun foo(a: Int> b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: }? = null) {Any
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: String, b: Any?) {}
}

fun main(args: Array<Int>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Tb b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, ,: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseaInt>() {
    override fun foo(<: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : ( {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    DerivedBase<Int>()).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Any> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: T?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) Int}
}

// Concrete type HAS to be primitive
class Derived : Base<{>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b. Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived():foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(foo: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().args(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

Array

fun main(args: // Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooAnya: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: (?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : >() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<StringBase<Int>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>override) {
    ( fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>fun) {
    override fun foo(a: Int, b: Any?) {}
}

( main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    override fun foo(a: Int, b: Any?) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: (?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    DerivedAny).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<(42)>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().fooInt
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Derived().foo(42), b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    T
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : ?() {
    override fun foo(a: Int, b: AnyBase<Int>) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
42

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(// Duplicated bug: KT-38681)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>{ ()
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> Any
    // Also reproes if abstract
    open fun foo(a: T, b: {? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

42 main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(fun)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: 42) {
    // Must omit argument for parameter with default value
    Derived().foo(Array<String>)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<,>() {
    override fun foo(a: IntInt b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

Any
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: // Also reproes on interface?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Base? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Any<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() foo
    override fun {(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> foo
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().{(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) (a: Int, b: Any?)
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo{} {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    .
    Derived()// Must omit argument for parameter with default valuefoo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<{}> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) T
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {main
}

fun }(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooArray<String> {}
}

fun main(args: (a: Int, b: Any?)) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract a Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(class: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b(a: Int, b: Any?) Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo: {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}: T, b: Any? = null) {}
}

a

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
a

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(// Duplicated bug: KT-38681: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
Array<String> Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: class) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: args) {}
}

fun main(Any?: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) null
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: AnyString = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<?>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}>() {
    override fun foo(a: Int, b: Any?) {}
}

Int
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() ,
    override fun foo(a: Int{ b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: ,, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: IntT b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<>> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<IntT() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = Int) {}
}

// Concrete type HAS to be primitive
class Derived : Base<null>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract String Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<class>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
42 Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(class)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<args> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(T: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : ) {
    override fun foo(a: Int, b: Any?Base<Int>() {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) String
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<{}>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

:
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a// Also reproes on interface Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() )

fun main(args: Array<String>{
    override fun foo(a: Int, b: Any?) {}
} {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override Any foo(a: Int, b: fun?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : main<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun Base(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<// Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}> {
    T
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open Array<String> foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: fun) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, :b Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base, {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int<T> b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooInta: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: (, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = nullString {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<)>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú{
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) // Original bug: KT-31946}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : ? {
    override fun foo(a: Int, b: AnyBase<Int>()) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú.
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived()// Original bug: KT-31946foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    Derived().foo(42) fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    open
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(:, ba: T Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T{
    override fun foo(a: Int, b: Any?) {}
} b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() ,

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(foo, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun a: T(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
Any class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: abstract?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() // Must omit argument for parameter with default value

fun main(args: Array<String>) {
    {
    override fun foo(a: Int, b: Any?) {}
}
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = Base<Int>) {}
}

// Concrete type HAS to be primitive
class Derived : null() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base?() {
    override fun foo(a: Int, b: Any<Int>) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: ) = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>Any? {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class (args: Array<String>) : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainDerived {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived foo Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().:(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

.
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived()// Concrete type HAS to be primitivefoo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

Derived()
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    // Concrete type HAS to be primitive.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: args: Array<String>? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(Any) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class ,<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: IntBase b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = nullInt {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: ), b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any{} = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) ?
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: }? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
Any
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null() {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

{ main(args: Array<String>) fun
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<main>() {
    override fun foo(a: Int, b: Any?) {}
}

fun Int(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<42> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(T)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    (42)
    Derived().foo// Must omit argument for parameter with default value
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo<String>a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array() {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>{
    // Must omit argument for parameter with default value
    Derived().foo(42)
} )
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
Derived().foo(42)

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    }
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun <(args: ArraymainString>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : a<Int>() {
    override fun foo(Base: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: fun, b: Any?) {}
}

Int main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any} = null) {?
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}: Any?) {}
}

fun main(args: Array<String>) b
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: 42?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(Any)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, {}) b: Any?
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42(a: Int, b: Any?)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {{
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() }
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>Array {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ()<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
, class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Tabstract b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: ()? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    DerivedAny.foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: foo = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun Any?(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(Derived: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class a : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, Array) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: b: Any? = null<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class b: Any? = null<T> {
    // Also reproes if abstract
    open fun foo(a: T, Base) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract ( Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derivedclass).foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? a: Int null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(=, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>Int {
    override fun foo(a: (), b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    fun fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override open foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Any>() {
    override fun foo(a: Int, b: Int?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
} fun foo(a: Int, b: Any?) {}
}

override
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b{} Any?) :
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : a() {
    override fun foo(Base<Int>: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    > fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Intopen() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(b, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, a: T: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: ,Int b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooAnya: Int, b: (?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<String>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Int>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Tb: Any? = null ,) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Tnull b: Any? = ,) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainStringargs: Array<(>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(): Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42args
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, 42) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(b: Any?)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<b: Any?>() {
    override fun foo(a: Int, Int) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract args: Array<String> Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(class) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(aAny? T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: :) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<{>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) Int
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: }, b: Any? = null) {}
T

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: obj, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Long>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¢// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : (() -> String)?() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y~// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // IGNORE_BACKEND: JS_IR
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().vals(42)
}
yß// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun AssertionError(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(box: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// !LANGUAGE: +InlineClasses
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(x: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : String() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y•// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Double = 1.0, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, prefix: String = "") {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : box<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: String, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yî// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : C() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(B: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: String = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : y<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    foo().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T: Any> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y„// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun box(): String {
    val rem = u % ub
    if (rem != uc) throw AssertionError("$rem")

    return "OK"
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun f(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<String>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: toString, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, v: T) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<testLong> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().E()
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, x: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun Double(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, ok: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Cell>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Double>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¢// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Exception>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>(1) {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun u(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: testFun1, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    inline fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y«// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    companion object {
        protected val F: String = "OK"
    }

    inner class E {
        fun foo() = F
    }
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: String) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Int() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, A: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class a : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<toUInt>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(2)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: a<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    10.toString()
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Long<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, e: E?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    companion fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<C>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// IGNORE_BACKEND: JVM_IR
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Outer().Inner("OK")
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: MutableIterator<*>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun s(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
reified class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<SlashPlayer>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yà// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo() {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(0)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(Int: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo("OK")
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun Int(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(i, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(40)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun xs(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : foo<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yä// Original bug: KT-31946
// Duplicated bug: KT-38681

class TestClass {
    companion object {
        inline operator fun <T> invoke(task: () -> T) = task()
    }
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun MAX_BYTE(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÇ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    fun <T> T.genericThisRef1() = fun () = this
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Int, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(1)
}
y≠// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
    return "OK"
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yì// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(val x: Int) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<foo> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: Outer, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: prop, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y˜// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
    val b = B()
    val a = A(b).a()

    if (a.b !== b) return "failed"

    return "OK"
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yè// Original bug: KT-31946
//WITH_RUNTIME

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    ss().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, T) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y•// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(component1: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : A<IC>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Double, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¢// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T : Any> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // KJS_WITH_FULL_RUNTIME
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, value: String) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : n<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y±// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

inline fun catchAll(x: String, block: () -> Unit): String {
    try {
        block()
    } catch (e: Throwable) {
    }
    return x
}
yY// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

data class A(val x: String, val y: String)

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: i<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yç// Original bug: KT-31946
// Duplicated bug: KT-38681

class Test {
    private val hello: String
        get() { return "hello" }

    fun sayHello() : String = hello
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yí// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Int) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y√// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
    val x = when (val t1 = peek()) {
        Test.A -> {
            when (
                val t2 = when(val y = peek()) {
                    Test.A -> Test.A
                    Test.B -> Test.B
                    else -> Test.OTHER
                }
            ) {
                Test.A ->
                    when (val t3 = peek()) {
                        Test.A -> "OK"
                        else -> "other 3"
                    }

                else -> "other 2"
            }
        }

        else -> "other 1"
    }
    return x
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Outer, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yh// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun box(): String {
    return (Base::result).get(Derived())
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: NoiseMaker<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun objArray(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yt// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    fun box() = Variants.valueStr
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    fun f(): Int {
    try {
        return 0
    }
    finally {
        try { // culprit ?? remove this try-catch and it works.
        } catch (ignore: Exception) {
        }
    }
}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y°// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: String, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : your<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    A().foo(42)
}
yü// !LANGUAGE: +InlineClasses
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Double() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÜ// Original bug: KT-31946
// Duplicated bug: KT-38681

// box
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Int>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, var value: Int) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y˝// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    val plain = 239
    public val read : Int
      get() = 239

    public var readwrite : Int = 0
      get() = field + 1
      set(n : Int) {
        field = n
      }
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, s: () -> Int) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yˆ// Original bug: KT-31946
// See KT-6206 Always generate hashCode() and equals() for data classes even if base classes have non-trivial analogs

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y<// Original bug: KT-31946
// Duplicated bug: KT-38681

class A {
    val y get() = "K"
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Inner() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yM// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun box(): String =
        Test.ok
yÇ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // !LANGUAGE: +InlineClasses
    Derived().foo(42)
}
yˇ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {
    for (x in ByteArray(5)) {
        if (x != 0.toByte()) return "Fail $x"
    }
    return "OK"
}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, val i: Int) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : i<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y;// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    fun box(): String {
    val a = Array(2) { DoubleArray(3) }

    for (i in 1..1) {
        for (j in 0..2) {
            a[i][j] += a[i - 1][j]
        }
    }

    if (a[0][0] != 0.0) return "fail ${a[0][0]}"

    return "OK"
}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Int().foo(42)
}
y∑// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : SomeClass(3.14, 42, "No", "OK", "Yes") {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun HashMap(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: foo, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y§// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(0.0F, null)
}
y¶// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class AssertionError<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(1L)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(44)
}
yè// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(i: Int) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class f : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(111)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: w, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<Outer> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, a: A<String>) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo()
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(msg: String, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(toString: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(n: Int, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Char?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : x<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Array<Any>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yª// Original bug: KT-31946
// Duplicated bug: KT-38681

public abstract class BaseClass() {
    protected abstract val kind : String

    protected open val kind2 : String = " kind1"

    fun debug() = kind + kind2
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, property: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¶// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<(Int)->LongArray>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y•// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(val id: String?, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: I32? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yí// WITH_RUNTIME
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun T(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : test<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class box : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: KProperty<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, v: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun box() : String {
    val b = Box<Long>(x@ (1 + 2))
    val expected: Long? = 3L
    return if (b.value == expected) "OK" else "fail"
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yå//KT-3276
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y´// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    NoSuchElementException().foo(42)
}
yí// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    protected fun foo() = "OK"
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // !LANGUAGE: +InlineClasses
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<Content>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// DONT_TARGET_EXACT_BACKEND: JS_IR
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(val i : Int, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Foo() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
        ++count
        if (count > 1) {
            throw AssertionError("Loop should be executed once")
        }
    }
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class result : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, a: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    1.toByte().plus(1)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Double?, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun box(): String = Impl().foo()
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class u3<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yS// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {

}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo("foo")
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Any?, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yì// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(other: Int) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, val aname: String) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yØ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(dest : Array<in String>, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    other.size
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(test: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : String<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y©// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, val fn: () -> String) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun y(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    String().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(300)
}
yı// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {
    val result = A().result
    if (result != 6) return "fail: $result"
    return "OK"
}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y·// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // EA-38323 - Illegal field modifiers in class: classObject field in C must be static and final 
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yâ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main() {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: String<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(s: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
ys// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    val a = "OK"
    val b = A.a
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yá// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// WITH_RUNTIME
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÑ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun box(): String {
    val list1 = ArrayList<Int>()
    val range1 = MaxI..MaxI
    for (i in range1) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>(MaxI)) {
        return "Wrong elements for MaxI..MaxI: $list1"
    }

    val list2 = ArrayList<Int>()
    val range2 = MaxB..MaxB
    for (i in range2) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>(MaxB.toInt())) {
        return "Wrong elements for MaxB..MaxB: $list2"
    }

    val list3 = ArrayList<Int>()
    val range3 = MaxS..MaxS
    for (i in range3) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Int>(MaxS.toInt())) {
        return "Wrong elements for MaxS..MaxS: $list3"
    }

    val list4 = ArrayList<Long>()
    val range4 = MaxL..MaxL
    for (i in range4) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Long>(MaxL)) {
        return "Wrong elements for MaxL..MaxL: $list4"
    }

    val list5 = ArrayList<Char>()
    val range5 = MaxC..MaxC
    for (i in range5) {
        list5.add(i)
        if (list5.size > 23) break
    }
    if (list5 != listOf<Char>(MaxC)) {
        return "Wrong elements for MaxC..MaxC: $list5"
    }

    return "OK"
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¢// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun test(p: T): T {
        return p
    }
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÎ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    fun M.doTest(): String {
    var s = ""
    for ((a, b) in 0.rangeTo(2)) {
      s += "$a:$b;"
    }
    return s
}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    a().foo(42)
}
yü// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().toString()
}
y±// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

class A(val x: String) {
    constructor(y: Int = sideEffect(), z: (Int) -> Int = { it + sideEffect() }) : this("$y:${z(y)}") {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, r: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: MyString, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yl// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    fun foo(t: T): String
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Char<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y±// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    array.forEach { println(it.asList()) }
}
y£// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo("b", null)
}
yß// Original bug: KT-31946
// Duplicated bug: KT-38681

// !LANGUAGE: +ProperIeee754Comparisons
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yØ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: InlineNotNullReference, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(f: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Your() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(k)
}
y§// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, p: KProperty<*>) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Local() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yﬂ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {
    if (MAX_LONG != "${Long.MAX_VALUE}") return "fail template"
    if (MAX_LONG != "" + Long.MAX_VALUE) return "fail \"\" +"
    if (MAX_LONG != "".plus(Long.MAX_VALUE)) return "fail \"\".plus"
    if (MAX_LONG != (String::plus)("", Long.MAX_VALUE)) return "fail String::plus"
    if (MAX_LONG != (""::plus)(Long.MAX_VALUE)) return "fail \"\"::plus"

    if (PREFIX + MAX_LONG != "max = ${Long.MAX_VALUE}") return "fail template with prefix"
    if (PREFIX + MAX_LONG != PREFIX + Long.MAX_VALUE) return "fail \"$PREFIX\" +"
    if (PREFIX + MAX_LONG != PREFIX.plus(Long.MAX_VALUE)) return "fail \"$PREFIX\".plus"
    if (PREFIX + MAX_LONG != (String::plus)(PREFIX, Long.MAX_VALUE)) return "fail String::plus($PREFIX, ...)"
    if (PREFIX + MAX_LONG != (PREFIX::plus)(Long.MAX_VALUE)) return "fail \"$PREFIX\"::plus"

    return "OK"
}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Tr<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun i(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    items.add(this)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(toInt: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yû// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: String) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yù// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun Long(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, B: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y¢// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    StringBuilder().foo(42)
}
y¶// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : AssertionError<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
operator class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y_// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

open class Z(val s: Int) {
    open fun a() {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    d().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// KJS_WITH_FULL_RUNTIME
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: box<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y†// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class getFirst<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y~// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // KJS_WITH_FULL_RUNTIME
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Game<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yú// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
}
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
abstract class Base<T> {

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {// Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
 Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÅ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}


fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
classDerived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract  Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b:Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÉ
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, : Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yã// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : <Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yã// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun (a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yî// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y/// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}



fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ArrayString>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T,b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args:Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yf// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) 
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interfaceabstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: ?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived :Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yñ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    overridefun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>(){
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b:  = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: , b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: , b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yé// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, ) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>){
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = ) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yO// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> 

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int> {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived.foo(42)
}
yì// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : () {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yx// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}


class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstractclass Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? =null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yè// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: ) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract classBase<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: ? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: ) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open funfoo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun (args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yë// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived :  {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {// Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any?= null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open  foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class  : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) 
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun (a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yp// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() 

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class BaseT> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitiveclass Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y*// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}


yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String> {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yî// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
     fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yá// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    ().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived  Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>( {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T>{
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y// Original bug: KT-31946
// Duplicated bug: KT-38681



// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, ) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
     fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().(42)
}
yH// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) 
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun fooa: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() 
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?){}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any? {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: <String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b:Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yx// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any?  null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstractopen fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int,b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}


// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> 
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yï// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun fooa: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yó// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default valueDerived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun mainargs: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yî// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
 class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yì// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    .foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived()foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, : Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yô// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

 main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived).foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived: Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a:T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

funmain(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) }
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)

yÄ// Original bug: KT-31946
// Duplicated bug: KT-38681


abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    openfun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a:Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yö// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) 
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yW// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) }
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo
}
yò// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class <T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : BaseInt>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yõ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÅ// Original bug: KT-31946


// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
yÜ// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}

fun main(args: Array<String>) {
    // Must omit argument for parameter with default value
    Derived().foo(42)
}
y}// Original bug: KT-31946
// Duplicated bug: KT-38681

// Also reproes on interface
abstract class Base<T> {
    // Also reproes if abstract
    open fun foo(a: T, b: Any? = null) {}
}

// Concrete type HAS to be primitive
class Derived : Base<Int>() {
    override fun foo(a: Int, b: Any?) {}
}


    // Must omit argument for parameter with default value
    Derived().foo(42)
}
ˇˇ