¿iexportTaggdefaultnoriginalSampleyS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
gmutantsŸy‰// Original bug: KT-35045
// Duplicated bug: KT-14628


val m = false
try
{fun main() {
    println(Resources.b.cMp3)
}}
catch(e: Exception){}
finally{}


open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y`// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}


val c = true
if (c) {open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}} else {open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}}


object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y„// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}


val s = true
when (s) {
 true -> {object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}}
 else -> {object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}}
}

y¨// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() 
val e = false
when (e) {
 true -> {{
    println(Resources.b.cMp3)
}}
 else -> {{
    println(Resources.b.cMp3)
}}
}


open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y™// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot
val f = false
when (f) {
 true -> {(val path: String)}
 else -> {(val path: String)}
}
 {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    
val u = true
try
{println(Resources.b.cMp3)}
catch(e: Exception){}
finally{}

}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yå// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    
val c = false
when (c) {
 true -> {open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")}
 else -> {open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")}
}

    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÌ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    
val y = true
if (y) {inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")} else {inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")}

}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‹// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    
val o = true
if (o) {val aPng = File("a.png")} else {val aPng = File("a.png")}


    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    
val l = false
when (l) {
 true -> {object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }}
 else -> {object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }}
}


    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    
val b = false
try
{val bGif = File("b.gif")}
catch(e: Exception){}
finally{}

}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    
val o = false
try
{println}
catch(e: Exception){}
finally{}
(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(
val z = false
try
{val path: String}
catch(e: Exception){}
finally{}
) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y™// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory
val b = false
when (b) {
 true -> {(val name: String)}
 else -> {(val name: String)}
}
 : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÏ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File
val u = true
when (u) {
 true -> {(val name: String, val path: String = "${this@ContentRoot.path}/${name}")}
 else -> {(val name: String, val path: String = "${this@ContentRoot.path}/${name}")}
}

}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : 
val k = false
try
{ContentRoot}
catch(e: Exception){}
finally{}
("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y“// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = 
val l = true
when (l) {
 true -> {File("a.png")}
 else -> {File("a.png")}
}


    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y”// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = 
val c = false
when (c) {
 true -> {File("b.gif")}
 else -> {File("b.gif")}
}

}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = 
val a = false
try
{File}
catch(e: Exception){}
finally{}
("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        
val b = false
try
{val cMp3 = File("c.mp3")}
catch(e: Exception){}
finally{}


        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yË// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        
val l = false
when (l) {
 true -> {object x : Directory("x") {
            val cMp3 = "c.mp3"
        }}
 else -> {object x : Directory("x") {
            val cMp3 = "c.mp3"
        }}
}

    }

    val bGif = File("b.gif")
}
yŠ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = 
val n = true
when (n) {
 true -> {File}
 else -> {File}
}
("b.gif")
}
y–// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(
val h = true
when (h) {
 true -> {Resources.b.cMp3}
 else -> {Resources.b.cMp3}
}
)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yƒ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(
val n = true
if (n) {val name: String} else {val name: String}
) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : 
val y = false
try
{ContentRoot}
catch(e: Exception){}
finally{}
("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(
val t = true
try
{val name: String}
catch(e: Exception){}
finally{}
, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, 
val u = false
try
{val path: String = "${this@ContentRoot.path}/${name}"}
catch(e: Exception){}
finally{}
)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(
val n = true
when (n) {
 true -> {"samples"}
 else -> {"samples"}
}
) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : 
val y = true
try
{Directory}
catch(e: Exception){}
finally{}
("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = 
val m = true
try
{File("c.mp3")}
catch(e: Exception){}
finally{}


        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(
val p = true
try
{Resources.b}
catch(e: Exception){}
finally{}
.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.
val n = true
try
{cMp3}
catch(e: Exception){}
finally{}
)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: 
val v = true
try
{String}
catch(e: Exception){}
finally{}
) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y–// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = 
val v = false
if (v) {"${this@ContentRoot.path}/${name}"} else {"${this@ContentRoot.path}/${name}"}
)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : 
val o = false
if (o) {ContentRoot} else {ContentRoot}
("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yŽ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(
val p = false
when (p) {
 true -> {"a.png"}
 else -> {"a.png"}
}
)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yw// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = 
val g = true
if (g) {File} else {File}
("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y˜// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            
val l = true
when (l) {
 true -> {val cMp3 = "c.mp3"}
 else -> {val cMp3 = "c.mp3"}
}

        }
    }

    val bGif = File("b.gif")
}
yŽ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(
val g = false
when (g) {
 true -> {"b.gif"}
 else -> {"b.gif"}
}
)
}
y}// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(
val a = false
if (a) {Resources} else {Resources}
.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.
val b = false
when (b) {
 true -> {b}
 else -> {b}
}
.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y¨// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(
val s = true
when (s) {
 true -> {"${this@ContentRoot.path}/${name}"}
 else -> {"${this@ContentRoot.path}/${name}"}
}
)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(
val k = true
try
{"b"}
catch(e: Exception){}
finally{}
) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y|// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : 
val v = true
if (v) {Directory} else {Directory}
("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yŽ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = 
val b = false
when (b) {
 true -> {"c.mp3"}
 else -> {"c.mp3"}
}

        }
    }

    val bGif = File("b.gif")
}
yŒ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: 
val f = true
when (f) {
 true -> {String}
 else -> {String}
}
) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y~// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : 
val o = true
if (o) {ContentRoot} else {ContentRoot}
("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: 
val n = false
when (n) {
 true -> {String}
 else -> {String}
}
, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: 
val m = true
try
{String}
catch(e: Exception){}
finally{}
 = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y›// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${
val k = true
when (k) {
 true -> {this@ContentRoot.path}
 else -> {this@ContentRoot.path}
}
}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‹// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${
val x = false
when (x) {
 true -> {name}
 else -> {name}
}
}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : 
val t = true
try
{Directory}
catch(e: Exception){}
finally{}
("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(
val o = true
try
{"c.mp3"}
catch(e: Exception){}
finally{}
)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yœ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${
val s = false
when (s) {
 true -> {this@ContentRoot.path}
 else -> {this@ContentRoot.path}
}
}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yw// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${
val x = true
if (x) {name} else {name}
}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y—// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${
val x = false
when (x) {
 true -> {this@ContentRoot}
 else -> {this@ContentRoot}
}
.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‹// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.
val l = false
when (l) {
 true -> {path}
 else -> {path}
}
}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(
val l = true
try
{"x"}
catch(e: Exception){}
finally{}
) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${
val o = true
try
{this@ContentRoot}
catch(e: Exception){}
finally{}
.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‹// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.
val w = false
when (w) {
 true -> {path}
 else -> {path}
}
}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yŠ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${
val c = true
when (c) {
 true -> {this}
 else -> {this}
}
@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y}// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : 
val u = false
if (u) {Directory} else {Directory}
("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yw// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${
val v = true
if (v) {this} else {this}
@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y‰// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this
val a = false
try
{@ContentRoot}
catch(e: Exception){}
finally{}
.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yˆ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this
val b = true
try
{@ContentRoot}
catch(e: Exception){}
finally{}
.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    (println(Resources.b.cMp3))
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y // Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    (inner
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: (String)) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yB// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            (
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: (String)) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${(name)}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.(path)}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")!!

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"!!)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory!!("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"!!
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name!!}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: (String)?) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : (ContentRoot)?("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: (String)?) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : (ContentRoot)?("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: (String)?, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: (String)? = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : (Directory)?("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : (Directory)?("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources!!.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val ${this@ContentRoot.path}: String) {
    open inner class Directory(val name: String) : ContentRoot("path/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : (Directory"x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/"")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png${name})

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this).path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x"@ContentRoot {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${{
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}.path}/${name}")
}

object Resources : ContentRoot("samples") this@ContentRoot
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : }("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
ContentRoot
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
ContentRoot

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@}.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class ((val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : DirectoryDirectory"x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) val name: String
    open inner class Directory({) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
ySContentRoot
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : // Original bug: KT-35045("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") "b.gif"
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File({)
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : b {
    val aPng = File("a.png")

    object ContentRoot("samples") : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${namec.mp3")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "}"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${)}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b"this@ContentRoot.path {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b{cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") .
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun ContentRoot() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : main("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    samples
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    name inner class Directory(val open: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${)}/${name}"this@ContentRoot.path
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.}}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
path

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : ("x")("b") {
        val cMp3 = File("c.mp3")

        object x : DirectoryDirectory {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.path)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.cMp3}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ("b")("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : DirectoryContentRoot {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(b.gif) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(""b"")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot${path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/.name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot@path}/${name}")
    inner class File(val name: String, val path: String = "${this.ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources Directory ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : :("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File()b.gif""
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${String}/${name}")
    inner class File(val name: this@ContentRoot.path, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = }("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
File
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samplesb) {
    val aPng = File("a.png")

    object " : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : val bGif = File("b.gif")("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    ContentRoot
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : :("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x ContentRoot Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: ") {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(Stringa.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

Directory main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : fun("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(this.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${Resources.b@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() ContentRoot

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : {
    println(Resources.b.cMp3)
}("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    ("c.mp3")
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = Fileprintln(Resources.b.cMp3)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() @ContentRoot

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this{
    println(Resources.b.cMp3)
}.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory" : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif(val name: String))
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}"c.mp3"${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = /
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")@ContentRoot.path}/${name}")
    this
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    "(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(printlnb.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"Directory
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : )("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${"@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = this${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.c.mp3}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "path"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${b@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("this") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3""

        object x : Directory("x)) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${"@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}this)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${${.path}/this@ContentRootname}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : )("${this@ContentRoot.path}/${name}"ContentRoot
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val pathString String) {
    open inner class Directory(val name: :) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    ((Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Fileprintln"a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${nameContentRoot("samples")")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : } {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : "("${this@ContentRoot.path}/${name}ContentRoot)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File({, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") val name: String
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) inner ContentRoot("${this@ContentRoot.path}/${name}")
    : class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: ("x")) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : DirectoryString {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.bStringcMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: . = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot."a.png"}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(path)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}File${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = /("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.("x")}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directorypath {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main=) {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif ( File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${}.path}/${name}")
this@ContentRoot

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class "b"(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(ContentRoot) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String= : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String ) "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisval cMp3 = File("c.mp3").path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        @ContentRoot

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = (("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = FileFile"b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${String}/${name}")
    inner class File(val name: String, val path: this@ContentRoot.path = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(open path: String) {
    val inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${@ContentRootthis.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

"c.mp3" class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(open)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.bbcMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(".") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    val cMp3 = "c.mp3" class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            open inner
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() open
    println(Resources.b.cMp3)
}

{ class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory""b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = (c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : "("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(Directoryb.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : =("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif Directory File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${"${this@ContentRoot.path}/${name}"@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = this)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

{
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
} main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") fun
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: ", val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(Stringb") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Directory("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : File("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") File
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = {("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() Directory("x")

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : {
    println(Resources.b.cMp3)
} {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class ${(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "Directorythis@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("("a.png")/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File${this@ContentRoot.path}

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("samples")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("${this@ContentRoot.path}/${name}") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        ("b.gif") cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = Fileval
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this"ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png@)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = ()
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File"${this@ContentRoot.path}/${name}""c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class b.gif(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("Directory")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FileResourcesval name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object ( : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${}}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        this@ContentRoot.path
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    val(Resources.b.cMp3)
}

open class ContentRoot(println path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : val("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    Directory bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.))
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"cMp3

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : File("${this@ContentRoot.path}/${name}")
    inner class ContentRoot(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${val}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        name cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: this) {
    open inner class Directory(val name: String) : ContentRoot("${String@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.File.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = b("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: File("b.gif")) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = String
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    bGif

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val val aPng = File("a.png") = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(b.gif) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(""x"")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot( {
    val aPng = File("a.png")

    object b : Directory("samples")"b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}object${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        / x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: StringaPng val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val , = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name/ String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}:${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resourcesinnerb.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    . class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

samples

object Resources : ContentRoot("open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

String main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: fun) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = val bGif = File("b.gif")c.mp3"
        }
    }

    "
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${("b")@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directorythis {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
"samples"

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(// Duplicated bug: KT-14628) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory" : ContentRoot((val name: String)${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : "("samples") {
    val aPng = File(ContentRoota.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.String}/${name}")
    inner class File(val name: String, val path: path = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: } = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        String
    }

    val bGif = File("b.gif")
}
ySsamples
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("// Original bug: KT-35045") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "@this${ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open val ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        class cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${"}")
}

object Resources : ContentRoot("samplesname) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File() name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"val
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

val name: String

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(fun main() {
    println(Resources.b.cMp3)
}, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}this@ContentRoot${name}")
    inner class File(val name: String, val path: String = "${/.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

@ContentRoot class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisopen.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : x("Directory") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(Directory("x"))
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : "${this@ContentRoot.path}/${name}" {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRootDirectory("b") {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : (val path: String) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = c.mp3("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "File"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val namec.mp3 String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(":")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(File)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = "a.png"("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    File class inner(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : val aPng = File("a.png")("samples") {
    ContentRoot

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b=cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif . File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

"

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"Directory

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : )("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.pathobject Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}/${name}")
}

}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner "x" File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(class) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val "c.mp3": String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(name)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(() {
        val cMp3 = File("c.mp3")

        object x : Directory"b""x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val nameDirectory String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : :("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootaPngpath}/${name}")
}

object Resources : ContentRoot("samples") {
    val . = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("b.gif") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("x")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: File) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = String("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources File ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = :("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner / File(val name: String, val path: String = "${this@ContentRoot.path}class${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    File(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = println("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisDirectory("b").path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : @ContentRoot {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: :) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources String ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.}.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
b

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun .() {
    println(Resources.bmaincMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    "
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val this@ContentRoot: String, val path: String = "${name.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: ", val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(Stringx") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

"c.mp3" main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = fun
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("x") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("samples") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: bGif) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val String = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/Directoryname}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ${("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directoryval( name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) Directory
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : {("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

( mainfun) {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.Directory("x")}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : path {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : File("b.gif")("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = ContentRoot
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "File("c.mp3")/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = ${this@ContentRoot.path}

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(Directory("x")) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : "samples" {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    class
}

open class ContentRoot(val path: String) {
    open inner println(Resources.b.cMp3) Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.{.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") b
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

val Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        object cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : x("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object ContentRoot : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisobjectContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    @ b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${".path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}this@ContentRoot)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
})
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

"${this@ContentRoot.path}/${name}"
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val class: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner name File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}"c.mp3"${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(/)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@object.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

ContentRoot Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val nameDirectory String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : :("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    : class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b inner Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    c.mp3
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "println(Resources.b.cMp3)"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samplesval cMp3 = "c.mp3") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            "
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(""") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3samples)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: "a.png" = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(String)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val .: String) : ContentRoot("${this@ContentRootnamepath}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

aPng class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val open = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"c.mp3")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(("x")c.mp3")

        object x : Directory" {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun mainval {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        () cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(${this@ContentRoot.path} name: String, val path: String = "val/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    Directory b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : object("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(c.mp3)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(""${this@ContentRoot.path}/${name}"")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    })
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    Resources.b.cMp3

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/"c.mp3"name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = ${
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(= path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 val File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: "samples") : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(String) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : (val name: String, val path: String = "${this@ContentRoot.path}/${name}")("${this@ContentRoot.path}/${name}")
    inner class FileContentRoot
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@("x").path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : DirectoryContentRoot {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : path("${this@ContentRoot.ContentRoot}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name) String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b": {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : "("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(ContentRootsamples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootFilepath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = .("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this"ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b@) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, ))
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b"val path: String = "${this@ContentRoot.path}/${name}" {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val "c.mp3": String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = path
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String{ )
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.@)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thiscMp3ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${name}/${this@ContentRoot.path}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val val = File("c.mp3")

        object x : Directory("x") {
            cMp3 cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(this) : ContentRoot("${val name: String@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val namesamples String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(":") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${Directory.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : this@ContentRoot("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = ("b.gif")File
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: ") : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3String
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3"
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot()samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${Resources}")
}

object name : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    Directory class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : inner("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this).path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"@ContentRoot

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("name/${${this@ContentRoot.path}}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}val cMp3 = "c.mp3"${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            /
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name{ String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") :
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

ContentRoot main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : fun("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootDirectorypath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : .("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val String: name, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    "
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"))
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Fileobject b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    (val name: String, val path: String = "${this@ContentRoot.path}/${name}")

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: Stringc.mp3 {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = ")"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: ("${this@ContentRoot.path}/${name}")) {
    open inner class Directory(val name: String) : ContentRootString
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = FileDirectory

        object x : ("c.mp3")("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(.) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRootval path: Stringpath}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println((.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FileResourcesval name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot((samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File""c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.File}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = path("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Fileb
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("(val name: String, val path: String = "${this@ContentRoot.path}/${name}")") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.name.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val b: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this{ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") @
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(bGif)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val "${this@ContentRoot.path}/${name}" = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisval.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    @ContentRoot aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun mainval {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, () path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println((.b.cMp3)
}

open class ContentRootResourcesval path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() (
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot{"${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class cMp3(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val File = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    . class File(val name: String, val path: String = "${this@ContentRootinnerpath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : (val name: String, val path: String = "${this@ContentRoot.path}/${name}")
    inner class FileContentRoot("${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(()
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File"${this@ContentRoot.path}/${name}""b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoota.pngval path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("(")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRootContentRoot
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ("${this@ContentRoot.path}/${name}")("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name=")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif } File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val pathval bGif = File("b.gif") String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    :
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "a.png")
}

object Resources : ContentRoot("samples") {
    val aPng = File("${this@ContentRoot.path}/${name}")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${"@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3this)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}}${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
/

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/"c.mp3"name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(${)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(x)

        object x : Directory(""c.mp3"") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this"x".path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(@ContentRoot) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: }) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
String
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

samples main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("fun") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "Directorythis@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ${("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources } ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
:
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
)

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"}
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.cMp3.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val b = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println("c.mp3".b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = Resources
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File("samples")val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot( {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println((val path: String))
}

open class ContentRootResources.b.cMp3 {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${File.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = this@ContentRoot("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: Directory) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : String("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this:.path}/${name}")
}

object Resources @ContentRoot ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: }) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
String

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main") {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = (c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Directory
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ContentRoot("${this@ContentRoot.path}/${name}")("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"this@ContentRoot
    inner class File(val name: String, val path: String = "${).path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    ContentRoot("samples") class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : inner {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : val("samples") {
    ContentRoot aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.ContentRoot.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : b("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String @ContentRoot "${this=.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${"}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = this@ContentRoot.pathc.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: val cMp3 = File("c.mp3"), val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        String

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    Directory(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : println("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: "b") {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(String) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("x"b") {
        val cMp3 = File("c.mp3")

        object x : Directory(") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") name
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(path)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val Resources.b.cMp3: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("c.mp3")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("${this@ContentRoot.path}/${name}")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory) : ContentRoot("${this@ContentRoot.path}/${name}"(val name: String)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") "a.png"
    val aPng = File({)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: )) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b"String {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(ContentRoot.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Resources("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "(this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory${"b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: =, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 String File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: b.gif = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("String")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

. main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootfunpath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class DirectoryContentRootval name: String) : (("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: "c.mp3") {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(String)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
val cMp3 = File("c.mp3")

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        }

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    String(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: println = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, b path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("val") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun open() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    main inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory${name} : ContentRoot("${this@ContentRoot.path}/(val name: String)")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    File
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val /: String = "${this@ContentRoot.path}path${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("x/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("${this@ContentRoot.path}") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : "("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(ContentRootx") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"(val name: String, val path: String = "${this@ContentRoot.path}/${name}")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : =("b") {
        val cMp3 Directory File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
class

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner // Duplicated bug: KT-14628 Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"{

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") )
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : "c.mp3"("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = ContentRoot
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot."}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3path
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : val("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    ContentRoot bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) Directory

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resourcesopenb.cMp3)
}

. class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(""samples) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: object) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    String b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : samples("ContentRoot") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FilebGif
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val (val name: String, val path: String = "${this@ContentRoot.path}/${name}") = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    ((Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directoryprintln"x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val ": String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(pathsamples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = })
"${this@ContentRoot.path}/${name}"

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(cMp3a.png")

    object b : Directory("b") {
        val " = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

{
            val cMp3 = "c.mp3"
        }

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    "b"(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(println) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(b.gif.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("Resources")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class a.png(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("File")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") ContentRoot("${this@ContentRoot.path}/${name}")
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class )(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"ContentRoot
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : "samples"("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(ContentRoot) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun this() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${main@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/val name: Stringname}")
    inner class File(${, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FileFile
}

object Resources : ContentRoot("samples") {
    val aPng = (val name: String, val path: String = "${this@ContentRoot.path}/${name}")("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}ContentRoot("samples"))
}

object Resources : " {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(")
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(Resources.b.cMp3a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
"x"

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(}) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(@ContentRoot name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${thisval.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory()b") {
        val cMp3 = File("c.mp3""

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun mainval {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(() name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources"b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x.) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(")/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"${this@ContentRoot.path}
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    Directory class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : inner("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : File("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = Directory("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: Directory("x") = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : String {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun mainDirectory {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ()("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner val aPng = File("a.png") File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    class

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name"a.png" String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(:)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

(val name: String, val path: String = "${this@ContentRoot.path}/${name}")

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Filefun main() {
    println(Resources.b.cMp3)
}
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${b}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object name : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

("samples") Resources : ContentRootobject {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) Directory("b")

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
} {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(${name})
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/Resources.b.cMp3")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${(}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = Filethis@ContentRoot.path"b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = ("b"))
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory"${this@ContentRoot.path}/${name}" {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources { ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") :
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class {
            val cMp3 = "c.mp3"
        }(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") File
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot)"samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"(
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(b.gif)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(""${this@ContentRoot.path}/${name}"")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}path${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot./}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : }("x") {
            val cMp3 = "c.mp3"
        Directory
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this"c.mp3".path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = @ContentRoot
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    "b.gif"

    val bGif = File(})
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRootval {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            ("samples") cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val String: String, val path: name = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(}.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.pathResources/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}""b"
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory()) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: ) = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"String

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootxpath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(".") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            cMp3 val = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("c.mp3")"b") {
        val cMp3 = File(

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Fileval

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            ("a.png") cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRootval {
    ("samples") aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x Directory :("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(Directory("x"))

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : "a.png" {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val }: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
path

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisa.pngContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("@")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : object x : Directory("x") {
            val cMp3 = "c.mp3"
        }("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        ContentRoot
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.:.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources b ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRootStringpath}/${name}")
    inner class File(val name: String, val path: . = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = samples${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(""") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: name) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${String}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    val cMp3 = File("c.mp3")
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        println(Resources.b.cMp3)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open ContentRoot class(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("=this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 ${ "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : val bGif = File("b.gif")("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    ContentRoot
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") =
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 { "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3"${this@ContentRoot.path}/${name}")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = (${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory""b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"("x")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : x("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("ContentRoot") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    ContentRoot
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : println(Resources.b.cMp3)("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(ResourcesContentRootb.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : .("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}${name}/")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRootDirectory"${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : (("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot/path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}.${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory("x") : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(val name: String) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.Directory}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : path("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${val}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    this@ContentRoot.path aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path(/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory}"b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisnameContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${@}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) File

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    ${ inner class Directory(val name: String) : ContentRoot("openthis@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/{name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") ${
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    File inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = open("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") File

    val bGif = {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, samples)
}

object Resources : ContentRoot("val path: String = "${this@ContentRoot.path}/${name}"") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${thisb.gifContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("@")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    File("b.gif") class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = inner
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

"

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRootDirectory {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : (val path: String)("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3""

        object x : Directory("x") {
            val cMp3 = "c.mp3)
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val ("samples"): String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRootname {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("ContentRootthis@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ${("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
} class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") inner
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("xbGif) {
            val cMp3 = "c.mp3"
        }
    }

    val " = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) "${this@ContentRoot.path}/${name}"
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = {)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

Directory Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : object("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${"${this@ContentRoot.path}/${name}".path}/${name}")
    inner class File(val name: String, val path: String = this@ContentRoot)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File({
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    })

    object b : Directory("b") "a.png"

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() Resources.b.cMp3
    println({)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRootFileval path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = (("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : this@ContentRoot.path("${ContentRoot}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = c.mp3)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(""${this@ContentRoot.path}/${name}"")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    b(Resources.println.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(b.gifx") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(""")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b "c.mp3" Directory("b") {
        val cMp3 = File(:)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS"
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(// Original bug: KT-35045b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class =(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 File "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: :) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x String Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ("x")("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : DirectoryContentRoot {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: )) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"String
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: val cMp3 = "c.mp3") {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            String
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.val)
}

open class ContentRoot(val path: String) {
    open inner class Directory(cMp3 name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : ) {
        val cMp3 = File("c.mp3"Directory("b")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory()val name: String : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(String)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: Resources.b.cMp3, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
File("c.mp3")

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = }

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3:
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path) String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${"}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(namec.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.File}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = path("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val b.gif: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("name")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : DirectoryDirectory("x") {
        val cMp3 = File("c.mp3")

        object x : ("b") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String{ {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") )
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : b.gif("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("ContentRoot")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

a.png class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("open")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") bGif
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val { = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = }

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
File("c.mp3")
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
object x : Directory("x") {
            val cMp3 = "c.mp3"
        }

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : val("${this@ContentRoot.path}/${name}")
    inner class File(ContentRoot name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(aPng.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val Resources = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open ("a.png") ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Fileclass

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: =, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif String File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources."a.png".cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(b)

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.val aPng = File("a.png"))
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    cMp3

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.(val name: String, val path: String = "${this@ContentRoot.path}/${name}"))
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FilecMp3
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}("a.png")${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File/

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samplesobject b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }) {
    val aPng = File("a.png")

    "

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = "("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3File
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: StringDirectory("x") val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : , {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class FileDirectory
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : (val name: String, val path: String = "${this@ContentRoot.path}/${name}")("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    val
}

object Resources : ContentRoot("samples") {
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}") aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x"c.mp3") {
            val cMp3 = "
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${name@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${this}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    : class File(val name: String, val pathinner String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.this)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${cMp3@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    val cMp3 = "c.mp3"(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            println
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

ContentRoot("samples") Resources : object {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/(")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory${name}"b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    ContentRoot inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : open("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${{
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") this@ContentRoot
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("("b.gif")")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = Filec.mp3
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    :

    object b val aPng = File("a.png") Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.x)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("cMp3") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name. String, val path: String = "${this@ContentRoot:path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    inner
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}") class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisb.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("@ContentRoot") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/{
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") ${name}

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, })
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    val path: String = "${this@ContentRoot.path}/${name}"

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.pathx/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("}") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : )("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"Directory
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples"} {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
)
yS// Original bug: KT-35045
"

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}// Duplicated bug: KT-14628)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() (

open class ContentRoot{
    println(Resources.b.cMp3)
}val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/objectname}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        ${ x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    )
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}"open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples""x" {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory()) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(String${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: " = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class DirectoryDirectory : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : (val name: String)("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${thispath.@ContentRoot}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : "("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gifDirectory)
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisFile.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = @ContentRoot("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3Directory("b")
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : ) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRootval name: String
    inner class File(("${this@ContentRoot.path}/${name}"), val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val ": String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3path
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"c.mp3
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(")")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: object, val path: String = "${this@ContentRoot.path}/${name}")
}

String Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String} val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
,
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner ( Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Fileclassval name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    (
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directoryinner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")"x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: ", val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(Stringa.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(File("c.mp3"))
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = "${this@ContentRoot.path}/${name}"

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open val aPng = File("a.png") ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    class

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRootobject Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

("${this@ContentRoot.path}/${name}")
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val " = File(bGifb.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${ContentRoot("samples")}/${name}")
}

object Resources : this@ContentRoot.path {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    (printlnResources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("("x")") {
        val cMp3 = File("c.mp3")

        object x : Directoryb {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"Directory

    object b : )("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    " bGif = File("b.gifval)
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open File("a.png") ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = class

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${val cMp3 = File("c.mp3")}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        this@ContentRoot.path

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : Directory("x")("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ContentRoot {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val File("b.gif") = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = aPng
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.("a.png")}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Filepath

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot${this@ContentRoot.path}path}/${name}")
    inner class File(val name: String, val path: String = "./${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(x${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(""") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = =("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif File File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = File)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = "${this@ContentRoot.path}/${name}"("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples"Directory {
    val aPng = File("a.png")

    object b : )("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun ${() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/mainname}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

Directory main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : fun("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : cMp3("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val ContentRoot = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("val cMp3 = File("c.mp3")") {
    val aPng = File("a.png")

    object b : Directory("b") {
        samples

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class DirectoryContentRoot : (val name: String)("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

object class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    open b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    }
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    a.png(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("println")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("a.png") {
    val aPng = File("samples")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : val cMp3 = "c.mp3"("x") {
            Directory
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(/)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}"${this@ContentRoot.path}/${name}"${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${b.gif@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("this")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this=.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng @ContentRoot File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directoryc.mp3val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "("
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File"c.mp3"
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = (val name: String, val path: String = "${this@ContentRoot.path}/${name}")
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = Directory)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : "${this@ContentRoot.path}/${name}"("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}"b")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val val bGif = File("b.gif"): String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    path
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.@}/${name}")
    inner class File(val name: String, val path: String = "${thispathContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String"samples" : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot()) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = Directory

    object b : File("a.png")("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("=") {
    val aPng samples File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRootc.mp3 {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "("samples")"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${Directory}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : name("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = val

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    File("a.png") bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: b.gif, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("String")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") ("x")
        val cMp3 = File("c.mp3")

        object x : Directory{ {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
val

open class ContentRoot(} path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Base("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = T("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("OK") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    box(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(-1) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("OK")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${rangeTo}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory() {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@test3Nested.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y'// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, r: String)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : String("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("OK") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yt// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object A {
  val x: Int = 610
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("Sample") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@A.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: Int?) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "OK"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.d)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.String}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y&// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, b: Float)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    asResult(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("OK")
}
yg// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object : Inner() {
                    override fun toString() = foo()
                }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = bar("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = B4("OK")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("fail") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Long("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = String("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("O") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.Foo)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    private class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.y)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : FooStr("OK") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${arrayOf}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@Foo.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y_// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val v1 = A(-10.toFloat()).hashCode()
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    inline class Z2(val z: Z1)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y1// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot()
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    open class A(val result: String) {
    constructor(x: Int, y: Int = 99) : this("$x$y")
}
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${thisf@.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File('3')

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${String@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("OK") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : Int?("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yò// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object {
        val getBunny = { bunny }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@foo.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
        return "Wrong elements for (MaxL - 2).toLong()..MaxL: $list4"
    }

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y:// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() prop = 2

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yã// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object Test {
    val a = "OK"
}

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${String}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y·// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
    private constructor(s1: String, s2: String) : this(s1 + s2)

    companion object {
        fun p1(s: String) = Composed("O", s)
    }
}
    }

    val bGif = File("b.gif")
}
y2// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Z<String, X>()
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val list1 = ArrayList<Int>()

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.a6.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val zdouble : Double? = 1.0

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y4// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    class Foo {
    val a: Int = 42
    val b by Delegate(0)
}
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : String("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}OK${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = t()
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : A<String>("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("OK")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(hashCode.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: s, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println({ x, y -> x + y })
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val result: String
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.A.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y1// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    abstract class Foo<T> {
    fun hello(id: T) = "O$id"
}
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y5// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "OK")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val p: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}OK${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y]// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val b = InlineNullablePrimitive(1)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y`// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(
        public val first: A
) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("fail1: ") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yo// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    class Delegate: ReadOnlyProperty<Test, String> {
    override fun getValue(thisRef: Test, property: KProperty<*>) = "OK"
}
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@a.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val prop1: String)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val box = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : Game("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object : Any() {
                override fun toString() = outerProp + arg
            }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : c("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.Char}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.run)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("zzz") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot() {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("Fail") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("Wrong elements for 3u.toUShort()..9u.toUShort() step 2: $list3") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("abc") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val prop: Int by Delegate()

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("Fail") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y]// Original bug: KT-35045
// Duplicated bug: KT-14628

fun shouldReturn11() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File()

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: toString) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y,// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Fileprivate constructor(val s: String)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${A.prop}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y€// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
    var c = from.i

    operator fun next(): C {
        val next = C(c)
        c++
        return next
    }

    operator fun hasNext(): Boolean = c <= to.i
}

    val bGif = File("b.gif")
}
y`// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val name: String
        get() = "OK"

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.msg}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÍ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
            override fun toString(): String = "OK"
        }

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y@// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    add(i)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    Test(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(range5.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${sideEffects}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yX// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("4, 1, 2, 3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${a}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yG// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : String("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Unit(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    test(0)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : T1, T2 {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val a: Any?) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${thisa@.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yY// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(111, 222, ::foo) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : String("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val t: T) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y	// Original bug: KT-35045
// Duplicated bug: KT-14628

fun box(): String {
    val plusZero: Any = 0.0
    val minusZero: Any = -0.0
    val nullDouble: Double? = null
    if (plusZero is Double) {
        // Smart casts behavior in 1.2
        when (plusZero) {
            nullDouble -> {
                return "fail 1"
            }
            -0.0 -> {
                return "fail 2"
            }
            else -> {}
        }

        if (minusZero is Double) {
            // Smart casts behavior in 1.2
            when (plusZero) {
                nullDouble -> {
                    return "fail 3"
                }
                minusZero -> {
                    return "fail 4"
                }
                else -> {}
            }
        }
    }

    return "OK"
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(my.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y4// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("a")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y†// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "Wrong elements for (MinI + 2) downTo MinI step 1: $list1"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: y) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${b}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}O${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : C() {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yX// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class AssertionError(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÇ// Original bug: KT-35045
// Duplicated bug: KT-14628

// KJS_WITH_FULL_RUNTIME
fun box(): String {
    val l = ArrayList<Int>()
    l.add(1)
    val x = l[0] * 2
    if (x != 2) return "Fail: $x}"
    return "OK"
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val String: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y%// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    operator fun getValue(thisRef: Any?, kProp: Any) = x

    operator fun setValue(thisRef: Any?, kProp: Any, value: T) {
        x = value
    }
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yE// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${a1.prop}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val list = File("b.gif")
}
y]// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(operation :  Any.() -> Any) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y—// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
    fun bar(): String {
        fun <T> foo(t:() -> T) : T = t()
        foo { }
        return "OK"
    }
}
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@N.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = P("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    foo(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(b.component1().cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yG// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inline class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : GenericBase<Str>() {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yË// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object : ResultReceiver<T>() {
        override fun receive(result: Result<T>) {
            f(result)
        }
    }
y\// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("Fail: Outer.ok")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val foo: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("K") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yG// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(null)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${a.component1()}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yj// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${foo().contentDeepToString()}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : A<Int>(17) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.result}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : A(), B {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val f = File("b.gif")
}
yE// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${Foo.bar}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y^// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(Request("239")) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${l[0]}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yD// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(i) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : published("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = foo()

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : BK("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = String("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${Long}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val x: Int) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    c(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val minusZero: Any = -0.0
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "K"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(sorted.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y4// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : FooTrait<String>
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${A@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Pair<String, T>("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : A("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "1"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "A"
        }
    }

    val bGif = File("b.gif")
}
y!// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
    inner class Inner<T>(val t: T) {
        fun box() = t
    }
}

    val bGif = File("b.gif")
}
y\// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("$three$one") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.test.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${add}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y]// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : KMutableProperty1Impl("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yX// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("foobar") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val b = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object B : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Int("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y]// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("1", "2", "3") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y^// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        companion object {
        fun values() = "O"
        fun valueOf() = "K"
    }
    }

    val bGif = File("b.gif")
}
y–// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
    operator fun getValue(thisRef: Any?, prop: KProperty<*>): T = notImplemented()
    operator fun setValue(thisRef: Any?, prop: KProperty<*>, value: T): Nothing = notImplemented()
}

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(5u) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object A : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File()
}
yo// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = AssertionError("Should be executed once")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : IFoo {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("Fail ") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("Fail") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${set.add("foo")}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y^// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}fail: b.x = ${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y3// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : T("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("fail")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y5// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(Unit)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yY// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}fail5: ${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y…// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

companion object {
        fun foo() = A()
    }
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val K = "K"

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : A(listOf(1).map { it.toString() }.joinToString())
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : el("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = (Singleton::ok)()

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(null, 0.0) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.foo}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : defineFunc("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object minus : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println()
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y4// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : MooableTextField
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y„// Original bug: KT-35045
// Name of the getter should be 'getaBcde' according to JavaBean conventions

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "B.bar"
        }
    }

    val bGif = File("b.gif")
}
yE// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(4u)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : A<T>("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yX// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}fail: ${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yY// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Comparable<Any>("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}O${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object c : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Byte("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : A<String>() {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("fail")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: UByte, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y¦// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
        fun result() = this@bar
    }
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.x}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${foo@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@bar.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object MAX_VALUE : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : A("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.A)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yY// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = arrayOf(arrayOf(1))
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(-0.0F) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : Base("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("O")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("b")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Long(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    class B
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y8// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(S("OK"))
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(42) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y3// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(42)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.name}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yþ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object Foo {
    var a: Int = 42
    var d by Delegate(0)
}

    val bGif = File("b.gif")
}
yÕ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    var inner = 1
    operator fun getValue(t: Any?, p: KProperty<*>): Int = inner
}
yi// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("Should be executed once") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@f.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun e() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y[// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${this.toInt()}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : A by a, B {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
ya// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}not an IntArray${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y@// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : B() {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y"// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val callback: Callback)
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${sideEffects}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("O") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.pp}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val box: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@test3.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@B.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y&// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    interface Ordinaled {
    val ordinal: Int
}
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val b: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${local()}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yi// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}Should be executed once${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.MinUI}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yT// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.Test2}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object {
            inner class Inner {
                val x = "O" + k
            }

            val innerX = Inner().x

            override fun toString() = innerX
        }
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}:${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y'// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : A()
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.x}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yZ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("fail: $s") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y=// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    A()
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = z("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class foo(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y=// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    C()
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y>// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot(x + y + z + 1)
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Foo("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yŸ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun box(): String {
    val p1: KProperty1<Bar, String> = Bar::foo
    if (p1 is KMutableProperty<*>) return "Fail: p1 is a KMutableProperty"

    val p2 = Baz().ref()
    if (p2 is KMutableProperty<*>) return "Fail: p2 is a KMutableProperty"

    val p3 = Bar("")::foo
    if (p3 is KMutableProperty<*>) return "Fail: p3 is a KMutableProperty"

    return p1.get(Bar("OK"))
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val t: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(key: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(t) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${A}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y)// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : Foo()
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot(42) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(a: A<String>) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory() : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y[// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : ArrayList<String>("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yå// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object OHolder {
    val O = "O"
}

    val bGif = File("b.gif")
}
yW// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("Wrong elements for MaxUI..MinUI: $xs")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yV// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(private val r: Long, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    Foo(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yK// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(a.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val Int: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS}
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
// Original bug: KT-35045
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
        val cMp3 = File("c.mp3")
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
    println(Resources.b.cMp3)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yS// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}
    object b : Directory("b") {
open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")


        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x: Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y'// Original bug: KT-35045
// Duplicated bug: KT-14628



open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class (val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() 
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"}
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

openclass ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner  File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yI// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
     class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

     b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File(b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        objectx : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = ("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") 
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
             cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
     inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng  File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File(c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3= File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y;// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: , val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y;// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: ) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class (val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot() {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) :ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÃ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3)

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yE// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b :  {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot"${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String  "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: ) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples"){
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y;// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object  : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name:String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3  "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File()

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yõ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

 class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y1// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = )
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String)  ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
     class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y2// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() 

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    (Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(valname: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File"c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y)// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") 
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b" {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y:
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yÝ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") 

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b) {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) 
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory( name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main) {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 =File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = ${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner classFile(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples) {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples" {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif)
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 ="c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class Fileval name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }}

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory(x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(valname: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    innerclass File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)


open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val  = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open innerclass Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yH// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png"

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y
// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File(a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File"a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory(b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open classContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif= File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

     bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = ("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
         cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")

yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val  = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val  = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y:// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y$// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : 
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File( name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3  File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val : String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x  Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y=// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources :  {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRootpath}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class (val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : ("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y/// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) 

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y8// Original bug: KT-35045


fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    valaPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String ="${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory() {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String): ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val : String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path:  = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources :ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name})
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File"b.gif")
}
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = 
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner classDirectory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot( path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            valcMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = 

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open  class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name:String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x" {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") 
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")


object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x :Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng =File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.bcMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yF// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = 

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = ("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yú// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path:String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b"){
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources..cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3= "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b  Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    printlnResources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main(){
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

 Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String,val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yU// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}


yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif  File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main( {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String,  path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory"b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x) {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x"){
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yE// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x :  {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val : String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}y>// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yC// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yJ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object  : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yN// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources  ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b :Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot"samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif"
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

funmain() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yP// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
     aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif =File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    objectb : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yM// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

         x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yQ// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        valcMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String= "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yL// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = 
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    openinner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

objectResources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yA// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            
        }
    }

    val bGif = File("b.gif")
}
yO// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
yR// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {
    val aPng = File("a.png")

    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") 
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
y// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") 
y7// Original bug: KT-35045
// Duplicated bug: KT-14628

fun main() {
    println(Resources.b.cMp3)
}

open class ContentRoot(val path: String) {
    open inner class Directory(val name: String) : ContentRoot("${this@ContentRoot.path}/${name}")
    inner class File(val name: String, val path: String = "${this@ContentRoot.path}/${name}")
}

object Resources : ContentRoot("samples") {


    object b : Directory("b") {
        val cMp3 = File("c.mp3")

        object x : Directory("x") {
            val cMp3 = "c.mp3"
        }
    }

    val bGif = File("b.gif")
}
ÿÿ