¿iexportTaggdefaultnoriginalSampleyÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
gmutantsŸyu// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract


val q = true
if (q) {@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}} else {@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}}


@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}


val x = false
try
{@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}}
catch(e: Exception){}
finally{}

yT// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() 
val u = false
if (u) {{
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}} else {{
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}}


@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) 
val q = false
try
{{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}}
catch(e: Exception){}
finally{}

y// Original bug: KT-39113
// Duplicated bug: KT-39113

import 
val a = false
if (a) {kotlin.contracts.ExperimentalContracts} else {kotlin.contracts.ExperimentalContracts}

import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y&// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import 
val b = true
when (b) {
 true -> {kotlin.contracts.InvocationKind}
 else -> {kotlin.contracts.InvocationKind}
}

import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import 
val t = true
if (t) {kotlin.contracts.contract} else {kotlin.contracts.contract}


@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y_// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    
val d = false
when (d) {
 true -> {mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }}
 else -> {mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }}
}

}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(
val p = true
try
{block: () -> Unit}
catch(e: Exception){}
finally{}
) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y>// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    
val l = false
if (l) {contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }} else {contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }}

    block()
}
yû// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    
val b = true
if (b) {block()} else {block()}

}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import 
val m = true
when (m) {
 true -> {kotlin.contracts}
 else -> {kotlin.contracts}
}
.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.
val w = true
if (w) {ExperimentalContracts} else {ExperimentalContracts}

import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import 
val u = true
when (u) {
 true -> {kotlin.contracts}
 else -> {kotlin.contracts}
}
.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.
val o = true
when (o) {
 true -> {InvocationKind}
 else -> {InvocationKind}
}

import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import 
val c = false
when (c) {
 true -> {kotlin.contracts}
 else -> {kotlin.contracts}
}
.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yý// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.
val i = false
if (i) {contract} else {contract}


@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@
val z = false
when (z) {
 true -> {ExperimentalContracts}
 else -> {ExperimentalContracts}
}

fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    
val x = true
if (x) {mapOf("" to "")} else {mapOf("" to "")}
.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y<// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").
val m = false
if (m) {forEach { (a, b) ->
        runBlocking {
            a
        }
    }} else {forEach { (a, b) ->
        runBlocking {
            a
        }
    }}

}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@
val f = true
when (f) {
 true -> {ExperimentalContracts}
 else -> {ExperimentalContracts}
}

/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    
val k = false
when (k) {
 true -> {contract}
 else -> {contract}
}
 {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yú// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    
val a = false
if (a) {block} else {block}
()
}
yú// Original bug: KT-39113
// Duplicated bug: KT-39113

import 
val h = true
if (h) {kotlin} else {kotlin}
.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.
val t = false
try
{contracts}
catch(e: Exception){}
finally{}
.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import 
val g = false
when (g) {
 true -> {kotlin}
 else -> {kotlin}
}
.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.
val h = false
when (h) {
 true -> {contracts}
 else -> {contracts}
}
.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yú// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import 
val s = true
if (s) {kotlin} else {kotlin}
.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.
val v = true
try
{contracts}
catch(e: Exception){}
finally{}
.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yú// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    
val k = false
if (k) {mapOf} else {mapOf}
("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").
val n = true
try
{forEach}
catch(e: Exception){}
finally{}
 { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y5// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract 
val y = false
if (y) {{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }} else {{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }}

    block()
}
y4// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach 
val i = false
if (i) {{ (a, b) ->
        runBlocking {
            a
        }
    }} else {{ (a, b) ->
        runBlocking {
            a
        }
    }}

}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y4// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract 
val j = true
if (j) {{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }} else {{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }}

    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@
val e = true
try
{ExperimentalContracts}
catch(e: Exception){}
finally{}

fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(
val z = true
try
{"" to ""}
catch(e: Exception){}
finally{}
).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yG// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach 
val x = false
when (x) {
 true -> {{ (a, b) ->
        runBlocking {
            a
        }
    }}
 else -> {{ (a, b) ->
        runBlocking {
            a
        }
    }}
}

}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@
val b = true
if (b) {ExperimentalContracts} else {ExperimentalContracts}

/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y8// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        
val f = false
when (f) {
 true -> {callsInPlace(block, InvocationKind.EXACTLY_ONCE)}
 else -> {callsInPlace(block, InvocationKind.EXACTLY_ONCE)}
}

    }
    block()
}
y÷// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(
val x = false
if (x) {""} else {""}
 to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" 
val h = false
try
{to}
catch(e: Exception){}
finally{}
 "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y÷// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to 
val a = false
if (a) {""} else {""}
).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        
val g = false
if (g) {runBlocking {
            a
        }} else {runBlocking {
            a
        }}

    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yø// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> 
val a = true
if (a) {Unit} else {Unit}
) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y$// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        
val a = true
if (a) {callsInPlace(block, InvocationKind.EXACTLY_ONCE)} else {callsInPlace(block, InvocationKind.EXACTLY_ONCE)}

    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { 
val y = false
when (y) {
 true -> {(a, b)}
 else -> {(a, b)}
}
 ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        
val q = false
if (q) {runBlocking {
            a
        }} else {runBlocking {
            a
        }}

    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        
val d = false
if (d) {callsInPlace} else {callsInPlace}
(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yû// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { 
val z = false
if (z) {(a, b)} else {(a, b)}
 ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        
val m = true
try
{runBlocking}
catch(e: Exception){}
finally{}
 {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (
val k = true
when (k) {
 true -> {a}
 else -> {a}
}
, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, 
val w = true
when (w) {
 true -> {b}
 else -> {b}
}
) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking 
val g = false
try
{{
            a
        }}
catch(e: Exception){}
finally{}

    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(
val z = false
when (z) {
 true -> {block}
 else -> {block}
}
, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y#// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, 
val k = false
when (k) {
 true -> {InvocationKind.EXACTLY_ONCE}
 else -> {InvocationKind.EXACTLY_ONCE}
}
)
    }
    block()
}
y!// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking 
val r = false
when (r) {
 true -> {{
            a
        }}
 else -> {{
            a
        }}
}

    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, 
val g = false
when (g) {
 true -> {InvocationKind}
 else -> {InvocationKind}
}
.EXACTLY_ONCE)
    }
    block()
}
y
// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.
val v = false
try
{EXACTLY_ONCE}
catch(e: Exception){}
finally{}
)
    }
    block()
}
yö// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            
val a = false
if (a) {a} else {a}

        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y	// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            
val q = true
try
{a}
catch(e: Exception){}
finally{}

        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import (kotlin.contracts).contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").(forEach { (a, b) ->
        runBlocking {
            a
        }
    })
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        (callsInPlace(block, InvocationKind.EXACTLY_ONCE))
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, (b)) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking ({
            a
        })
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts!!
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin!!.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }!!
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ""!!).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }!!
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (() -> Unit)?) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@(ExperimentalContracts)?
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@(ExperimentalContracts)?
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> (Unit)?) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts!!.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts!!.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin!!.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin!!.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@block
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(ExperimentalContracts, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) ("" to "")
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ExperimentalContracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@kotlin.contracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }) {
    contract Unit
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import runBlocking.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun kotlin(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (InvocationKind.EXACTLY_ONCE, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, a)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
to kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" import "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import Unit.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> kotlin.contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    kotlin
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

contract
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    import kotlin.contracts.ExperimentalContracts {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}

}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach (block, InvocationKind.EXACTLY_ONCE) (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace{
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking fun
    }
}

@ExperimentalContracts
/*inline*/ {
            a
        } runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOfExperimentalContracts.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@("" to "")
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (EXACTLY_ONCE, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.a)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin@ExperimentalContractscontracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

.
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "{ (a, b) ->
        runBlocking {
            a
        }
    }).forEach "
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a( b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking,block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { a(, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> contract) {
    Unit {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import @ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

kotlin
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

"" to "" kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(import).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to )).forEach { (a, b"" ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts..
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "")ExperimentalContractsforEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contractsblockExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    .()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin..contractsInvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

@

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contractExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import { (a, b) ->
        runBlocking {
            a
        }
    }
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach kotlin.contracts.ExperimentalContracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
@ExperimentalContracts

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

import kotlin.contracts.contract
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" () -> Unit "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: to) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlinblockcontracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(., InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

block()

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    @ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main" {
    mapOf("" to ()").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@(
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockExperimentalContracts)
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking block
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    {()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { block ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    (a, b)()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { )a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit( {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

{ kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract import
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import .
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKindkotlin.contracts.ExperimentalContractsEXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { {
            a
        } ->
        runBlocking (a, b)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main"") {
    mapOf(( to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

(block: () -> Unit)
/*inline*/ fun runBlocking@ExperimentalContracts {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, ()) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockb
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ->.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) kotlin.contracts
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.@ExperimentalContracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

contracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking (block, InvocationKind.EXACTLY_ONCE)
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace{
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { runBlocking ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun (a, b)(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun {
            a
        }() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking main
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import ).contract

@ExperimentalContracts
fun main(kotlin.contracts {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts(InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { .a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.{ (a, b) ->
        runBlocking {
            a
        }
    }.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach contracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingEXACTLY_ONCEblock: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.()
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () callsInPlace(block, InvocationKind.EXACTLY_ONCE) Unit) {
    contract {
        ->
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import forEach.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").kotlin { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.block.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    contracts()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ".ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("kotlin.contracts to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach Unit
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> { (a, b) ->
        runBlocking {
            a
        }
    }) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import a.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            kotlin
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) (
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.(a, b).InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { contracts ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach a (a, b) ->
        runBlocking {
            {
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking @
            a
        }
    }
}

{ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin->contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) .
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun b() {
    mapOf("" to "").forEach { (a, main) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    forEach { (a, b) ->
        runBlocking {
            a
        }
    }("" to "").mapOf
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

block

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    @ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.forEach.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").contracts { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOfcallsInPlace"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        ((block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@b
fun main() {
    mapOf("" to "").forEach { (a, ExperimentalContracts) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(@ExperimentalContracts to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

""
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking (
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace{
            a
        }block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
            a
        } (a, b) ->
        runBlocking {
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import EXACTLY_ONCE.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.kotlin)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to a").forEach { (a, b) ->
        runBlocking {
            "
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        block(callsInPlace, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
/*inline*/ fun runBlocking(block: () -> Unit) ExperimentalContracts
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    ""("" to mapOf).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { block ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking((a, b): () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
{
            a
        }

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking import kotlin.contracts.contract
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
(a, b) main() {
    mapOf("" to "").forEach { fun ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@a
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            ExperimentalContracts
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    {
            a
        }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        )
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (runBlocking {
            a
        } -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import ).contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (kotlin.contracts -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ""a.forEach { (), b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract forEach { (a, b) ->
        runBlocking {
            a
        }
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ).contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (kotlin -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
b

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, // Duplicated bug: KT-39113) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.Unit.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import runBlocking.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        kotlin {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking (block: () -> Unit)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking{
            a
        } {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
@ExperimentalContracts main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

fun
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(runBlocking {
            a
        }" to "").forEach { (a, b) ->
        "
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" ) "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCEto
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@()
/*inline*/ fun runBlocking(block: ExperimentalContracts -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.(
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingExperimentalContractsblock: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    (mapOf"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        )
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> UnitrunBlocking {
            a
        } {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

{

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@kotlin.contracts.ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import { (a, b) ->
        runBlocking {
            a
        }
    }

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach kotlin.contracts.contract
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
"" to ""

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(// Duplicated bug: KT-39113).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main({
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract )
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to :").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block" () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@("" to "")
fun main() {
    mapOfExperimentalContracts.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" (a, b) "").forEach { to ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.(
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ExperimentalContracts) -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("InvocationKind to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, ".EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to { (a, b) ->
        runBlocking {
            a
        }
    }").forEach "
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.)
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> UnitInvocationKind {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

kotlin.contracts.contract
import kotlin.contracts.InvocationKind
import import kotlin.contracts.ExperimentalContracts

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import a

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (kotlin.contracts.contract, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

:ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block@ () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) InvocationKind
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        (
    }
    blockcallsInPlace(block, InvocationKind.EXACTLY_ONCE))
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

callsInPlace(block, InvocationKind.EXACTLY_ONCE) kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        import
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        block
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(runBlocking {
            a
        }, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(blockblock InvocationKind.EXACTLY_ONCE)
    }
    ,()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) kotlin.contracts
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.contracts
import kotlin.InvocationKind.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import block
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(kotlin.contracts.InvocationKind, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ(
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking// Original bug: KT-39113block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (@ExperimentalContracts -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        ExperimentalContracts
    }
}

@}
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (} -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
)
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.forEach { (a, b) ->
        runBlocking {
            a
        }
    }.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").contracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
ExperimentalContracts

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@// Duplicated bug: KT-39113
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@contract
/*inline*/ fun runBlocking(block: () -> Unit) {
    ExperimentalContracts {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE}
    }
    block()
)
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{
fun main() {
    mapOf("" to "").forEach ExperimentalContracts (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() ExperimentalContracts
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@{
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { ExperimentalContracts ->
        runBlocking {
            a
        }
    }
}

@(a, b)
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts(contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block.)
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
block: () -> Unit kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(import) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
contract
import kotlin.contracts.import kotlin.contracts.InvocationKind

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: EXACTLY_ONCE) -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.()
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.Unit.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() ExperimentalContracts

@{
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

block
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(@ExperimentalContracts, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

"
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

)ExperimentalContracts
/*inline*/ fun runBlocking(block: (@ -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import callsInPlace(block, InvocationKind.EXACTLY_ONCE).contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        kotlin
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract .
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> ,) {
    contract {
        callsInPlace(blockUnit InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
ExperimentalContracts kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@import
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        /*inline*/
    }
}

@ExperimentalContracts
} fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

forEach { (a, b) ->
        runBlocking {
            a
        }
    }

@ExperimentalContracts
fun main() {
    mapOf("" to "").import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import ().contracts.contract

@ExperimentalContracts
fun mainkotlin {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.{.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() contracts
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        InvocationKind.EXACTLY_ONCE
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, })
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import (
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingkotlin.contracts.ExperimentalContractsblock: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

InvocationKindExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, @.EXACTLY_ONCE)
    }
    block()
}
yÔkotlin
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import // Original bug: KT-39113.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin(a, b)contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { . ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.Unit.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@}
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    ExperimentalContracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(block()).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    "" to ""
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.runBlocking {
            a
        }.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        contracts
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ).contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCEkotlin
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

()
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block@ExperimentalContracts
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.InvocationKind
import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
)

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main(// Duplicated bug: KT-39113 {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.block)
    }
    EXACTLY_ONCE()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) ExperimentalContracts
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { block ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace((a, b), InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main" {
    mapOf("() to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
            a
        }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts@ExperimentalContractscontract

.
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@()
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ExperimentalContracts -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import {
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) kotlin.contracts.ExperimentalContracts
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts{InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) .
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import to.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" kotlin "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, @) ->
        runBlocking {
            a
        }
    }
}

bExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (() -> Unit, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: a) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(runBlocking {
            a
        }).forEach { (a, b) ->
        "" to ""
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    block {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    contract()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking InvocationKind
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, {.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contractsmapOfcontract

@ExperimentalContracts
fun main() {
    .("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{
fun main() ExperimentalContracts
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import runBlocking.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        kotlin {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "callsInPlace).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        "(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@"
fun main() {
    mapOf(ExperimentalContracts" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    a("" to "").forEach { (a, b) ->
        runBlocking {
            mapOf
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.{ (a, b) ->
        runBlocking {
            a
        }
    }
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach InvocationKind
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> )) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCEUnit
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
block

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    import kotlin.contracts.contract()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b{ ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract )
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.}

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        contract
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, brunBlocking ->
        ) {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").{
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
} { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) forEach
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            )
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block(a
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } -> Unit) {
    ()
    block()
}
yÔ"
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("// Original bug: KT-39113 to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import (.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlacekotlin.contractsblock, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts(InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main.) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking((: () -> Unit) {
    contract {
        callsInPlaceblockblock, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    {
            a
        }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts."
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to InvocationKind").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { runBlocking {
            a
        } ->
        (a, b)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import )

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (kotlin.contracts.contract -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
.

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts// Duplicated bug: KT-39113InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

) kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ""import.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            ExperimentalContracts
        }
    }
}

@a
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ).contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ""kotlin.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").() { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockforEach
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlincontractcontracts.InvocationKind
import kotlin.contracts..

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
fun main() {
    ExperimentalContracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun mainblock: () -> Unit {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(()) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ(
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { // Original bug: KT-39113a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
forEach kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").import { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import Unit

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> kotlin.contracts.contract) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
} "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) to
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{ (a, b) ->
        runBlocking {
            a
        }
    }
fun main() {
    mapOf("" to "").forEach ExperimentalContracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main(-> {
    mapOf("" to "").forEach { (a, b) )
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun )() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block(main
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

contract

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    @ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
} {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contractsaInvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            .
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach } (a, b) ->
        runBlocking {
            a
        }
    {
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

// Original bug: KT-39113
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contractsacontract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            .
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.(.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlacecontractsblock, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    block()("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    mapOf
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.contract
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    ExperimentalContracts {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts)contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE.
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").InvocationKind.EXACTLY_ONCE { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, forEach)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import @ExperimentalContracts.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

kotlin
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts..
import kotlinInvocationKindcontracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, block()) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    b
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a@ExperimentalContracts b) ->
        runBlocking {
            a
        }
    }
}

,
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, bblock() ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    )
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun maincallsInPlace(block, InvocationKind.EXACTLY_ONCE) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        ()
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}.contract

@ExperimentalContracts
fun main() kotlin.contracts

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { ({
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract a
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

mapOf
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    import kotlin.contracts.ExperimentalContracts("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind.EXACTLY_ONCE
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

(
fun main@ExperimentalContracts) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import a.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (kotlin, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.a

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            contract
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ->) ( Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract contracts
    block()
}
yÔ// Original bug: KT-39113
InvocationKind

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.// Duplicated bug: KT-39113
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import { (a, b) ->
        runBlocking {
            a
        }
    }.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach kotlin.contracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin..contractsExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingcontract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }block: () -> Unit) {
    (
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    runBlocking("" to "").forEach { (a, b) ->
        mapOf {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@)
fun main(ExperimentalContracts {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    (block, InvocationKind.EXACTLY_ONCE)("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlacemapOf
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
(a, b)

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { import kotlin.contracts.contract ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    kotlin.contracts
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } () -> Unit) {
    contract :
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking block
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking({
            a
        }: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract )
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contractscontractsInvocationKind
import kotlin...contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main{
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
} ()

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> block) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    Unit()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            }
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    a
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contractsExperimentalContractscontract

@.
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "{).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) "
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach @ExperimentalContracts
}

{ (a, b) ->
        runBlocking {
            a
        }
    }
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import @ExperimentalContracts.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

kotlin
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
EXACTLY_ONCE main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.fun)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import @ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

kotlin.contracts
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ExperimentalContracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@kotlin.contracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() a
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            {
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract kotlin
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

@ kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

importExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import fun
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
kotlin.contracts.ExperimentalContracts main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.b.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, contracts) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        (
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking}block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.@ExperimentalContracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

contracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        }
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ () -> Unit runBlocking(block: fun) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            callsInPlace(block, InvocationKind.EXACTLY_ONCE)
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        a
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.}.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    contracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { @ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}a, b) ->
        runBlocking {
            a
        }
    }
}

(
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach ( (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: {) -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        fun {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ runBlocking runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

to
fun main() {
    mapOf("" @ExperimentalContracts "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
InvocationKind.EXACTLY_ONCE

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, // Duplicated bug: KT-39113)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    import kotlin.contracts.ExperimentalContracts
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
block

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(// Duplicated bug: KT-39113: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.mapOf
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    InvocationKind("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import InvocationKind.EXACTLY_ONCE
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, kotlin.contracts.InvocationKind)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import import kotlin.contracts.InvocationKind.contracts.ExperimentalContracts
kotlin
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun (main) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import {.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach kotlin.contracts (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        block {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    runBlocking()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(blockUnit () -> :) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    callsInPlace(block, InvocationKind.EXACTLY_ONCE)
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import () -> Unit

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: kotlin.contracts.contract) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").block { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(forEach, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } fun runBlocking(block: () -> Unit) {
    contract /*inline*/
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

contract
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

()ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block@
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    block {
        callsInPlace(contract, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").{ (a, b) ->
        runBlocking {
            a
        }
    } forEach
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ block runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    fun()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

fun

@ExperimentalContracts
/*inline*/ @ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
} runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
.

import kotlin// Duplicated bug: KT-39113contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> UnitExperimentalContracts {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract import kotlin.contracts.ExperimentalContracts
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOfInvocationKind"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, (.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(" to """).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(blockEXACTLY_ONCE () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.:)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.contract

@InvocationKind
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
ExperimentalContracts

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@import kotlin.contracts.contract
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ({
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } -> Unit) {
    contract )
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun mapOf("" to "")() {
    main.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import ().contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockkotlin
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { -> (a, b)
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.(

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlacecontractblock, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main(Unit {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> )) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        InvocationKind.EXACTLY_ONCE(block, callsInPlace)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach contract
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    { (a, b) ->
        runBlocking {
            a
        }
    } {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.() -> Unit.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    :
}

@ExperimentalContracts
/*inline*/ fun runBlocking(blockmapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    } () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }.contracts.contract

@ExperimentalContracts
fun main() {
    kotlin
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@to
fun main() {
    mapOf("" ExperimentalContracts "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.InvocationKind.EXACTLY_ONCE
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, ExperimentalContracts)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import (.contracts.contract

@ExperimentalContracts
fun main() {
    mapOfkotlin"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() }
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
{

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract ExperimentalContracts
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "runBlocking {
            a
        }).forEach { (a, b) ->
        "
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").() -> Unit
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: forEach { (a, b) ->
        runBlocking {
            a
        }
    }) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").contract { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    forEach {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.}

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    contract
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf."" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind(EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun maina) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            (
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

mapOf("" to "")

@ExperimentalContracts
fun main() {
    import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.block: () -> Unit.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contractsaExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            .
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import (block: () -> Unit).contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingkotlin.contracts {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    runBlocking {
            a
        }("" to "").forEach { (a, b) ->
        mapOf
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import block: () -> Unit.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(kotlin) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        )(block, InvocationKind.EXACTLY_ONCE)
    }
    block(callsInPlace
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import @ExperimentalContracts.contracts.InvocationKind
import kotlin.contracts.contract

kotlin
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun InvocationKind.EXACTLY_ONCE() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, main)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { () -> Unita, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, ).EXACTLY_ONCE)
    }
    block(InvocationKind
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
( main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockfun)
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun mainblock: () -> Unit) {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(() {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.import
InvocationKind kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract InvocationKind
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

block()ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    @
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

.
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

block
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(@ExperimentalContracts, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin{contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract .
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" block "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    to()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

.
import kotlinimport kotlin.contracts.ExperimentalContractscontracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind}EXACTLY_ONCE)
    .
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block( () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block:)
}
yÔ"
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(// Original bug: KT-39113" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

InvocationKind.EXACTLY_ONCEExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, @)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.)

@ExperimentalContracts
fun main() {
    mapOf("" to ""contract.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@}
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    ExperimentalContracts
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import /*inline*/.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
kotlin fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@EXACTLY_ONCE
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.ExperimentalContracts)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@->
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () ExperimentalContracts Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin{contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract .
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to { (a, b) ->
        runBlocking {
            a
        }
    }).forEach ""
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to contract).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    "" {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: {) -> Unit) {
    contract (
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.block: () -> Unit.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(contracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts(ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace.block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
main kotlin.contracts.contract

@ExperimentalContracts
fun import() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach InvocationKind (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, {.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf()" to """.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.{
            a
        }.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking contracts
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contractsaExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (., b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    Unit.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> mapOf("" to "")) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlinInvocationKindcontracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, ..EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import "" to "".contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(kotlin).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
{ (a, b) ->
        runBlocking {
            a
        }
    }

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach // Duplicated bug: KT-39113
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { InvocationKind.EXACTLY_ONCE ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, (a, b))
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun mainmapOf) {
    (("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, block) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    b()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    import kotlin.contracts.InvocationKind
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.""
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(InvocationKind to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
)
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCEimport kotlin.contracts.InvocationKind
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach (block, InvocationKind.EXACTLY_ONCE)
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace{ (a, b) ->
        runBlocking {
            a
        }
    }
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@{
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking ExperimentalContracts
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.).InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main(contracts {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import runBlocking {
            a
        }

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        kotlin.contracts.contract
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.:
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(blockExperimentalContracts () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import @ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

kotlin.contracts.ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ExperimentalContracts.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@kotlin
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(Unit).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> "" to "") {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.contracts.InvocationKind
import kotlin.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(a" to "").forEach { (", b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (ExperimentalContracts -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ".InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("kotlin.contracts to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (afun b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ , runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@block
/*inline*/ fun runBlocking(ExperimentalContracts: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" block "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(to, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

a

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun callsInPlace() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        main(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@fun
/*inline*/ ExperimentalContracts runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import (

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    blockkotlin.contracts.contract)
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract contracts
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

(
fun main() {
    mapOf("" to "").forEach { @ExperimentalContractsa, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() EXACTLY_ONCE
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.{)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin@contracts.InvocationKind
import kotlin.contracts.contract

.ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking ,
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block{ InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
" kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to import").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> callsInPlace(block, InvocationKind.EXACTLY_ONCE)) {
    contract {
        Unit
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin}contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    .
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "}).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    "
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import (block, InvocationKind.EXACTLY_ONCE)
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlacekotlin.contracts.ExperimentalContracts
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@block
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    ExperimentalContracts()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

contract kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    import {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@InvocationKind
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.block: () -> Unit
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(ExperimentalContracts) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contract.contracts

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin@ExperimentalContractscontracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

.
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import import kotlin.contracts.contract.InvocationKind
kotlin.contracts

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf((block, InvocationKind.EXACTLY_ONCE) to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace""
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) .
    contract {
        callsInPlace(block, InvocationKind{EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking .
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind{
            a
        }EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contractsforEachcontract

@ExperimentalContracts
fun main() {
    mapOf("" to "").. { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    )(block
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    EXACTLY_ONCE("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.mapOf)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
block: () -> Unit

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(import kotlin.contracts.contract) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlinbcontracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, .) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
a

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            import kotlin.contracts.contract
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlinacontracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            .
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@block
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    ExperimentalContracts()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    () -> Unit.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: mapOf("" to "")) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
() kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: import -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOfblock"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace((, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract kotlin.contracts
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.contracts
import kotlin.ExperimentalContracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ{ (a, b) ->
        runBlocking {
            a
        }
    }
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach // Original bug: KT-39113
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

a kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (import, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun mainb {
    mapOf("" to "").forEach { (a, ()) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}
import kotlin.contracts.contract

InvocationKind

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, bblock ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(), InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun (a, b)() {
    mapOf("" to "").forEach { main ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

.
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    {
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

blockExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(@, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
contract

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    } {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").,
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(blockforEach { (a, b) ->
        runBlocking {
            a
        }
    } InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (Unit -> )) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

main
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun import kotlin.contracts.ExperimentalContracts() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    } (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        ExperimentalContracts
    }
}

@runBlocking {
            a
        }
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import runBlocking.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        kotlin.contracts {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin./*inline*/.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
contracts fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        block
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(}: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
runBlocking

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        // Duplicated bug: KT-39113 {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@runBlocking
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun ExperimentalContracts(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin""contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to .).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

InvocationKind.EXACTLY_ONCE
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, @ExperimentalContracts)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import {

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach kotlin.contracts.contract (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> String) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÞ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.properties.Delegates.notNull

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÁ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        InlineDouble(-0.0)
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y‹// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    "".withIndex()
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "Z").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import Derived(o).foo(42)
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    box {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {k -> result += k}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yà// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { val result: String ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@String
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yš// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract { it }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("OK").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Any) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    Inner()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (index, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¾// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, "RED")
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*!!!*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¦// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
        z
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

enum
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (list1, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import A("OK").foo().result()

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        box(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    M("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÝ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: (n: ULong?) -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    alist()
}
y»// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    return if (value[0] == 44 && value[1] == "example") 1 else 0
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            y
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import a.add(1)

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "Wrong elements for 8.toShort() downTo 3.toShort() step 2: $list3").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (l3, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.xl)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.s)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.doIf

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.properties.ReadOnlyProperty
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    test {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" String "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@String
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÈ// Original bug: KT-39113
// WITH_RUNTIME

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(list4, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Int
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.LightVariable.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Int) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y_// Original bug: KT-39113
// Duplicated bug: KT-39113



@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    bar()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import ic.value.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    Y()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import Delegates.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" or "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y½// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Foo
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.AssertionError
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import nx.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÙ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Suppress("UNCHECKED_CAST")
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@doTest
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(it.asList()).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract { field }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(3, {x -> x}).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y”// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").A("OK")
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(MaxUL, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.reflect.KProperty
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¯// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
    object : First { }
}
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        containsValue {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(x) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    l {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, (b::f).get())
    }
    block()
}
y²// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        width += width
    }
    block()
}
y§// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    testNullable(NX(null), null)
    block()
}
y²// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace()
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf().forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¸// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { _, xx -> Str(xx.string + "123") }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.i
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "OK").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import t.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(varProp, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.String.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    x {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(invoker, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yµ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
        return u1 // box
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" = "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { i.toByte() }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y˜// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

fun box() : String {
  if( (10.foo1())() != "23910") return "foo1 fail"
  if( (10.foo2())(1) != 11 ) return "foo2 fail"

  if(1.(fun Int.() = this + 1)() != 2) return "test 3 failed";
  if(  {1}() != 1)  return "test 4 failed";
  if(  {x : Int -> x}(1) != 1)  return "test 5 failed";
  if(  1.(fun Int.(x: Int) = x + this)(1) != 2) return "test 6 failed";
  if(  t.(fun Any.() = this)() != t)  return "test 7 failed";
  if(  (fooT1<String>("mama"))() != "mama")  return "test 8 failed";
  if(  (fooT2<String>("mama"))("papa") != "mamapapa")  return "test 9 failed";
  return "OK"
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import z2.foo("", 0).contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            getO
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { this }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Any
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@A<A<String>>
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Array<Boolean>
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.box.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.reflect.KProperty1
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y‘// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    test()
    block()
}
yš// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").(A::foo)(B())
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y½// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, a2.a)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace("O", InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(Unit, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    box("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.param.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" != "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y8// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            {
    for (x in LongArray(5)) {
        if (x != 0.toLong()) return "Fail $x"
    }
    return "OK"
}
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Any
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        testNotNullPrimitive(a, a, a, a)
    }
    block()
}
yÚ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(numberArg: Number = 0.0) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.b.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¼// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        {
    return x in 1..2
}
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" e "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import foo.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y³// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(a)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { () ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    size()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    element5("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        T(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

operator
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking() {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun K(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yâ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Target(AnnotationTarget.EXPRESSION)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y—// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {}
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yõ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@file:UseExperimental(ExperimentalTypeInference::class)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    result {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, r) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.T.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÞ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { var (a, b) = A() ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÁ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import Host.x

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.String
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        result {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        String {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { 1 foo 2}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import MAX_VALUE.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¦// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    return KMutableProperty1Impl().returnType
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yí// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
        j -> Array(1) { k -> x(i, j, k)  }
    }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        ref(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yž// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract { it.age }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").String { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y // Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").(A::foo)(this, 111)
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y²// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        A()
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import A.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÈ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, My.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, baz.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@success
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import b.foo(0, 0).contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        foo {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.z

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y½// Original bug: KT-39113
// Duplicated bug: KT-39113

import Foo.bar0 as bar
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@C
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÈ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { Test.ok() }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.IntArray.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import a.cond1.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@B
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" is "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.prop.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@A<String, K>
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").a { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yª// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { prop, old, new -> }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block("")
}
y¿// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, c.prop)
    }
    block()
}
yâ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import (3u.toUByte() downTo 5u.toUByte()).reversed()
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(42).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        MyThrowable {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Base
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y½// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import d3.foo()
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yý// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
        column("OK") {
            result = name
        }
    }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.x
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(B, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@getReceiver
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import Outer().Inner().callback()

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> ok) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { it + value2 }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(i: Int) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" % "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").str { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y–// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {}
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    o()
}
y•// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    get(Z(42))
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import A().test()[5].i.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.box)
    }
    block()
}
y¿// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        {
    foo(i.superclass())
}
    }
    block()
}
yà// Original bug: KT-39113
// Duplicated bug: KT-39113

import a.add(try { foo() } catch (e: Throwable) { "OK" })
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yô// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        foo(foo) // 1st foo is a local fun, second is a captured local var
        foo()
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.Inner.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("FAIL" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import String.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y“// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    create()
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Int?
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y§// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {a -> "" + a.size}
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    x()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import Any.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { x++ }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    A("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    i("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    Int("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Anno
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.invoke.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import log.toString()
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.a.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.S.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

fun box(): String {
    val key by Whatever
    return {
        object {
            val keys = key
        }.keys
    } ()
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        ArrayList {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        foo(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(bDefault: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" run "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> add) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÃ// Original bug: KT-39113
// KT-5612

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.Outer.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Test) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yv// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

fun box(): String {
    ByteDelegateTest().test()

    return "OK"
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import L(4L).foo().contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" ?: "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { t: Any? ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: List<String>) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    if (Z().x != 1234) throw AssertionError()
    if (L().x != 1234L) throw AssertionError()
    if (S().x != "foobar") throw AssertionError()

    return "OK"
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.z
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    v2("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y›// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract { 42u }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.x
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y:// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        {
    val a1 = Actor(1, "Jeff", "Bridges")

    val a1c = a1.copy()
    if (a1c.id != a1.id) return "Failed: a1.copy().id==${a1c.id}"

    val a2 = Actor(2, "Jeff", "Bridges")
    if (a2 == a1) return "Failed: a2==a1"

    // Assume that our hashCode is good enough for this test :)
    if (a2.hashCode() == a1.hashCode()) return "Failed: a2.hashCode()==a1.hashCode()"

    a1.toString()

    return "OK"
}
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    list2()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.foo.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@x
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        doTest {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.range0
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, FunctionNodeImpl())
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.component1)
    }
    block()
}
yÞ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.collections.AbstractIterator

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import 'a'.baz().contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        list1 {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.reflect.*
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y‘// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        {
            open class B() {
                open fun s() : String = "O"
            }

            val o = object : B() {
                override fun s(): String = "K"
            }

            B().s() + o.s()
        }()
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@A
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import String.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y·// Original bug: KT-39113
// Duplicated bug: KT-39113

import X.B.value
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    Set {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, (MinUS + 2u).toUShort())
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

data
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÂ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Int
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { result = "O"; "K"}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(KProperty, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        i(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import a6.prop.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" ! "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            xz
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    n("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import FOO.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.result.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.t.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÝ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            str2 = str
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    get {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(String, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yå// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    if (!foo(1.toByte())) return "fail 1"
    if (!foo((1.toByte()).inc())) return "fail 2"

    return "OK"
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yæ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.UninitializedPropertyAccessException

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {"$a:$b;"}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@b
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yé// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    run {
        return u1 // box
    }.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÙ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(w: Wrapper<T>, x: Any?) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.getOk.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.i)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" ua "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¶// Original bug: KT-39113
// Duplicated bug: KT-39113

import a.result
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y×// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> rangeTo) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.a)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            T
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(y, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y’// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").foo()
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    a {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yº// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        test(listOf("a", "b"))
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> ZImpl2) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" x "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@file:Suppress("SECONDARY_CONSTRUCTOR_WITH_BODY_INSIDE_INLINE_CLASS")
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.String.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÇ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@Suppress
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.recivier.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").D()
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.x.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÚ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

fun doTest(): String {
    var s = ""
    for ((a, b) in 0.toLong().rangeTo(2.toLong())) {
      s += {"$a:$b;"}()
    }
    return s
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("OK" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y„// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    State.O
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yº// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
                show()
            }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import o.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    test("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.T
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¾// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, "239")
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import order.append("X")

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y›// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { 42 }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import doSimple.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(box: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yŽ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    StringCell("O").x
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÞ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking { if (prop == 1) "OK" else "fail" }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.s

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@X
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import string.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        getValue(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import A.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import box.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yþ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@Suppress("SECONDARY_CONSTRUCTOR_WITH_BODY_INSIDE_INLINE_CLASS")
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import Test.OK.s.string

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, String.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { ok() }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÀ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@p
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import B.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

override
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, ref.set(x, Z(1234)))
    }
    block()
}
yã// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        {
        list1.add(i)
        if (list1.size > 23) break
    }
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        s {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yá// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (val expected: Int) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import box.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import B.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÁ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import list1.add(i)
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yœ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {x -> x}
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").i { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import C().test().contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yJ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        {
    fun local(i: Int = 1) : Int {
        return i
    }

    if (local() != 1) return "Fail 1"
    if (local(2) != 2) return "Fail 2"

    return "OK"
}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¿// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¡// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract { Test.ok() }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, zap.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.reflect.KProperty
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yì// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(arrayOf(Color.BLUE, Color.GREEN)).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> l) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y§// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    if (c != null) {
        flag = true
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¶// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

fun box(): String {
    if (!true) {
        return "fail"
    } else {
        return "OK"
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
        xz = Z(42)
        xl = L(1234L)
        xs = Str("abc")
        xo = Obj("def")
    }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(zs: List<Foo2<Any>>) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y·// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        invoke()
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÁ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import a.asResult()
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.count.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        bar {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import a.foo().contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import T.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        {
    if (Test().testDefault() != 42) throw AssertionError()
    if (Test().testDefault(Z(123)) != 123) throw AssertionError()

    if (Test().testOverridden() != 42) throw AssertionError()
    if (Test().testOverridden(Z(123)) != 123) throw AssertionError()

    return "OK"
}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y®// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {
            a
        }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.Double.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// no local optimizations
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÕ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    String("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yØ// Original bug: KT-39113
// Duplicated bug: KT-39113

import intArrayOf.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import ZImpl2().value.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÞ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "OverOver: ").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÖ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Double) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.String
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(val z: String) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(0).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.Long.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, Long.EXACTLY_ONCE)
    }
    block()
}
yÈ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import Outer.Nested().test
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÚ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { e: Exception ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
@ExperimentalContracts
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }


@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
}yÔ@ExperimentalContracts
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

// Original bug: KT-39113
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÔ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }

}
    }
@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import .contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlockingblock: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {(a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE
    }
    block()
}
yÈ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        (block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKindEXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÌ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
     {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ( -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import .InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin..InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¹// Original bug: KT-39113


import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "".forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlincontracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import .contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }}
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(""  "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y”// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract 
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }block()
}
y¯// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block(
}
y»// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking 
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÈ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach {  ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b)->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, .EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf(""to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¾// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}


/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin..ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () ->Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y´// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind


@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ funrunBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contractsExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import .contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¿// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
 fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contractsInvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
importkotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ).forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, ) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to"").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y‹// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlincontracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf"" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¹// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, )
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main( {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    ()
}
y¿// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yw// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) 
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin..contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to ""). { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> ) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract{
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    
    block()
}
yu// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() 

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÊ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf.forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract 
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach{ (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKindimport kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlaceblock, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a,b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
         {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit){
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
 kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()

y}// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

importkotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yË// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a}
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach  (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    ("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "")forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y®// Original bug: KT-39113
// Duplicated bug: KT-39113

import 
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }


@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y2// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}


y§// Original bug: KT-39113
// Duplicated bug: KT-39113


import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block,InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y•// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach 
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¾// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract


fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() 
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contractscontract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y»// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import 

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÐ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun () {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
 kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) 
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yµ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import 
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y°// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace
    }
    block()
}
yÄ// Original bug: KT-39113
// Duplicated bug: KT-39113

import .ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y»
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y¤// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

import .contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
y®// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts

import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlincontracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÁ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÍ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÆ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÉ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun (block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block)
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContractsfun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
funmain() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÑ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/  runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking 
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking{
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block:  -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ()-> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ()  Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContractsimport kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÅ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    .forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: ) -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÒ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf( to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÎ// Original bug: KT-39113
// Duplicated bug: KT-39113

 kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
yÏ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a, b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)}
    block()
}
yÓ// Original bug: KT-39113
// Duplicated bug: KT-39113

import kotlin.contracts.ExperimentalContracts
import kotlin.contracts.InvocationKind
import kotlin.contracts.contract

@ExperimentalContracts
fun main() {
    mapOf("" to "").forEach { (a b) ->
        runBlocking {
            a
        }
    }
}

@ExperimentalContracts
/*inline*/ fun runBlocking(block: () -> Unit) {
    contract {
        callsInPlace(block, InvocationKind.EXACTLY_ONCE)
    }
    block()
}
ÿÿ