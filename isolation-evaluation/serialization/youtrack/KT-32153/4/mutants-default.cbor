øiexportTaggdefaultnoriginalSampleyW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
gmutantsüyç// Original bug: KT-34213
// Duplicated bug: KT-32153


val c = false
try
{suspend fun anotherOuterSuspendFun(){
}}
catch(e: Exception){}
finally{}

suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}

val v = true
try
{suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}}
catch(e: Exception){}
finally{}

yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()
val n = false
try
{{
}}
catch(e: Exception){}
finally{}

suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yQ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() 
val l = false
if (l) {{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}} else {{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}}

y¢// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    
val q = true
if (q) {suspend fun anotherInnerSuspendFun(){
    }} else {suspend fun anotherInnerSuspendFun(){
    }}

    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    
val f = true
try
{suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }}
catch(e: Exception){}
finally{}

    doInnerSuspend()
}
yà// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    
val s = false
if (s) {doInnerSuspend()} else {doInnerSuspend()}

}
yë// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()
val x = true
when (x) {
 true -> {{
    }}
 else -> {{
    }}
}

    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yÁ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() 
val g = false
if (g) {{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }} else {{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }}

    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    
val v = true
try
{doInnerSuspend}
catch(e: Exception){}
finally{}
()
}
yº// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        
val y = false
when (y) {
 true -> {Unit.let {
            doInnerSuspend()
        }}
 else -> {Unit.let {
            doInnerSuspend()
        }}
}


        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yõ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        
val z = false
when (z) {
 true -> {doInnerSuspend()}
 else -> {doInnerSuspend()}
}


        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yµ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        
val a = false
when (a) {
 true -> {Unit.let {
            println()
        }}
 else -> {Unit.let {
            println()
        }}
}


        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        
val x = false
try
{Unit.let {
            anotherOuterSuspendFun()
        }}
catch(e: Exception){}
finally{}


        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y∞// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        
val k = true
if (k) {Unit.let {
            anotherInnerSuspendFun()
        }} else {Unit.let {
            anotherInnerSuspendFun()
        }}

    }
    doInnerSuspend()
}
y{// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        
val e = true
if (e) {Unit} else {Unit}
.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y£// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.
val f = true
if (f) {let {
            doInnerSuspend()
        }} else {let {
            doInnerSuspend()
        }}


        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yô// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        
val k = false
when (k) {
 true -> {doInnerSuspend}
 else -> {doInnerSuspend}
}
()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y{// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        
val l = true
if (l) {Unit} else {Unit}
.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.
val z = true
try
{let {
            println()
        }}
catch(e: Exception){}
finally{}


        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        
val b = false
try
{Unit}
catch(e: Exception){}
finally{}
.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yø// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.
val b = false
when (b) {
 true -> {let {
            anotherOuterSuspendFun()
        }}
 else -> {let {
            anotherOuterSuspendFun()
        }}
}


        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        
val v = false
try
{Unit}
catch(e: Exception){}
finally{}
.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.
val w = false
try
{let {
            anotherInnerSuspendFun()
        }}
catch(e: Exception){}
finally{}

    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.
val r = true
when (r) {
 true -> {let}
 else -> {let}
}
 {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.
val y = false
try
{let}
catch(e: Exception){}
finally{}
 {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yé// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.
val h = false
when (h) {
 true -> {let}
 else -> {let}
}
 {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yé// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.
val h = false
when (h) {
 true -> {let}
 else -> {let}
}
 {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yü// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let 
val w = true
if (w) {{
            doInnerSuspend()
        }} else {{
            doInnerSuspend()
        }}


        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y¨// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let 
val n = false
when (n) {
 true -> {{
            println()
        }}
 else -> {{
            println()
        }}
}


        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y®// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let 
val p = false
if (p) {{
            anotherOuterSuspendFun()
        }} else {{
            anotherOuterSuspendFun()
        }}


        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y∫// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let 
val c = true
when (c) {
 true -> {{
            anotherInnerSuspendFun()
        }}
 else -> {{
            anotherInnerSuspendFun()
        }}
}

    }
    doInnerSuspend()
}
yü// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let 
val c = true
if (c) {{
            doInnerSuspend()
        }} else {{
            doInnerSuspend()
        }}


        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let 
val q = false
try
{{
            println()
        }}
catch(e: Exception){}
finally{}


        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y®// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let 
val h = false
if (h) {{
            anotherOuterSuspendFun()
        }} else {{
            anotherOuterSuspendFun()
        }}


        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yß// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let 
val m = true
if (m) {{
            anotherInnerSuspendFun()
        }} else {{
            anotherInnerSuspendFun()
        }}

    }
    doInnerSuspend()
}
yà// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            
val t = false
if (t) {doInnerSuspend()} else {doInnerSuspend()}

        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            
val z = true
try
{println()}
catch(e: Exception){}
finally{}

        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            
val u = true
try
{anotherOuterSuspendFun()}
catch(e: Exception){}
finally{}

        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yê// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            
val v = false
if (v) {anotherInnerSuspendFun()} else {anotherInnerSuspendFun()}

        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            
val s = true
try
{doInnerSuspend()}
catch(e: Exception){}
finally{}

        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yî// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            
val m = false
when (m) {
 true -> {println()}
 else -> {println()}
}

        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            
val q = true
try
{anotherOuterSuspendFun()}
catch(e: Exception){}
finally{}

        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            
val h = true
try
{anotherInnerSuspendFun()}
catch(e: Exception){}
finally{}

        }
    }
    doInnerSuspend()
}
yô// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            
val n = false
when (n) {
 true -> {doInnerSuspend}
 else -> {doInnerSuspend}
}
()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y~// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            
val s = true
if (s) {println} else {println}
()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            
val n = false
try
{anotherOuterSuspendFun}
catch(e: Exception){}
finally{}
()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            
val q = true
if (q) {anotherInnerSuspendFun} else {anotherInnerSuspendFun}
()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        (Unit).let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let ({
            anotherInnerSuspendFun()
        })
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()!!
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit!!.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }!!
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let!! {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }!!

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit!!.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() Unit
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        {.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
    } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()suspend
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() )
    suspend fun anotherInnerSuspendFun({{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun({
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend())
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend(()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let anotherInnerSuspendFun
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspendUnit.let {
            println()
        })
}
yW// Original bug: KT-34213
()

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend// Duplicated bug: KT-32153 {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit{
            doInnerSuspend()
        }let .

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

anotherInnerSuspendFun fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            suspend()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Unit.let {
            anotherOuterSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        let {
            doInnerSuspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        ).let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(Unit
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend //case 3: use inline fun, but no recurse, works well anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        fun
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
) fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(suspend
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFunUnit{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        ().let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
            println()
        }
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun{
            println()
        }){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        {
            anotherInnerSuspendFun()
        }.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit{
            anotherOuterSuspendFun()
        }let .

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspendanotherInnerSuspendFun)

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            (()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        let.Unit {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend {
            doInnerSuspend()
        } anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let fun

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    } fun anotherInnerSuspendFun(){
    suspend
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        anotherInnerSuspendFun

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            }()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        let

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.} {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            doInnerSuspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
suspend fun outerSuspendFun() suspend fun anotherOuterSuspendFun(){
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun({
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend())
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        let {
            doInnerSuspend()
        }.Unit

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend {
            doInnerSuspend()
        } outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let fun

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFunUnit.let {
            anotherInnerSuspendFun()
        })
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        (
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun}
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    } fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        suspend

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let ()

        //case 2: no inline fun but recurse, works well
        doInnerSuspend{
            doInnerSuspend()
        }

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
            anotherInnerSuspendFun()
        }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
    }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend))
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend((

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun({
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() )
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend } anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    fun
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun())
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend({

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        UnitanotherOuterSuspendFunlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            .()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunUnit {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        ().let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend(

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
} fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() suspend
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend())

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspendanotherOuterSuspendFun {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            ()()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun )(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(anotherInnerSuspendFun
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()Unit.let {
            anotherInnerSuspendFun()
        }
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        {
}
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

Unit.let {
            anotherOuterSuspendFun()
        } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        suspend

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.{
            anotherInnerSuspendFun()
        } {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let let
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitletlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.. {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        Unit.let {
            println()
        }

        //case 3: use inline fun, but no recurse, works well
        doInnerSuspend()

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

doInnerSuspend
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            suspend fun anotherOuterSuspendFun(){
}()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            Unit
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        doInnerSuspend().let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    let {
            anotherOuterSuspendFun()
        } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.suspend

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        ()
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend//case 3: use inline fun, but no recurse, works well
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspendlet

        //case 3: use inline fun, but no recurse, works well
        Unit.() {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        let {
            anotherInnerSuspendFun()
        }
        Unit.//case 5: use inline fun, and call another inner suspend fun, works well
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend({ {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let )
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendanotherInnerSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            ()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            ()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFundoInnerSuspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            println()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

Unit fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        suspend.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            )
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

)
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(suspend fun anotherOuterSuspendFun(){
}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(.{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit)let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunfun {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend () doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        {
            anotherOuterSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let Unit.let {
            doInnerSuspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun{
            println()
        }) {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        anotherOuterSuspendFun().let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            Unit
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWprintln()
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            // Original bug: KT-34213
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        let
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.//case 1: use inline fun and recurse, CompilationException {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()doInnerSuspend()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    {
}
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.} {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        let

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        .
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit//case 1: use inline fun and recurse, CompilationExceptionlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        println()
        Unit.let {
            //case 3: use inline fun, but no recurse, works well
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{ fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let suspend
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            println()
        }.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW{
            anotherOuterSuspendFun()
        }
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let // Original bug: KT-34213

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWsuspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
// Original bug: KT-34213
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        Unit()

        //case 3: use inline fun, but no recurse, works well
        doInnerSuspend.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend {
            println()
        } outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let fun

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun(
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.) {
            doInnerSuspend(let
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun())
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend({
}

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    {
            doInnerSuspend()
        } fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let suspend

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    . fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unitsuspendlet {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

) fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(suspend{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.{
            anotherInnerSuspendFun()
        } {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let let
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
println() fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            suspend
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun println(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            anotherOuterSuspendFun()
        } {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunsuspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    (
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun println()(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            anotherInnerSuspendFun
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        anotherOuterSuspendFun.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            Unit()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    Unit.let {
            anotherInnerSuspendFun()
        } fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        suspend
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{ fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() suspend
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

//case 5: use inline fun, and call another inner suspend fun, works well
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        suspend fun anotherOuterSuspendFun(){
}
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
) fun outerSuspendFun(suspend {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
doInnerSuspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    suspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    } {
    suspend fun anotherInnerSuspendFun(){
    }
    )
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.{

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let let {
            doInnerSuspend()
        }
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.( {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFunlet)
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()doInnerSuspend
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        {
}()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.) {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(let
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend.)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit(let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        ()()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFundoInnerSuspend
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() //case 5: use inline fun, and call another inner suspend fun, works well
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        {
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    {
    } fun anotherInnerSuspendFun()suspend
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    //case 5: use inline fun, and call another inner suspend fun, works well fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        suspend
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFunUnit)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        (.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        {
            anotherInnerSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit.let {
            doInnerSuspend()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        )

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun Unit(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        anotherOuterSuspendFun.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend doInnerSuspend() anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        fun

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        (
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println//case 2: no inline fun but recurse, works well)
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        anotherInnerSuspendFun.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Unit()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(let {
            doInnerSuspend()
        }{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.)

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.{ {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let let
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() // Duplicated bug: KT-32153
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    {
            anotherOuterSuspendFun()
        } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let suspend

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
Unit fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        suspend.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendanotherOuterSuspendFun())
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            (
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(anotherOuterSuspendFun {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            )()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.)

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(let {
            doInnerSuspend()
        }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        UnitanotherOuterSuspendFunlet {
            .()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        )
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(//case 3: use inline fun, but no recurse, works well
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.doInnerSuspend()

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    let {
            println()
        }
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend anotherInnerSuspendFun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            fun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        (.let {
            anotherInnerSuspendFunUnit)
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() (
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend{)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    //case 5: use inline fun, and call another inner suspend fun, works well
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        }
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunsuspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}){
}
(
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
fun fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend suspend doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
} fun anotherOuterSuspendFun(){
}
suspend
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let anotherOuterSuspendFun()
            {
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWfun
// Duplicated bug: KT-32153

suspend // Original bug: KT-34213 anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.anotherInnerSuspendFun() {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            let
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun anotherOuterSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            outerSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        .Unitlet {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    println fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            suspend()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() (
    doInnerSuspend{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    })
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.{ {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let let
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    //case 1: use inline fun and recurse, CompilationException
    suspend fun doInnerSuspend() {
        }
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() doInnerSuspend()
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    {
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspendUnit) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        (.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit}let {
            anotherOuterSuspendFun()
        .

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() Unit
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        {.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            let()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.doInnerSuspend {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() //case 1: use inline fun and recurse, CompilationException
        {
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            }
        }
    }
    doInnerSuspend()
anotherInnerSuspendFun()
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendlet)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.( {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        anotherInnerSuspendFun
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            //case 3: use inline fun, but no recurse, works well()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        .

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit}let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend{
            println()
        })
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    {
            anotherInnerSuspendFun()
        } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let suspend
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunlet {
            doInnerSuspend()
        } {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.()

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(Unit.let {
            anotherOuterSuspendFun()
        } {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        )

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun //case 1: use inline fun and recurse, CompilationException() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        outerSuspendFun
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            .
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        UnitdoInnerSuspend()let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend suspend fun anotherInnerSuspendFun(){
    } outerSuspendFun() {
    fun
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(anotherInnerSuspendFun
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            )()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(doInnerSuspend{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        )()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

} fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    suspend
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            println()
        }
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunlet {
            anotherInnerSuspendFun()
        }{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.()
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    Unit.let {
            println()
        }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        UnitanotherInnerSuspendFunlet {
            .()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
}
suspend fun outerSuspendFun() {
yW// Original bug: KT-34213
doInnerSuspend()

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        // Duplicated bug: KT-32153

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.()
    }
    doInnerSuspendlet {
            anotherInnerSuspendFun()
        }
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.{ {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let let
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.{
            anotherInnerSuspendFun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let let {
            println()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(anotherOuterSuspendFun() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            )
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            //case 3: use inline fun, but no recurse, works well
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        doInnerSuspend()
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun anotherOuterSuspendFun()() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            doInnerSuspend
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
} fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        suspend
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFundoInnerSuspend()){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            (
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun .(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        UnitanotherOuterSuspendFunlet {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            doInnerSuspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    anotherOuterSuspendFun()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    doInnerSuspend() fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    suspend
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun{{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()()
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        {
            anotherInnerSuspendFun()
        }.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
{
            anotherInnerSuspendFun()
        }
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()anotherOuterSuspendFun()
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            {
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit)let {
            println(.
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWlet {
            anotherOuterSuspendFun()
        }
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.// Original bug: KT-34213

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunlet {
            anotherInnerSuspendFun()
        }){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.(
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(( {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun))
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        suspend

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        Unit
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        //case 2: no inline fun but recurse, works well.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }() {
    suspend fun anotherInnerSuspendFun(){
    }
    outerSuspendFun
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}) (
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun Unit() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        outerSuspendFun.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    let {
            anotherInnerSuspendFun()
        }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.suspend fun anotherInnerSuspendFun(){
    }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun}){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        (

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun{
    } {
    suspend fun anotherInnerSuspendFun()()
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(. {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit)let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let anotherOuterSuspendFun()
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            {
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
            anotherOuterSuspendFun()
        } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let suspend

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        anotherOuterSuspendFun.let {
            Unit()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit//case 5: use inline fun, and call another inner suspend fun, works welllet {
            anotherOuterSuspendFun()
        }

        .
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            let()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.doInnerSuspend {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            )
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(println()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun anotherOuterSuspendFun(){
}
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.Unit

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        let {
            println()
        }.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun({
            anotherOuterSuspendFun()
        }{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let )

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        }.let {
            anotherOuterSuspendFun()
        Unit

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }{
    }
    suspend fun doInnerSuspend() ()
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun {
            println()
        }() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let outerSuspendFun

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        anotherInnerSuspendFun.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Unit()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            let
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.doInnerSuspend() {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFundoInnerSuspend
        }
    }
    ()()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    anotherOuterSuspendFun fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            suspend()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        ().let {
            doInnerSuspendUnit
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        //case 2: no inline fun but recurse, works well.let {
            doInnerSuspend()
        }

        Unit
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        let {
            doInnerSuspend()
        }
        Unit.//case 1: use inline fun and recurse, CompilationException

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun{
            anotherInnerSuspendFun()
        }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let ()
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
            doInnerSuspend()
        }
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        )

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun(Unit.let {
            doInnerSuspend()
        }
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
doInnerSuspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            suspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWsuspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    // Original bug: KT-34213
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend Unit doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        fun.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(//case 2: no inline fun but recurse, works well{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        )
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    ) fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(suspend
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend anotherInnerSuspendFun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            fun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspendlet)

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.( {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let println

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
            doInnerSuspend()
        }()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}(){
}
suspend fun outerSuspendFun() anotherOuterSuspendFun
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunUnit{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        ().let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            println()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            doInnerSuspend()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun}){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend Unit doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        fun.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            println
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            doInnerSuspend()()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun{
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
} ()
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.}
    let {
            anotherInnerSuspendFun()
        }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFundoInnerSuspend {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    ()()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        doInnerSuspend().let {
            Unit
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
let

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.// Duplicated bug: KT-32153 {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(({
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun)){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
Unit.let {
            println()
        }
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun}){
(
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
            anotherOuterSuspendFun()
        }()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let doInnerSuspend

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWdoInnerSuspend()
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            // Original bug: KT-34213
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

let {
            println()
        }
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.suspend fun anotherOuterSuspendFun(){
}

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunanotherInnerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            ()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    suspend
    } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            )
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(println()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun {() outerSuspendFun
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    () fun anotherInnerSuspendFunsuspend{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        {
            anotherOuterSuspendFun()
        }
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let //case 2: no inline fun but recurse, works well

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

anotherOuterSuspendFun() fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            suspend
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        let.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.Unit {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() anotherInnerSuspendFun
    suspend fun {(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(println()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            )
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()let
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.{
    } {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

outerSuspendFun fun anotherOuterSuspendFun(){
}
suspend fun suspend() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            anotherInnerSuspendFun()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            doInnerSuspend()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    (
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspendsuspend fun anotherInnerSuspendFun(){
    })
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(.{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit)let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            (()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFunprintln)
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        doInnerSuspend

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    Unit.let {
            doInnerSuspend()
        }()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    let fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.suspend {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit(let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun.)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.println {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            let()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            }()
        anotherOuterSuspendFun

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(( {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend))
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        .
        Unit//case 1: use inline fun and recurse, CompilationExceptionlet {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun((){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW)
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(// Original bug: KT-34213
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()anotherInnerSuspendFun()
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            let
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.doInnerSuspend() {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() ()
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun{
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let (
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend{)
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            let()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.doInnerSuspend {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunanotherOuterSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            ()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(} {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        )
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
            println()
        } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let suspend

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    ( fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendsuspend)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        {
            anotherInnerSuspendFun()
        }
        Unit.let //case 5: use inline fun, and call another inner suspend fun, works well
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend//case 1: use inline fun and recurse, CompilationException {
        ()
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWsuspend
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    // Original bug: KT-34213 fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let doInnerSuspend
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        {()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun {(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let anotherOuterSuspendFun
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.anotherInnerSuspendFun {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            let()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    //case 5: use inline fun, and call another inner suspend fun, works well
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        suspend fun anotherInnerSuspendFun(){
    }
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.() {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFunlet
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    (
    doInnerSuspendsuspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    })
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
() fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendsuspend
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFunUnit{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        ().let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.}

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        let {
            anotherOuterSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
            anotherOuterSuspendFun()
        }
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend { anotherInnerSuspendFun()fun
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        }.let {
            anotherInnerSuspendFun()
        Unit
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.()

        //case 2: no inline fun but recurse, works well
        doInnerSuspendlet {
            doInnerSuspend()
        }

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let doInnerSuspend()
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    {
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun.){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit(let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend((
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun))
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun({
    })
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun({
})
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun //case 2: no inline fun but recurse, works well() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        outerSuspendFun
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let (

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun{
            println()
        })
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend({
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let )
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        anotherInnerSuspendFun().let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Unit
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let .

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit{
            doInnerSuspend()
        }let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        {

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit.let {
            doInnerSuspend()
        }
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit//case 4: use inline fun, and call another outer suspend fun, works welllet {
            println()
        }

        .
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        doInnerSuspend.let {
            Unit()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.)

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(let {
            println()
        }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let Unit

        //case 4: use inline fun, and call another outer suspend fun, works well
        {
            println()
        }.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun())
        }
    }
    doInnerSuspend(
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun({ {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let )
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        Unit
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        //case 3: use inline fun, but no recurse, works well.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend {
    } outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()fun
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
}

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        // Duplicated bug: KT-32153

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

//case 4: use inline fun, and call another outer suspend fun, works well
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        suspend fun anotherOuterSuspendFun(){
}
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let println
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspendanotherInnerSuspendFun()) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            (
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun {
            println()
        }(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let anotherInnerSuspendFun

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend { outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() fun
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
{ fun outerSuspendFun() suspend
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend () anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            printlnfun
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend{
            doInnerSuspend()
        }) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let (

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun//case 5: use inline fun, and call another inner suspend fun, works well {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        ()
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    doInnerSuspend() fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        suspend

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()()
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun{{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun {
            doInnerSuspend()
        }() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let outerSuspendFun

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let doInnerSuspend
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    {()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.anotherInnerSuspendFun {
            let()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let let

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.{
            anotherOuterSuspendFun()
        } {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunsuspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}{
}
()
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend Unit.let {
            anotherOuterSuspendFun()
        } doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        fun

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

println fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            suspend()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(({
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend)) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        {
            println()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        {
            anotherInnerSuspendFun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit.let {
            println()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(suspend{
}
suspend fun outerSuspendFun() {
    ) fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() suspend
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        let.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.Unit {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFunsuspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }) {
    suspend fun anotherInnerSuspendFun(){
    }
    (
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
            anotherInnerSuspendFun()
        }
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let doInnerSuspend()
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        doInnerSuspend().let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    Unit
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            anotherOuterSuspendFun()
        }
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    suspend fun doInnerSuspend() suspend fun anotherInnerSuspendFun(){
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
//case 5: use inline fun, and call another inner suspend fun, works well fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        suspend
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFunsuspend){
    }
    ( fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun}
        }
    ()
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend{
            println()
        } {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let ()

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            let
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.println() {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        anotherInnerSuspendFun

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Unit.let {
            doInnerSuspend()
        }()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let println
            {()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println{)
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let (
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
{

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() // Duplicated bug: KT-32153
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
Unit.let {
            println()
        } fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        suspend

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    doInnerSuspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            suspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend {
            anotherOuterSuspendFun()
        } anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let fun

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    } fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() suspend
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.doInnerSuspend {
            let()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
{
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()println()
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend({ {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let )
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend)) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend((
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(}{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        )

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yWdoInnerSuspend
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        // Original bug: KT-34213()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun})
        }
    }
    doInnerSuspend()
(
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
            anotherInnerSuspendFun()
        }
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let println()
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let anotherInnerSuspendFun()

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
            anotherOuterSuspendFun()
        }
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun().
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit{let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun({
            anotherInnerSuspendFun()
        } {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let )
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend({
            anotherInnerSuspendFun()
        }
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let )
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            {
            anotherInnerSuspendFun()
        }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let anotherOuterSuspendFun()
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()suspend
}
{ fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.println() {
            let
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    //case 2: no inline fun but recurse, works well
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        }
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun())
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.anotherOuterSuspendFun() {
            let
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() let
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.{ {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitletlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.. {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let doInnerSuspend
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    {()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendUnit.let {
            anotherOuterSuspendFun()
        })
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        (

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend({
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    } )
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }(){
    }
    anotherInnerSuspendFun
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun )(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(anotherOuterSuspendFun
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
. fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitsuspendlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    . fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unitsuspendlet {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        {
            anotherInnerSuspendFun()
        }.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let Unit
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend//case 4: use inline fun, and call another outer suspend fun, works well {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        ()
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
            anotherInnerSuspendFun()
        } fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let suspend
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    suspend fun anotherOuterSuspendFun(){
}
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

doInnerSuspend()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        suspend fun anotherOuterSuspendFun(){
}

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

let
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.suspend fun anotherOuterSuspendFun(){
} {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
            anotherOuterSuspendFun()
        }()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let println

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let Unit.let {
            anotherInnerSuspendFun()
        }
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        {
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    anotherOuterSuspendFun()
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(doInnerSuspend() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    )
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let println()

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
            doInnerSuspend()
        }
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend Unit.let {
            anotherOuterSuspendFun()
        } outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        fun

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        {
            doInnerSuspend()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(let {
            anotherOuterSuspendFun()
        }{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.)

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
{
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(()
}
yWsuspend
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    // Original bug: KT-34213 fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    )
    doInnerSuspend(suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.Unit {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        let.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend println anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            fun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            Unit
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        anotherOuterSuspendFun().let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(anotherOuterSuspendFun()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            )
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
suspend

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    // Duplicated bug: KT-32153 fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    {
            println()
        } fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let suspend

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            Unit.let {
            anotherInnerSuspendFun()
        }()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        doInnerSuspend
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        {.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let Unit
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend doInnerSuspend() doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            fun
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            ()()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            printlndoInnerSuspend
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let )

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend({
            anotherOuterSuspendFun()
        }
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

{
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let suspend fun anotherOuterSuspendFun(){
}
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        let
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.//case 1: use inline fun and recurse, CompilationException {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.} {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        let
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() println
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() let
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.{ {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunlet{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.() {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun doInnerSuspend()(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            anotherInnerSuspendFun
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun{
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }){
    }
    suspend fun doInnerSuspend() (
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let let
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.{ {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let doInnerSuspend()

        //case 2: no inline fun but recurse, works well
        {
            doInnerSuspend()
        }

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()}
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    {
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            printlnlet)
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.( {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFunUnit{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        ().let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend(()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    {
            println()
        } fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let suspend

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit(let {
            anotherOuterSuspendFun.)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        doInnerSuspend

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    }()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(doInnerSuspend
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    )()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend . anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitfunlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

//case 1: use inline fun and recurse, CompilationException
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        suspend fun anotherOuterSuspendFun(){
}
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            ()()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspendprintln
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend{
            anotherOuterSuspendFun()
        }
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let ()

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun((
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend))
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        println

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            }()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() ()
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend{
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
            anotherOuterSuspendFun()
        }
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend)

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitlet. {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspendlet {
            println()
        })

        //case 3: use inline fun, but no recurse, works well
        Unit.(

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() Unit.let {
            println()
        }
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        {

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let )
            anotherInnerSuspendFun({
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        ().let {
            anotherInnerSuspendFunUnit
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun//case 4: use inline fun, and call another outer suspend fun, works well{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        ()
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let ()

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun{
            anotherOuterSuspendFun()
        }
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            (()
        }
    }
    doInnerSuspendanotherInnerSuspendFun)
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        }.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        Unit
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() doInnerSuspend
    {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun())
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend({ {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
let fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.suspend {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let let
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.{ {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend()) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        UnitUnit.let {
            anotherOuterSuspendFun()
        }let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        .

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspendUnit
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        ().let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
anotherInnerSuspendFun fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            suspend()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun().
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit{let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend Unit.let {
            anotherInnerSuspendFun()
        } doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        fun
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()Unit
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        {
    }.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
doInnerSuspend()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        }

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(( {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun))
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(( {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend))

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()anotherInnerSuspendFun()
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        {()

        //case 3: use inline fun, but no recurse, works well
        Unit.let doInnerSuspend
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend())
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
Unit
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        }.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        {
            anotherInnerSuspendFun()
        }
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let //case 4: use inline fun, and call another outer suspend fun, works well
    }
    doInnerSuspend()
}
yWanotherInnerSuspendFun()
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            // Original bug: KT-34213
        }
    }
    doInnerSuspend()
}
y4// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { this + it }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        String.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            withIndex()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y\// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            mutableListOf(NullableInt(1))
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
inline fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        foo.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y&// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.Z()

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y-// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { it }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            exception()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.hasNext {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y;// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Tester().box()

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yL// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    foo()
}
y ü// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    C.ZZZ().c
    return "OK"
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    companion fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y_// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.myNotEquals {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Del(intArrayOf(1, 2, 3))

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y;// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        // KJS_WITH_FULL_RUNTIME
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y=// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.listOf('1', '2', '3', '4')

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yQ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            C()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y›// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            {
    // with possible local optimizations
    if (1.toDouble() in emptyCharSequence.indices != range1.contains(1.toDouble())) throw AssertionError()
    if (1.toDouble() !in emptyCharSequence.indices != !range1.contains(1.toDouble())) throw AssertionError()
    if (!(1.toDouble() in emptyCharSequence.indices) != !range1.contains(1.toDouble())) throw AssertionError()
    if (!(1.toDouble() !in emptyCharSequence.indices) != range1.contains(1.toDouble())) throw AssertionError()
    // no local optimizations
    if (element17 in emptyCharSequence.indices != range1.contains(element17)) throw AssertionError()
    if (element17 !in emptyCharSequence.indices != !range1.contains(element17)) throw AssertionError()
    if (!(element17 in emptyCharSequence.indices) != !range1.contains(element17)) throw AssertionError()
    if (!(element17 !in emptyCharSequence.indices) != range1.contains(element17)) throw AssertionError()
}
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y8// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {x -> x.toLong()}
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            testS()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y+// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {it}

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            C(2)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    return M<Int>(p)?.nulled() == 1
}
    doInnerSuspend()
}
y3// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        from.i

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

open fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun a(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            t()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yK// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
        return@myMap
    }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y ì// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    return f(arg)
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Int {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yá// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
        foo(foo) // 1st foo is a local fun, second is a captured local var
        foo()
    }
    }
    doInnerSuspend()
}
y8// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.bar(x)

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        B.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    list4()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.x {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y2// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y>// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { "OK" }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.IntFoo {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y3// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {it}

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yD// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            add()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            Generic(Host())
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {x -> x}

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    return object : SomeClass(3.14, 42, "No", "OK", "Yes") {
    }.result()
}
    doInnerSuspend()
}
y_// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend(from, to)

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        inner.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
    if (foo()!! > 239) return "Fail"
    return "OK"
}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { "$s!!" }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yD// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        // !LANGUAGE: +InlineClasses
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            toString()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun l(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Child.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        valProp.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.Array {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y¶// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            val x = object : Callable {
                override fun call(b: Boolean) {
                    if (b) {
                        x()
                    } else {
                        try {
                            x()
                        } catch(t: Throwable) {
                        }
                    }
                }
            }
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    override fun f() = "O"
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.TWO {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun closure(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        test()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { x, y -> x + y }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { (x, y) -> x + y }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y+// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        A::two.get(a)
    }
    doInnerSuspend()
}
yL// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            box()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    fun box(): String {
    val a1: Byte = 1 + 1
    val a2: Short = 1 + 1
    val a3: Int = 1 + 1
    val a4: Long = 1 + 1
    val a5: Double = 1.0 + 1
    val a6: Float = 1f + 1
    val a7: Char = 'A' + 1
    val a8: Int = 'B' - 'A'

    if (a1 != 2.toByte()) return "fail 1"
    if (a2 != 2.toShort()) return "fail 2"
    if (a3 != 2) return "fail 3"
    if (a4 != 2L) return "fail 4"
    if (a5 != 2.0) return "fail 5"
    if (a6 != 2f) return "fail 6"
    if (a7 != 'B') return "fail 7"
    if (a8 != 1) return "fail 8"

    return "OK"
}
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yK// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun get() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    withIndex()
}
yg// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
        inner = i
    }
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yF// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun field(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yQ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { this::bar.isInitialized }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
    var count = 0
    for (i in (M downTo M).reversed()) {
        ++count
        if (count > 1) {
            throw AssertionError("Loop should be executed once")
        }
    }
    if (count != 1) throw AssertionError("Should be executed once")
    return "OK"
}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
    val z = "K"
    open class A(val x: String) {
        constructor() : this("O")

        val y: String
            get() = z
    }

    class B : A()

    val b = B()

    return b.x + b.y
}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.String {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.v3 {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y5// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        // KJS_WITH_FULL_RUNTIME
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
ys// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
    val s = MyClass(Z("OK"))
    return s.test()
}
        }
    }
    doInnerSuspend()
}
yE// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun nine(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y?// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        // !LANGUAGE: +InlineClasses
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
        ok = "OK"
    }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        u.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
ya// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(val x: Int) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()x + y
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y.// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { ++x }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y`//fun main(args : Array<String>) {
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y0// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { field }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun nullable() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    inline fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y ˆ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    fun test(): String {
        return s
    }
    doInnerSuspend()
}
yD// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun box(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y>// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { ::foo.isInitialized }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun String(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun a() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y:// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.arrayOfNulls<String>(1)

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        e.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y'// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        // KJS_WITH_FULL_RUNTIME
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.arrayOfNulls<String>(0)

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { methodVar }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yD// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Int()
        }
    }
    doInnerSuspend()
}
y(// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        foo.result
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

fun equals1(a: Double, b: Double?) = a == b
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {a -> "" + a.size}

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        mmmap.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        En.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yi// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
    val a = A(5)

    val av = a::v
    if (av() != 5) return "fail1: ${av()}"
    if (av.get() != 5) return "fail2: ${av.get()}"
    av.set(7)
    if (a.v != 7) return "fail3: ${a.v}"

    val af = a::f
    if (af(10) != 70) return "fail4: ${af(10)}"

    val ag = a::g
    if (ag(10) != 700) return "fail5: ${ag(10)}"

    val aw = a::w
    if (aw() != 7000) return "fail6: ${aw()}"
    if (aw.get() != 7000) return "fail7: ${aw.get()}"
    aw.set(5)
    if (a.v != 15) return "fail8: ${a.v}"

    val fu = F::u
    if (fu() != 0) return "fail9: ${fu()}"
    if (fu.get() != 0) return "fail10: ${fu.get()}"
    fu.set(8)
    if (F.u != 8) return "fail11: ${F.u}"

    val x = 100

    fun A.lf() = v * x;
    val alf = a::lf
    if (alf() != 1500) return "fail9: ${alf()}"

    return "OK"
}
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.j {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        this.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.add {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y§// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
ye// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
    val f = object {
        val a by del()
    }

    f.a
}

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.result {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
        ok = "OK"
    }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y ‹// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    val ss = foo(arr)
    return if (ss == "0:10;1:20;2:30;3:40;") "OK" else "fail: '$ss'"
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        answer.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        // WITH_RUNTIME
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            IC("OK")
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    SomeClass.run()
    return "OK"
}
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            h()
        }
    }
    doInnerSuspend()
}
y*// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {a}
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        id.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y-// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { ok }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.String {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun A(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yl// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            lazy {
            a
        }
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y`// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    private fun createAnonObject() =
        object : IFoo, IBar {
            override fun foo() = "foo"
            fun qux(): String = "qux"
        }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Int.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    inner fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.Result {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yD// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun Int(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y9// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        // CHECK_CASES_COUNT: function=box count=0
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun list1() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        String()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        T.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { it + "K" }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y∑// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    val a = Augmented(0)
    val single = "$a"
    if (single != "1") return "Fail 1: $single"

    val asAny = AsAny(42)
    val asAnyString = "$asAny"
    if (asAnyString != "AsAny: 42") return "Fail 2: $asAnyString"

    val b = Augmented(1)
    val two = "$a and $b"
    if (two != "1 and 2") return "Fail 3: $two"

    val d = AugmentedAndAsAny(a, asAny)
    if (d.toString() != "AugmentedAndAsAny(a=1, b=AsAny: 42)") return "Fail 4: $d"

    return "OK"
}
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            String()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        // KJS_WITH_FULL_RUNTIME
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yç// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
    val i : Int? = 0
    val j = i?.plus(3) //verify error
    return "OK"
}
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
operator fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y,// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { t }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.String {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

inline fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y^// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { "reifiedIsNot<MutableMap.MutableEntry<*, *>>(entry)"}

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            test()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y8// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { a, b -> a + b }
    }
    doInnerSuspend()
}
yF// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Array()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
abstract fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { result(+"Stuff") }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// !LANGUAGE: +InlineClasses
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            block()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yb// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        simpleFldGetter.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            3
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        O.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        C.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y:// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
        x++
    }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.a {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    UInt()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.b {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y*// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        list1.add(i)

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// IGNORE_BACKEND: NATIVE
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            add()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            foo()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            C()
        }
    }
    doInnerSuspend()
}
y9// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        // !LANGUAGE: +InlineClasses
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    fun Test.Nested.foo() = value
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yp// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        UnsupportedOperationException.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    ZImpl()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

fun box(): String {
    val a1x = A1("asd")
    if (a1x.f() != "asd#const2#") return "fail1: ${a1x.f()}"
    if (a1x.toString() != "A1(prop1=asd)") return "fail1s: ${a1x.toString()}"
    val a1y = A1()
    if (a1y.f() != "default#const2#empty") return "fail2: ${a1y.f()}"
    if (a1y.toString() != "A1(prop1=default)") return "fail2s: ${a1y.toString()}"
    val a1z = A1(5)
    if (a1z.f() != "5#const2#int") return "fail3: ${a1z.f()}"
    if (a1z.toString() != "A1(prop1=5)") return "fail3s: ${a1z.toString()}"

    val a2x = A2("asd")
    if (a2x.f() != "asd#const2#") return "fail4: ${a2x.f()}"
    val a2y = A2(123.0)
    if (a2y.f() != "default#const2#empty") return "fail5: ${a2y.f()}"
    val a2z = A2(5)
    if (a2z.f() != "5#const2#int") return "fail6: ${a2z.f()}"

    return "OK"
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y<// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { result += "B.mp," }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.b {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    put(index, elem)
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            doTest()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y\// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            run {
        ok = "OK"
    }
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            Throwable()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            range1()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        String.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.invoke {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun String() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y;// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { result(+"Stuff") }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y0// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
fun box(): String {
    val o = object : B() {
        override fun foo(t: String) = "o"
    }
    val zb: B = Z
    val ob: B = o
    val za: A<String> = Z
    val oa: A<String> = o

    return when {
        Z.foo("")  != "Z" -> "Fail #1"
        o.foo("")  != "o" -> "Fail #2"
        zb.foo("") != "Z" -> "Fail #3"
        ob.foo("") != "o" -> "Fail #4"
        za.foo("") != "Z" -> "Fail #5"
        oa.foo("") != "o" -> "Fail #6"
        else -> "OK"
    }
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
        width += width
    }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.downTo {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Unit {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { (x, y, z) -> x + y + z.toString() }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.checkLess(0, 0.5)

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Test {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            foo("OK")
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// KJS_WITH_FULL_RUNTIME

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.str {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y6// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { arr[it] < 0 }
    }
    doInnerSuspend()
}
yK// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        // This function tells the constructor which cells are alive
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
    if (x.iic.compareTo(II(42)) != 0) throw AssertionError()
}
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.equals2 {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

override fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y•// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
    var s = ""
    for ((a, b) in l) {
      s += "$a:$b;"
    }
    return s
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y Ë// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
        if (y == 6) {
            result = "a"
        } else {
            result = "b"
        }
    }
y/// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { "OK" }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    s()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        A.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
protected fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y+// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        // !LANGUAGE: +InlineClasses
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    val vals = iarray(789, 678, 567, 456, 345, 234, 123, 12)
    val diffs = HashSet<Int>()
    for (i in vals.indices)
        for (j in i..vals.lastIndex())
             diffs.add(vals[i] - vals[j])
    val size = diffs.size

    if (size != 8) return "Fail $size"
    return "OK"
}
    doInnerSuspend()
}
y0// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        c.f

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yL// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            Int()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    public fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y9// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { it.age }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y*// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        M().doTest()
    }
    doInnerSuspend()
}
yX// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    operator fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yd// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    override fun greet(): String {
        return "OK"
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yâ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //KT-10934 compiler throws UninferredParameterTypeConstructor in when block that covers all types
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y,// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { 5 }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println("OK")
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    fun box(): String {
    val b = B()
    b.init()
    return b.x
}
    doInnerSuspend()
}
ya// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            ArrayList<String>()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    fun box(): String = Test?.a
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.thisRef {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        D.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    inner fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yL// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        box()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { ok() }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        Double()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y<// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { this + it }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y@// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
        ok = "OK"
    }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { z(2) !in z(1) .. z(3) }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Foo.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y{// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
    if (!test(Base())) return "Fail #1"
    if (!test(Derived1())) return "Fail #2"
    if (!test(Derived2())) return "Fail #3"
    return "OK"
}
    doInnerSuspend()
}
yZ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.superb {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun C(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y>// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //test for appropriate
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yB// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            s()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        baz.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yY// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        append.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { { Host.ok() } }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun test() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yE// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            foo(0)
        }
    }
    doInnerSuspend()
}
y_// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            {
    return "" + this
}
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Any.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yK// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun box() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yÙ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
    val ax: Byte? = 0.toByte()
    val an: Byte? = null
    val bx: Byte = 0.toByte()
    val by: Byte = 1.toByte()

    return when {
        nx != 0.toByte() -> "Fail 0"
        nx == 1.toByte() -> "Fail 1"
        !(nx == 0.toByte()) -> "Fail 2"
        !(nx != 1.toByte()) -> "Fail 3"
        nx != x -> "Fail 4"
        nx == y -> "Fail 5"
        !(nx == x) -> "Fail 6"
        !(nx != y) -> "Fail 7"
        nn == 0.toByte() -> "Fail 8"
        !(nn != 0.toByte()) -> "Fail 9"
        nn == x -> "Fail 10"
        !(nn != x) -> "Fail 11"
        ax != 0.toByte() -> "Fail 12"
        ax == 1.toByte() -> "Fail 13"
        !(ax == 0.toByte()) -> "Fail 14"
        !(ax != 1.toByte()) -> "Fail 15"
        ax != x -> "Fail 16"
        ax == y -> "Fail 17"
        !(ax == x) -> "Fail 18"
        !(ax != y) -> "Fail 19"
        ax != bx -> "Fail 20"
        ax == by -> "Fail 21"
        !(ax == bx) -> "Fail 22"
        !(ax != by) -> "Fail 23"
        an == 0.toByte() -> "Fail 24"
        !(an != 0.toByte()) -> "Fail 25"
        an == x -> "Fail 26"
        !(an != x) -> "Fail 27"
        an == bx -> "Fail 28"
        !(an != bx) -> "Fail 29"
        else -> "OK"
    }
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y2// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.A(239.toLong())

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            u1()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        box.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yÂ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
        try {
            val p = 1
            return "OK"
        }
        catch(e: Exception) {
            -2
        }
        finally {
            -3
        }
    }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yv// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
                val x = ok
                { x }
            }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Int.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y_// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {invokeOrder += "$captured"; "$captured"}

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y(// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.bar()
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.v2 {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yÕ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        fn(
                try { zap(s1) } catch (e: Exception) { "" },
                try { zap(s2) } catch (e: Exception) { "" }
        )

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    val sb = StringBuilder()
    operator fun String.unaryPlus() {
        sb.append(this)
    }

    +"OK"
    return sb.toString()!!
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.operation(if (3 > 2) a else b)

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yF// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let { return "def" }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            something()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y °// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
fun testWithDefaultL2() = withDefaultL2(fn = { L(it.long + 1L) })
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
data fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y;// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        x.compareTo(y)

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            ArrayList<Int>()
        }
    }
    doInnerSuspend()
}
yû// KOTLIN_CONFIGURATION_FLAGS: CONSTRUCTOR_CALL_NORMALIZATION_MODE=preserve-class-initialization
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        o()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let { t -> t.foo() }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            toByte()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            toUByte()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yC// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            it()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yF// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            Outer()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun E() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y[// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.indices {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y2// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        value.length

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.i {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun public(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y3// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        5u.toUShort()

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y$// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    fun box(): String {
    val aByte = arrayListOf<Byte>(1)
    var bByte: Byte = 1
    
    val aShort = arrayListOf<Short>(1)
    var bShort: Short = 1
    
    val aInt = arrayListOf<Int>(1)
    var bInt: Int = 1
    
    val aLong = arrayListOf<Long>(1)
    var bLong: Long = 1

    val aFloat = arrayListOf<Float>(1.0f)
    var bFloat: Float = 1.0f
    
    val aDouble = arrayListOf<Double>(1.0)
    var bDouble: Double = 1.0

    aByte[0]--
    bByte--

    if (aByte[0] != bByte) return "Failed post-decrement Byte: ${aByte[0]} != $bByte"

    aByte[0]++
    bByte++

    if (aByte[0] != bByte) return "Failed post-increment Byte: ${aByte[0]} != $bByte"
    
    aShort[0]--
    bShort--

    if (aShort[0] != bShort) return "Failed post-decrement Short: ${aShort[0]} != $bShort"

    aShort[0]++
    bShort++

    if (aShort[0] != bShort) return "Failed post-increment Short: ${aShort[0]} != $bShort"
    
    aInt[0]--
    bInt--

    if (aInt[0] != bInt) return "Failed post-decrement Int: ${aInt[0]} != $bInt"

    aInt[0]++
    bInt++

    if (aInt[0] != bInt) return "Failed post-increment Int: ${aInt[0]} != $bInt"

    aLong[0]--
    bLong--

    if (aLong[0] != bLong) return "Failed post-decrement Long: ${aLong[0]} != $bLong"

    aLong[0]++
    bLong++

    if (aLong[0] != bLong) return "Failed post-increment Long: ${aLong[0]} != $bLong"
    
    aFloat[0]--
    bFloat--

    if (aFloat[0] != bFloat) return "Failed post-decrement Float: ${aFloat[0]} != $bFloat"

    aFloat[0]++
    bFloat++

    if (aFloat[0] != bFloat) return "Failed post-increment Float: ${aFloat[0]} != $bFloat"

    aDouble[0]--
    bDouble--

    if (aDouble[0] != bDouble) return "Failed post-decrement Double: ${aDouble[0]} != $bDouble"

    aDouble[0]++
    bDouble++

    if (aDouble[0] != bDouble) return "Failed post-increment Double: ${aDouble[0]} != $bDouble"

    return "OK"
}
    doInnerSuspend()
}
y8// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.Test(S("OK"))

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yO// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun String() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y1// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {"$a:$b;"}
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        D.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y-// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { it }
    }
    doInnerSuspend()
}
y7// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let { return "def" }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            String()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.A {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y0// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {mInt(0)}
    }
    doInnerSuspend()
}
yâ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        // Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y>// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let { it + value2 }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y+// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        index.toInt()
    }
    doInnerSuspend()
}
yπ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            {
    val l = ArrayList<Int>()
    l.add(1)
    val x = l[0] == 2
    if (x != false) return "Fail: $x}"
    return "OK"
}
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.p {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yå// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            {
        return "Wrong elements for MaxB..MaxB: $list2"
    }
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        box.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
        //case 1: use inline fun and recurse, CompilationException
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
}
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yW// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
        //case 3: use inline fun, but no recurse, works well
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

            doInnerSuspend()
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit. {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspendfun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() 
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend) {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let{
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y À// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend(
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

 fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        .let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun({
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y0// Original bug: KT-34213
// Duplicated bug: KT-32153


suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun () {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
     fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        ()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y/// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let 

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }//case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun({
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()}

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yA// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            ()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend(
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun{
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun)
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun)
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works welldoInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yH// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun () {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let{
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yA// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun (){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y6// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let 

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend funanotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y+// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let{
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
     fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unitlet {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y'// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let 
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun) {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works wellUnit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    ()
}
y'// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let 

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit. {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend)
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
 fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unitlet {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        .let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit. {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y#// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()}

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit. {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()}

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }}
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){

suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y,// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            ()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()

yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        .let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println)
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun(
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yA// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            ()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend funanotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }//case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()}
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y2// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yG// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y#// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend(

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspendfun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yA// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun (){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let{
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend)
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y?// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend  outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println(
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend  doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yS// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        .let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y(// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y Ë// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() 
    doInnerSuspend()
}
y?// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            
        }
    }
    doInnerSuspend()
}
yI// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            ()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend funouterSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend  anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works wellUnit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let 
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun{
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()//case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let 
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y<// Original bug: KT-34213


suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let 
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y>
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun(
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend( {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun(){
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y&// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unitlet {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){}
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspendfun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend(){
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun()
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
y// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works wellUnit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()}
yJ// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {//case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fundoInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() 
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unitlet {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
x~// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() 
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let 
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend)

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspendfun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yV// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
x`// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}

yM// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }//case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yT// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend  anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yU// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yP// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun()
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yN// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationExceptionUnit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yE// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException

            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }
    }
    doInnerSuspend()
}
yR// Original bug: KT-34213
// Duplicated bug: KT-32153

suspend fun anotherOuterSuspendFun(){
}
suspend fun outerSuspendFun() {
    suspend fun anotherInnerSuspendFun(){
    }
    suspend fun doInnerSuspend() {
        //case 1: use inline fun and recurse, CompilationException
        Unit.let {
            doInnerSuspend()
        }

        //case 2: no inline fun but recurse, works well
        doInnerSuspend()

        //case 3: use inline fun, but no recurse, works well
        Unit.let {
            println()
        }

        //case 4: use inline fun, and call another outer suspend fun, works well
        Unit.let {
            anotherOuterSuspendFun()
        }

        //case 5: use inline fun, and call another inner suspend fun, works well
        Unit.let {
            anotherInnerSuspendFun()
        }

    doInnerSuspend()
}
ˇˇ