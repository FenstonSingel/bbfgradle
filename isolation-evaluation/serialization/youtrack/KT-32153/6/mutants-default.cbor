�iexportTaggdefaultnoriginalSampleyC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
gmutants�y.// Original bug: KT-32153
// Duplicated bug: KT-32153


val z = true
if (z) {// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()} else {// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()}

fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yy// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()

val v = false
try
{fun outer() {
    suspend fun inner(): Int = eval { inner() }
}}
catch(e: Exception){}
finally{}

yy// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = 
val f = true
when (f) {
 true -> {f()}
 else -> {f()}
}

fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() 
val g = true
if (g) {{
    suspend fun inner(): Int = eval { inner() }
}} else {{
    suspend fun inner(): Int = eval { inner() }
}}

yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(
val t = false
if (t) {f: suspend () -> Int} else {f: suspend () -> Int}
): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = 
val j = true
try
{f}
catch(e: Exception){}
finally{}
()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    
val g = false
when (g) {
 true -> {suspend fun inner(): Int = eval { inner() }}
 else -> {suspend fun inner(): Int = eval { inner() }}
}

}
yy// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): 
val y = true
when (y) {
 true -> {Int}
 else -> {Int}
}
 = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = 
val f = false
when (f) {
 true -> {eval { inner() }}
 else -> {eval { inner() }}
}

}
yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = 
val s = true
try
{eval}
catch(e: Exception){}
finally{}
 { inner() }
}
yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): 
val s = true
try
{Int}
catch(e: Exception){}
finally{}
 = eval { inner() }
}
yy// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval 
val d = false
try
{{ inner() }}
catch(e: Exception){}
finally{}

}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval 
val e = true
when (e) {
 true -> {{ inner() }}
 else -> {{ inner() }}
}

}
yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> 
val f = true
try
{Int}
catch(e: Exception){}
finally{}
): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yx// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { 
val n = true
try
{inner()}
catch(e: Exception){}
finally{}
 }
}
y~// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { 
val p = false
when (p) {
 true -> {inner()}
 else -> {inner()}
}
 }
}
y{// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { 
val g = true
when (g) {
 true -> {inner}
 else -> {inner}
}
() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
(fun outer() {
    suspend fun inner(): Int = eval { inner() }
})
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { (inner()) }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int!! = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner!!() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): (Int)? = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: (() -> Int)?): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): (Int)? = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> (Int)?): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f{ inner() })
fun outer() {
    suspend fun inner(): Int = eval (
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): f() = Int
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ()()
fun outer() {
    suspend fun inner(): Int = eval { innerf }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend f) -> Int): Int = (()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer{
    suspend fun inner(): Int = eval { inner() }
}) (
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend { -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval () inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalIntf: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): ( = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun suspend() {
    outer fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
-> fun eval(f: suspend () suspend Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int : f()
fun outer() {
    suspend fun inner()= Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int{
    suspend fun inner(): Int = eval { inner() }
}: Int = f()
fun outer() )
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
inner() outer() {
    suspend fun inner(): Int = eval { fun }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): fun = f()
Int outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): { inner() } = f()
fun outer() {
    suspend fun inner(): Int = eval Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval{ inner() }: Int = f()
fun outer() {
    suspend fun inner(): Int = eval (f: suspend () -> Int)
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    : fun inner()suspend Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() (
    suspend fun inner(): Int = eval { inner{) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
eval fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = suspend { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    ) fun inner(suspend: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
= fun eval(f: suspend () -> Int): Int suspend f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer= {
    suspend fun inner(): Int () eval { inner() }
}
yCeval
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun // Original bug: KT-32153(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval): Int = f((f: suspend () -> Int)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval({: suspend () -> Int): Int = f()
fun outer() f
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
inner outer() {
    suspend fun fun(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    eval fun inner(): Int = suspend { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int inner f()
fun outer() {
    suspend fun inner(): Int = eval { =() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f{
fun outer() {
    suspend fun inner(): Int = eval () inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend { inner() } -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval ()
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(
fun outer() {
    suspend fun inner()): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): ( = f()
fun outer() {
    suspend fun innerInt): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int): Int = f()
fun outer() {
    suspend fun inner(): () -> Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = {
fun outer() {
    suspend fun inner(): Int = eval f() inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): suspend = f()
fun outer() {
    Int fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f({
    suspend fun inner(): Int = eval { inner() }
}
fun outer() )
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> {): Int = f()
fun outer() {
    suspend fun inner(): Int = eval Int inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> ()): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { innerInt }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> }): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = }()
fun outer() {
    suspend fun inner(): Int = eval { inner() f
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend Int inner(): fun = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend {) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval ( inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
( outer() {
    suspend fun innerfun): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () ) Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner(-> }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): eval = f()
fun outer() {
    suspend fun inner(): Int = Int { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = fun()
fun outer() {
    suspend f inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int){ inner() } Int = f()
fun outer() {
    suspend fun inner(): Int = eval :
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): } = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
{
    suspend fun inner(): Int = eval { inner() }
} fun eval(f: suspend () -> Int): Int = f()
fun outer() suspend
yC// Original bug: KT-32153
)

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(// Duplicated bug: KT-32153 {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

suspend fun inner(): Int = eval { inner() }
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    // The issue persists even without inlining.
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): f = Int()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: outer () -> Int): Int = f()
fun suspend() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innerinner(): Int = eval { () }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalfun: Int = f()
fun outer() {
    suspend (f: suspend () -> Int) inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
) fun eval(f: suspend () -> Int): Int = f(suspend
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> eval): Int = f()
fun outer() {
    suspend fun inner(): Int = Int { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: {
    suspend fun inner(): Int = eval { inner() }
} () -> Int): Int = f()
fun outer() suspend
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(fun
) outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () = Int): Int = f()
fun outer() {
    suspend fun inner(): Int -> eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() }
    suspend fun inner(): Int = eval { inner() {
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int {
    suspend fun inner(): Int = eval { inner() }
} f()
fun outer() =
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { (inner) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outereval {
    suspend fun inner(): Int = () { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(outer
fun )() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer:) {
    suspend fun inner()( Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: ) () -> Int): Int = f(suspend
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(: Int = f()
fun outer(f: suspend () -> Int)) {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

=
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int // The issue persists even without inlining. eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(ffun suspend () -> Int): Int = f()
: outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
: fun eval(f: suspend () -> Int)suspend Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(() -> Int: suspend f): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner=): Int ( eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    { fun inner(): Int = eval suspend inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f)
fun outer() {
    suspend fun inner((): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intsuspend fun inner(): Int = eval { inner() }: Int = f()
fun outer() {
    )
}
yCeval
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = // Original bug: KT-32153 { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(: Int = f()
fun outer() {
    suspend fun inner(f: suspend () -> Int)): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = finner)
fun outer() {
    suspend fun ((): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend fun -> Int): Int = f()
fun outer() {
    suspend () inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int)() Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner: }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): fun = f()
fun outer() {
    suspend Int inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

fun
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
// The issue persists even without inlining. outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval()f: suspend () -> Int): Int = f()
fun outer( {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun eval { inner() }() {
    suspend fun inner(): Int = outer
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = } { inner() eval
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () ( Int): Int = f()
fun outer->) {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

:
fun outer() {
    suspend fun inner()// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f() Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
Int
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem. = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> ()): Int = f()
fun outer() {
    suspend fun innerInt: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ffun outer() {
    suspend fun inner(): Int = eval { inner() }
})
(
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innereval { inner() }): Int = (
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend eval inner(): Int = fun { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): = = f()
fun outer() {
    suspend fun inner(): Int Int eval { inner() }
}
yCfun
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend // Original bug: KT-32153 inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = }
fun outer() {
    suspend fun inner(): Int = eval { inner() }
f()
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> )): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner(Int }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend Int eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): fun = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outerinner()) {
    suspend fun inner(): Int = eval { ( }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
f fun eval(f: suspend () -> Int): Int = suspend()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int -> Int): Int = f()
fun outer() {
    suspend fun inner(): () = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

eval
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = // The issue persists even without inlining. { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
inner fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun suspend(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

{
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() // The issue persists even without inlining.
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

(f: suspend () -> Int)
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval// The issue persists even without inlining.: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
Int fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): suspend = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
Int fun eval(f: suspend () -> Int): suspend = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (( -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner)) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun ((f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { innereval) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = outer()
fun f() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

Int
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): // The issue persists even without inlining. = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): inner() = eval { Int }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend = -> Int): Int () f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): } = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
Int
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(fun): Int = f()
fun outer() {
    suspend f: suspend () -> Int inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innerInt: () = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> IntInt: Int = f()
fun outer() {
    suspend fun inner(): ) = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intfun: Int = f()
) outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer)( {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun :(f: suspend () -> Int)eval Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend outer): Int = f()
fun () -> Int() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(suspend): Int = f()
fun outer() {
    f: suspend () -> Int fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
eval

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = // Duplicated bug: KT-32153 { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalinner: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { (f: suspend () -> Int)() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun Int(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): eval = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun fun(f: suspend () -> Int): Int = f()
fun outer() {
    suspend eval inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): fun outer() {
    suspend fun inner(): Int = eval { inner() }
} = f()
Int
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(): Int = f()
fun outer(f: suspend () -> Int) {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalfunf: suspend () -> Int): Int = f()
fun outer() {
    suspend ( inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> :)Int Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
inner()

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { // Duplicated bug: KT-32153 }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = ( { innereval) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

inner()
fun outer() {
    suspend fun inner(): Int = eval { // The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f=)
fun outer() {
    suspend fun inner(): Int ( eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f{
    suspend fun inner(): Int = eval { inner() }
} suspend () -> Int): Int = f()
fun outer() :
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun { inner() }(): Int = eval inner
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int) -> (): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = inner()()
fun outer() {
    suspend fun inner(): Int = eval { f }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun fun outer() {
    suspend fun inner(): Int = eval { inner() }
}(f: suspend () -> Int): Int = f()
eval
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ()): Int = f()
fun outer() -> Int {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
Int fun eval(f: suspend () -> suspend): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int inner eval { =() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ( -> Int): Int = f()
fun outer()) {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner{ inner() }: Int = eval ()
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(inner): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { f: suspend () -> Int() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int): () -> Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = inner()
fun outer() {
    suspend fun inner(): Int = eval { f() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: Int () -> suspend): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalIntf: suspend () -> (): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun outer(f: suspend () -> Int): Int = f()
fun eval() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ()): Int = f()
fun outer() {
    suspend fun inner() -> Int: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = suspend fun inner(): Int = eval { inner() }()
fun outer() {
    f
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
fun fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend suspend inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (fun -> Int): Int = f()
fun outer() {
    suspend ) inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): () = eval { innerInt }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int f =()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

f: suspend () -> Int
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(// The issue persists even without inlining.): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int -> ()): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer)) {
    suspend fun inner((: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: Int () -> Int): suspend = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f) suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(:: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: () () -> Int): Int = f()
fun outer() {
    suspend fun innersuspend: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend fun outer() {
    suspend fun inner(): Int = eval { inner() }
}) -> Int): Int = f()
(
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
suspend fun inner(): Int = eval { inner() }
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f): Int = f: suspend () -> Int()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
{ inner() } outer() {
    suspend fun inner(): Int = eval fun
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: (): Int = f()
fun outer() {
    suspend fun innersuspend () -> Int): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
(f: suspend () -> Int) fun evalsuspend: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
)
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intf: Int = )()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): = Int f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> =): Int = f()
fun outer() {
    suspend fun inner(): Int Int eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): ) = eval { inner(Int }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intsuspend: Int = f()
fun outer() {
    ) fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(:f suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): ( = f()
fun outer() {
    suspend fun inner(): Int = eval { innerInt) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend): Int = f()
fun outer() {
    suspend () -> Int fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outerInt {
    suspend fun inner(): () = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> :): Int = f()
fun outer() {
    suspend fun inner()Int Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = inner()
fun outer() {
    suspend fun f(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> f()): Int = Int
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend fun inner(): Int = eval { inner() }): Int = f()
fun outer() {
    suspend () -> Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner()( Int = eval { inner:) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): () = f()
fun outer() {
    suspend fun innerInt: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval()): Int = f()
fun outer(f: suspend () -> Int {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun f(f: suspend () -> Int): Int = eval()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outereval { inner() } {
    suspend fun inner(): Int = ()
}
yC{ inner() }
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval // Original bug: KT-32153
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f))
fun outer(( {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner({ inner() }: Int = eval )
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend f eval(f: suspend () -> Int): Int = fun()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = (()
fun outerf) {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = eval { inner() }()
fun outer() {
    suspend fun inner(): Int = f
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
}
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

)
fun outer(// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
() fun eval(f: suspend () -> Int): Int = fsuspend
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: (): Int = fsuspend () -> Int)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (inner() -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { ) }
}
yC(
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner// Original bug: KT-32153): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> (): Int = f()
fun outer() {
    suspend fun innerInt): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int)inner Int = f()
fun outer() {
    suspend fun :(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = { inner() } eval
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun {
    suspend fun inner(): Int = eval { inner() }
}(f: suspend () -> Int): Int = f()
fun outer() eval
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(finner() suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { : }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> {
    suspend fun inner(): Int = eval { inner() }
}): Int = f()
fun outer() Int
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): {
    suspend fun inner(): Int = eval { inner() }
} = f()
fun outer() Int
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (Int -> )): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () Int ->): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f:)
fun outer() {
    suspend fun inner()( Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner} }
()
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

Int
fun outer() {
    suspend fun inner(): // The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f() = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = { inner() }
fun outer() {
    suspend fun inner(): Int = eval f()
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
) fun eval(f: suspend () -> Intsuspend: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = {()
fun outer() f
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> f): Int = Int()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(eval
fun outer() {
    suspend fun inner(): Int = ) { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (inner -> Int): Int = f()
fun outer() {
    suspend fun )(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner()inner Int = eval { :() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> )): Int = f()
fun outer(Int {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> inner()): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { Int }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(inner {
    suspend fun inner(): Int = eval { )() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: ( () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { innersuspend) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f{ inner() } suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval :
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval({ inner() }): Int = f()
fun outer() {
    suspend fun inner(): Int = eval f: suspend () -> Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { }() }
inner
yC// Original bug: KT-32153
// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()

// Duplicated bug: KT-32153
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int{ inner() }: Int = f()
fun outer() {
    suspend fun inner(): Int = eval )
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int fun f()
fun outer() {
    suspend = inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): suspend fun inner(): Int = eval { inner() } = f()
fun outer() {
    Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend eval) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = ( { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(suspend: suspend () -> Int): Int = f()
fun outer() {
    f fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
inner
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): inner = f()
fun outer() {
    suspend fun inner(): Int = eval { Int() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): { inner() } = eval Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
} fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
suspend
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: )): Int = f()
fun outer(suspend () -> Int {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend f eval(fun: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = =()
fun outer() {
    suspend fun inner(): Int f eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer{ inner() } {
    suspend fun inner(): Int = eval ()
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = fInt)
fun outer() {
    suspend fun inner(): ( = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): () = f()
fun outer() {
    suspend fun inner(): Int = eval { innerInt }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: ) () -> Intsuspend: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () inner Int): Int = f()
fun outer() {
    suspend fun ->(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend suspend) -> Int): Int = f()
fun outer() {
    ( fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int) -> Int): ( = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yCInt
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): // Original bug: KT-32153 = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> inner): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { Int() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int ) eval { inner(= }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun fun(f: suspend () -> Int): Int = f()
eval outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalsuspend: Int = f()
fun outer() {
    (f: suspend () -> Int) fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun Int(): inner = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend suspend): Int = f()
fun outer() {
    () -> Int fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f( suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner:) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer({
    suspend fun inner(): Int = eval { inner() }
} )
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
: fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner()suspend Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() { inner() }
    suspend fun inner(): Int = eval {
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outerfun) {
    suspend ( inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
()
suspend fun eval(f: suspend () -> Int): Int = f// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ffun outer() {
    suspend fun inner(): Int = eval { inner() }
}
()
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: { inner() }): Int = f()
fun outer() {
    suspend fun inner(): Int = eval suspend () -> Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun { inner() }(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval eval
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun ()(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innereval: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(fsuspend fun inner(): Int = eval { inner() } suspend () -> Int): Int = f()
fun outer() {
    :
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    eval { inner() } fun inner(): Int = suspend
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ()()
fun outerf {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
() fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innersuspend: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalsuspendf: suspend () -> Int): Int = f()
fun outer() {
    ( fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> suspend): Int = f()
fun outer() {
    Int fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> }): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
Int
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = fsuspend
fun outer() {
    () fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f) suspend (: -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { } }
inner()
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> (): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { innerInt) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer()) {
    suspend fun inner(): Int = eval { inner( }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalinner: Int = f()
fun outer() {
    suspend fun (f: suspend () -> Int)(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(fInt suspend () -> Int): : = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): ( = eval { innerInt) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f({
fun outer() )
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (Int -> Int): Int = f()
fun outer() {
    suspend fun inner(): ) = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ) -> Int(): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): inner = f()
fun outer() {
    suspend fun Int(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

(
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend // The issue persists even without inlining.) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner()): Int = eval { inner( }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalff: suspend () -> Int): Int = (()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval{
    suspend fun inner(): Int = eval { inner() }
}: Int = f()
fun outer() (f: suspend () -> Int)
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
suspend outer() {
    fun fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend )): Int = f(() -> Int
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int)() Int = f:
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend () -> Int eval(f: suspend fun): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

()
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer// The issue persists even without inlining. {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalInt: Int = f()
fun outer() {
    suspend fun inner(): (f: suspend () -> Int) = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
{
    suspend fun inner(): Int = eval { inner() }
}
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
eval { inner() }
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f=
fun outer() {
    suspend fun inner(): Int () eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
) fun eval(f: suspend (suspend -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int eval f()
fun outer() {
    suspend fun inner(): Int = = { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend Int) -> Int): Int = f()
fun outer() {
    suspend fun inner(): ( = eval { inner() }
}
yCsuspend
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
// Original bug: KT-32153 fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = )
fun outer() {
    suspend fun inner(): Int = eval { inner(f() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intinner: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { )() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Intfun outer() {
    suspend fun inner(): Int = eval { inner() }
}: Int = f()
)
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
->
suspend fun eval(f: suspend () // I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem. Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = (f)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend }) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
(
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun innerinner): Int = eval { (() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalsuspendf: ( () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(Int: ) = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval inner() { }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> { inner() }): Int = f()
fun outer() {
    suspend fun inner(): Int = eval Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun ()inner: Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (: -> Int)) Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

inner
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { // The issue persists even without inlining.() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval }
{ inner() }
yCf
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(// Original bug: KT-32153: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (outer -> Int): Int = f()
fun )() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outersuspend fun inner(): Int = eval { inner() }) {
    (
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
Int outer() {
    suspend fun inner(): fun = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalf(): Int = (f: suspend () -> Int)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = finner
fun outer() {
    suspend fun ()(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> suspend fun inner(): Int = eval { inner() }): Int = f()
fun outer() {
    Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): { = eval Int inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> )): Int = f(Int
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    { inner() } fun inner(): Int = eval suspend
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outerInt) {
    suspend fun inner(): ( = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: Int): suspend () -> Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalIntf: suspend () -> Int): ( = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend () eval(f: suspend () -> Int): Int = f()
fun outerfun {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
( fun eval(f: suspend () -> Int): Int = f()
fun outersuspend) {
    suspend fun inner(): Int = eval { inner() }
}
yCfun outer() {
    suspend fun inner(): Int = eval { inner() }
}
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
// Original bug: KT-32153
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: eval () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = suspend { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outersuspend fun inner(): Int = eval { inner() } {
    ()
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(){ Int = eval : inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun Int() {
    suspend fun inner(): outer = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend suspend fun inner(): Int = eval { inner() }): Int = f()
fun outer() {
    () -> Int
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (): Int = f()
fun outer() {
    suspend fun inner() -> Int): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
( fun eval(f: suspend suspend) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(= {
    suspend fun inner(): Int ) eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: () () -> Int): Int = fsuspend
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
suspend () -> Int

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: // Duplicated bug: KT-32153): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun eval(): Int = inner { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend : -> Int): Int = f()
fun outer() {
    suspend fun inner()() Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ()): Int = f() -> Int
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int){ Int = f()
fun outer() :
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(inner() {
    suspend fun inner(): Int = eval { ) }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend ) eval(f: suspend () -> Intfun: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = { inner() }()
fun outer() {
    suspend fun inner(): Int = eval f
}
yC// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// Original bug: KT-32153
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun test3(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Double = eval { inner() }
}
yI// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { doSomething() }
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = block()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yL// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { x, _, y -> x + y }
}
yO// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner(object: B {}) }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { String() }
}
y^// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    Foo.foo()
    return if (result == "(Stuff)") "OK" else "Fail $result"
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): String = eval { inner() }
}
yI// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(i: Int) {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f("O")
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun Int(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = Int { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = a { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(s: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y]// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): UnsupportedOperationException = eval { inner() }
}
yM// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Comparable<*> = eval { inner() }
}
yL// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): () -> String = eval { inner() }
}
yK// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Exception()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ss()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun str(): Int = eval { inner() }
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Outer()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: operator () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> String): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Alias()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
operator fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y/// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// KJS_WITH_FULL_RUNTIME
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun Foo(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = y()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> test): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// See KT-6206 Always generate hashCode() and equals() for data classes even if base classes have non-trivial analogs

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    const fun inner(): Int = eval { inner() }
}
yg// Original bug: KT-32153
// Duplicated bug: KT-32153

// KT-5786 NoSuchMethodError: no accessor for private fun with default arguments
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = size()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yz// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {
        column("OK") {
            result = name
        }
    }
}
yI// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): GameError = eval { inner() }
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Boolean): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box(): String {
    val ax: Long? = 0L
    val an: Long? = null
    val bx: Long = 0L
    val by: Long = 1L

    return when {
        0L != nx -> "Fail 0"
        1L == nx -> "Fail 1"
        !(0L == nx) -> "Fail 2"
        !(1L != nx) -> "Fail 3"
        x != nx -> "Fail 4"
        y == nx -> "Fail 5"
        !(x == nx) -> "Fail 6"
        !(y != nx) -> "Fail 7"
        0L == nn -> "Fail 8"
        !(0L != nn) -> "Fail 9"
        x == nn -> "Fail 10"
        !(x != nn) -> "Fail 11"
        0L != ax -> "Fail 12"
        1L == ax -> "Fail 13"
        !(0L == ax) -> "Fail 14"
        !(1L != ax) -> "Fail 15"
        x != ax -> "Fail 16"
        y == ax -> "Fail 17"
        !(x == ax) -> "Fail 18"
        !(y != ax) -> "Fail 19"
        bx != ax -> "Fail 20"
        by == ax -> "Fail 21"
        !(bx == ax) -> "Fail 22"
        !(by != ax) -> "Fail 23"
        0L == an -> "Fail 24"
        !(0L != an) -> "Fail 25"
        x == an -> "Fail 26"
        !(x != an) -> "Fail 27"
        bx == an -> "Fail 28"
        !(bx != an) -> "Fail 29"
        else -> "OK"
    }
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yJ// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { Derived1("OK") }
}
y/// Original bug: KT-32153
// Duplicated bug: KT-32153

// KJS_WITH_FULL_RUNTIME
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(receiver: String): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun value(): Int = eval { inner() }
}
yJ// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Delegate()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y=// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = ztest(C())
}
yI// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
open abstract fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun x(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// culprit ?? remove this try-catch and it works.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {"$a:$b;"}
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Float): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y5// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { A() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Byte?): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
    val o = object : A<Int> {
        override fun foo(t: Int) = if (t == 42) "OK" else "Fail $t"
    }
    return Derived(o).foo(42)
} }
}
y:// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(val fn: IFn): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y0// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
open fun foo(): Int {
        return 2
    }
yH// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = Delegates { inner() }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { it }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { "" }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = {res += "K"; "K"}()
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = body()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { s() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun Int(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    open fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Base()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y;// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
  if (1 != 0) {
  }
  return "OK"
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    inline fun inner(): Int = eval { inner() }
}
yH// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = String()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yO// Original bug: KT-32153
// !LANGUAGE: +ProperIeee754Comparisons

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// no local optimizations

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y:// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {}
}
yH// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(String: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun String(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = Any { inner() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = str()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = foo2()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box(): String {
    val outer = Outer.Inner()
    return "OK"
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = A()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// KJS_WITH_FULL_RUNTIME

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { Z() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { Box(f) }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box() = A().ok()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yi// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    fun box(): String {
    val b = A.B()
    val c = A.C<String>()
    return "OK"
}
}
y5// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(a: Any): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y:// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: Password): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = X { inner() }
}
yL// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { AssertionError() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Any = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { "def" }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    val result = "OK"

    class Local {
        fun foo() = result
    }

    val member = Local::foo
    val instance = Local()
    return member(instance)
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = toString { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
final fun foo(): T = t!!
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { a() }
}
yu// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(arg1: A, arg2: String, funRef:A.(String) -> String) {
    suspend fun inner(): Int = eval { inner() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = result { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    val a1 = A(5, 10)
    if (a1.prop != "5") return "fail0: ${a1.prop}"
    if (a1.parentProp != "15") return "fail1: ${a1.parentProp}"
    if (sideEffects != "minus-one#zero#0.7#first#second#third") return "fail2: ${sideEffects}"

    sideEffects = ""
    val a2 = A(123)
    if (a2.prop != "123#int") return "fail3: ${a2.prop}"
    if (a2.parentProp != "126") return "fail4: ${a2.parentProp}"
    if (sideEffects != "minus-one#zero#0.5#first#second#fourth") return "fail5: ${sideEffects}"

    sideEffects = ""
    val a3 = A()
    if (a3.prop != "7#int") return "fail6: ${a3.prop}"
    if (a3.parentProp != "10") return "fail7: ${a3.parentProp}"
    if (sideEffects != "minus-one#zero#0.5#first#second#fourth#fifth") return "fail8: ${sideEffects}"
    return "OK"
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun b() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { this }
}
yD// Original bug: KT-32153
// !LANGUAGE: +InlineClasses

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(
        val id: String?,
        val name: String,
        val distance: Int) {
    suspend fun inner(): Int = eval { inner() }
}
y9// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = add(i)
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = test3("1", "2")
}
yc// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
            _oldVersion = 1
        } }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { FOO.x }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { run1 {
        if (a < 0)
          return@run1 "Fail 2"
        else "OK"
    } }
}
y}// Original bug: KT-32153
// Duplicated bug: KT-32153

// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = x { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = listOf(1, 2, 3)
}
y8// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: String): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yz// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    fun box(): String {
    val x = "OK"
    class Aaa {
        val y = x
    }

    return Aaa().y
}
}
y+// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    fun box() = My().my
}
yH// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { return "def" }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    val list1 = ArrayList<UInt>()
    for (i in (3u..9u step 2).reversed()) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<UInt>(9u, 7u, 5u, 3u)) {
        return "Wrong elements for (3u..9u step 2).reversed(): $list1"
    }

    val list2 = ArrayList<UInt>()
    for (i in (3u.toUByte()..9u.toUByte() step 2).reversed()) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<UInt>(9u, 7u, 5u, 3u)) {
        return "Wrong elements for (3u.toUByte()..9u.toUByte() step 2).reversed(): $list2"
    }

    val list3 = ArrayList<UInt>()
    for (i in (3u.toUShort()..9u.toUShort() step 2).reversed()) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<UInt>(9u, 7u, 5u, 3u)) {
        return "Wrong elements for (3u.toUShort()..9u.toUShort() step 2).reversed(): $list3"
    }

    val list4 = ArrayList<ULong>()
    for (i in (3uL..9uL step 2L).reversed()) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<ULong>(9u, 7u, 5u, 3u)) {
        return "Wrong elements for (3uL..9uL step 2L).reversed(): $list4"
    }

    return "OK"
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { "OK" }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: vararg () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yZ// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
    return obj.Foo().arg()
} }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box(): String {
    var res = ""
    // nullable variable
    val en2: Any? = En.A
    if (en2 is En) {
        when (en2) {
            En.A -> {res += "O"}
            En.B -> {}
            En.C -> {}
        }

        when (en2 as En) {
            En.A -> {res += "K"}
            En.B -> {}
            En.C -> {}
        }
    }

    return res
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun T() {
    suspend fun inner(): Int = eval { inner() }
}
yR// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {
    object : First { }
}
}
yT// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evaluateAndCheckOrder(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { ->
        object {
            override fun toString(): String {
                return "child" + y
            }
        }
    }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = MaxL { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { result }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { foo() }
}
yE// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    fun f(s: String?): String {
    return "$s"
}
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box(): String {
    xs[0] = 12u
    val t = xs[0]
    if (t != 12u) throw AssertionError("$t")

    return "OK"
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yX// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    var i = 0
    do continue while (i++ < 3)

    return "OK"
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = Impl()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun Ucn(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = test()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y:// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = outer()
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
   val v1 = A(10.toByte()).hashCode()
   val v2 = (10.toByte() as Byte?)!!.hashCode()
   return if( v1 == v2 ) "OK" else "$v1 $v2"
} }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = M { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { { ok }() }
}
y=// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { 3 }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { x() }
}
y\// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = foo(2, 2) {a -> "" + a.size}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun Short(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yD// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun String() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun test(aZ: Z, aL: L, aS: S) = "${aZ.x} ${aL.x} ${aS.x}"
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y{// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
  return if (Test().sayHello() == "hello") "OK" else "fail"
} }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: const () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yH// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {

        } }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun foo(): String = 10.toString()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yK// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = lastIndex()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { E() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = a()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yJ// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box(): String {
    val a = A("start")

    try {
        test(a)
    } catch(e: RuntimeException) {

    }

    if (a.value != "start, try, finally1, finally2") return "fail: ${a.value}"

    return "OK"
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {

  fun local():Int {
    return 10;
  }

  class A {
      val test = local()
  }

  return if (A().test == 10) "OK" else "fail"
} }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    var count = 0
    for (i in M downTo M) {
        ++count
        if (count > 1) {
            throw AssertionError("Loop should be executed once")
        }
    }
    if (count != 1) throw AssertionError("Should be executed once")
    return "OK"
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { "K" }
}
yt// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { if (1 == 1) {
            i++
        } else {
        } }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun box(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun bx() {
    suspend fun inner(): Int = eval { inner() }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

fun foo(a: A, block: (Int, A, String) -> String): String = block(1, a, "#")
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    val list1 = ArrayList<UInt>()
    for (i in (MaxUI - 2u)..MaxUI step 2) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<UInt>(MaxUI - 2u, MaxUI)) {
        return "Wrong elements for (MaxUI - 2u)..MaxUI step 2: $list1"
    }

    val list2 = ArrayList<UInt>()
    for (i in (MaxUB - 2u).toUByte()..MaxUB step 2) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<UInt>((MaxUB - 2u).toUInt(), MaxUB.toUInt())) {
        return "Wrong elements for (MaxUB - 2u).toUByte()..MaxUB step 2: $list2"
    }

    val list3 = ArrayList<UInt>()
    for (i in (MaxUS - 2u).toUShort()..MaxUS step 2) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<UInt>((MaxUS - 2u).toUInt(), MaxUS.toUInt())) {
        return "Wrong elements for (MaxUS - 2u).toUShort()..MaxUS step 2: $list3"
    }

    val list4 = ArrayList<ULong>()
    for (i in MaxUL - 2u..MaxUL step 2) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<ULong>(MaxUL - 2u, MaxUL)) {
        return "Wrong elements for MaxUL - 2u..MaxUL step 2: $list4"
    }

    return "OK"
}
yU// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    if (takeAny(AsInt(123)) != "asInt: 123") return "fail"
    if (takeAny(AsAny(321)) != "asAny: 321") return "fail"

    if (takeAny(AsInt(getInt())) != "asInt: 10") return "fail"
    if (takeAny(AsInt(id(20))) != "asInt: 20") return "fail"

    if (takeAny(AsAny(id(30))) != "asAny: 30") return "fail"

    return "OK"
}
yF// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = containsValue(null)
}
yN// Original bug: KT-32153
// Duplicated bug: KT-32153

fun box () : String {
   val b = 4.toByte()
   val s = 5.toShort()
   val c: Char = 'A'
   return if( "$b" == "4" && " $b" == " 4" && "$s" == "5" && " $s" == " 5" && "$c" == "A" && " $c" == " A") "OK" else "fail"
}
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yc// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    operator fun A.plusAssign(s: String) {
    pp += s
    log += "pp = $pp;"
}
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: inline () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yb// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {
                this.foo()
            }
}
y8// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = Z1(n)
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    val n = En.N
    val a = En.A

    if (wrap1(n) != null) throw AssertionError()
    if (wrap1(a) == null) throw AssertionError()
    if (wrap1(a)!!.x != a) throw AssertionError()

    if (wrap2(n) != null) throw AssertionError()
    if (wrap2(a) == null) throw AssertionError()
    if (wrap2(a)!!.z.x != a) throw AssertionError()

    if (wrapN(n) != null) throw AssertionError()
    if (wrapN(a) == null) throw AssertionError()
    if (wrapN(a)!!.z!!.x != a) throw AssertionError()

    return "OK"
}
yJ// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (String) -> Unit): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yJ// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { x(1) in list() }
}
yK// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = InlineDouble(Double.NaN)
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = y { inner() }
}
yL// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun box(): String {
    val list1 = ArrayList<Int>()
    val range1 = 5 downTo 10
    for (i in range1) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>()) {
        return "Wrong elements for 5 downTo 10: $list1"
    }

    val list2 = ArrayList<Int>()
    val range2 = 5.toByte() downTo 10.toByte()
    for (i in range2) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>()) {
        return "Wrong elements for 5.toByte() downTo 10.toByte(): $list2"
    }

    val list3 = ArrayList<Int>()
    val range3 = 5.toShort() downTo 10.toShort()
    for (i in range3) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Int>()) {
        return "Wrong elements for 5.toShort() downTo 10.toShort(): $list3"
    }

    val list4 = ArrayList<Long>()
    val range4 = 5L downTo 10L
    for (i in range4) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Long>()) {
        return "Wrong elements for 5L downTo 10L: $list4"
    }

    val list5 = ArrayList<Char>()
    val range5 = 'a' downTo 'z'
    for (i in range5) {
        list5.add(i)
        if (list5.size > 23) break
    }
    if (list5 != listOf<Char>()) {
        return "Wrong elements for 'a' downTo 'z': $list5"
    }

    return "OK"
}
y9// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(val x: Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yM// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = lt(42L, ONEL)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    private fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(private val r: Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {
        var x = 0L
        val y = x[0]++
        return if (y == 0L) "OK" else "Failed, y=$y"
    }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = Test { inner() }
}
yC// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(x: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y&// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    fun foo(): Int
}
yr// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(a: Int = 1,
        b: Int = 2,
        c: Int = 3,
        d: Int = 4,
        e: Int = 5,
        f: Int = 6,
        g: Int = 7,
        h: Int = 8,
        i: Int = 9,
        j: Int = 10,
        k: Int = 11,
        l: Int = 12,
        m: Int = 13,
        n: Int = 14,
        o: Int = 15,
        p: Int = 16,
        q: Int = 17,
        r: Int = 18,
        s: Int = 19,
        t: Int = 20,
        u: Int = 21,
        v: Int = 22,
        w: Int = 23,
        x: Int = 24,
        y: Int = 25,
        z: Int = 26,
        aa: Int = 27,
        bb: Int = 28,
        cc: Int = 29,
        dd: Int = 30,
        ee: Int = 31,
        ff: Int = 32,
        gg: Int = 33,
        hh: Int = 34,
        ii: Int = 35,
        jj: Int = 36,
        kk: Int = 37,
        ll: Int = 38,
        mm: Int = 39,
        nn: Int = 40): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: open () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y$// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    return bar()
}
yG// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = first()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y�// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { {
    upcast<(Int)->ByteArray>(::ByteArray)(10)
    upcast<(Int)->IntArray>(::IntArray)(10)
    upcast<(Int)->ShortArray>(::ShortArray)(10)
    upcast<(Int)->LongArray>(::LongArray)(10)
    upcast<(Int)->DoubleArray>(::DoubleArray)(10)
    upcast<(Int)->FloatArray>(::FloatArray)(10)
    upcast<(Int)->BooleanArray>(::BooleanArray)(10)

    return "OK"
} }
}
yM// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend (Result<T>) -> Unit): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    enum fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = k { inner() }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// KJS_WITH_FULL_RUNTIME
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// TARGET_BACKEND: JVM
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y+// Original bug: KT-32153
// Duplicated bug: KT-32153

// !API_VERSION: 1.0
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y8// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = foo()
}
yC// Original bug: KT-32153
fun outer() {

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
// Duplicated bug: KT-32153
    suspend fun inner(): Int = eval { inner() }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun () {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner():  = eval { inner() }
}
y<// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
 fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend  eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner): Int = eval { inner() }
}
y<// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
     fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> ): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int= f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ()-> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y<// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f:  () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend funinner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer( {
    suspend fun inner(): Int = eval { inner() }
}
y8// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval 
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspendfun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer) {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int):  = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner() Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f)
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = ()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ()  Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int):Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int =  { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun evalf: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer(){
    suspend fun inner(): Int = eval { inner() }
}
y?// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun (f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner: Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ( -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend  -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
funouter() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner( }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y-// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y<// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {  }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int) Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval {inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspendfun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y(// Original bug: KT-32153


// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
xx// Original bug: KT-32153
// Duplicated bug: KT-32153


fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int  eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ) -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f:suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { () }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() 
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
 outer() {
    suspend fun inner(): Int = eval { inner() }
}
y*
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y3// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = 
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int  f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = 
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f(
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(: Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int =f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner) }
}
y �// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.

suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () ->Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner():Int = eval { inner() }
}
y>// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun (): Int = eval { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() 
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }

yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval{ inner() }
}
y2// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: ): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend funeval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153


// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() 
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval  inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int: Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y@// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend  inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner()}
}
y// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()

yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend() -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int =eval { inner() }
}
yA// Original bug: KT-32153
// Duplicated bug: KT-32153// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
y:// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend ): Int = f()
fun outer() {
    suspend fun inner(): Int = eval { inner() }
}
yB// Original bug: KT-32153
// Duplicated bug: KT-32153

// The issue persists even without inlining.
// I suspect that a recursive suspending call of a local function in a Î» expr is essentially the problem.
suspend fun eval(f: suspend () -> Int): Int = f()
fun outer() {
    suspend fun inner(): Int= eval { inner() }
}
��