øiexportTaggdefaultnoriginalSampley3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
gmutantsüyi// Original bug: KT-38666
// Duplicated bug: KT-28956


val p = false
try
{fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}}
catch(e: Exception){}
finally{}


fun main() {
    foo()
}
yl// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}


val h = false
if (h) {fun main() {
    foo()
}} else {fun main() {
    foo()
}}

y+// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() 
val e = false
if (e) {{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}} else {{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}}


fun main() {
    foo()
}
ya// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() 
val p = false
if (p) {{
    foo()
}} else {{
    foo()
}}

y˜// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    
val q = false
if (q) {lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }} else {lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}

    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yà// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    
val q = true
when (q) {
 true -> {print(" done with explicit label")}
 else -> {print(" done with explicit label")}
}

}

fun main() {
    foo()
}
yY// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    
val y = false
if (y) {foo()} else {foo()}

}
yi// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    
val k = false
try
{listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
catch(e: Exception){}
finally{}

    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yX// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    
val v = true
if (v) {print} else {print}
(" done with explicit label")
}

fun main() {
    foo()
}
yi// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    
val m = false
try
{foo}
catch(e: Exception){}
finally{}
()
}
yW// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    
val x = true
if (x) {lit@} else {lit@}

    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y|// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    
val q = false
when (q) {
 true -> {listOf(1, 2, 3, 4, 5)}
 else -> {listOf(1, 2, 3, 4, 5)}
}
.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yÎ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).
val a = false
when (a) {
 true -> {forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
 else -> {forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
}

    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yY// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    
val v = true
if (v) {listOf} else {listOf}
(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yi// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).
val c = false
try
{forEach}
catch(e: Exception){}
finally{}
 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yn// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(
val u = true
if (u) {" done with explicit label"} else {" done with explicit label"}
)
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach 
val e = true
try
{{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
catch(e: Exception){}
finally{}

    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(
val b = false
when (b) {
 true -> {1}
 else -> {1}
}
, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 
val a = true
try
{2}
catch(e: Exception){}
finally{}
, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yU// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 
val q = false
if (q) {3} else {3}
, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 
val h = true
try
{4}
catch(e: Exception){}
finally{}
, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 
val x = true
try
{5}
catch(e: Exception){}
finally{}
).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach 
val v = true
try
{{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }}
catch(e: Exception){}
finally{}

    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        
val h = true
try
{if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)}
catch(e: Exception){}
finally{}

    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yj// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        
val d = true
if (d) {if (it == 3) return@lit} else {if (it == 3) return@lit}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yo// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
val f = true
when (f) {
 true -> {print(it)}
 else -> {print(it)}
}

    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yY// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
val b = false
if (b) {print} else {print}
(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yn// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (
val q = false
when (q) {
 true -> {it == 3}
 else -> {it == 3}
}
) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yp// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) 
val v = true
when (v) {
 true -> {return@lit}
 else -> {return@lit}
}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (
val n = true
try
{it}
catch(e: Exception){}
finally{}
 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it 
val f = true
try
{==}
catch(e: Exception){}
finally{}
 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 
val b = false
when (b) {
 true -> {3}
 else -> {3}
}
) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yh// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(
val l = true
try
{it}
catch(e: Exception){}
finally{}
)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yj// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return
val k = true
when (k) {
 true -> {@lit}
 else -> {@lit}
}
 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).(forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        (if (it == 3) return@lit) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (print)(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print!!(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3!!, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4!!, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }!!
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)!!
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit!! // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it != 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yK// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if ((it)?.equals(3) ?: (3 === null)) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    (
    
    print}" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it main 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun ==() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun fooprint {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    ()(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it it 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(==)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    print
    
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo done with explicit label {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("()")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(print(" done with explicit label"))
    }
    
    it
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    print(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    listOf(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() }

fun main() {
    foo()
{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, )).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(5 {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }.)
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
// Duplicated bug: KT-28956

fun foo() {
    // Original bug: KT-38666
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    4
    listOf(1, 2, 3, lit@, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    fun
    
    print(" done with explicit label")
}

} main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, it, 5).forEach {
        if (4 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf)1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"(
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it (it) 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print==
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    )
    
    print(" done with explicit label"lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3{ 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() ,
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }(1, 2, 3, 4, 5).forEach listOf
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(())
    }
    
    print(" done with explicit label")
}

fun mainit {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        printprint
    }
    
    (it)(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 3, 2, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
         done with explicit label(it)
    }
    
    print("print")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    if (it == 3) return@lit@
    listOf(1, 2, 3, 4, 5).forEach {
        lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo)) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it(
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) fun@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

return main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf((, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main1) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf((it), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print1
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ()
    
    print(" done with explicit label")
}

fun mainlistOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2,  done with explicit label, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("3")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foolit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }) {
    (
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

main

fun fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() print(it)
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        {
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, (it), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print3
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnprint(it) // local return to the caller of the lambda, i.e. the forEach loop
        @lit
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (fun) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

it == 3 main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it 3 ==) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, it, 3, 4, 5).forEach {
        if (2 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

}

fun main() {
    foo()
fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    litforEach
    listOf(1, 2, 3, 4, 5).@ {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo{) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() (
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 23 3, 4, 5).forEach {
        if (it == ,) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun fooprint) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ((it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) print@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    return(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOfit1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(()
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo{) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach (
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf( done with explicit label, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("1")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(foo(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    1
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, ==, 5).forEach {
        if (it 4 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    it
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(lit@)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo{ ()
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        it(print)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main5) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() lit
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@{ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print5" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print)" done with explicit label")
}

fun main() {
    foo((
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 2, 3, 4, 5).1
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, forEach, 5).4 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)return@litforEach {
        if (it == 3) . // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnfoo // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    @lit()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print{
    foo()
}" done with explicit label")
}

fun main() (
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    print(it)
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        lit@
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }@
    lit
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, ), 4, 53.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

(

fun mainfun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf((, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print1" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

{

fun main() fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3@lit 4, 5).forEach {
        if (it == 3) return, // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    it.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(listOf(1, 2, 3, 4, 5))
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    (
    
    print(" done with explicit label")
}

fun mainlistOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"1
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if ((" done with explicit label") == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    printit
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 4, 3, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(}, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    1
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo(4 {
    lit@
    listOf(1, 2, 3, ), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(")
    }
    
    print(it done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun listOf() {
    lit@
    foo(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    4(1, 2, 3, listOf, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf({
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 2, 3, 4, 5).forEach 1
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3print(it)
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        // Original bug: KT-38666
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        ( // local return to the caller of the lambda, i.e. the forEach loop
        printif (it == 3) return@litit)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun (() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    printfoo" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() if
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        { (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf21, (, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 5).,
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, foo, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    3()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)foo()forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    .
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) main@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun return() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() ()

fun main() {
    foo{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3it 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(,)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() ()
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main{ {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    (
    
    print(" done with explicit label")
}

fun main() {
    foolit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, return@lit, 3, 4, 5).forEach {
        if (it == 3) 2 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ==).forEach {
        if (it 5 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == () return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main3) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 4, 5).forEach 3
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4) 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label",
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, print(it)).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        5
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() fun main() {
    foo()
}

{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (() return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    printit == 3" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() listOf
    lit@
    {(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(foo, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    1()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
     done with explicit label.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("listOf(1, 2, 3, 4, 5)")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5.)forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3{
    foo()
} return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() )
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo)) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3( return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it  done with explicit label 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("==")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

)

fun main() {
    foo(fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ((1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foolistOf)
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print() done with explicit label")
}

fun main() {
    foo("
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

foo() foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    fun
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(} done with explicit label")
"

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2) 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label",
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == )) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(3 {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, main, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun 3() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, print, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    2(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() "
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print({ done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    // local return to the caller of the lambda, i.e. the forEach loop(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit listOf
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(lit {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    " done with explicit label"
    
    print(lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) @returnlit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    "@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(lit done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 3).forEach {
        if (it == 5) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) print(" done with explicit label") // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    return@lit
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1@ 2, 3, 4, 5).forEach {
        if (it == 3) return,lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        print // local return to the caller of the lambda, i.e. the forEach loop
        if (it == 3) return@lit(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ((1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    printlistOf" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    }(" done with explicit label")
}

fun main() {
    foo()
print
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, ==, 3, 4, 5).forEach {
        if (it 2 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    {(1, 2, 3, 4, 5).forEach listOf
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foolit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    ()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(print)
    }
    
    it(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit4
    listOf(1, 2, 3, @, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }, 3, 4, 5).forEach 2
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, return@lit, 4, 5).forEach {
        if (it == 3) 3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf,1( 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 4, 5).forEach ,
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (return == 3) it@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    print(" done with explicit label")
    
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit3
    listOf(1, 2, @, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, if (it == 3) return@lit, 4, 5).forEach {
        3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo( {
    lit@
    listOf()1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() 4
    lit@
    listOf(1, 2, 3, {, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}() foo

fun main() {
    foo()
}
y3@
// Duplicated bug: KT-28956

fun foo() {
    lit// Original bug: KT-38666
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3( return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print)" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    "
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(lit@ done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5{
    foo()
}.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() )
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (printit)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }).forEach 5
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, ,4 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (return@lit) it == 3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, it, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(4)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit ()
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo// local return to the caller of the lambda, i.e. the forEach loop
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    foo()
    
    print(" done with explicit label")
}

fun main() {
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(// local return to the caller of the lambda, i.e. the forEach loop, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit 1
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foolit@) {
    (
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach  done with explicit label
    
    print("{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, fun, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

3 main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"@lit
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf((, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo1)
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    )
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(lit@ {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        fun
    }
    
    print(" done with explicit label")
}

if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it) main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ()(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    fooprint
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) " done with explicit label" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(return@lit)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
return

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) // Duplicated bug: KT-28956@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    lit@
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (main == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun it() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach )
    
    print(" done with explicit label")
}

fun main({
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it " 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label==)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, (), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo3
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) " done with explicit label"@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(return)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach (
    
    print{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(@lit, 2, 3, 4, 5).forEach {
        if (it == 3) return1 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, fun).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

5 main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return" done with explicit label"lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(@)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (print == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        it(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it print(it) 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ==
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

( mainfun) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, @).forEach {
        if (it == 3) return5lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    listOf(1, 2, 3, 4, 5).forEach lit@
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, if).forEach {
        5 (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).return@lit {
        if (it == 3) forEach // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

) main(fun {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    litit
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(@)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit==
    listOf(1, 2, 3, 4, 5).forEach {
        if (it @ 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)itforEach {
        if (. == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

5 foo() {
    lit@
    listOf(1, 2, 3, 4, fun).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    return@lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, (, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print3" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    {.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() listOf(1, 2, 3, 4, 5)
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it), 5).forEach {
        4
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2fun main() {
    foo()
} 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

,
y3// Original bug: KT-38666
print

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        // Duplicated bug: KT-28956(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach )
    
    print(" done with explicit label")
}

fun main() {
    foo({
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(" done with explicit label")it)
    }
    
    print(
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (() == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun mainit {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foomain) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun (() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach if (it == 3) return@lit
        { // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"fun main() {
    foo()
}
}

)
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)(" done with explicit label")forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print.
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
foo()

fun main() {
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(()
    }
    
    print(" done with explicit label")
}

fun mainit) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo(3 {
    lit@
    listOf(1, 2, ), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun 2() {
    lit@
    listOf(1, foo, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit )
        print(it// local return to the caller of the lambda, i.e. the forEach loop
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        )(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(print
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit print
        print(it)
    }
    
    // local return to the caller of the lambda, i.e. the forEach loop(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ,(1, 2, 3, 4listOf 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5))forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it.
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(foo())
    }
    
    print(" done with explicit label")
}

fun main() {
    it
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    )
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(lit@
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5(.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main)) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun @lit() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnfoo // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 4, 3, 2, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        {
    foo()
} (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() if
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, ==, 4, 5).forEach {
        if (it 3 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it" done with explicit label"
    }
    
    print())
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() (

fun main{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

lit@ foo() {
    fun
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }.forEach )
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnmain // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun @lit() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5). done with explicit label {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("forEach")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }).5
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOffoo.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    (1, 2, 3, 4, 5)()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 3, 3, 4, 5).forEach {
        if (it == 2) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, ,, 32 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if litit == 3) return@( // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        main
    }
    
    print(" done with explicit label")
}

fun if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, {, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() 4
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } 3, 4, 5).forEach ,
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main((1, 2, 3, 4, 5) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, @lit, 5).forEach {
        if (it == 3) return4 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return done with explicit label // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("@lit")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo(@ {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return)lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun fooif (it == 3) return@lit {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        () // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf31, 2, (, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1print 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        ,(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @lit
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlit@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return( // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print@lit" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }.forEach listOf(1, 2, 3, 4, 5)
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }() {
    foo
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    print
    
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it " 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(== done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, }, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    4
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    ()
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun mainlit@ {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (// local return to the caller of the lambda, i.e. the forEach loop) return@lit it == 3
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) () // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    fooreturn@lit
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    @(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlistOflit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, forEach, 4, 5).3 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } foo() {
    fun
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, foo).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    5()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun }() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
foo

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5 done with explicit label.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(")")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it foo 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    ==()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    " done with explicit label"@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(lit)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) ) // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(return@lit {
    foo()
}
y3)
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"// Original bug: KT-38666
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach (
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print{" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
(

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print// Duplicated bug: KT-28956it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(1, 2, 3, 4, 5))
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf done with explicit label1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("(")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        fun main() {
    foo()
} (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

if
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) it // local return to the caller of the lambda, i.e. the forEach loop
        print(return@lit)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print({
    foo()
})
}

fun main() " done with explicit label"
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo4 {
    lit@
    listOf(1, 2, 3, (), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2fun 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

, main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOfif.forEach {
        (1, 2, 3, 4, 5) (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(14 2, 3, ,, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, @, 4, 5).forEach {
        if (it == 3) return3lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    it(1, 2, 3, 4, 5).forEach {
        if (listOf == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        fun main() {
    foo()
}
    }
    
    print(" done with explicit label")
}

if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun main() {
    foo()
}

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, if (it == 3) return@lit, 5).forEach {
        4 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3. 4, 5),forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if ( done with explicit label) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("it == 3")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    ()
    
    print(" done with explicit label")
}

fun mainlit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, ), 4, 5).forEach {
        if (it == 33 return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
listOf

fun foo() {
    lit@
    // Duplicated bug: KT-28956(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo(" done with explicit label") {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print()
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 53.forEach {
        if (it == )) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@print(" done with explicit label") // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    lit
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    "
    
    print(" done with explicit labellit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    }(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
listOf
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if ()) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"it == 3
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, (, 3, 4, 5).forEach {
        if 2it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun fooprint(it)) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, ), 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"2
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        print
    }
    
    if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach )
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it{
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        fun main() {
    foo()
}
    }
    
    print(" done with explicit label")
}

print(it)
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, return, 4, 5).forEach {
        if (it == 3) 3@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOfprint(" done with explicit label").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    (1, 2, 3, 4, 5)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
print(" done with explicit label")

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    // Duplicated bug: KT-28956
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    )
    
    print(" done with explicit label")
}

fun main() {
    foo(lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, print(" done with explicit label")).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    5
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo@lit
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() foo

fun main() {
    {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)3forEach {
        if (it == .) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    5
    listOf(1, 2, 3, 4, lit@).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(")")
}

fun main() {
    foo( done with explicit label
}
y3it
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(// Original bug: KT-38666)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    litif
    listOf(1, 2, 3, 4, 5).forEach {
        @ (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo()() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

} foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    fun
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
    foo()
}
    
    print(" done with explicit label")
}

fun main() {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(1
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo,) {
    lit@
    listOf(1, 2, 3, 4( 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@" done with explicit label" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(lit)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == }) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
3

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        "
    }
    
    print(" done with explicit labelprint(it))
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun fooit == 3 {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (()) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) " // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit labelreturn@lit)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    )
    
    print(" done with explicit label")
}

fun main() {
    foo(}
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"listOf(1, 2, 3, 4, 5)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == fun main() {
    foo()
}) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

3
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    }@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    lit
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
     done with explicit label
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("lit@")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("fun")
}

 done with explicit label main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(")")
}

fun main( done with explicit label {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3print return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    )(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, ,3 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it done with explicit label
    }
    
    print(")")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(})
    it
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).()
    
    print(" done with explicit label")
}

fun main() {
    fooforEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it1
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1{ 2, 3, 4, 5).forEach ,
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, foo(), 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    4
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    }
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
lit@
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun mainfoo() {
    ()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    return(1, 2, 3, 4, 5).forEach {
        if (it == 3) listOf@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach it
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print({)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
" done with explicit label"

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(// Duplicated bug: KT-28956)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 5, 3, 4, 2).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
// Duplicated bug: KT-28956

// Original bug: KT-38666

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return(it) // local return to the caller of the lambda, i.e. the forEach loop
        print@lit
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, fun main() {
    foo()
}, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

3
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    foo()
    
    print(" done with explicit label")
}

fun main() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, )).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(5
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    (
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun mainlit@) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo5)
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(" done with explicit label")
    }
    
    print(it)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)."
    
    print(forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, }, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
3
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == print) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        3(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach " done with explicit label"
    
    print({
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    })
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach 3
        if (it == {) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun it() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(foo)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).{
    foo()
} {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() forEach
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3} return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    )
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (lit) return@it == 3 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, {).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() 5
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@( // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun mainlit) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).if (it == 3) return@lit {
        forEach // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, " done with explicit label", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(3)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, // local return to the caller of the lambda, i.e. the forEach loop).forEach {
        if (it == 3) return@lit 5
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, forEach).5 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo@lit {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if it( == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    2(1, listOf, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).== {
        if (it forEach 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, return@lit, 5).forEach {
        if (it == 3) 4 // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
foo

fun main() {
    }()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach print(" done with explicit label")
    
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    {@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() lit
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, it).forEach {
        if (5 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        foo()(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    print
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (()) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    fooit == 3
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit,
    listOf(1, 2, 3, 4@ 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2forEach 3, 4, 5)., {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    (
    
    print(" done with explicit label")
}

fun main}) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == (" done with explicit label")) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print3
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    return@lit(1, 2, 3, 4, 5).forEach {
        if (it == 3) listOf // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, (, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main3) {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    {
    
    print(" done with explicit label")
}

fun main() }
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    {(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() listOf
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4) 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(, {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo@lit) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return( // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        main // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun if (it == 3) return@lit() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(,, 2, 3, 41 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

( foo() {
    lit@
    listOffun1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, ), 3, 4, 52.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, foo()).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    5
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, }, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
4

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).it == 3 {
        if (forEach) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print)
}

fun main() {
    foo((" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf((" done with explicit label"), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print1
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach foo
    
    print(" done with explicit label")
}

fun main() {
    {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun )(foo {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3{ return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() )
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    5@
    listOf(1, 2, 3, 4, lit).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    ((1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        printlistOfit)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it) 5).forEach {
        ,
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo5) {
    lit@
    listOf(1, 2, 3, 4, ().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    {
    foo()
}
    
    print(" done with explicit label")
}

fun main() }
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, (, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print2it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    ()foo
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4( 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print," done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
        {
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == print(" done with explicit label")) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    3
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

{
    foo()
}

fun main() fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3{
    foo()
} 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() ,
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(,, 2, 31 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)." {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(forEach done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(1, 2, 3, 4, 5)
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

( main() {
    foofun)
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    print.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        listOf(1, 2, 3, 4, 5)(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit }
        print(it)
    }
    
    print(" done with explicit label")
// local return to the caller of the lambda, i.e. the forEach loop

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
} main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    (" done with explicit label")(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    printlistOf
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    )(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(listOf {
    foo()
}
y3listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
// Duplicated bug: KT-28956

fun foo() {
    lit@
    // Original bug: KT-38666
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
         done with explicit label
    }
    
    print("print(it)")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() ()

fun main{
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
} {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(})
    }
    
    print(" done with explicit label")
it

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3" return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label))
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun 3() {
    lit@
    listOf(1, 2, foo, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    } forEach
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)  done with explicit label // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("return@lit")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, ), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(3
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, print, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    4(" done with explicit label")
}

fun main() {
    foo()
}
y8// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@iterator // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ù// Original bug: KT-38666
// Duplicated bug: KT-28956

fun <T> test(v: T): T {
    val a: T = if (v !is A) v else v
    return a
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 0) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @X
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y œ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).testNotNullPrimitive(a, a, a, a)
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y
// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // no local optimizations
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun size() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y#// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    Box().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (Int == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 10, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¶// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    Outer.Other()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 23, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 1, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (c.prop != 1) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    someInt()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(0)
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnOUTER@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(42, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo("42")
}
y ¡// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {"$a:$b;"}
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    A()
}
yP// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box() : String {
    return C("K").test(A("O"))
}
y<// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, Result(42), 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y;// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (KProperty0 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    plus(10)
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) "OK" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    this(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 23, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    String()
}
yP// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (Derived().foo(Str("OK")).str != "OK") return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yw// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return if (a.component1() == 42) a.component2() else a.component1().toString() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (String == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).f {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(true)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(5, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        A("Fail")
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 42, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ⁄// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    f@{
        if (a > 0)
          return@f "OK"
        else "Fail 1"
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 6).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(17)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 1).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it && 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    t(" done with explicit label")
}

fun main() {
    foo()
}
y=// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(y.toString())
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ∏// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).toShort()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    a(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 1L) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y8// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    callback()
}
yL// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo("Should be executed once")
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(1)
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, "", 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 0).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(3, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 2) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yv// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box() =
    when (val y = x) {
        in 0..2 -> "OK"
        else -> "Fail: $y"
    }
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (x + y) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y|// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun noiseMaker(f: NoiseMaker.() -> Unit) {
    val noiseMaker = NoiseMaker()
    noiseMaker.f()
}
y>// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if(d.f() != "D f") return "fail f" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, i, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ≤// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).Z()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).D {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(5)
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    toUShort(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("OK")
}

fun main() {
    foo()
}
y // Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("fail 1")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (list2 == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it + 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @C
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun downTo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yr// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        {
    val a = A()
    val b = A()

    a.iterator()

    a.add(0L)
    a.remove(0L)

    a.addAll(b)
    a.removeAll(b)
    a.retainAll(b)
    a.clear()

    return "OK"
}
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y@// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    val d1 = D(42L, 'a')
    val d2 = D(42L, 'a')
    if (d1 != d2) return "Fail equals"
    if (d1.hashCode() != d2.hashCode()) return "Fail hashCode"
    if (d1.toString() != d2.toString()) return "Fail toString"
    if (d1.foo() != d2.foo()) return "Fail foo"
    return "OK"
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 0, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (fn == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    list1(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y •// Original bug: KT-38666
// Duplicated bug: KT-28956

operator fun plus(x : String) : MyString {
        s += x
        return this
    }

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 1) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        wrap2("a")
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        getMyStr(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ω// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { it }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y%// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    UInt(1).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (sum != 5) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y#// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    foo().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 10, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(2, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(result)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y8// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (FooImpl == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf("it", 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (OK == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 1, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(String)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, "O").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ≈// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {a, b -> a +b}
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ú// Original bug: KT-38666
// Duplicated bug: KT-28956

fun box(): String {
    return object : MyInterface {

    }.something()
}

fun main() {
    foo()
}
y  // Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { foo(it::simple) }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y>// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (f == "xyzzy") "OK" else "fail" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¥// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    AnnotationTarget.EXPRESSION
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yV// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        weirdMix(x) { _, xx, _, _ -> Z(xx.int + 1) }
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (a == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ÿ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    test1@ for(i in 1..2) {
        continue@test1
        return false
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("O")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, y, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (o == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y •// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    8u.toUByte()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(p1)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y@// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(i.superclass(), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() { "O" }
y>// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (a.asResult() != 10) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        foo(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (string == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, "a", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    Actor()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 1, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    test(" done with explicit label")
}

fun main() {
    foo()
}
yÄ// Auto-generated by org.jetbrains.kotlin.generators.tests.GenerateRangesCodegenTestData. DO NOT EDIT!
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    toString(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print()
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    run()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun invoke() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    String(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnf@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        String(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, {}).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(0, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("A")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 0.0, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(null)
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 0, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yB// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // This used to be problematic because of an attempt to load kotlin/Nothing class
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yY// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (L::test.invoke(L(1234L)) != 1234L) throw AssertionError() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 42) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    d(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @getter
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yA// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (z2.x != 0) throw AssertionError() // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // This is access to InnerSubclass.bar which is inherited from Foo.bar
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y@// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(ImpulsMigration)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(0)
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    String(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 5, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    Companion(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun takeInt(i: Int) {}
y&// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun i() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    f()
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        f()
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, key, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    toByte()
}
y õ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
        list2.add(i)
        if (list2.size > 23) break
    }

fun main() {
    foo()
}
y ƒ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
        test = 0
        for (i in t.indices) {
            test = test * 10 + (i + 1)
        }
    }

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 6u, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun Boolean() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y;// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if(aai.t.t != 1)  return "fail" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ø// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    f.inlinedIdExtension()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    Id@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ø// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    (A() as A?)!!.action()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(box)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(-0.0F, 0.0F).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1UL, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y+// Original bug: KT-38666
// See also KT-6299

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    bar(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    Primitive(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@getter // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// KJS_WITH_FULL_RUNTIME

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    invoke()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, "O", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    s()
}
y4// Original bug: KT-38666
// !LANGUAGE: +InlineClasses

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    test1()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        size(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ‹// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    loop@ for (i in 1..10) {
        if (c >= 5) continue@loop
        c++
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (x = B4()) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yd// Original bug: KT-38666
// Auto-generated by GeneratePrimitiveVsObjectEqualityTestData. Do not edit!

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ˘// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        column("OK") {
            result = name
        }
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yr// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (log != "true;true;false;true;true;true;false;true;") {
        return "fail"
    } // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¿// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { x + y }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ü// Original bug: KT-38666
// Duplicated bug: KT-28956

operator fun <T> MyList<T>.plusAssign(element: T) {}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@B // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, A()).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 123L, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).I32 {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    bar@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    plus(3)
}

fun main() {
    foo()
}
y!// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    B().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y<// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(3, {x -> x})
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (l[0] != -1) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yG// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(arrayOf("OK", "Fail"), 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yH// http://youtrack.jetbrains.com/issue/KT-2167
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (T == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    toUByte()
}
y!// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("fail6: ")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, x).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ∞// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    Foo().simpleFldGetter()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yG// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    AssertionError("Should be executed once").forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 2, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yÜ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(
            Array(1, {
                x-> Array(1, {y -> "OK"})
            })
    )
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y;// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    GenericBase()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun get() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun zero() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// KJS_WITH_FULL_RUNTIME
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("abc")
}

fun main() {
    foo()
}
yÑ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
        return "Wrong elements for (3.toByte()..9.toByte() step 2).reversed(): $list2"
    }
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 5uL) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¨// Original bug: KT-38666
// Duplicated bug: KT-28956

public fun <T, R> myWith(receiver: T, block: T.() -> R): R {
    return receiver.block()
}

fun main() {
    foo()
}
yB// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ArrayList<Int>()).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    Foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 1L, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).A {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ”// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { x, y -> arrayOf("$x-$y") }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y8// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    reversed()
}
y=// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // Auto-generated by GeneratePrimitiveVsObjectEqualityTestData. Do not edit!
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @self
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).result {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(foo)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@C // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y}// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box(): String {
    val i = 1
    if (i.getter != 1) return "getter failed"

    return "OK"
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        Class()
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // !LANGUAGE: +ProperIeee754Comparisons
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 42, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@bar // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @bar
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y'// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box(): String {
    val list1 = ArrayList<Int>()
    val range1 = MaxI..MaxI
    for (i in range1) {
        list1.add(i)
        if (list1.size > 23) break
    }
    if (list1 != listOf<Int>(MaxI)) {
        return "Wrong elements for MaxI..MaxI: $list1"
    }

    val list2 = ArrayList<Int>()
    val range2 = MaxB..MaxB
    for (i in range2) {
        list2.add(i)
        if (list2.size > 23) break
    }
    if (list2 != listOf<Int>(MaxB.toInt())) {
        return "Wrong elements for MaxB..MaxB: $list2"
    }

    val list3 = ArrayList<Int>()
    val range3 = MaxS..MaxS
    for (i in range3) {
        list3.add(i)
        if (list3.size > 23) break
    }
    if (list3 != listOf<Int>(MaxS.toInt())) {
        return "Wrong elements for MaxS..MaxS: $list3"
    }

    val list4 = ArrayList<Long>()
    val range4 = MaxL..MaxL
    for (i in range4) {
        list4.add(i)
        if (list4.size > 23) break
    }
    if (list4 != listOf<Long>(MaxL)) {
        return "Wrong elements for MaxL..MaxL: $list4"
    }

    val list5 = ArrayList<Char>()
    val range5 = MaxC..MaxC
    for (i in range5) {
        list5.add(i)
        if (list5.size > 23) break
    }
    if (list5 != listOf<Char>(MaxC)) {
        return "Wrong elements for MaxC..MaxC: $list5"
    }

    return "OK"
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    Double()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun String() {
    foo()
}
y:// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(runInline)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun IFoo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yZ// Original bug: KT-38666
//KT-3217 Invoke convention after function invocation doesn't work

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y9// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (!B() != false) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y8// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(valProp)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y\// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) throw AssertionError("$name: $actual != $expected") // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print()
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        a(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y=// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        MyThrowable("test")
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    storage()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, "q", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yö// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    if (0.0 > -0.0) return "fail 0"
    if (greater1(0.0, -0.0)) return "fail 1"
    if (greater2(0.0, -0.0)) return "fail 2"
    if (greater3(0.0, -0.0)) return "fail 3"
    if (greater4(0.0, -0.0)) return "fail 4"

    // Smart casts behavior in 1.2
    if (!greater5(0.0, -0.0)) return "fail 5"

    return "OK"
}

fun main() {
    foo()
}
y6// Original bug: KT-38666
// Duplicated bug: KT-28956

fun String() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    value(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    str@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y π// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).Delegate()
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ú// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    B.p
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y(// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    KMutableProperty1Impl()
}

fun main() {
    foo()
}
y ó// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    l1@ "s"
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ÿ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    loop@ for (i in a) {
        if (c >= 5) continue@loop
        c++
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    bb(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    l1@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, "c", 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ø// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    log.append("<clinit>")
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y"// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    cl().forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        res(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(1)
}

fun main() {
    foo()
}
y  // Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { outerProp + arg }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y9// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    append(msg)
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        {
        return "Wrong elements for 3..9 step 2: $list1"
    }
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    A(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("fail")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    fu()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (res == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yG// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (getAndCheck({ 42 }, { 42 })) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y7// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    component1(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y`// Original bug: KT-38666
// Duplicated bug: KT-28956

fun box(): String {
    val s = "captured";

    class A(val param: String = "OK") {
        val s2 = s + param
    }

    if (A().s2 != "capturedOK") return "fail 1: ${A().s2}"

    if (A("Test").s2 != "capturedTest") return "fail 2: ${A("Test").s2}"

    return "OK"
}

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(Test)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 42, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(C(2))
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    Box(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun i() {
    foo()
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun Base() {
    foo()
}
y // Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // !LANGUAGE: +VariableDeclarationInWhenSubject
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y¢// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box(): String {
    val foo = Foo<String>()

    val list = listOf("OK")
    val sorted = foo.test(list)

    return list.first()
}
yr// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun box(): String {
    if (A.prop != "OK") return "fail ${A.prop}"
    return result
}
y3// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 0, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
ys// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return "Wrong elements for (MinB + 2).toByte() downTo MinB step 1: $list2" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y	// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // KJS_WITH_FULL_RUNTIME
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
xk// Original bug: KT-38666
// Duplicated bug: KT-28956

fun a(): IntArray? = null

fun main() {
    foo()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(name)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y9// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    MAX_VALUE()
}
y5// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 333) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¬// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { bar(ok) }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
yL// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (sourcesInfo = relativePath("OK")) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Œ// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { ::foo.isInitialized }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y4// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, "", 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y_// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return if (t.test == 1234) "OK" else "Fail: ${t.test}" // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ¡// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach { 1 foo 2}
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y9// Original bug: KT-38666
// Duplicated bug: KT-28956

fun compareTo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y3// Original bug: KT-38666
    listOf(1, 2, 3, 4, 5).forEach {

fun foo() {
    lit@
// Duplicated bug: KT-28956
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ô// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    (" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2,3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if ( == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main(){
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print( done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo( {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, , 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y)// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label"
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)}
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")


fun main() {
    foo()
}
y*// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo) {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}


y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) returnlit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3,4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if(it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main( {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print("")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it  3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo(){
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

 main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() 
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == ) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y)// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)  // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ∑// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach 
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, , 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        printit)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, , 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666


fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    (1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y Ø// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, ).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    .forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y,// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if () return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label)
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() 
    foo()
}
y2// Original bug: KT-38666// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y ê// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it ==3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y$// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf.forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        (it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

 foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {foo()
}
y «// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y*// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1,2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach 
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    ()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4,5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y1// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
         (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
x\// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() 

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo(
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it== 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y,// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5). {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

funfoo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y.// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    
}
y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
         // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

funmain() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit// local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main) {
    foo()
}
y Ò// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit 
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    @
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach{
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo)
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun () {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@ // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3)return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
xR// Original bug: KT-38666
// Duplicated bug: KT-28956



fun main() {
    foo()
}
y0// Original bug: KT-38666
// Duplicated bug: KT-28956

fun () {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y&// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() 
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print" done with explicit label")
}

fun main() {
    foo()
}
y/// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y-// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) @lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5)forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
y2// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()

y// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loop
        print(it)
    }
    
    
}

fun main() {
    foo()
}
y*// Original bug: KT-38666
// Duplicated bug: KT-28956

fun foo() {
    lit@
    listOf(1, 2, 3, 4, 5).forEach {
        if (it == 3) return@lit // local return to the caller of the lambda, i.e. the forEach loopprint(it)
    }
    
    print(" done with explicit label")
}

fun main() {
    foo()
}
ˇˇ